[package]
name = "qip-chain"
description = "On-chain state: blocks, traces, pools, bridges, gas and the mempool."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
qip-core.workspace = true
qip-contracts.workspace = true
# The JSON-RPC adapter's socket. `qip_transport::http` is the sanctioned
# outbound HTTP client in this workspace: blocking, one connection per request,
# and bounded in every dimension a peer chooses. `qip-transport` is a library
# crate below this service, so the dependency edge is legal.
qip-transport.workspace = true

[lints]
workspace = true
