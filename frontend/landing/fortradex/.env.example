# Where Sign In / Get Started send people. The portal's public origin —
# locally the portal dev server, in deployment the Cloud Run / app domain.
NEXT_PUBLIC_ALGORIK_PORTAL_URL=http://127.0.0.1:3400
