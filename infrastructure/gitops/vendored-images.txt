# Third-party images the platform adopts, mirrored into its own registry.
#
# Binary Authorization's default rule denies every image the platform's
# attestor has not signed, and that is the point of it — so a third-party
# controller can run here in one of two ways: an exemption pattern that
# admits its upstream repository unsigned for ever, or an adoption. This
# file is the adoption path: `.github/workflows/vendor.yml` copies each
# source digest into the environment's Artifact Registry byte-for-byte,
# proves the digest survived the copy, scans it with the same gate the
# platform's own images pass, and attests it with the same attestor. The
# admission policy stays exactly one rule with no holes, and "which foreign
# code runs in our cluster" is answered by this file's git history.
#
# Sources are named by digest, never by tag. A tag is whatever its owner
# says it is today; the digest is what was reviewed when the line was
# added. Moving to a new upstream version is a new digest in a new commit.
#
# Format: <source-ref-by-digest> <destination-path> <destination-tag>
# The destination lands at <registry>/<destination-path>:<destination-tag>
# in the target environment's registry. The tag is for humans browsing the
# registry; everything that matters references the digest.

# Argo CD v3.5.2 (core install), per ADR 0017. Digest is the multi-arch
# index for quay.io/argoproj/argocd:v3.5.2, resolved 2026-08-30.
quay.io/argoproj/argocd@sha256:e2aadfae709d904e87f46ba4aa49601d827b3022db22cd4d03aae816a2e7097b vendor/argocd v3.5.2

# cert-manager v1.21.1 — Kargo's prerequisite (webhook certificates). All
# three scanned 0 CRITICAL at adoption. Digests are the multi-arch indexes
# for the v1.21.1 tags, resolved 2026-08-31.
quay.io/jetstack/cert-manager-controller@sha256:416a2d76870d996460e62bd7f521bf14fa017be9e3e904aab92163a331fcb61a vendor/cert-manager-controller v1.21.1
quay.io/jetstack/cert-manager-webhook@sha256:d8b3961b51c8c7320633f8208dc46bf88aa13804d0f7cbe48a096b2c523cee42 vendor/cert-manager-webhook v1.21.1
quay.io/jetstack/cert-manager-cainjector@sha256:ccf6b919ec0500745a47a910118f834f9636d0aac1ff221245cd2557ed8c7c98 vendor/cert-manager-cainjector v1.21.1

# Kargo v1.11.2 — the promotion engine (ADR 0017). Scanned 0 CRITICAL at
# adoption. Digest is the multi-arch index for ghcr.io/akuity/kargo:v1.11.2,
# resolved 2026-08-31.
#
# Deliberately NOT here: ghcr.io/dexidp/dex. The full Argo CD install ships
# it for SSO, this platform has no SSO configured, and v2.45.1 scanned with
# five CRITICALs (a stale alpine OpenSSL and a vulnerable gRPC in dex
# itself). The install excludes the dex Deployment instead of this file
# acknowledging findings in a component nothing uses.
ghcr.io/akuity/kargo@sha256:e1028c697953736421d37abb9266faf6e6876e8f5982811862b38022a9c2fb6f vendor/kargo v1.11.2

# Redis, Argo CD's cache. Deliberately NOT the 8.2.3 its v3.5.2 manifests
# pin: that image sits on a stale alpine 3.22.3 whose OpenSSL carries a
# fixable CRITICAL (CVE-2026-31789), while 8.4.2-alpine's rebuild on 3.22.4
# scans clean of criticals. Argo CD uses redis as a pure cache, so it takes
# redis's own rebuild cadence rather than inheriting upstream's stale pin.
# Digest is the multi-arch index for
# public.ecr.aws/docker/library/redis:8.4.2-alpine, resolved 2026-08-30.
public.ecr.aws/docker/library/redis@sha256:e1b6db24cb4fdd89f4bc9be09f671ea3bec92fbd7042554f76c34aa2be9b59ad vendor/redis 8.4.2-alpine
