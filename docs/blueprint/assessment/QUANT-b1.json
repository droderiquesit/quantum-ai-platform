[
  {
    "id": "QUANT-001",
    "status": "PARTIAL",
    "current": "qip-quantum (provider::SimulatedProvider/HostedProvider, qaoa::solve, statevector::StateVector, solver::{ClassicalSolver,QuantumInspiredSolver,IbmQuantumSolver}, benchmark::SolverBenchmark) plus qip-optimization-engine::router::ComputeRouter implement exactly one workload family: hybrid QAOA/QUBO portfolio allocation. No workload-family registry and no shared benchmark harness spanning families exists.",
    "evidence": [
      "backend/crates/libs/qip-quantum/src/lib.rs:35-55",
      "backend/crates/services/qip-optimization-engine/src/router.rs:196-388",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4322-4325",
      "backend/crates/libs/qip-agents/src/capability.rs:41 (Capability::RunQuantumOptimization — a governance flag with no solver call site; grep -rn RunQuantumOptimization backend/crates found only capability.rs and qip-investment-agents/src/manifests.rs:383)",
      "test: backend/crates/services/qip-optimization-engine/tests/optimization.rs (24 tests, one family; 98 is the whole qip-optimization-engine crate)"
    ],
    "gap": "No named registry of the seven blueprint workload families (graph/search, feature/ensemble selection, quantum kernels/variational models, quantum reservoir/extreme-learning, scenario/hypothesis selection, structured symbolic/combinatorial search) or a shared harness enumerating them and their build status; only portfolio allocation (QUANT-017) exists.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization --no-fail-fast (the one family's tests); no acceptance test enumerates a workload-family registry because none exists to enumerate",
    "work_item": "E10",
    "tier": "T2",
    "size": "L",
    "notes": "The umbrella requirement; QUANT-013 and QUANT-017/018 are the named family entries in this batch, and QUANT-017 is the only one built. NON_RUST/NEW_DEPENDENCY apply only if the QML/kernel/reservoir families are pursued (would need Qiskit, C5 of ADR 0099's conflict register); the registry mechanism itself needs no new dependency. verify: integrated/deployable true->false. PlatformConfig::quantum_enabled defaults false (config.rs:534) and its only setter with_quantum() (config.rs:573) has no caller in backend/crates/apps/*/src; qip-api builds PlatformConfig::default() (main.rs:139) without it, so ComputeRouter's quantum branch executes in no composition root, and qip_quantum::benchmark::SolverBenchmark (the only harness) has no caller outside tests (grep of SolverBenchmark/ClassicalValidator/HostedProvider outside qip-quantum and tests/: none). Only the classical half of the one family runs in a deployed binary. Also corrected the test count: tests/optimization.rs is 24 tests (cargo test -p qip-optimization-engine: 'test result: ok. 24 passed' for optimization.rs), not 98."
  },
  {
    "id": "QUANT-002",
    "status": "PARTIAL",
    "current": "qip_optimization_engine::router::ComputeRouter::solve (router.rs:290-388) chooses the quantum answer only when RoutingPolicy::quantum_margin (router.rs:163, default 0.01 at :186) is cleared — an objective-only margin, checked every routing call rather than as a one-time promotion.",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:145-166 (RoutingPolicy incl. quantum_margin)",
      "backend/crates/services/qip-optimization-engine/src/router.rs:290-388 (solve, margin selection at 340-360)",
      "backend/crates/libs/qip-quantum/src/benchmark.rs:1-26 (module doc: quality is the only axis that decides anything)"
    ],
    "gap": "The gate checks only the objective margin; cost and latency are never compared against the classical run. qip_quantum::benchmark::BenchmarkReport records modelled runtime and money beside quality, but that module has no production caller (qip-optimization-engine/src/lib.rs gaps), and even there its own doc says quality alone decides. No promotion-gate workflow exists distinct from the per-cycle routing decision, so if a quantum path ever won on objective while being slower or dearer, ComputeRouter::solve would still choose it.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization -- a_quantum_answer_that_ties_loses_to_the_classical_baseline a_genuinely_better_feasible_quantum_answer_is_used_and_the_gain_is_measured; a three-record test admitting only the candidate that wins on objective, cost and latency together does not exist",
    "work_item": "E10",
    "tier": "T2",
    "size": "M",
    "notes": "verify: integrated/deployable true->false; the objective-margin comparison (router.rs:340-368) runs only when a quantum run exists, and PlatformConfig::quantum_enabled defaults false (config.rs:534) and its only setter with_quantum() (config.rs:573) has no caller in backend/crates/apps/*/src; qip-api builds PlatformConfig::default() (main.rs:139) without it, so ComputeRouter's quantum branch executes in no composition root. Corrected the default citation from router.rs:239 (inside quantum_assessment) to :186. Status PARTIAL confirmed."
  },
  {
    "id": "QUANT-003",
    "status": "PARTIAL",
    "current": "qip_quantum::qaoa::QaoaResult and solver::SolverCandidate are candidate-only types; benchmark::ValidatedSolution (benchmark.rs:76-107) has private fields, one constructor (ClassicalValidator::validate at :153-208) and no Deserialize. In production, qip-optimization-engine's ComputeRouter never calls this validator; instead it re-scores every candidate's weights (quantum included) against the real PortfolioProblem via its own score()/worst_violation() (router.rs:528-624) before a quantum answer can ever be chosen — an analogous, independently-tested guarantee for the one family that exists.",
    "evidence": [
      "backend/crates/libs/qip-quantum/src/benchmark.rs:76-107",
      "backend/crates/libs/qip-quantum/src/qaoa.rs:49",
      "backend/crates/services/qip-optimization-engine/src/router.rs:528-624"
    ],
    "gap": "The named type-level mechanism (ValidatedSolution/ClassicalValidator) is real and tested but has zero call sites outside qip-quantum's own tests and qip-optimization-engine's tests (current-state evidence, grep-confirmed); production relies on ComputeRouter's separate re-scoring instead, so the specific artifact the requirement's verification names is not reached from a composition root. Further: the production conversion path (QaoaResult -> SolverRun -> RoutingDecision weights) never passes through ClassicalValidator, and SolverRun/RoutingDecision/QaoaResult all derive Deserialize with public fields, so the 'nothing converts a candidate into a usable solution except the validator' property the verification names does not hold for the router path.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-quantum --test solvers -- a_solution_that_fails_classical_validation_is_refused_however_good_the_claim a_validated_solution_reports_the_recomputed_objective_rather_than_the_claim; cargo test -p qip-optimization-engine --test optimization -- an_infeasible_quantum_answer_is_rejected_however_good_its_objective",
    "work_item": "E10",
    "tier": "T1",
    "size": "S",
    "notes": "verify: integrated/deployable true->false. PlatformConfig::quantum_enabled defaults false (config.rs:534) and its only setter with_quantum() (config.rs:573) has no caller in backend/crates/apps/*/src; qip-api builds PlatformConfig::default() (main.rs:139) without it, so ComputeRouter's quantum branch executes in no composition root; ClassicalValidator/ValidatedSolution have no non-test caller. Evidence lines checked (benchmark.rs:76 ValidatedSolution, :153 validate; qaoa.rs:49 QaoaResult). Status PARTIAL confirmed."
  },
  {
    "id": "QUANT-004",
    "status": "COMPLETE",
    "current": "backend/crates/tests/qip-acceptance/tests/architecture.rs's transitive Cargo dependency-graph checks, each with a vacuity anchor: nothing_that_vetoes_executes_or_moves_money_can_reach_a_quantum_solver, a_quantum_solver_cannot_reach_anything_that_vetoes_executes_or_moves_money, and (the edge-specific form) no_edge_cell_can_reach_a_quantum_solver.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:986-1024",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1027-1057",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1059-1086"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test architecture -- nothing_that_vetoes_executes_or_moves_money_can_reach_a_quantum_solver a_quantum_solver_cannot_reach_anything_that_vetoes_executes_or_moves_money no_edge_cell_can_reach_a_quantum_solver",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "Strongest-evidenced requirement in this batch: three independent tests, each proving both directions of absence and each anchored against renaming/deleting the solver crate making the test vacuous. Static/structural rather than a runtime e2e crossing, which is why e2e is recorded false despite the guarantee being stronger than most e2e tests provide."
  },
  {
    "id": "QUANT-005",
    "status": "COMPLETE",
    "current": "qip_optimization_engine::router::ComputeRouter::solve always computes solve_classical first (router.rs:290-300, fn at :400); qip_quantum::benchmark::SolverBenchmark::run computes the baseline first and returns an error before any entrant runs if it fails (benchmark.rs:462-479). This is ADR 0006, already standing.",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:290-300,400-431",
      "backend/crates/libs/qip-quantum/src/benchmark.rs:462-479",
      "docs/adr/0006-classical-baseline-always.md",
      "backend/crates/runtime/qip-kernel/src/platform.rs:2650-2681,12148 (SolverRoutingJournal)"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization -- the_classical_baseline_is_always_computed; cargo test -p qip-quantum --test solvers -- the_benchmark_computes_a_classical_baseline_even_when_a_quantum_solver_is_available a_benchmark_whose_classical_baseline_fails_reports_nothing_at_all; cargo test -p qip-kernel --test solver_routing -- a_cycle_that_sizes_a_proposal_journals_the_solver_and_the_classical_baseline_it_was_measured_against (full Platform::run_cycle, journal read back from the durable log, but with PlatformConfig::default() i.e. no quantum provider: it asserts chosen is not quantum and measured_quantum_advantage is None, so it demonstrates the baseline journaling, not a quantum run beside its baseline)",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "The kernel test runs a whole cycle (SENSE through journaling) and reads the comparison back from the durable log rather than from memory, which is the closest thing to e2e evidence in this batch. verify: kept COMPLETE (router.rs:296 computes the classical baseline unconditionally with '?' before any provider call, on the ComputeRouter::solve path reached from qip-api via Platform::new platform.rs:4323 and PortfolioConstructor construction.rs:366; the_classical_baseline_is_always_computed attaches a SimulatedProvider and asserts both runs exist - ran: 'test result: ok. 24 passed' for optimization.rs, 18 passed for solvers.rs, 2 passed for qip-kernel solver_routing). e2e true->false: the kernel cycle test runs with no quantum provider and qip-acceptance e2e.rs section 9 calls SolverBenchmark directly rather than through a stage, so no cross-stage test shows a quantum result beside its baseline. Residual (not status-changing): the verification's property form over generated instances does not exist, and no router-level test proves a failing classical solve aborts before the provider is called (SolverBenchmark's form is tested). The quantum branch itself executes in no composition root, so in deployed binaries the invariant holds vacuously."
  },
  {
    "id": "QUANT-006",
    "status": "MISSING",
    "current": "nothing — no Lane concept (0-4 or any time-budget scheduling model) exists anywhere in the workspace. Quantum work, when quantum_enabled is set, runs synchronously inside qip-kernel's central DECIDE stage on PlatformConfig::cycle_interval (config.rs:258, default 5 minutes at :539), not on a minutes-to-days schedule.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/config.rs:258,539",
      "backend/crates/runtime/qip-kernel/src/platform.rs:13075 (stage_decide)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4322-4325 (router construction inside Platform::new)"
    ],
    "gap": "No Lane 4 (or any lane) concept is implemented; nothing declares a time budget per workload family, and quantum work is architecturally indistinguishable from every other computation the DECIDE stage performs in the same 5-minute cycle.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "no test exists; would be an inspection asserting every quantum workload family declares a Lane 4 time budget of minutes or longer, and no Lane 0-3 workload references qip-quantum or a Quantum Gateway",
    "work_item": "E10",
    "tier": "T3",
    "size": "L",
    "notes": "A platform-wide lane/scheduling model is a cross-cutting ARCH concern; this row scores only the quantum-specific placement, which cannot exist before the model does."
  },
  {
    "id": "QUANT-007",
    "status": "PARTIAL",
    "current": "qip_quantum::provider::HostedProvider/HostedTransport (provider.rs) submit and then poll on the calling thread, bounded by HostedTransport::max_polls (provider.rs:314, default 120 at :335) and poll_interval (:307, default 5s at :334); IbmQuantumConfig carries a QueuePolicy (max queue wait, shots, circuits/job, mode). infrastructure/egress/envoy.yaml:338-410 already declares loopback listeners for both the current and legacy IBM Quantum APIs, documented as sitting beside qip-api/qip-deepbrain and the execution node.",
    "evidence": [
      "backend/crates/libs/qip-quantum/src/provider.rs:287-335 (HostedTransport fields and defaults: poll_interval 5s, max_polls 120)",
      "backend/crates/libs/qip-quantum/src/provider.rs:712-739 (submit-then-poll loop, synchronous on the calling thread)",
      "infrastructure/egress/envoy.yaml:338-410",
      "backend/crates/libs/qip-quantum/src/solver.rs:741,767 (IbmQuantumConfig, QueuePolicy)"
    ],
    "gap": "solve_qubo blocks the calling thread across the whole poll loop instead of returning a job handle at once; nothing collects a result later by event or by polling a handle from outside the call. HostedProvider is constructed only in tests (grep for ::new/::connected/::with_credential outside tests/ found zero hits), and no environment applies the egress proxy or a node that would reach it (execution_nodes = {} everywhere).",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-quantum --test hosted -- a_job_that_does_not_finish_inside_the_budget_yields_its_identifier_not_an_answer a_service_that_goes_quiet_mid_poll_produces_an_error_and_never_a_result; an integration test with a stub endpoint holding a job queued past a caller's cycle boundary, proving submission returns without waiting, does not exist",
    "work_item": "E10",
    "tier": "T2",
    "size": "L",
    "notes": "verify: status and flags confirmed (HostedProvider/IbmQuantumSolver have no non-test caller; hosted.rs 'test result: ok. 19 passed'). Corrected evidence: QueuePolicy lives in solver.rs:767, not provider.rs."
  },
  {
    "id": "QUANT-008",
    "status": "PARTIAL",
    "current": "Edge half structurally proven: no_edge_cell_can_reach_a_quantum_solver (architecture.rs:1027-1057). Central half: quantum_enabled defaults false (config.rs:534) and only SimulatedProvider — in-process, no I/O — is ever attached (platform.rs:4323-4325); stage_decide (platform.rs:13075) calls ComputeRouter::solve synchronously before stage_act places orders (platform.rs:13326) in the same cycle.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1027-1057",
      "backend/crates/runtime/qip-kernel/src/config.rs:534",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4323-4325,13075,13326",
      "backend/crates/services/qip-optimization-engine/src/router.rs:171,188 (time_budget declared, never read: grep -rn time_budget backend/crates outside tests finds only these two lines)",
      "backend/crates/tests/qip-acceptance/tests/stress.rs:1576 (router-level unavailable-provider test)"
    ],
    "gap": "Nothing structurally stops a future central-cycle wiring of a blocking HostedProvider into the same synchronous stage_decide call that runs before stage_act; the property holds today only because SimulatedProvider is the sole production-reachable provider and quantum_enabled defaults off — a configuration discipline, not a structural guarantee like the edge one. No chaos test (the requirement's own specified verification method) exercises a failing or unreachable quantum gateway against cycle cadence. Also RoutingPolicy::time_budget (router.rs:171, default 60s at :188, documented as 'wall time the router will spend before returning what it has') is read nowhere in the workspace, so a slow provider is bounded only by the provider's own poll budget (HostedTransport: 120 polls x 5s = 10 minutes) - a declared control that cannot fire. The nearest existing test is router-level, not cadence-level: qip-acceptance stress.rs quantum_being_unavailable_produces_a_classical_answer_that_says_why, plus optimization.rs a_failed_quantum_attempt_leaves_the_classical_answer_standing / an_unreachable_provider_behaves_exactly_like_no_provider; none covers a provider that never answers.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "QUANT-007"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test architecture -- no_edge_cell_can_reach_a_quantum_solver (edge half proven); a chaos test asserting cycle cadence survives an unreachable/slow quantum gateway does not exist (rg quantum backend/crates/tests/qip-acceptance/tests/chaos.rs backend/crates/tests/qip-acceptance/tests/resilience.rs: no matches)",
    "work_item": "E10",
    "tier": "T1",
    "size": "M",
    "notes": "The edge guarantee is real and strong; the gap is specific to the central cycle, which has no analogous structural test. verify: status PARTIAL confirmed (no_edge_cell_can_reach_a_quantum_solver ran: 'test result: ok. 3 passed' with the two sibling quantum tests; chaos.rs/resilience.rs contain no 'quantum'). Added the unenforced RoutingPolicy::time_budget and the stress.rs router-level test to evidence/gap."
  },
  {
    "id": "QUANT-009",
    "status": "MISSING",
    "current": "nothing. AlphaFamily::Microstructure (qip-optimization-engine/src/universe.rs:55) is an unrelated strategy-taxonomy label with no quantum connection (rg -n microstructure across qip-quantum and qip-optimization-engine found only universe.rs/tiers.rs/horizons.rs taxonomy hits, none about a quantum benchmark baseline).",
    "evidence": [],
    "gap": "No quantum-assisted feature/ensemble/path-selection or hybrid-learning experiment targets microstructure, execution or reflex models at all, so there is no rule tying such an experiment's baseline to the registered classical microstructure model.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "QUANT-020"
    ],
    "blocked_by": "",
    "verification": "no test; would be a harness check that refuses a benchmark record whose classical baseline is not the registered microstructure model for that task",
    "work_item": "E10",
    "tier": "T3",
    "size": "M",
    "notes": "Depends on QUANT-020 (feature/ensemble selection), itself not built."
  },
  {
    "id": "QUANT-010",
    "status": "MISSING",
    "current": "nothing — no Causal Agency / Intervention Engine crate exists in this workspace at all (no crate under backend/crates named agency/intervention; find confirmed). qip-world-model/src/causal.rs and qip-kernel's causal_review.rs/central/causal.rs implement causal *review* of proposals, not an intervention/action-search engine, and none references qip-quantum.",
    "evidence": [],
    "gap": "The consumer this requirement needs (Causal Agency / Intervention Engine, blueprint Phase 10) does not exist in the repository, so there is nothing for quantum-assisted combinatorial search to plug into.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "no test; would be an integration test feeding a permitted action set and constraints and asserting the search returns ranked, constraint-satisfying, classically-compared candidate plans reaching the (not yet built) agency pipeline and nothing else",
    "work_item": "E16",
    "tier": "T3",
    "size": "XL",
    "notes": "P3 in the register because it depends on the Causal Agency / Intervention Engine, which is entirely unbuilt. External-action outputs of that future engine would separately be BLOCKED under ADR 0021/paper-trading precedence; the ranking/search itself is shadow-only compatible once the engine exists."
  },
  {
    "id": "QUANT-011",
    "status": "MISSING",
    "current": "nothing — qip-world-model has no quantum reference at all (rg -il quantum backend/crates/services/qip-world-model/src: empty) and no branch-selection mechanism, quantum or classical, exists to choose which counterfactual branches receive expensive simulation.",
    "evidence": [],
    "gap": "No scenario/ensemble search selects high-information branches or intervention/no-intervention branches for simulation or experiment design; the World Model has no such selector to extend with a quantum-assisted path.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "no test; would compare, for a fixed simulation budget, the branch set a selector picks against a classical diversity/information selector's choice from the same candidate pool",
    "work_item": "E10",
    "tier": "T3",
    "size": "L",
    "notes": "P3 in the register: intervention/no-intervention branches need the Agency engine (QUANT-010, also missing); selecting world-model scenario subsets without interventions is the separate QUANT-025 (P2)."
  },
  {
    "id": "QUANT-012",
    "status": "PARTIAL",
    "current": "qip_optimization_engine::router::ComputeRouter's score()/worst_violation() (router.rs:528-624) re-evaluates every candidate's weights — quantum included — against the real PortfolioProblem (not the relaxed QUBO), including bounds and the cardinality limit the QP/QUBO relaxations drop, and solve() only ever chooses a quantum answer that is_usable() (feasible). Separately, qip_quantum::benchmark::ClassicalValidator::validate (benchmark.rs:153-208) re-evaluates a SolverCandidate's assignment against the QUBO objective directly and refuses a claim that misses the recomputation by more than tolerance; HostedProvider recomputes energy from the assignment on the way in as a second, independent check (provider.rs, module doc lines 41-53).",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:340-360,528-624",
      "backend/crates/libs/qip-quantum/src/benchmark.rs:109-208",
      "backend/crates/libs/qip-quantum/src/provider.rs:41-53"
    ],
    "gap": "The requirement's two refusals are split across mechanisms and neither mechanism does both where it matters. (1) The production-linked check, ComputeRouter's score()/worst_violation() (router.rs:528-624), re-evaluates the weights against the real PortfolioProblem and refuses infeasible answers, but it discards the provider's claimed value (QaoaResult.energy) rather than comparing it, so a provider whose claim disagrees with its own assignment is not refused on that ground. (2) ClassicalValidator::validate (benchmark.rs:153-208) and HostedProvider's inbound recomputation refuse a claim/recomputation mismatch, but they re-evaluate only against the QUBO, so they cannot see constraints the QUBO relaxation dropped, and neither has a non-test caller. (3) The router's quantum branch executes in no composition root (quantum_enabled has no setter in apps/*/src), and the 'benchmark verdict or promotion' consumers do not run in production (SolverBenchmark has no production caller; no promotion gate exists, QUANT-002). Needed: one classical validator, called by ComputeRouter on every quantum candidate, that refuses both a claim mismatch and a real-problem infeasibility, with a unit test for each refusal.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization -- an_infeasible_quantum_answer_is_rejected_however_good_its_objective; cargo test -p qip-quantum --test solvers -- a_solution_that_fails_classical_validation_is_refused_however_good_the_claim an_assignment_of_the_wrong_shape_is_refused_before_it_is_scored",
    "work_item": "E10",
    "tier": "T0",
    "size": "S",
    "notes": "Scoped to the one family that exists (portfolio allocation). The production check is ComputeRouter's own re-scoring rather than qip_quantum::benchmark::ClassicalValidator, which exists and is tested but has no production caller (the assessor read this as not a gap; the verify note below explains why it is one). verify: COMPLETE -> PARTIAL, integrated/deployable true->false. The verification check requires 'the validator' to refuse both a claim mismatch and a dropped-constraint violation; the production-linked router refuses only infeasibility and ignores the claim, and the validator that refuses mismatches checks only the QUBO and is unreachable from any composition root. Tests re-run and passing (optimization.rs 24, solvers.rs 18, hosted.rs 19), but they demonstrate the two halves in two different, partly unreachable mechanisms."
  },
  {
    "id": "QUANT-013",
    "status": "MISSING",
    "current": "nothing that runs clustering, selection or model selection over an evidence graph, quantum or classical. Evidence modules do exist but none is a graph workload: qip-reasoning-engine/src/evidence.rs (Evidence with reliability, EvidenceSet independence grouping), qip-mesh's L0 immutable EvidenceStore (tests/evidence_store.rs), qip-streaming/src/provenance.rs, qip-lifecycle/src/evidence.rs (strategy-promotion evidence). None references qip-quantum.",
    "evidence": [
      "backend/crates/services/qip-reasoning-engine/src/evidence.rs:184,298 (Evidence, EvidenceSet)",
      "backend/crates/services/qip-mesh/tests/evidence_store.rs (L0 immutable EvidenceStore)"
    ],
    "gap": "No evidence graph exists for a quantum-assisted or classical clustering/selection method to run against.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "no test; would run a quantum-assisted clustering and a classical clustering on the same evidence-graph snapshot and record quality, runtime and cost for both",
    "work_item": "E08",
    "tier": "T3",
    "size": "L",
    "notes": "verify: status MISSING confirmed, but corrected 'current': the assessor's 'find backend/crates for evid*/truth*/provenance* returns nothing' is false - it returns qip-lifecycle/src/evidence.rs, qip-reasoning-engine/src/evidence.rs, qip-mesh/tests/evidence_store.rs, qip-streaming/src/provenance.rs and others. None is an evidence-graph clustering/selection workload."
  },
  {
    "id": "QUANT-014",
    "status": "MISSING",
    "current": "No guard exists, but the surface the prohibition protects partly does: qip_reasoning_engine::evidence::Evidence carries a public `reliability` field documented as 'how much to trust it' (evidence.rs:199-200), EvidenceSet groups by originating source for corroboration, and qip-mesh holds an L0 immutable EvidenceStore. Today neither qip-reasoning-engine nor qip-mesh reaches qip-quantum and qip-quantum reaches neither (cargo tree -e normal: 0 matches each; qip-quantum depends only on qip-core, qip-numerics, qip-transport), so the prohibition holds by absence - but qip-kernel reaches both qip-quantum and qip-reasoning-engine, and Evidence.reliability is a public field, so nothing structural stops a kernel change from writing a quantum output into trust.",
    "evidence": [
      "backend/crates/services/qip-reasoning-engine/src/evidence.rs:184-200 (Evidence, pub reliability)",
      "backend/crates/libs/qip-quantum/Cargo.toml [dependencies] (qip-core, qip-numerics, qip-transport only)"
    ],
    "gap": "No test pins the absence. An architecture-graph test in QUANT-004's pattern (qip-quantum cannot reach qip-reasoning-engine/qip-mesh evidence writers, and vice versa, with a vacuity anchor) is buildable now against the existing evidence crates and does not need the Evidence & Truth Fabric; the kernel-side seam (a quantum result never becomes an Evidence or changes its reliability) needs a unit test at the kernel. The second half of the verification (feeding a quantum clustering result through the pipeline) waits on QUANT-013.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "QUANT-013"
    ],
    "blocked_by": "",
    "verification": "no test; once an evidence store exists, would need an architecture-graph test (QUANT-004's pattern) proving no writer reachable from qip-quantum can set its authenticity/trust fields, plus a unit test feeding a quantum clustering result through the evidence pipeline and asserting every item's authenticity/confidence is unchanged",
    "work_item": "E08",
    "tier": "T2",
    "size": "M",
    "notes": "P0 prohibition with nothing yet built for it to guard; the gap is that the surface it would protect does not exist, not that a known violation exists. verify: status MISSING kept (no guard or test implements the prohibition), but corrected 'current' and 'gap': the assessor said no evidence trust field exists; qip-reasoning-engine's Evidence.reliability is one, and a structural test is buildable today rather than waiting on the fabric."
  },
  {
    "id": "QUANT-015",
    "status": "PARTIAL",
    "current": "The one family that exists (portfolio allocation) has all three elements, but only as hardcoded fields rather than a declared, refusable registration: solve_classical (router.rs:400, fn body 400-431) is the baseline, PortfolioProblem::objective_at (problem.rs:284) is the objective both paths are scored on, and RoutingPolicy::quantum_margin (router.rs:163, default 0.01 at :186) is the threshold.",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:400-431",
      "backend/crates/services/qip-optimization-engine/src/problem.rs:284",
      "backend/crates/services/qip-optimization-engine/src/router.rs:145-166"
    ],
    "gap": "No workload-registration API or type exists in qip-quantum or qip-optimization-engine; the three elements are implicit RoutingPolicy/PortfolioProblem struct fields for the single hardcoded family rather than a contract a family registration could be refused for lacking.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization -- every_routing_decision_records_the_classical_objective (indirect evidence the three elements exist for the one family); a dedicated 'registering a workload family is refused when it lacks a baseline / a shared objective / a threshold' unit test does not exist because no registration mechanism exists",
    "work_item": "E10",
    "tier": "T1",
    "size": "M",
    "notes": "verify: integrated/deployable true->false; the threshold (quantum_margin) is only evaluated in ComputeRouter's quantum branch, which executes in no composition root, and no registration contract exists. Corrected default citation router.rs:239 -> :186. Status PARTIAL confirmed."
  },
  {
    "id": "QUANT-016",
    "status": "PARTIAL",
    "current": "qip_optimization_engine::router::ComputeRouter::quantum_assessment (router.rs:232-283) declines a continuous problem unless RoutingPolicy::quantum_on_continuous, declines a problem below RoutingPolicy::minimum_assets_for_quantum (default 50), and declines a problem above the provider's max_qubits — each with a named reason returned as quantum_note; qip-kernel journals quantum_note every cycle via SolverRoutingJournal (platform.rs:2650-2681,12148).",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:232-283",
      "backend/crates/runtime/qip-kernel/src/platform.rs:2650-2681,12148"
    ],
    "gap": "The 'exceeds the provider's qubits' decline branch (router.rs:268-277) has no dedicated test — no test in optimization.rs constructs a problem larger than a provider's max_qubits and asserts the decline and its reason (grep for 'exceeds the provider' / a qubits-sized assertion in tests/optimization.rs found none).",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization -- a_continuous_problem_never_goes_to_a_quantum_solver a_small_discrete_problem_does_not_justify_a_quantum_attempt a_simulated_result_is_labelled_as_not_being_evidence_of_advantage; the max_qubits decline branch needs a fourth test",
    "work_item": "E10",
    "tier": "T0",
    "size": "S",
    "notes": "Three of the four required cases (continuous, below minimum, eligible-attempt) are named-test-covered; only the above-max-qubits case is untested, which is why this is PARTIAL rather than COMPLETE. verify: integrated/deployable true->false. quantum_assessment runs on every production solve but returns at its first check ('no quantum provider is configured', router.rs:233-239) in every deployed binary, because PlatformConfig::quantum_enabled defaults false (config.rs:534) and its only setter with_quantum() (config.rs:573) has no caller in backend/crates/apps/*/src; qip-api builds PlatformConfig::default() (main.rs:139) without it; the continuous/minimum-size/max-qubits declines are evaluated only in tests. Confirmed no router test for the max_qubits decline (grep 'exceeds the provider' across backend/crates tests: none; quantum.rs the_simulator_refuses_a_problem_it_cannot_hold and hosted.rs the_device_the_service_describes_bounds_the_problem_independently_of_configuration are provider-level refusals, not the router's recorded decline). Status PARTIAL confirmed."
  },
  {
    "id": "QUANT-017",
    "status": "PARTIAL",
    "current": "qip_optimization_engine::router::ComputeRouter solves PortfolioProblem classically every time (router.rs:400-431) and by QAOA via qip_quantum when a provider is attached (router.rs:456-469, solve_quantum). qip-kernel wires SimulatedProvider only when quantum_enabled (platform.rs:4322-4325; config.rs:252,534 default false). Cardinality and generic linear (budget/exposure) constraints are enforced against the real problem in worst_violation() (router.rs:558-624); family-level diversification (correlation clustering) is a separate, classical-only mechanism (families.rs) not fed by the quantum solve.",
    "evidence": [
      "backend/crates/services/qip-optimization-engine/src/router.rs:400-469,558-624",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4322-4325",
      "backend/crates/runtime/qip-kernel/src/config.rs:252,534"
    ],
    "gap": "No production deployment has ever set quantum_enabled=true — PlatformConfig::with_quantum() (config.rs:573-575) has zero callers in crates/apps/**/src, grep-confirmed — and only SimulatedProvider is ever attachable in production code, so no hardware QAOA allocation has ever been produced; RoutingDecision::quantum_note itself says a simulated result is not evidence of advantage. Family-level diversification (families.rs) is classical-only and separate from the QUBO/QAOA solve.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization --no-fail-fast (optimization.rs: 24 tests; 98 across the crate); no kernel or acceptance e2e test ever sets quantum_enabled=true through a full Platform::run_cycle (rg quantum_enabled backend/crates/runtime/qip-kernel/tests: no hits)",
    "work_item": "E10",
    "tier": "T1",
    "size": "M",
    "notes": "The most complete family in-tree; the remaining gap is entirely about reaching a deployed process with quantum_enabled=true, not about missing solver logic. verify: integrated/deployable true->false - the row's own gap states with_quantum() has zero callers in apps, which contradicts integrated=true; the QUBO/QAOA allocation executes in no composition root. Corrected test count (optimization.rs 'test result: ok. 24 passed'). Status PARTIAL confirmed."
  },
  {
    "id": "QUANT-018",
    "status": "MISSING",
    "current": "nothing — qip-arbitrage (backend/crates/edge/qip-arbitrage) has classical multi-leg search already (search.rs, graph.rs, legs.rs, pricing.rs) but no quantum or QUBO reference at all (rg -il quantum backend/crates/edge/qip-arbitrage/src: empty).",
    "evidence": [],
    "gap": "No QUBO/graph formulation of multi-leg cycle/path selection exists anywhere. qip-arbitrage lives in crates/edge, where no_edge_cell_can_reach_a_quantum_solver (architecture.rs:1027-1057) structurally forbids it from calling a solver directly, so this selection could only ever reach a cell as a pre-computed central-side policy, which does not exist yet either.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "no test; would score recorded multi-leg opportunity graphs' selected cycle/path sets for feasibility against inventory, settlement, capital and concurrency constraints, and against a classical graph solver on the same instance",
    "work_item": "E11",
    "tier": "T2",
    "size": "L",
    "notes": "Must land as a central pre-computed policy handed to cells, never a direct edge-to-solver call, per the structural boundary architecture.rs already proves (no_edge_cell_can_reach_a_quantum_solver)."
  }
]
