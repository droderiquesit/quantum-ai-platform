[
  {
    "id": "EXEC-020",
    "status": "MISSING",
    "current": "nothing. No per-venue, per-mode legal/operational-support registry exists anywhere in the tree. The only mode-shaped gate in-tree is qip_edge::cell::Cell's GATE_LIVE_VENUE (backend/crates/edge/qip-edge/src/cell.rs:68, refused at cell.rs:2960 and cell.rs:3864), which refuses every order to a live-class venue outright (the paper-trading boundary) -- a coarse live-vs-paper class check, not a per-venue registry distinguishing which of EXEC-001/013-019's eight modes is enabled for that venue.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/cell.rs:68 (GATE_LIVE_VENUE)",
      "backend/crates/edge/qip-edge/src/cell.rs:3864 (metrics.refusal(GATE_LIVE_VENUE) inside Cell::send)",
      "backend/crates/edge/qip-edge/tests/paper_boundary.rs:229-301 (proves the live-class refusal; a different, coarser property than a per-mode registry)",
      "grep -rn 'supported_mode|enabled_mode|VenueMode|permitted_mode' backend/crates --type rust: no matches"
    ],
    "gap": "No config type, gate or registry exists that enables or disables an individual execution mode (continuous quoting, order taking, liquidity provision, routing, derivatives, event contracts, decentralised venues, physical marketplaces) per venue based on legal/operational support; with nothing built, the fail-closed default ('with nothing enabled, every mode is refused') happens to hold only because no mode-aware order path exists at all, not because a registry enforces it.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-001",
      "EXEC-013",
      "EXEC-014",
      "EXEC-015",
      "EXEC-016",
      "EXEC-017",
      "EXEC-018",
      "EXEC-019"
    ],
    "blocked_by": "",
    "verification": "Test needed: construct a simulated venue with no mode enabled and assert an order attempt in each of the eight modes is refused before an order object exists (mirroring GATE_LIVE_VENUE's own paper_boundary.rs pattern), then enable one mode and assert only that mode admits.",
    "work_item": "E11",
    "tier": "T2",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-021",
    "status": "COMPLETE",
    "current": "qip_edge::quoting::QuoteBudget/Admission (backend/crates/edge/qip-edge/src/quoting.rs) is the per-venue token-bucket cancel/replace budget. Cell::spend_requote (cell.rs:7767) charges a requote its two messages (cancel+replace) against the venue's QuoteBudget and refuses under GATE_QUOTE_BUDGET (cell.rs:164) when it cannot fund both; Cell::quote_depletion/requote_fundable/spend_requote (cell.rs:7719-7776) are called directly from qip-edge-node's Requoter (backend/crates/apps/qip-edge-node/src/reprice.rs:537,575,607), a composition-root source file.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/cell.rs:164 (GATE_QUOTE_BUDGET) and :7767 (Cell::spend_requote)",
      "backend/crates/apps/qip-edge-node/src/reprice.rs:537,575,607 (Requoter calls quote_depletion/requote_fundable/spend_requote)",
      "backend/crates/edge/qip-edge/tests/quoting.rs:462 a_cancel_is_funded_from_the_reserve_after_placements_have_spent_the_rest (a pure cancel is still admitted after the replace budget is spent)",
      "backend/crates/edge/qip-edge/tests/quoting.rs:755 a_requote_is_refused_rather_than_funded_from_the_reserve_a_mass_cancel_needs (a replace beyond budget is refused before it is sent)",
      "backend/crates/edge/qip-edge/src/quoting.rs:794 the_bucket_refills_at_the_configured_rate_and_never_past_its_capacity"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-edge --test quoting -- a_cancel_is_funded_from_the_reserve_after_placements_have_spent_the_rest a_requote_is_refused_rather_than_funded_from_the_reserve_a_mass_cancel_needs",
    "work_item": "E11",
    "tier": "T0",
    "size": "S",
    "notes": "Behaviour and tests are hardened; the only gap is qip-edge-node's non-deployment (execution_nodes = {} in dev/stage/test/prod terraform.tfvars), a deployment gap shared by the whole edge chain and not specific to this requirement."
  },
  {
    "id": "EXEC-022",
    "status": "MISSING",
    "current": "nothing implements a venue-specific microstructure model. qip_market::microstructure::MicrostructureMetrics::compute (backend/crates/libs/qip-market/src/microstructure.rs:39) computes quoted/effective/realised spread, price impact, order-flow and quote imbalance and Kyle's lambda from a window of quotes/trades, but takes no venue parameter and has zero call sites outside its own crate's tests. qip_kernel::quote_loop (backend/crates/runtime/qip-kernel/src/quote_loop.rs) computes its own bar-resolution volatility/adverse-selection/imbalance proxies (module doc, quote_loop.rs:22-70) as one venue-agnostic set of statistics per instrument, and its own doc comment explicitly distinguishes its proxy from MicrostructureMetrics rather than reusing it (quote_loop.rs:37-43).",
    "evidence": [
      "backend/crates/libs/qip-market/src/microstructure.rs:16-39 (MicrostructureMetrics: defined, no venue field, unreached in production)",
      "backend/crates/runtime/qip-kernel/src/quote_loop.rs:22-70 (module doc: one venue-agnostic proxy set)",
      "grep -n 'MicrostructureMetrics::compute' backend/crates/runtime backend/crates/apps backend/crates/edge --type rust: no matches outside the definition and a doc comment"
    ],
    "gap": "No model or parameter set varies by venue anywhere in the pricing path; MicrostructureMetrics is defined and unit-tested in isolation but never called from production code, and the only production pricing loop (quote_loop.rs) applies one QuotePolicy uniformly regardless of venue.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-002"
    ],
    "blocked_by": "",
    "verification": "Test needed: replay the same order flow against two simulated venues with different microstructure parameters and assert each venue gets its own queue-value/fill-probability estimate, scored against that venue's own replayed fills. No such fixture exists today.",
    "work_item": "E11",
    "tier": "T2",
    "size": "L",
    "notes": ""
  },
  {
    "id": "EXEC-023",
    "status": "PARTIAL",
    "current": "qip_edge::reservation::RegionTable/RegionAllocation (backend/crates/edge/qip-edge/src/reservation.rs:121,150,522) is the one capital ledger any working order in a Cell commits against -- Cell::return_region_capital_for_unfilled and the region_allocation match sites (cell.rs:4337,6405,7386) are generic to any strategy's working order, not scoped to arbitrage -- wired from a composition-root file, backend/crates/apps/qip-edge-node/src/allocation.rs, via Cell::with_region_allocation (cell.rs:1275). Because the ledger is generic to Cell rather than scoped to one strategy kind, any deployed strategy, arbitrage included, reserves through it, and a second hold against already-committed capital is refused whole rather than reduced.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/reservation.rs:121,150,522 (RegionAllocation, Rebase, RegionTable)",
      "backend/crates/edge/qip-edge/src/cell.rs:1275 (Cell::with_region_allocation), :4337-4360 (return_region_capital_for_unfilled, generic to any working order)",
      "backend/crates/apps/qip-edge-node/src/allocation.rs (composition-root wiring)",
      "backend/crates/edge/qip-edge/src/reservation.rs:634 a_second_hold_against_the_same_capital_is_refused_whole_rather_than_reduced",
      "backend/crates/edge/qip-edge/src/reservation.rs:731 one_cells_sweep_leaves_another_cells_holds_standing",
      "backend/crates/runtime/qip-kernel/src/quote_loop.rs:13-20 (module doc: no path from market making to an order, a broker or a venue exists)"
    ],
    "gap": "The ledger is structurally shared (any strategy that reaches Cell::work's order path reserves through it) but no market-making strategy exists in-tree -- qip-strategy has no market-making compiled-strategy type, and the central quote_loop never sends an order by its own module doc's own admission -- so no test interleaves a market-making hold against an arbitrage hold on the same pool. The requirement's specific claim (MM and arbitrage sharing one fabric) is structurally plausible but unverified, because MM never reserves anything.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-001"
    ],
    "blocked_by": "",
    "verification": "Test needed: deploy both a market-making strategy and an arbitrage cycle to the same Cell/region and interleave their reservation requests, asserting the pool total is never exceeded. Cannot be written until a market-making strategy exists (EXEC-001/002).",
    "work_item": "E11",
    "tier": "T2",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-024",
    "status": "MISSING",
    "current": "nothing. No 'Market Factory' actor exists anywhere in the tree. qip_execution_engine::origination::OriginationMandate::admit (backend/crates/services/qip-execution-engine/src/origination.rs:223) computes and admits a bound (the five-gate origination check, see EXEC-027) but constructs no market, calls no create/list function and publishes nothing; the module's own header states it deliberately 'does not construct an Order, name a venue, or reach a Broker' (origination.rs:22-26). qip_brokers::exchange::SimulatedExchange::list (exchange.rs:380) and list_synthetic (exchange.rs:405) are the simulated venue's own catalogue: the only non-test caller is qip-edge-node's SimulatedGateway::ensure_listed (backend/crates/apps/qip-edge-node/src/gateway.rs:569-586), which lists a synthetic instrument at the in-process paper venue when an order names one. That models an external venue's catalogue; it is not the platform creating a market and publishes no pricing rules.",
    "evidence": [
      "backend/crates/services/qip-execution-engine/src/origination.rs:1-38 (module doc: constructs no Order, names no venue, reaches no Broker)",
      "backend/crates/services/qip-brokers/src/exchange.rs:380,405 (SimulatedExchange::list / list_synthetic, the simulated venue's own catalogue)",
      "backend/crates/apps/qip-edge-node/src/gateway.rs:569-586 (SimulatedGateway::ensure_listed -> list_synthetic: the only non-test caller; unconnected to origination)",
      "grep -rni 'market.factory|MarketFactory|pricing_rule|PricingRule|create_market|CreatedMarket' backend/crates --include=*.rs: no matches"
    ],
    "gap": "No Market Factory exists to create a market or publish anything about it; only the pre-creation approval gate (OriginationMandate) exists, and it is itself unreached from any composition root (see EXEC-027).",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-006"
    ],
    "blocked_by": "",
    "verification": "Test needed once EXEC-006 exists: assert a created simulated market has a versioned pricing-rule document recorded in the event log, and that every quote made in it reproduces from the rule version in force at the time.",
    "work_item": "E11",
    "tier": "T3",
    "size": "L",
    "notes": "verify: status unchanged (MISSING confirmed; no Market Factory, pricing-rule document or rule versioning found under backend/, frontend/ or infrastructure/). Corrected a false claim in 'current': SimulatedExchange::list is not called from the qip-cli demo (qip-cli/src/demo/mod.rs has no call to it); its one non-test caller is qip-edge-node's SimulatedGateway::ensure_listed via list_synthetic. Evidence updated to match."
  },
  {
    "id": "EXEC-025",
    "status": "MISSING",
    "current": "nothing. No created-market concept exists in the tree (see EXEC-006/EXEC-024), so there is nothing for a continuous economic-validity measurement to run against. No periodic/continuous measurement loop for a created market exists in qip-kernel's LEARN stage or anywhere else.",
    "evidence": [
      "grep -rin 'economically valid|economic validity' backend/crates --type rust: no matches outside this requirement's own text",
      "backend/crates/services/qip-execution-engine/src/origination.rs (the only origination-adjacent code; admits a bound, does not create or monitor a market)"
    ],
    "gap": "No created market exists to measure, and no measurement cadence or 'invalid finding' record exists for one.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-006"
    ],
    "blocked_by": "",
    "verification": "Test needed once EXEC-006 exists: replay a simulated created market while its volume decays until its economics turn negative, and assert measurements appear at a fixed cadence with an 'invalid' finding recorded at the turn.",
    "work_item": "E11",
    "tier": "T3",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-026",
    "status": "MISSING",
    "current": "nothing. As with EXEC-025, no created market exists to re-evaluate. EXEC-007's own venue/product/jurisdiction permission gate is not found wired to any create action (see EXEC-024/EXEC-027), so there is also no one-time permission check for a continuing check to repeat against.",
    "evidence": [
      "grep -rin 'venue.*permit|product.*permit|governance.*permit' backend/crates --type rust: hits are in unrelated compliance/entitlement modules (qip-financial/src/constraints.rs, qip-capital/src/ledger/*), none of which gate market creation",
      "backend/crates/services/qip-execution-engine/src/origination.rs (no re-evaluation loop of any kind)"
    ],
    "gap": "No created market exists to re-evaluate, and no continuing legal-validity check of any kind exists.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-007"
    ],
    "blocked_by": "",
    "verification": "Test needed once EXEC-006/EXEC-007 exist: revoke one of the three permissions for a simulated created market and assert an invalid finding within one measurement cycle, then restore it and assert the finding clears.",
    "work_item": "E11",
    "tier": "T3",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-027",
    "status": "PARTIAL",
    "current": "qip_execution_engine::origination::OriginationMandate::admit (origination.rs:223) implements the blueprint's five-gate governed-approval check, including gate five, ClassApproval -- a human operator identity, digest and per-instrument-class match (origination.rs:339-358). OriginationMandate is built with private fields, one constructor and no #[derive(Deserialize)] so it 'cannot be decoded into existence out of a config file, a policy frame or any other payload' (origination.rs:12-20), a property qip-acceptance checks by source inspection.",
    "evidence": [
      "backend/crates/services/qip-execution-engine/src/origination.rs:223 (OriginationMandate::admit, the five gates)",
      "backend/crates/services/qip-execution-engine/src/origination.rs:339-358 (gate five: per-class human ClassApproval)",
      "backend/crates/services/qip-execution-engine/src/origination.rs:476 a_request_that_clears_all_five_gates_is_admitted",
      "backend/crates/tests/qip-acceptance/tests/quote_loop.rs:245 an_origination_mandate_cannot_be_decoded_into_existence",
      "grep -n 'OriginationMandate::admit|origination::' backend/crates/runtime backend/crates/apps --type rust: zero matches outside qip-execution-engine's own quoting.rs QuoteReference::Originated variant",
      "backend/crates/services/qip-execution-engine/src/origination.rs:177-185 (ClassApproval: operator and digest are free-text pub String fields)",
      "backend/crates/apps/qip-edge-node/src/gateway.rs:569-586 (SimulatedGateway::ensure_listed -> SimulatedExchange::list_synthetic, ungated)"
    ],
    "gap": "The approval gate is implemented and unit/acceptance-tested but reached from nowhere: no composition root (qip-api/fastbrain/deepbrain/qip-edge-node/qip-cli) ever calls OriginationMandate::admit or constructs QuoteReference::Originated, and there is no create/list/seed function for the gate to actually stand in front of, so the requirement's own verification (an unapproved proposal stops at the first gate against a live create path; an approved one is admitted) cannot be exercised because that path does not exist. Two further shortfalls against the statement's 'recorded approval': ClassApproval (origination.rs:177-185) carries the operator as a plain pub String and the digest as a String, so 'authenticated operator identity' is a doc-comment claim, not something the type or a check proves; and admit records nothing anywhere (the service performs no I/O and nothing journals the approval or the refusal), so no approval is recorded in the event log. Separately, a 'list' operation is already reachable without the gate: qip-edge-node's SimulatedGateway::ensure_listed (gateway.rs:569-586) lists synthetic instruments at the in-process paper venue. That is the venue simulator modelling a catalogue rather than the platform listing a market, but the architecture test this requirement names would have to draw that line explicitly.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-006",
      "EXEC-008",
      "EXEC-009"
    ],
    "blocked_by": "",
    "verification": "Test needed: an architecture test asserting create/list/seed are reachable only from a governed-workflow function that itself requires an OriginationMandate. Cannot be written until EXEC-006 gives the gate something to stand in front of.",
    "work_item": "E11",
    "tier": "T2",
    "size": "M",
    "notes": "verify: status and flags unchanged (PARTIAL confirmed: gate implemented and tested, no composition-root caller found by grep over backend/crates/apps and runtime). Extended 'gap' with two shortfalls the assessor did not name -- the approval is neither authenticated by type nor recorded to the event log -- and noted the ungated simulated-venue listing path an EXEC-027 architecture test would have to exclude."
  },
  {
    "id": "EXEC-028",
    "status": "PARTIAL",
    "current": "The checklist's individual pieces exist unevenly. qip_execution_engine::quoting (fair-value/skew/half-spread math) and qip_kernel::quote_loop (backend/crates/runtime/qip-kernel/src/quote_loop.rs, reached from Platform via quote_loop::review at platform.rs:13584,13791) assemble QuoteInputs and produce a QuoteDecision/QuotePair for the cycle report only. EXEC-021's cancel/replace budget and EXEC-023's reservation fabric are hardened at the edge (qip-edge). But quote_loop's own module doc states explicitly there is 'no path from it to an order, a broker or a venue' (quote_loop.rs:13-20), and no market-making CompiledStrategy type exists in qip-strategy, so nothing ever quotes continuously and sends orders end to end.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/quote_loop.rs:1-20 (module doc: 'sent nowhere', no path from it to an order/broker/venue)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:13584,13791 (quote_loop::review reached from Platform)",
      "backend/crates/edge/qip-edge/src/quoting.rs + tests/quoting.rs (EXEC-021's budget, hardened)",
      "backend/crates/edge/qip-edge/src/reservation.rs (EXEC-023's fabric, hardened)",
      "ls backend/crates/edge/qip-strategy/src: catalogue.rs, compile.rs, ir.rs, lib.rs, model.rs, program.rs, runtime.rs -- no market-making strategy type among them"
    ],
    "gap": "The checklist names a whole loop (continuous two-sided quoting that actually sends and fills orders while staying within every limit, reproducible from the event log). The pieces the loop would be built from exist in isolation, but the loop itself does not: the central quote review never reaches a broker, and no market-making strategy is ever compiled and deployed to an edge Cell.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-001",
      "EXEC-002",
      "EXEC-003",
      "EXEC-004",
      "EXEC-005",
      "EXEC-021",
      "EXEC-022",
      "EXEC-023"
    ],
    "blocked_by": "",
    "verification": "Test needed: a qip-acceptance e2e.rs scenario quoting continuously on two simulated venues for a replayed day, staying within rate/budget/reservation limits, with every quote and fill reproducible from the event log. None exists because the order-sending half of market making does not exist.",
    "work_item": "E11",
    "tier": "T3",
    "size": "XL",
    "notes": "The live form of this target is BLOCKED under ADR 0003/0021 by design (paper-trading boundary) and is never built; the gap scored here is entirely in the paper/simulated form, which nothing in ADR 0099's conflict register prevents building."
  },
  {
    "id": "EXEC-029",
    "status": "MISSING",
    "current": "nothing end to end. The only fragment is the unreached origination approval gate (EXEC-027); creation (EXEC-006), seeding (EXEC-008), continuous inventory/risk management (EXEC-009), pricing-rule publication (EXEC-024) and continuous economic/legal validity (EXEC-025/EXEC-026) are all unimplemented.",
    "evidence": [
      "backend/crates/services/qip-execution-engine/src/origination.rs (the only origination-adjacent code, unreached from any composition root)",
      "grep -rn 'market creation|MarketCreation|create_market' backend/crates --type rust: only a label string in backend/crates/apps/qip-cli/src/blueprint.rs:45 ('Execution venue mesh, market making, market creation'), no functional code"
    ],
    "gap": "The whole governed-market-creation path is missing: EXEC-006 (create), EXEC-008 (seed), EXEC-009 (manage), EXEC-024 (publish rules) and EXEC-025/026 (continuous validity) are all unimplemented, and EXEC-027's gate, while implemented, is reached from nothing.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-006",
      "EXEC-007",
      "EXEC-008",
      "EXEC-009",
      "EXEC-024",
      "EXEC-025",
      "EXEC-026",
      "EXEC-027"
    ],
    "blocked_by": "",
    "verification": "Test needed: in simulation, a proposed market passes the permission gate and governed approval, is created and seeded, and publishes its rules; it is risk-managed under one-sided flow and found invalid when a permission is revoked, with each step in the event log. None of this exists to test.",
    "work_item": "E11",
    "tier": "T3",
    "size": "XL",
    "notes": "The live form is BLOCKED under ADR 0021 by design and never built; the paper/simulated form scored here is not blocked by ADR 0099's register, it simply does not exist yet."
  },
  {
    "id": "EXEC-030",
    "status": "PARTIAL",
    "current": "The prohibition half holds: infrastructure/egress/envoy.yaml (the one committed proxy bootstrap) names no venue/broker/exchange host, and infrastructure/terraform/modules/egress-proxy/variables.tf:57-59 refuses at plan time any allowlist entry whose host contains 'venue', 'broker' or 'exchange', so venue traffic cannot be routed through the shared loopback Envoy. The execution-node module declares a separate, per-venue direct egress firewall rule (modules/execution-node/main.tf:569-590, google_compute_firewall.venue, for_each = var.shadow_mode ? {} : var.venues) beside the Google-APIs and central-plane rules. But no direct venue path exists in any process or environment: the root passes shadow_mode = true as a literal (asserted by infrastructure.rs), so the venue rule is never created; execution_nodes = {} everywhere; QIP_VENUE_FEED_ENDPOINT and QIP_DROP_COPY_ENDPOINT are not read or dialled by qip-edge-node -- they appear only in missing_production_requirements (backend/crates/apps/qip-edge-node/src/main.rs:657-668) as things the deployment has NOT supplied; and the only real-venue order-entry adapter (qip_brokers::rest, used by RestGateway) speaks plaintext HTTP/1.1 and names 'a TLS-terminating egress proxy in front of this adapter' as a standing requirement (qip-brokers/src/rest.rs:512-515), which the proxy refuses to be for a venue.",
    "evidence": [
      "infrastructure/terraform/modules/egress-proxy/variables.tf:57-59 (plan-time refusal of any allowlist entry naming a venue/broker/exchange)",
      "infrastructure/egress/envoy.yaml (the one committed bootstrap; contains no venue/broker/exchange host)",
      "infrastructure/terraform/modules/execution-node/main.tf:559-590 (per-venue direct egress rule, created only when shadow_mode is false)",
      "backend/crates/apps/qip-edge-node/src/main.rs:657-668 (QIP_VENUE_FEED_ENDPOINT / QIP_DROP_COPY_ENDPOINT listed as missing production requirements, not read)",
      "backend/crates/services/qip-brokers/src/rest.rs:512-515 (REST adapter requires a TLS-terminating egress proxy; qip_transport::http refuses https)",
      "backend/crates/tests/qip-acceptance/tests/egress.rs:1680 the_proxy_offers_no_route_to_a_venue_and_cannot_carry_an_order (proves the prohibition half; its premise check only asserts the variable names appear in main.rs)",
      "backend/crates/tests/qip-acceptance/tests/egress.rs:1635 neither_the_fast_path_nor_an_execution_node_can_reach_a_proxy_that_is_not_its_own",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:1864-1884 (venue egress rule gated on shadow_mode; root passes shadow_mode = true as a literal)"
    ],
    "gap": "Only the prohibition is built. The positive half -- latency-sensitive traffic taking a direct venue path, over the internet where no private path exists -- does not exist anywhere: no venue feed or drop-copy session is implemented in qip-edge-node, the per-venue egress rule is structurally disabled at the root (shadow_mode = true literal) and no environment provisions a node, and a direct TLS session to an internet venue cannot be opened from the node at all because the in-tree HTTP client has no TLS stack (ADR 0009) and needs a TLS-terminating proxy -- the exact generic-proxy hop the requirement forbids. That last point is the requirement's own recorded open conflict (loopback Envoy vs 'generic proxy path'); it is not resolved by egress.rs, which only proves the proxy carries no venue route. No test or plan shows venue egress reaching a venue endpoint.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test egress -- the_proxy_offers_no_route_to_a_venue_and_cannot_carry_an_order",
    "work_item": "E14",
    "tier": "T0",
    "size": "S",
    "notes": "The requirement's own note flagged an open question about whether the node's loopback Envoy counts as a 'generic proxy path'. It remains open: egress.rs proves the proxy has no venue route, not that a direct venue path exists. The TLS half of the positive requirement sits against ADR 0099 C2 (transport security: no in-tree TLS / rustls refused) and the venue-egress rule against the paper-trading boundary (shadow_mode literal), but a simulated/sandbox venue feed and drop-copy session inside the node are buildable in-tree, so this is PARTIAL rather than BLOCKED. verify: downgraded COMPLETE -> PARTIAL and integrated true -> false. The assessor's 'current' stated that QIP_VENUE_FEED_ENDPOINT / QIP_DROP_COPY_ENDPOINT are 'read and dialled by the node outside the shared egress proxy'; main.rs:657-668 shows they are only named in missing_production_requirements, i.e. never read. The cited tests prove only the prohibition (no venue route through the proxy); nothing demonstrates the direct venue path the requirement's infra-plan check asks for, and the one direct-path resource (google_compute_firewall.venue) can never be created from committed root config."
  },
  {
    "id": "EXEC-031",
    "status": "PARTIAL",
    "current": "infrastructure/terraform/modules/connectivity/main.tf creates a regional google_compute_router per attachment region (main.tf:59-82) and PARTNER-type google_compute_interconnect_attachment entries (main.tf:92-118), plus a Private Service Connect endpoint for Google APIs, wired into the root at infrastructure/terraform/main.tf:805 (module \"connectivity\"). It supports only Partner Interconnect, not Dedicated Interconnect (no interconnect_type=DEDICATED resource exists), is off by default (enable_partner_interconnect defaults to false, variables.tf:964-967) and has an empty partner_interconnects map default (variables.tf:970-987); no environment tfvars overrides either.",
    "evidence": [
      "infrastructure/terraform/modules/connectivity/main.tf:59-118 (Cloud Router + PARTNER attachment)",
      "infrastructure/terraform/variables.tf:964-987 (enable_partner_interconnect default false, partner_interconnects default {})",
      "infrastructure/terraform/main.tf:805 (module \"connectivity\" wired into root)",
      "grep -rn 'enable_partner_interconnect' infrastructure/environments/*/terraform.tfvars: no matches (never overridden true)",
      "find infrastructure/terraform/modules/connectivity -name '*.tftest.hcl': no matches (no plan-time test harness)"
    ],
    "gap": "Only Partner Interconnect is modelled (no Dedicated Interconnect resource); the module has no *.tftest.hcl proving the plan-time behaviour both fires on a bad value and admits a good one; and no environment has a real counterparty configured, so the module creates nothing anywhere today.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "terraform test needed: infrastructure/terraform/modules/connectivity has no tests/ directory. A harness with enable_partner_interconnect=true and one partner_interconnects entry should assert the attachment and router appear, and a plan with the flag false should assert neither does.",
    "work_item": "E14",
    "tier": "T1",
    "size": "M",
    "notes": "Actually reaching a private path also needs a real partner, circuit and pairing key that Terraform cannot create (module's own NOT-ORDERED.md); that is an external prerequisite, not one of ADR 0099's C1-C8 conflicts or the billing blocker. verify: status unchanged (PARTIAL); integrated true -> false. The integrated flag for Terraform means an environment enables it: enable_partner_interconnect defaults false, partner_interconnects defaults {}, and no environment tfvars sets either (grep over infrastructure/environments/*/terraform.tfvars: no matches), so local.attachments is empty and the module creates no router or attachment anywhere. Also confirmed no qip-acceptance test references modules/connectivity or interconnect, so tested stays false. The module is keyed by colocation site, not by a provider 'marked in committed configuration as supporting private connectivity', which the requirement's check assumes."
  },
  {
    "id": "EXEC-032",
    "status": "MISSING",
    "current": "nothing. No HA VPN resource (google_compute_vpn_gateway, google_compute_vpn_tunnel, google_compute_external_vpn_gateway, or a Cloud Router BGP session for VPN) exists anywhere in infrastructure/terraform.",
    "evidence": [
      "grep -rin 'vpn' infrastructure/terraform: no matches outside this requirement's own prose"
    ],
    "gap": "No fallback path exists at all. modules/connectivity provisions only the Partner Interconnect primary path (itself off by default per EXEC-031), so there is nothing to fail over to and no game-day exercise is possible.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "EXEC-031"
    ],
    "blocked_by": "",
    "verification": "Test needed once built: withdraw a provider's Interconnect attachment in a non-production environment and observe traffic switch to HA VPN tunnels in routing/connectivity logs. No HA VPN module exists to exercise.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-033",
    "status": "PARTIAL",
    "current": "The in-process shape exists: qip-edge-node's NodeGateway enum (Simulated(SimulatedGateway) | Live(RestGateway), backend/crates/apps/qip-edge-node/src/gateway.rs:1071-1084) holds its one venue seam inside the node process, and no separate microservice sits between Cell::work and the gateway. But no FIX or WebSocket order-entry session adapter exists: qip-protocols (fix.rs, itch.rs, sbe.rs) decodes market-data feed frames only (fix.rs:1 'FIX 4.4 tag=value decoding'; no logon/NewOrderSingle), and the passes that decode through it are fed by SimulatedFeed from the in-process simulator. The only real-venue adapter is RestGateway over qip_brokers::rest, which speaks plaintext HTTP/1.1 and lists 'a TLS-terminating egress proxy in front of this adapter' as a standing requirement (qip-brokers/src/rest.rs:512-515) -- for any TLS venue that is an HTTP proxy between the node and the venue, the conflict the requirement records. RestGateway is also refused alongside the simulated feed (main.rs:424-435), so no pass ever sends through it.",
    "evidence": [
      "backend/crates/apps/qip-edge-node/src/gateway.rs:1071-1084 (NodeGateway enum: Simulated | Live, both constructed in-process)",
      "backend/crates/services/qip-brokers/src/adapter.rs:310 (trait VenueAdapter)",
      "backend/crates/edge/qip-protocols/src/fix.rs:1-14 (FIX 4.4 market-data decoding only; no order-entry session)",
      "backend/crates/services/qip-brokers/src/rest.rs:512-515 (REST order entry requires a TLS-terminating proxy in front)",
      "backend/crates/apps/qip-edge-node/src/main.rs:424-435 (a simulated feed with a live gateway is refused, so passes never drive RestGateway)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs (dependency-graph checks; none asserts the no-HTTP-service order path)"
    ],
    "gap": "Direct FIX / WebSocket / venue-native order-entry sessions do not exist; the only real-venue adapter is plaintext REST that needs a TLS-terminating proxy in front of it for any TLS venue, which is a generic HTTP hop on the order path unless the loopback-Envoy question is recorded otherwise. And no architecture test asserts the requirement's check (sessions opened by adapter code linked into the Reflex process; no HTTP request to another service on the order path).",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "Test needed: an architecture.rs-style test asserting qip-edge-node's order-send path (Cell::send -> Placer -> NodeGateway) makes no HTTP client call except RestOrderEntryAdapter's own direct-to-venue dial, and that no HTTP request targets any other in-repo service.",
    "work_item": "E04",
    "tier": "T1",
    "size": "S",
    "notes": "verify: downgraded NEEDS-VALIDATION -> PARTIAL. NEEDS-VALIDATION asserts the code implements the requirement; it implements only the 'in-process' half. qip-protocols is feed decoding (FIX 4.4 market data), not a venue session; no FIX/WebSocket order-entry adapter exists; and the RestGateway path depends on a TLS proxy in front of it (rest.rs:512-515). Evidence expanded accordingly. The TLS dependency sits against ADR 0099 C2, but an in-process native adapter against a simulated venue is buildable in-tree, so not BLOCKED."
  },
  {
    "id": "EXEC-034",
    "status": "MISSING",
    "current": "nothing. qip-edge-node's NodeGateway (gateway.rs:1071-1084) holds exactly one venue session per node process -- SimulatedGateway's own doc reads 'The cell's paper gateway: one simulated venue, deterministically seeded' (gateway.rs:177). There is no per-venue connection pool, and no BTreeMap<VenueId, Gateway> or equivalent multi-venue session registry exists anywhere in qip-edge-node or qip-brokers.",
    "evidence": [
      "backend/crates/apps/qip-edge-node/src/gateway.rs:177-181 (SimulatedGateway doc: 'one simulated venue')",
      "backend/crates/apps/qip-edge-node/src/gateway.rs:1071-1084 (NodeGateway: a single Simulated|Live choice, not a map)",
      "grep -rn 'pool' backend/crates/services/qip-brokers/src -i: only AMM liquidity-pool matches in dex.rs, no connection-pool concept",
      "grep -n 'BTreeMap<VenueId|gateways' backend/crates/apps/qip-edge-node/src/*.rs: no matches"
    ],
    "gap": "A node today connects to exactly one venue; there is no concept of multiple simultaneous venue sessions to pool, let alone isolate from one another. Per-venue pooling needs multi-venue session support first.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "Test needed: with two simulated venues wired into one node, repeated forced disconnects at one leave the other's session count and order latency unchanged. Cannot be written today because a node holds one venue session.",
    "work_item": "E04",
    "tier": "T2",
    "size": "M",
    "notes": ""
  },
  {
    "id": "EXEC-035",
    "status": "MISSING",
    "current": "nothing. No buffer in the order-send/fill-decode path is pre-allocated at session setup; SimulatedGateway/RestGateway use ordinary Vec::new()/BTreeMap::new() collections sized on demand (e.g. gateway.rs:183 reports: Vec<ExecutionReport>, :186 working: BTreeMap<String, WorkingOrder>), with no with_capacity call or session-time allocation of order-path buffers.",
    "evidence": [
      "backend/crates/apps/qip-edge-node/src/gateway.rs:179-203 (SimulatedGateway fields: plain Vec/BTreeMap, no pre-sizing)",
      "grep -n 'pre.?alloc|with_capacity|reserve\\(' backend/crates/apps/qip-edge-node/src/gateway.rs: no matches"
    ],
    "gap": "No buffers are pre-allocated anywhere on the order path. Per the requirement's own note, proving zero allocation directly would need a GlobalAlloc wrapper, which needs unsafe -- forbidden workspace-wide (unsafe_code = \"forbid\") -- so even a future implementation can only be checked by benchmark and inspection, not by direct allocation counting.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "Test needed: a benchmark of the order-send/fill-decode path after warm-up, paired with inspection confirming session buffers are constructed once at setup and reused rather than per message. Neither exists today.",
    "work_item": "E04",
    "tier": "T2",
    "size": "S",
    "notes": ""
  },
  {
    "id": "EXEC-036",
    "status": "COMPLETE",
    "current": "infrastructure/terraform/modules/cloudrun/variables.tf's plane validation refuses the literal 'execution' among the twelve planes the module can name (variables.tf:74: 'The plane must be one of the twelve this module can name. execution is not one of them: the execution node is a Compute Engine machine, not a Cloud Run workload.'), and infrastructure/terraform/catalogue.tf documents the same exclusion by name (catalogue.tf:28-30). The execution node is provisioned only by infrastructure/terraform/modules/execution-node, a google_compute_instance module, never Cloud Run.",
    "evidence": [
      "infrastructure/terraform/modules/cloudrun/variables.tf:50-74 (plane validation, explicit refusal of 'execution')",
      "infrastructure/terraform/catalogue.tf:28-30 (comment documenting the exclusion of qip-edge-node by name)",
      "infrastructure/terraform/modules/execution-node/main.tf (Compute Engine machine, not Cloud Run)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:3073 every_deployable_has_its_own_cloud_run_identity (catalogue binaries are exactly qip-api, qip-fastbrain, qip-deepbrain -- no qip-edge-node)",
      "backend/crates/tests/qip-acceptance/tests/gitops.rs:2153 run_services + :2240 every_run_service_holds_the_invariants_its_catalogue_entry_and_the_cloud_run_module_held (every RunService is a catalogue entry or openobserve/portal, and its image must be the entry's binary)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:3395 every_cloud_run_workload_is_placed_in_a_declared_trust_zone_and_carries_its_tag (no Cloud Run workload in the execution zone)",
      "backend/crates/apps/{qip-api,qip-fastbrain,qip-deepbrain}/Cargo.toml: no qip-brokers dependency (no venue adapter linked into a Cloud Run binary)"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure -- the_execution_node_has_no_external_address_and_no_container_runtime; grep -n 'execution' infrastructure/terraform/modules/cloudrun/variables.tf for the plane validation.",
    "work_item": "E14",
    "tier": "T0",
    "size": "S",
    "notes": "deployable is false only because execution_nodes = {} in every environment tfvars, so no execution node is actually provisioned anywhere today; the prohibition itself is enforced at plan time regardless. verify: status unchanged (COMPLETE confirmed) but evidence corrected. The only test the assessor cited, infrastructure.rs:661 the_execution_node_has_no_external_address_and_no_container_runtime, checks access_config and container runtimes on the node, not the Cloud Run exclusion. The exclusion is actually proven by infrastructure.rs:3073 (catalogue binaries exactly api/fastbrain/deepbrain) together with gitops.rs's run_services matcher (no RunService outside the catalogue except openobserve/portal; image must equal the entry's binary). The cloudrun plane validation refusing 'execution' has no plan-time tftest of its own. The central binaries on Cloud Run execute only against qip-execution-engine's in-process SimulatedBroker, which is a simulator, not a venue session. Score together with API-010."
  },
  {
    "id": "EXEC-037",
    "status": "PARTIAL",
    "current": "The direct path exists in its permitted (paper) form: qip_edge::cell::Cell::work sends orders straight to the node's in-process SimulatedGateway (no Fabric broker, regional service or other region between them), and fills come back through the same gateway's execution reports; cancels go the same way (the Requoter's cancel/replace). qip-edge-node's pass.rs tests run orders, cancels and fills with no mesh, Fabric or mirror constructed at all, and mesh.rs shows the cell keeps running with the centre unreachable. The Fabric itself does not exist yet (ADR 0100 has only just decided its first build), so 'no Fabric broker in between' holds vacuously. In the deployed loop, however, serve() (backend/crates/apps/qip-edge-node/src/main.rs:776-935) runs the journal flush to the configured store (cell.flush at :852) and the blocking mesh exchange with the centre (:869) synchronously on the same thread immediately before each run_pass (:888): a failure of either is logged and the pass still runs, but a slow sink or centre delays the pass by up to its I/O timeout.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/cell.rs (module doc/status: 'No I/O inside on_bytes/work...Cell::flush is the sole blocking call')",
      "backend/crates/apps/qip-edge-node/src/main.rs:846-857 (flush failure is logged and serving continues) and :869-935 (mesh exchange then run_pass, serially on one thread)",
      "backend/crates/apps/qip-edge-node/tests/pass.rs:472 a_marketable_order_fills_on_the_pass_it_is_sent_once_the_venue_has_a_touch_to_take (no mesh/Fabric/mirror constructed in this test file)",
      "backend/crates/apps/qip-edge-node/tests/pass.rs:338 a_resting_order_the_venue_fills_on_a_later_pass_is_confirmed_and_the_node_keeps_trading",
      "backend/crates/apps/qip-edge-node/tests/pass.rs:874 a_node_pass_reprices_a_stale_resting_child_after_draining_gateway_events_first (cancel + replace sent directly)",
      "backend/crates/apps/qip-edge-node/tests/mesh.rs:375 a_tick_against_an_unreachable_centre_reports_it_and_leaves_the_cell_running (cell not halted, strategy still deployed, with the mesh peer dead; no order is placed in this test)",
      "backend/crates/apps/qip-edge-node/tests/mirror.rs (no test fails the store and then shows the journal catching up)"
    ],
    "gap": "The requirement's own chaos check is not demonstrated: no test makes the journal sink and the regional/central services unreachable and then shows, in the same run, that orders and cancels still send and fills still book, and no test shows the journal catching up when the sink returns (mirror.rs never fails the store; cell.rs's flush tests are happy-path). The two separate tests show order flow with nothing else present and cell survival with the centre dead, which is a weaker property. In addition the only venue is the in-process simulator, so the 'order gateway' leg of the edge is a function call, and the deployed serve loop serialises a blocking flush and a blocking centre exchange ahead of every pass -- not 'in between' the order and the venue, but a synchronous dependency on the loop that drives the order path, which the requirement's rationale warns against and no test bounds.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-edge-node --test pass -- a_marketable_order_fills_on_the_pass_it_is_sent_once_the_venue_has_a_touch_to_take; cargo test -p qip-edge-node --test mesh -- a_tick_against_an_unreachable_centre_reports_it_and_leaves_the_cell_running",
    "work_item": "E04",
    "tier": "T1",
    "size": "S",
    "notes": "A single test that fails the mirror's store and makes the mesh peer dead mid-run, shows an order still sends, a cancel still sends and a fill still books on that pass, then restores the store and shows the next flush ships the backlog in chain order, would close the verification gap. Moving the flush and the mesh exchange off the pass thread (or bounding their timeouts well below the pass cadence) would close the coupling. verify: downgraded COMPLETE -> PARTIAL. The assessor's own gap conceded that no test matches the requirement's chaos check; under the COMPLETE definition (a named test exercising that specific behaviour) that is disqualifying. Also found that the journal catch-up half is untested entirely and that main.rs's serve loop runs the blocking flush and mesh exchange synchronously before each run_pass; and that the Fabric the requirement names does not yet exist, so its absence from the path is vacuous rather than proven."
  },
  {
    "id": "EXEC-038",
    "status": "MISSING",
    "current": "nothing. qip-edge-node has no adapter for a physical or event venue. qip-prediction (backend/crates/services/qip-prediction/src/*, prediction/event market pricing and resolution logic) is not a dependency of qip-edge-node and has no feed/session component reaching a Cell at all -- it is pure valuation/resolution modelling with no venue-session code.",
    "evidence": [
      "backend/crates/services/qip-prediction/src/lib.rs:1-30 (module doc: proposition/resolution/price modelling only, no venue-session code)",
      "grep -rn 'qip.prediction|qip_prediction' backend/crates/apps/qip-edge-node: no matches",
      "grep -il 'event.venue|physical.venue|commerce.venue' backend/crates --type rust (non-test): only type declarations in category/degradation/contracts files, no feed adapter reaching algorik-node"
    ],
    "gap": "No physical or event venue ever reaches algorik-node (qip-edge-node) on any path, hot or otherwise; qip-prediction's modelling exists in complete isolation from the edge process.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "Test needed: an integration test in which the node's own adapter consumes a simulated event venue's messages on the pass that receives them, with no Fabric/regional-service call in between. No such adapter exists to test.",
    "work_item": "E17",
    "tier": "T3",
    "size": "L",
    "notes": ""
  }
]
