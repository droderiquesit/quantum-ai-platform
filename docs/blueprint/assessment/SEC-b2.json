[
  {
    "id": "SEC-026",
    "status": "MISSING",
    "current": "nothing — no Event & Control Fabric and no producer-epoch fencing exist in the tree. qip_events::bus::EventBus (backend/crates/libs/qip-events/src/bus.rs) is an in-process, single-writer, synchronous queue with no producer instance, epoch or network write. Adjacent but not this control: the edge cell refuses a region-share payload carrying a lower sequence than the one it applied (qip_edge_region_share_applied_total{outcome=refused_lower_sequence}), which fences a stale control message at one consumer, not a stale producer at a P0 topic.",
    "evidence": [
      "backend/crates/libs/qip-events/src/bus.rs:1-45 (module doc: synchronous, in-process, no producer/broker abstraction)",
      "grep -rln fenc backend/crates/ finds no match touching a Fabric producer or topic"
    ],
    "gap": "No fabric exists to fence a producer on. ADR 0100 (accepted 2026-09-25, nothing built by it) decides the first build in-tree on std TCP with no new crate: producer identity (producer_id, epoch), a new epoch fences the old one, and 'producer fencing' is one of its eight proving tests. So the semantics are buildable now under ADR 0002/0009; C2 blocks only mTLS and replication, neither of which this requirement needs.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "FABRIC-001",
      "FABRIC-058"
    ],
    "blocked_by": "",
    "verification": "No test exists. Once FABRIC-001/FABRIC-058 land, the property test would drive two producer instances with different epochs against one P0 topic and assert the lower-epoch instance's writes are refused across every interleaving.",
    "work_item": "E03",
    "tier": "T4",
    "size": "XL",
    "notes": "This is a Fabric-domain requirement scored under SEC because it names a security property (a superseded controller cannot act). It cannot be assessed independently of FABRIC-058's completion. verify: blocked_by C2 -> '' (status MISSING kept). ADR 0100 §4 builds producer-epoch fencing in-tree (qip-streaming::event_fabric) on std TCP without Tokio/QUIC/prost; the C2 attribution was wrong for this property. Re-checked with grep for event_fabric/producer_id/epoch fencing in backend/crates: no non-test implementation exists."
  },
  {
    "id": "SEC-027",
    "status": "MISSING",
    "current": "nothing — no Certificate Authority Service pool anywhere in the tree.",
    "evidence": [
      "grep -rn 'privateca|CertificateAuthority|CAS' infrastructure/terraform/ returns no match"
    ],
    "gap": "No google_privateca_ca_pool or certificate-template resource exists. Even if built, the certificates would have no consumer: the Fabric's QUIC/mTLS transport (FABRIC-009) that would present them needs a TLS stack, which is conflict C2.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "FABRIC-009"
    ],
    "blocked_by": "C2",
    "verification": "No test exists. terraform plan would need to show a CAS pool with a lifetime-capping issuance policy and every Fabric broker/Reflex VM/non-mesh workload's certificate sourced from it.",
    "work_item": "E13",
    "tier": "T2",
    "size": "M",
    "notes": "The CAS pool itself is ordinary Terraform (no new Rust dependency) and could be built ahead of the Fabric; using the certificates it issues in mTLS is what conflict C2 blocks."
  },
  {
    "id": "SEC-028",
    "status": "MISSING",
    "current": "nothing — no Fabric broker exists to hold an identity. modules/gitops-control-plane, modules/cicd and modules/secrets map every other workload identity (Cloud Run services, CI, the three GitOps controllers) to a GCP service account, but no broker process is in the tree.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:2091 every_service_account_terraform_creates_runs_something_or_signs_something — the pinned list of every google_service_account this repository creates names no broker"
    ],
    "gap": "No broker exists yet, so there is no identity to map. ADR 0100 now decides qip-fabricd as an in-tree broker on std TCP (no C2 dependency for the broker itself), with bearer-token identity per producer; the broker's own GCP service account and its scoped IAM are ordinary Terraform once fabricd has a host. What stays C2 is the requirement's 'its certificate identity names that account' clause (ADR 0100 §7: mTLS is BLOCKED(C2)); dedicated per-region broker VMs are C8, and execution_nodes = {} everywhere.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "FABRIC-001",
      "FABRIC-015",
      "FABRIC-008"
    ],
    "blocked_by": "C2",
    "verification": "No test exists. Once brokers exist, the plan-time check would confirm each broker VM's service account is its own (never shared) and its certificate identity names that account.",
    "work_item": "E03",
    "tier": "T4",
    "size": "L",
    "notes": "Also gated by C8 (dedicated per-region broker VMs) once brokers themselves exist. verify: status and flags unchanged; gap rewritten because it attributed the broker's absence to C2, which ADR 0100 (2026-09-25) removed for the broker. C2 remains correct for the certificate-identity half."
  },
  {
    "id": "SEC-029",
    "status": "PARTIAL",
    "current": "modules/gitops-control-plane provisions one GKE Autopilot cluster per environment with Workload Identity on (Autopilot enables it unconditionally), and the three controllers it runs — Config Connector (kcc), Argo CD (argocd), Kargo (kargo) — each get their own Google service account. The Kubernetes-side half is wired per environment overlay: each controller's KSA carries a distinct iam.gke.io/gcp-service-account annotation naming its own GSA.",
    "evidence": [
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:159 enable_autopilot = true",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:235-236 workload_identity_config { workload_pool = \"${var.project_id}.svc.id.goog\" }",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:503-517,539 google_service_account.kcc / .argocd / .kargo, roles/iam.workloadIdentityUser",
      "infrastructure/gitops/bootstrap/argocd/overlays/dev/kustomization.yaml:22 iam.gke.io/gcp-service-account: qip-dev-argocd@algorik-dev.iam.gserviceaccount.com",
      "infrastructure/gitops/bootstrap/kargo/overlays/dev/kustomization.yaml:20 iam.gke.io/gcp-service-account: qip-dev-kargo@algorik-dev.iam.gserviceaccount.com",
      "test: the_hook_that_proves_a_promotion_may_read_the_revision_it_proves (backend/crates/tests/qip-acceptance/tests/gitops.rs:4342) — asserts the PostSync hook's KSA carries the iam.gke.io/gcp-service-account annotation and that it names exactly the Argo CD identity modules/gitops-control-plane creates for that environment",
      "test: the_control_plane_cluster_is_private_autopilot_without_the_addon_and_no_wildcard_network (backend/crates/tests/qip-acceptance/tests/gitops.rs:4051) — asserts the cluster declares workload_identity_config (the 'every cluster has a workload pool' half)",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:74-86 workload_identity_members: argocd = [\"argocd/argocd-repo-server\", \"qip-run/qip-prove-serving\"] — two workloads bound to one GSA"
    ],
    "gap": "The cluster was applied in dev but is suspended out-of-band (ADR 0093), so nothing runs on it. Distinctness is not met: the PostSync proving hook (qip-run/qip-prove-serving) and Argo CD's repo-server (argocd/argocd-repo-server) are both bound to the one qip-<env>-argocd GSA, and the cited hook test asserts that sharing rather than refusing it. The requirement asks each GCP-calling workload for its own distinct Google service account. Config Connector's and Kargo's KSA<->GSA bindings are implemented but no named test asserts them.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "the_hook_that_proves_a_promotion_may_read_the_revision_it_proves (gitops.rs:4342); no equivalent test yet for the kcc/kargo KSAs.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "execution_nodes = {} everywhere and the cluster's own suspension (ADR 0093) both mean 'deployable' is false today even though the config is correct; this is the only GKE workload in the repository, so the requirement's broader claim about 'every GKE pod' is only as wide as this one cluster's three controllers. verify: status PARTIAL kept. Added the workload-pool test the assessor missed, and a finding the row omitted: the hook and the repo-server share the Argo CD GSA (gitops-control-plane/main.tf:83), which violates the 'own distinct' clause. Ran cargo test -p qip-acceptance --test gitops for both named tests: 'test result: ok. 2 passed; 0 failed'."
  },
  {
    "id": "SEC-030",
    "status": "INCORRECT",
    "current": "No Secret Manager CSI driver and no SecretProviderClass exist for the one GKE cluster. The control-plane controllers do consume Secret Manager values, but by a different route. infra.yml's bootstrap runs `gcloud secrets versions access` on the GitHub Actions runner and writes the values into Kubernetes Secret objects with kubectl. The Argo CD and Kargo GitHub App payloads become argocd/qip-repository and qip/qip-repository (githubAppPrivateKey). The Argo CD admin bcrypt hash is patched into argocd/argocd-secret. The assessor's claim that the controllers take no Secret Manager values was wrong.",
    "evidence": [
      "grep -rln 'SecretProviderClass|secrets-store|secret_manager_config' infrastructure/ — only a comment in catalogue.tf:4",
      ".github/workflows/infra.yml:1197-1256 reads qip-github-app-{argocd,kargo}-<env> from Secret Manager and builds kind: Secret objects carrying githubAppPrivateKey",
      ".github/workflows/infra.yml:1258-1261 kubectl apply of the Argo CD repository Secret",
      ".github/workflows/infra.yml:1287-1294 gcloud secrets versions access the admin hash, then kubectl patch secret argocd-secret"
    ],
    "gap": "The requirement's mechanism, a CSI volume under the workload's own Workload Identity, is absent. In its place a CI runner copies Secret Manager payloads into Kubernetes Secrets, which is the path this requirement (and SEC-031) exists to replace. The values also pass through the runner's scratch directory. Argo CD and Kargo read repository credentials only from Kubernetes Secrets, so conforming may need an ADR recording an exception like ADR 0031's, not just a SecretProviderClass.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A gitops/infra test should refuse any `kind: Secret` built from a Secret Manager read in infra.yml (or the owner should record an exception ADR), and assert every workload secret is a SecretProviderClass volume whose GSA holds secretAccessor only on what it mounts.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": "Only relevant while the GKE management cluster exists (ADR 0036/0093); if a controller never needs a Secret Manager value, this requirement may turn out to have no live instance to satisfy. verify: MISSING -> INCORRECT. The assessor said the controllers never needed Secret Manager values. infra.yml:1197-1294 in fact delivers three of them (two GitHub App private keys and the Argo CD admin hash) to GKE workloads as Kubernetes Secrets written from a CI runner. That is the non-CSI mechanism the requirement replaces. Flags describe the required CSI behaviour and are all false."
  },
  {
    "id": "SEC-031",
    "status": "INCORRECT",
    "current": "The committed manifests carry no credential: the only Kubernetes Secrets in infrastructure/gitops are vendored upstream objects with empty payloads (stringData: {}). The live Secrets are different. infra.yml's bootstrap persists long-lived credentials in them: the Argo CD and Kargo GitHub App private keys (githubAppPrivateKey in argocd/qip-repository and qip/qip-repository) and the Argo CD admin bcrypt hash in argocd-secret. No GCP credential is in any Kubernetes Secret, because the three controllers reach GCP through Workload Identity (SEC-029).",
    "evidence": [
      "infrastructure/gitops/bootstrap/kargo/upstream/install.yaml:164-178 (kind: Secret, name: kargo-api, stringData: {})",
      "infrastructure/gitops/bootstrap/argocd/upstream/install.yaml:31667-31683 (argocd-notifications-secret, argocd-secret; no data)",
      ".github/workflows/infra.yml:1233-1256 Secret objects carrying githubAppPrivateKey, applied at :1258-1261 (Argo CD) and :1765 (Kargo, phase 11)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:6106 no_credential_appears_in_a_kubernetes_manifest — repurposed to scan catalogue.tf only (doc comment :6095-6104)"
    ],
    "gap": "Scanning the live Secrets, as the verification requires, would find two long-lived private keys. A GitHub App private key mints installation tokens for GitHub's API and has no expiry until someone rotates it. That fits 'a static cloud API token' in plain reading. If the owner rules that 'cloud credential' means GCP only, this row reverts to NEEDS-VALIDATION: no GCP credential is present, but no test proves that either. Under both readings, nothing scans the bootstrap's Secret objects or the gitops tree for credential-shaped payloads.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A test reading infra.yml's bootstrap and every infrastructure/gitops/**/*.yaml `kind: Secret` should refuse a private key or token payload, or else pin an owner-approved exception.",
    "work_item": "E13",
    "tier": "T1",
    "size": "M",
    "notes": "No violation observed by inspection, but 'no test demonstrates it' is exactly the failure mode 01-testing-strategy.md warns about — a renamed test whose citations still point at it. verify: NEEDS-VALIDATION -> INCORRECT, implemented/integrated -> false. The row checked only the committed manifests. The verification also covers 'the live Secrets', and infra.yml:1197-1294 writes GitHub App private keys into them. The interpretation is stated in the gap; a GCP-only reading of 'cloud credential' would restore NEEDS-VALIDATION, not COMPLETE."
  },
  {
    "id": "SEC-032",
    "status": "MISSING",
    "current": "nothing — no model gateway exists in the tree. Adjacent: qip-deepbrain can install a hosted language-model adapter (Hugging Face, ADR 0037) whose token it resolves in-process through qip_core::secret's _FILE indirection (backend/crates/apps/qip-deepbrain/src/language.rs, config.rs). That is the reverse of a gateway, because the process that runs the agents holds the provider credential. No Secret Manager container for that token is declared in secret_names (infrastructure/terraform/main.tf:245-340).",
    "evidence": [
      "grep -rln 'model gateway|model_gateway|ModelGateway' backend/ infrastructure/ — no match"
    ],
    "gap": "There is no policy-enforcing model gateway for agent calls to Gemini/Claude/OpenAI, so there is nothing whose credentials could be scoped to Secret Manager or withheld from agents.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. CICD-034 states the identical requirement from the pipeline side and should be reconciled with this entry rather than scored twice.",
    "work_item": "E13",
    "tier": "T3",
    "size": "L",
    "notes": "Rated P2 here (autonomous-development plane, GCP Phase 9) versus P0 on CICD-034 — the matrix should carry one priority, not two. verify: status MISSING kept. The 'current' field now records the deepbrain in-process Hugging Face adapter. If it is enabled before a gateway exists, it would put the credential with the agents, which this requirement forbids."
  },
  {
    "id": "SEC-033",
    "status": "MISSING",
    "current": "nothing — depends entirely on SEC-032, which does not exist.",
    "evidence": [],
    "gap": "No model gateway exists to audit requests through.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "SEC-032"
    ],
    "blocked_by": "",
    "verification": "No test exists. Once a gateway exists, a request through it should append a record to the platform's own hash-chained event log naming the calling agent identity, the model and the time (no new managed service needed).",
    "work_item": "E13",
    "tier": "T2",
    "size": "M",
    "notes": "The event log already gives this an audit substrate with no new dependency once the gateway itself exists."
  },
  {
    "id": "SEC-034",
    "status": "COMPLETE",
    "current": "modules/cicd::google_iam_workload_identity_pool_provider.github, scoped by an attribute_condition to exactly this repository's branches. deploy.yml, vendor.yml, infra.yml and image.yml all authenticate to GCP via workload_identity_provider — none references a credentials_json / service-account key.",
    "evidence": [
      "infrastructure/terraform/modules/cicd/main.tf:31 google_iam_workload_identity_pool_provider \"github\"",
      "infrastructure/terraform/modules/cicd/main.tf:68 attribute_condition = \"attribute.repository == '${var.github_repository}' && attribute.ref.startsWith('refs/heads/')\"",
      ".github/workflows/deploy.yml:378,666 workload_identity_provider: ${{ steps.identity.outputs.provider }}",
      "test: the_pipeline_authenticates_without_a_long_lived_key (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:4748)",
      "test: the_pool_admits_only_branches_of_this_repository_and_says_so_truthfully (infrastructure.rs:4861)",
      "test: no_workflow_depends_on_a_repository_variable (infrastructure.rs:4216)"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": true,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure the_pipeline_authenticates_without_a_long_lived_key -- --exact (and the two tests named above); real workflow runs recorded in infrastructure/CLAUDE.md.",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "Applied in the one real environment (algorik-dev); every workflow run this repository has recorded used this pool. verify: COMPLETE kept. Ran cargo test -p qip-acceptance --test infrastructure for the named tests: 'test result: ok. 8 passed; 0 failed' (batch incl. the_pipeline_authenticates_without_a_long_lived_key and the_pool_admits_only_branches_of_this_repository_and_says_so_truthfully). grep confirms all four GCP-reaching workflows (deploy/vendor/infra/image) use workload_identity_provider and none names credentials_json. Weakness, not a downgrade: the key-absence assertion reads deploy.yml only, so a credentials_json added to vendor.yml, infra.yml or image.yml would fail no test. Run evidence: infra.yml run 52, deploy.yml run 33891084271 (infrastructure/CLAUDE.md)."
  },
  {
    "id": "SEC-035",
    "status": "PARTIAL",
    "current": "No google_service_account_key resource exists anywhere in the Terraform tree; scripts/check-secrets.sh scans for service-account JSON before every commit; CI authenticates entirely through the WIF pool (SEC-034).",
    "evidence": [
      "test: no_service_account_key_exists_anywhere_in_the_terraform (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:2073)",
      "01-security-and-safety.md forbids downloaded service-account keys 'in any file, including examples'",
      "scripts/check-secrets.sh:15 pattern '\"type\": \"service_account\"', run in CI at .github/workflows/ci.yml:400",
      "docs/security/credentials.md:225-226 'If a key already exists, it should be deleted after impersonation is set up. `gcloud iam service-accounts keys list` will show any.' — the out-of-band claude-builder account (roles/owner, scripts/bootstrap-deploy.sh:257-260) is not created by Terraform and so is outside the named test"
    ],
    "gap": "The repository half is met and tested. The Terraform tree creates no key, commits are scanned in CI, and CI authenticates through WIF. The platform half is not demonstrated. The verification asks that 'the project IAM shows no user-managed key on any service account' and that CI secret stores hold none, and no recorded inspection of either exists. The one out-of-band identity, claude-builder, holds roles/owner and has its own doc contemplating a pre-existing key. Nothing prevents key creation (SEC-069's org policy is absent).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure no_service_account_key_exists_anywhere_in_the_terraform -- --exact (passes); ./scripts/check-secrets.sh. Missing: a recorded `gcloud iam service-accounts keys list --managed-by=user` over every account in algorik-dev (including claude-builder) and a listing of repository/environment secrets.",
    "work_item": "E13",
    "tier": "T0",
    "size": "S",
    "notes": "SEC-069's Organization Policy (iam.disableServiceAccountKeyCreation), which would make key creation structurally impossible rather than merely absent, does not exist — the tree has no GCP organization to attach one to. The requirement as stated ('no key stored, and CI works without one') is met; the stronger org-policy backstop is a separate, unmet requirement. verify: COMPLETE -> PARTIAL, e2e -> false. The named test scans only infrastructure/terraform/**/*.tf. The requirement also covers 'anywhere else in the platform', and its check inspects the live project IAM for user-managed keys. That inspection has never been recorded. The repo's own credentials.md allows that claude-builder, an owner-level account Terraform does not manage, may already hold a key."
  },
  {
    "id": "SEC-036",
    "status": "MISSING",
    "current": "Cloud KMS/HSM signing exists only for container images: modules/binaryauthorization::google_kms_crypto_key.attestor (ASYMMETRIC_SIGN) backs the Binary Authorization attestor, and deploy.yml calls `gcloud beta container binauthz attestations sign-and-create` against it. Nothing signs a release, model or policy manifest with a KMS-held key. A separate, unrelated in-tree mechanism — qip_compliance::signing::SigningKey (backend/crates/libs/qip-compliance/src/signing.rs:40-113) — signs artifact/dataset provenance, but with a symmetric HMAC key (ADR 0043 §2), not a Cloud KMS/HSM key, and its own module doc says so explicitly (signing.rs:12 'HMAC is symmetric. Whoever can verify can also sign').",
    "evidence": [
      "infrastructure/terraform/modules/binaryauthorization/main.tf:54 google_kms_crypto_key.attestor (ASYMMETRIC_SIGN)",
      ".github/workflows/deploy.yml:557-561 gcloud beta container binauthz attestations sign-and-create",
      "backend/crates/libs/qip-compliance/src/signing.rs:40,95,104 SigningKey::sign/verifies — HMAC, not KMS"
    ],
    "gap": "No release, model or policy manifest is ever signed with a Cloud KMS/HSM key; the only KMS-backed signature target is a container image digest.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. Achievable without a new Rust dependency by extending the existing gcloud-in-CI attestation pattern (deploy.yml:557) to a manifest artifact's digest, the same way image signing already works.",
    "work_item": "E13",
    "tier": "T1",
    "size": "M",
    "notes": "CICD-054 holds the identical requirement from the pipeline side; SEC-001/SEC-024 hold verification of a signed artifact once one exists, which is also unmet today for manifests. verify: status unchanged; replaced a non-verbatim quotation of signing.rs with the module doc's actual words (line 12)."
  },
  {
    "id": "SEC-037",
    "status": "NEEDS-VALIDATION",
    "current": "modules/binaryauthorization::google_binary_authorization_policy.platform sets default_admission_rule { evaluation_mode = REQUIRE_ATTESTATION, enforcement_mode = ENFORCED_BLOCK_AND_AUDIT_LOG } with the build attestor. The root instantiates it unconditionally. The GKE control-plane cluster sets binary_authorization { evaluation_mode = PROJECT_SINGLETON_POLICY_ENFORCE }. Every catalogue RunService opts in with binaryAuthorization.useDefault: true, and scripts/deploy-frontends.sh deploys the portal and landing by digest with --binary-authorization=default.",
    "evidence": [
      "infrastructure/terraform/modules/binaryauthorization/main.tf:199-229 google_binary_authorization_policy.platform, default_admission_rule REQUIRE_ATTESTATION / ENFORCED_BLOCK_AND_AUDIT_LOG",
      "infrastructure/terraform/main.tf:772 module \"binary_authorization\" (no count)",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:264-266 binary_authorization { evaluation_mode = \"PROJECT_SINGLETON_POLICY_ENFORCE\" }",
      "infrastructure/gitops/envs/dev/api.yaml binaryAuthorization: { useDefault: true } (repeated per workload per environment)",
      "test: every_cloud_run_service_this_repository_deploys_is_subject_to_the_admission_policy (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:2490) — asserts only the opt-in half: modules/cloudrun names no binary_authorization, the two frontend deploys pass --binary-authorization=default and name a digest"
    ],
    "gap": "No test asserts the rule that does the refusing. grep over backend/crates/tests and every *.tftest.hcl finds no occurrence of REQUIRE_ATTESTATION, ENFORCED_BLOCK_AND_AUDIT_LOG, PROJECT_SINGLETON_POLICY_ENFORCE or evaluation_mode. Changing the default rule to ALWAYS_ALLOW, or the cluster to DISABLED, fails no test. The module header records that exact failure happening once before. The requirement's integration check has never been observed: an unattested image refused, then the same image admitted after attestation. Nothing currently runs under the policy: the cluster is suspended (ADR 0093), dev has no Cloud Run service since the 2026-09-13 teardown (infra.yml run 52 re-applied infrastructure only), and billing is disabled on the dev project (ADR 0099 C8).",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure every_cloud_run_service_this_repository_deploys_is_subject_to_the_admission_policy -- --exact passes but covers opt-in only. Needed: a test pinning default_admission_rule (REQUIRE_ATTESTATION, ENFORCED_BLOCK_AND_AUDIT_LOG, require_attestations_by the build attestor) and the cluster's PROJECT_SINGLETON_POLICY_ENFORCE, mutation-verified; and an observed refusal of an unattested digest.",
    "work_item": "E13",
    "tier": "T1",
    "size": "S",
    "notes": "The module's own header documents a previously fixed gap (enforcement once evaluated the implicit ALWAYS_ALLOW policy) — this is the corrected, applied form. verify: COMPLETE -> NEEDS-VALIDATION, tested/e2e -> false. The cited test proves that workloads opt in to the policy, not that the policy refuses anything, and no test pins the deny rule or the cluster's enforcement mode. The row's claim 'applied in algorik-dev, where api/fastbrain/deepbrain/openobserve all exist under this policy' is stale: those services were deleted on 2026-09-13, and run 52 recreated no Cloud Run service (infrastructure/CLAUDE.md)."
  },
  {
    "id": "SEC-038",
    "status": "PARTIAL",
    "current": "Inside Terraform, every google_project_iam_member uses a predefined or custom role (modules/cicd defines custom deploy_nodes/infra_storage roles), and no .tf file names roles/owner or roles/editor. Outside the plan, scripts/bootstrap-deploy.sh grants roles/owner on the project to the claude-builder service account. That account is not a documented break-glass account, and docs/security/credentials.md says its roles are 'Narrower than Owner, which is the point'. The GKE Autopilot cluster sets no node service account, so its nodes run as the Compute Engine default identity. In a project with no organisation policy, that identity is auto-granted roles/editor, and nothing in the repository narrows or checks it. The infra account holds projectIamAdmin, roleAdmin and serviceAccountAdmin, which lets it grant itself any role (module comments accept this deliberately).",
    "evidence": [
      "infrastructure/terraform/modules/cicd/main.tf:160,278 google_project_iam_custom_role.deploy_nodes / .infra_storage",
      "infrastructure/terraform/modules/cicd/main.tf:232-264 infra account roles incl. roles/resourcemanager.projectIamAdmin, roles/iam.roleAdmin",
      "scripts/bootstrap-deploy.sh:127,257-260 BOOTSTRAP_SA=claude-builder@…; gcloud projects add-iam-policy-binding … --role=\"roles/owner\"",
      "docs/security/credentials.md:228-230 'Roles claude-builder needs — Narrower than Owner, which is the point'",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf — no cluster_autoscaling.auto_provisioning_defaults.service_account",
      "test: no_workload_identity_can_delete_from_the_evidence_bucket (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:4044) — asserts no infrastructure/terraform/**/*.tf contains roles/owner or roles/editor (not roles/viewer)",
      "test: every_service_account_terraform_creates_runs_something_or_signs_something (infrastructure.rs:2091) — pins the account set, not role breadth"
    ],
    "gap": "Three identities break least privilege. (1) claude-builder is granted roles/owner by a committed script, contradicting the repository's own credentials doc. (2) The Autopilot nodes' default compute identity keeps whatever Google auto-grants, and nothing in the repository configures or verifies it. (3) The infra account can escalate itself through projectIamAdmin. The requirement's second check, every service account's roles listed against the operations it performs, exists only as prose in module comments. roles/viewer is asserted nowhere.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure no_workload_identity_can_delete_from_the_evidence_bucket -- --exact passes (owner/editor absent from Terraform). Missing: coverage of scripts/ and workflows for basic-role grants, a named node service account for the cluster, and a roles-per-account table asserted by a test.",
    "work_item": "E13",
    "tier": "T1",
    "size": "M",
    "notes": "verify: NEEDS-VALIDATION -> PARTIAL, tested false -> true. The assessor missed a test that does assert no Terraform file grants roles/owner or roles/editor. The row's claim 'No non-test file grants roles/owner' is false: scripts/bootstrap-deploy.sh:257-260 grants it to claude-builder. The Autopilot cluster also runs its nodes as the unnarrowed default compute identity. Treat the owner grant as INCORRECT if the owner does not designate claude-builder as the documented break-glass account."
  },
  {
    "id": "SEC-039",
    "status": "INCORRECT",
    "current": "Every committed path for granting a person access names an individual. The console_front_door output prints a grant command with --member='user:YOU@example.com', as do modules/iap-run's grant_command output (pinned by iap-run.tftest.hcl), infra.yml's post-apply instructions and the dev tfvars comment. scripts/bootstrap-deploy.sh binds the operator's own user:${ACCOUNT} as serviceAccountTokenCreator on claude-builder. iap_members / gitops_iap_members accept user: principals, and their validation refuses only allUsers and allAuthenticatedUsers. The plan itself names no individual today because every list is empty, and the repository defines no Cloud Identity group.",
    "evidence": [
      "infrastructure/terraform/modules/iap-run/variables.tf:64-96 variable \"iap_members\" — validation refuses only allUsers/allAuthenticatedUsers",
      "infrastructure/terraform/outputs.tf:477 grant = \"gcloud projects add-iam-policy-binding … --member='user:YOU@example.com'\"",
      "infrastructure/terraform/modules/iap-run/outputs.tf:60 grant_command … --member='user:YOU@example.com' (asserted by modules/iap-run/tests/iap-run.tftest.hcl:104)",
      ".github/workflows/infra.yml:1916-1917 'grant a person with: … --member='user:YOU@example.com''",
      "scripts/bootstrap-deploy.sh:280-282 gcloud iam service-accounts add-iam-policy-binding … --member=\"user:${ACCOUNT}\""
    ],
    "gap": "The requirement asks for human access bound to Cloud Identity groups. The repository's only access procedures bind individual user accounts, and a test pins the individual form of the grant command. Fixing it means refusing user: principals (except a named break-glass account) in both member variables, rewriting the grant commands and instructions to name a group, and changing the bootstrap to bind a group. Separately, the project has no organisation or group directory.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A validation on iap_members and gitops_iap_members refusing a user: prefix, a plan test proving it fires on user: and admits group:, and a test that the printed grant commands name group: would close it.",
    "work_item": "E13",
    "tier": "T1",
    "size": "M",
    "notes": "verify: MISSING -> INCORRECT. The row treated the empty lists as 'unexercised'. The repository in fact ships the forbidden form as its procedure: outputs.tf:477, iap-run outputs.tf:60 (pinned by iap-run.tftest.hcl:104), infra.yml:1917 and bootstrap-deploy.sh:281 all grant to user: principals."
  },
  {
    "id": "SEC-040",
    "status": "MISSING",
    "current": "nothing — no google_privileged_access_manager_entitlement or equivalent PAM resource exists anywhere in the tree.",
    "evidence": [
      "grep -rln 'google_privileged_access_manager|PrivilegedAccessManager|pam_entitlement' infrastructure/ — no match",
      "docs/adr/0076-a-per-person-operator-identity-is-the-consoles-asserted-subject-and-a-two-person-rule-needs-a-credential-this-platform-cannot-issue.md — records the platform's own console cannot yet do a two-person rule; does not address cloud-role PAM"
    ],
    "gap": "No PAM entitlement requiring a second approver exists for any sensitive cloud role.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A PAM entitlement Terraform resource plus an integration proof (a request granted only after a second, distinct approver acts) would close the gap.",
    "work_item": "E13",
    "tier": "T2",
    "size": "L",
    "notes": "ADR 0076 is adjacent but answers a different question (the console's own operator identity), not cloud IAM elevation."
  },
  {
    "id": "SEC-041",
    "status": "PARTIAL",
    "current": "modules/gitops-control-plane runs the one GKE cluster in the tree in Autopilot mode, which always enables the GKE metadata server and withholds the node's own service account from pods (Autopilot manages node pools directly, so there is no per-node-pool workload_metadata_config to set separately).",
    "evidence": [
      "infrastructure/terraform/modules/gitops-control-plane/main.tf:159 enable_autopilot = true",
      "test: the_control_plane_cluster_is_private_autopilot_without_the_addon_and_no_wildcard_network (backend/crates/tests/qip-acceptance/tests/gitops.rs:4051) — asserts enable_autopilot = true and workload_identity_config",
      "infrastructure/terraform/modules/gitops-control-plane/main.tf — no cluster_autoscaling { auto_provisioning_defaults { service_account } }, so Autopilot nodes run as the Compute Engine default service account"
    ],
    "gap": "The metadata-server half is met by Autopilot and asserted by a named test. The node-identity half is not met. The cluster names no node service account, so its nodes run as <project-number>-compute@developer.gserviceaccount.com. In a project with no organisation policy that account is auto-granted roles/editor, and nothing here narrows it to logging and monitoring. The test no_workload_runs_as_the_projects_default_compute_identity covers Cloud Run, the execution node and the image builder, but not this cluster. The cluster is also suspended (ADR 0093).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test gitops the_control_plane_cluster_is_private_autopilot_without_the_addon_and_no_wildcard_network -- --exact passes (Autopilot asserted). Missing: a dedicated node service account holding only logging.logWriter / monitoring.metricWriter (and the Autopilot minimum), set via auto_provisioning_defaults, with a test.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "Same cluster and suspension caveat as SEC-029. verify: status PARTIAL kept; tested false -> true, because the row said no test asserts Autopilot and gitops.rs:4073 does. Added the unmet half the row missed: no node service account is set, so nodes run as the default compute identity, whose roles are unnarrowed and unverified."
  },
  {
    "id": "SEC-042",
    "status": "PARTIAL",
    "current": "modules/secrets declares 16 empty Secret Manager containers (4 role tokens, session secret, venue credential, quantum token, capital-envelope key, market-data key, Alpaca pair, OpenObserve root creds, 2 GitHub App payloads, Argo CD admin password); catalogue.tf mounts them into Cloud Run workloads as files, never environment values; qip_core::secret (backend/crates/libs/qip-core/src/secret.rs:55,79,106) is the one resolver every process uses to read a credential, supporting only a direct value or a _FILE path, never an inline literal.",
    "evidence": [
      "infrastructure/terraform/modules/secrets/main.tf:121 google_secret_manager_secret.platform for_each var.secret_names",
      "infrastructure/terraform/modules/secrets/main.tf:179 google_secret_manager_secret_iam_member.venue_credential count = var.venue_credential_readable (structurally 0)",
      "test: every_cloud_run_service_is_internal_and_mounts_secrets_as_files_never_as_environment (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:568)",
      "test: every_secret_a_workload_mounts_is_created_by_terraform_and_granted_to_it (infrastructure.rs:3018)",
      "test: no_secret_this_repository_deploys_reaches_a_process_as_an_environment_value (infrastructure.rs:2805)",
      "test: the_venue_credential_is_unreadable_where_live_trading_is_impossible (infrastructure.rs:257)"
    ],
    "gap": "Two of the 16 containers are cryptographic key material (the capital-envelope key and the session secret, both HMAC keys per ADR 0043 §2) held in Secret Manager rather than Cloud KMS — the requirement's own text carves this distinction out ('Cryptographic key material belongs in Cloud KMS/HSM (SEC-002), not in Secret Manager'), and Cloud KMS does support an HMAC/MAC key purpose that these two could use instead.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": true,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure the_venue_credential_is_unreadable_where_live_trading_is_impossible -- --exact (and the three tests named above); applied in algorik-dev.",
    "work_item": "E13",
    "tier": "T2",
    "size": "S",
    "notes": "Load-bearing for the paper-trading boundary's layer 1 (venue_credential_readable is structurally 0 at every ceiling a plan can carry)."
  },
  {
    "id": "SEC-043",
    "status": "COMPLETE",
    "current": "Every Secret Manager secret modules/secrets creates carries a rotation block: rotation_period = \"7776000s\" (90 days) and a next_rotation_time, notifying the qip-secret-rotation-<env> Pub/Sub topic.",
    "evidence": [
      "infrastructure/terraform/modules/secrets/main.tf:121 google_secret_manager_secret.platform",
      "infrastructure/terraform/modules/secrets/main.tf:147-153 rotation { next_rotation_time = ..., rotation_period = \"7776000s\", topics { name = google_pubsub_topic.rotation.id } }",
      "infrastructure/terraform/modules/secrets/main.tf:78-114 google_pubsub_topic.rotation \"qip-secret-rotation-${var.environment}\"",
      "test: every_key_rotates_and_the_ones_that_hold_data_cannot_be_destroyed (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:3304) — counts exactly 2 rotation_period occurrences (the secrets KMS key and the secret containers) and asserts prevent_destroy on the KMS keys that hold data"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure every_key_rotates_and_the_ones_that_hold_data_cannot_be_destroyed -- --exact",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "Terraform never writes the new value itself (by design — no secret value is ever committed), so a rotation is announced on schedule but not performed automatically; SEC-044 covers the versioning half of an actual rotation. verify: COMPLETE kept; e2e true -> false. next_rotation_time is 2026-12-01, so no rotation notification has ever been published, and no run observes one. Ran the named test: 'test result: ok' (in the 8-test infrastructure batch). Test weakness: it counts the substring rotation_period in the raw file, so a comment could satisfy it, and it never asserts topics. Latent defect: next_rotation_time is a fixed literal outside ignore_changes. Once Secret Manager advances it after 2026-12-01, a later plan will try to set a past time back on all 16 secrets. This is likely to be refused, but it is not verified here."
  },
  {
    "id": "SEC-044",
    "status": "PARTIAL",
    "current": "Every committed path that writes a value adds a Secret Manager version and none destroys one. infra.yml's 'seed any credential that has no version' step (on `up` only, skipping a secret that already has an ENABLED version), scripts/bootstrap-deploy.sh and scripts/deploy-frontends.sh all seed only an empty secret. scripts/venue-signup/signup.mjs writeSecret calls `gcloud secrets versions add` unconditionally. bootstrap-deploy.sh documents rotation as 'add a version and restart the workloads'. No committed command disables or destroys a version.",
    "evidence": [
      ".github/workflows/infra.yml:666 gcloud secrets versions add \"$name\" --project \"$project\" --data-file=\"$scratch/payload\"",
      ".github/workflows/infra.yml step 'seed any credential that has no version' — gated on inputs.action == 'up' and on the target secret having no ENABLED version",
      "test: a_credential_with_no_version_is_seeded_before_the_apply_that_resolves_it (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:3198)",
      "scripts/venue-signup/signup.mjs:442-458 writeSecret — `gcloud secrets versions add <slot>` with no emptiness check",
      "infrastructure/gitops/envs/dev/api.yaml:150,160,170 (and fastbrain/deepbrain/portal/openobserve) secret items mount version `latest`"
    ],
    "gap": "The half that adds versions exists. The half that moves a consumer forward or back by version does not. Every RunService mounts version `latest`, so committed config cannot pin or roll back to a version, and a rollback would mean disabling the newest version out of band. No test or observed run shows that after a version is added the previous one stays enabled and readable by number, which is the requirement's integration check.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure a_credential_with_no_version_is_seeded_before_the_apply_that_resolves_it -- --exact — covers only the first-seed path.",
    "work_item": "E13",
    "tier": "T1",
    "size": "M",
    "notes": "A rotation runbook or workflow step that adds a version to an already-versioned secret (rather than skipping it) is the specific piece missing. verify: status PARTIAL kept; current and gap corrected. The row said nothing adds a version to an already-versioned secret, but signup.mjs writeSecret does. It also missed that consumers mount `latest`, which is the real obstacle to moving a consumer by version."
  },
  {
    "id": "SEC-045",
    "status": "MISSING",
    "current": "nothing — no google_project_iam_audit_config resource exists anywhere in the tree.",
    "evidence": [
      "grep -rn 'iam_audit_config|DATA_READ|audit_log_config' infrastructure/terraform/ — no match"
    ],
    "gap": "Admin-activity logs (changes to secrets/IAM) are on by Google default, but data-access logs (DATA_READ, which record a read of a secret's value) are not enabled by anything in this repository, so a secret read leaves no queryable record today.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A google_project_iam_audit_config for secretmanager.googleapis.com enabling DATA_READ and DATA_WRITE, plus a plan-time test asserting its presence, would close the gap.",
    "work_item": "E13",
    "tier": "T2",
    "size": "S",
    "notes": "SEC-059 holds the broader audit-logging requirement this is one instance of."
  },
  {
    "id": "SEC-046",
    "status": "PARTIAL",
    "current": "modules/secrets, modules/evidence, modules/data and modules/ai each set a KMS key on their storage: modules/secrets sets kms_key_name on the rotation Pub/Sub topic (line 86) and customer_managed_encryption on every Secret Manager secret (lines 135-136); modules/evidence sets default_kms_key_name on the evidence bucket; modules/data and modules/ai set per-store KMS keys on BigQuery/AlloyDB/Bigtable/Spanner and the Vertex training bucket respectively.",
    "evidence": [
      "infrastructure/terraform/modules/secrets/main.tf:86 kms_key_name = google_kms_crypto_key.secrets.id",
      "infrastructure/terraform/modules/secrets/main.tf:135-136 customer_managed_encryption { kms_key_name = google_kms_crypto_key.secrets.id }",
      "infrastructure/terraform/modules/evidence/main.tf (default_kms_key_name)",
      "infrastructure/terraform/modules/data/main.tf, infrastructure/terraform/modules/ai/main.tf (kms_key_name on their respective resources)",
      "test: the_evidence_bucket_is_versioned_and_retained (backend/crates/tests/qip-acceptance/tests/infrastructure.rs:4030) — asserts evidence.contains(\"default_kms_key_name\")",
      "google_storage_bucket without CMEK: modules/cloudrun/main.tf:382 collector_config and :459 config_files, modules/image-bake/main.tf:134 payload, modules/egress-proxy/main.tf:83 bootstrap, modules/public-edge/main.tf:137 static_shell"
    ],
    "gap": "Only the evidence bucket's CMEK is asserted by a named test. The Secret Manager, data and ai CMEK wiring has none. Coverage is also incomplete: modules/registry (Artifact Registry) sets no kms_key_name, and five buckets set no encryption block at all (cloudrun collector_config and config_files, image-bake payload, egress-proxy bootstrap, public-edge static_shell). modules/data and modules/ai create 0 resources in every environment, so their CMEK settings have never met a real plan or apply.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test infrastructure the_evidence_bucket_is_versioned_and_retained -- --exact; secrets and evidence are both applied in algorik-dev.",
    "work_item": "E13",
    "tier": "T1",
    "size": "S",
    "notes": "'Reached from production' is true only for the secrets and evidence halves; data and ai are complete-but-unused Terraform paired with a documented Rust-side adapter gap. verify: status PARTIAL and flags kept. Corrected a mis-description of secrets/main.tf:86, which is the Pub/Sub topic's key, and added five buckets with no CMEK that the gap omitted. The requirement's check names 'every storage bucket'."
  },
  {
    "id": "SEC-047",
    "status": "MISSING",
    "current": "nothing — no google_access_context_manager_service_perimeter resource exists anywhere. modules/network resolves Google APIs through a private DNS zone (main.tf:88-113) and variables.tf:1008 names a 'vpc-sc' (restricted-VIP) bundle option for it, but that is groundwork for a future perimeter's DNS routing, not a perimeter.",
    "evidence": [
      "grep -rln 'access_context_manager|service_perimeter' infrastructure/terraform/ — no match",
      "infrastructure/terraform/modules/network/main.tf:84-113 (private DNS zone; comment names VPC Service Controls as the reason the restricted-VIP set exists)",
      "infrastructure/terraform/variables.tf:1008 (private_google_access_bundle: vpc-sc | all-apis)"
    ],
    "gap": "No perimeter exists. VPC Service Controls also needs a GCP organization, which this configuration does not have and cannot create from inside the repository — an external prerequisite, not one of C1-C8.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A plan-time test asserting a perimeter containing prod/ledger, prod/data and prod/ai with Spanner/GCS/BigQuery/Bigtable/Vertex AI restricted would close the gap once an organization exists.",
    "work_item": "E13",
    "tier": "T4",
    "size": "XL",
    "notes": "No foundation-phase exit names this; the earliest sensible point per the requirement's own notes is when prod/ledger is created (GCP Phase 4). SEC-072's private-DNS groundwork reportedly points at the wrong address range per that requirement's own notes — worth checking before building on it."
  },
  {
    "id": "SEC-048",
    "status": "MISSING",
    "current": "modules/public-edge and modules/iap-edge each declare a Cloud Armor security policy with adaptive DDoS protection and a per-client rate limit, but neither sets any preconfigured WAF rule.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:59 google_compute_security_policy.edge",
      "infrastructure/terraform/modules/iap-edge/main.tf:130 google_compute_security_policy.edge",
      "grep -in waf infrastructure/terraform/modules/public-edge/main.tf infrastructure/terraform/modules/iap-edge/main.tf — no match"
    ],
    "gap": "No rule in either policy references a preconfigured WAF expression set (e.g. evaluatePreconfiguredExpr('xss-stable') or similar); only rate-limiting and geographic restriction are enforced.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A rule block using preconfigured_waf_config (or an evaluatePreconfiguredExpr match) in deny mode, plus a plan-time test asserting its presence, would close the gap.",
    "work_item": "E13",
    "tier": "T2",
    "size": "M",
    "notes": "API-003 and GCP-054 hold the same row from other domains. verify: status MISSING kept (grep -i 'waf|preconfigured|sqli|xss' on both edge modules finds nothing). Added for planning: the project's Cloud Armor quota is zero. infra.yml run 71 failed with \"Quota 'SECURITY_POLICY_RULES' exceeded. Limit: 0.0 globally\" (modules/iap-edge/README.md:25-29), so any WAF rule needs a quota grant before it can be applied."
  },
  {
    "id": "SEC-049",
    "status": "PARTIAL",
    "current": "Both modules declare adaptive_protection_config { layer_7_ddos_defense_config { enable = true } } on their Cloud Armor policy.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:68-69 adaptive_protection_config / layer_7_ddos_defense_config",
      "infrastructure/terraform/modules/iap-edge/main.tf:135-136 adaptive_protection_config / layer_7_ddos_defense_config"
    ],
    "gap": "No named test asserts adaptive_protection_config's presence (the_public_edge_attaches_every_backend_to_the_cloud_armor_policy checks rate_limit_options and origin.region_code, not this). Both modules are instantiated with 0 resources in every environment today — public_edge.hostnames defaults to [] and no environment sets it; gitops_portal_hostname is \"\" in dev — so the control has never been reached by a real plan or apply. It cannot be provisioned in dev either: infra.yml run 71 failed applying iap-edge's Cloud Armor policy with \"Quota 'SECURITY_POLICY_RULES' exceeded. Limit: 0.0 globally\" (ADR 0095; modules/iap-edge/README.md:25-29).",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists. A test asserting module.contains(\"adaptive_protection_config\") and layer_7_ddos_defense_config { enable = true } for both modules would close the gap.",
    "work_item": "E01",
    "tier": "T1",
    "size": "S",
    "notes": "Same 'built and gated to zero effect' status the current-state map records for both modules ([\"exists\",\"unused\",\"hardened\"]). verify: status PARTIAL kept; deployable true -> false, because the project has zero Cloud Armor security-policy-rule quota and run 71 proved it. grep for adaptive_protection/layer_7_ddos across backend/crates/tests and every *.tftest.hcl finds nothing, which confirms tested=false."
  },
  {
    "id": "SEC-050",
    "status": "PARTIAL",
    "current": "Both modules use rate_based_ban (a ban, not a throttle) with conform_action = \"allow\" and exceed_action = \"deny(429)\" on their Cloud Armor policy, keyed per client address.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:93,104-105 action = \"rate_based_ban\"; conform_action = \"allow\"; exceed_action = \"deny(429)\"",
      "infrastructure/terraform/modules/iap-edge/main.tf:142,153-154 same pattern",
      "test: a_reviewed_rate_limit_is_admitted_and_bans_rather_than_only_throttling (infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:199; infrastructure/terraform/modules/iap-edge/tests/iap-edge.tftest.hcl:230)",
      "test: a_rate_limit_of_zero_is_refused (public-edge.tftest.hcl:185; iap-edge.tftest.hcl:219)",
      "test: the_public_edge_attaches_every_backend_to_the_cloud_armor_policy (backend/crates/tests/qip-acceptance/tests/terraform_plan.rs:254) — asserts rate_limit_options is present on the attached policy"
    ],
    "gap": "Tested only at plan level, and only in part. iap-edge.tftest.hcl:230 asserts the priority-2000 rule's threshold and action = rate_based_ban. public-edge.tftest.hcl:199, despite its name, asserts only that one policy exists. terraform_plan.rs:254 asserts only the substring rate_limit_options. No test asserts exceed_action = deny(429) or per-IP keying, and the requirement's integration check has never run: over the limit gets 429 and a ban, under the limit is served. Neither module creates anything in any environment, and the project's Cloud Armor quota is zero (infra.yml run 71: \"Quota 'SECURITY_POLICY_RULES' exceeded. Limit: 0.0 globally\"), so the rate limit has never protected a backend and cannot be applied without a quota grant.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "terraform test -filter=tests/public-edge.tftest.hcl (run a_reviewed_rate_limit_is_admitted_and_bans_rather_than_only_throttling); cargo test -p qip-acceptance --test terraform_plan the_public_edge_attaches_every_backend_to_the_cloud_armor_policy -- --exact.",
    "work_item": "E14",
    "tier": "T0",
    "size": "S",
    "notes": "Closing the gap needs a Cloud Armor quota grant, an environment setting hostnames/gitops_portal_hostname, and an observed 429. Those are a product and rollout decision plus an external quota request, not a code defect. verify: status PARTIAL kept; deployable true -> false (Cloud Armor quota 0, run 71). The gap was corrected because it said the control is 'well-tested at the module level', but the public-edge plan test named 'bans_rather_than_only_throttling' checks only that a policy exists. Ran cargo test -p qip-acceptance --test terraform_plan the_public_edge_attaches_every_backend_to_the_cloud_armor_policy: 'test result: ok. 1 passed'. terraform test was not run."
  }
]
