[
  {
    "id": "API-001",
    "status": "PARTIAL",
    "current": "infrastructure/terraform/modules/public-edge (main.tf) implements the whole named chain in one module: google_compute_security_policy.edge (Cloud Armor, main.tf:59), google_compute_backend_bucket.static_shell with enable_cdn=true (main.tf:168,176), google_compute_managed_ssl_certificate.edge (main.tf:293), google_compute_target_https_proxy.edge (main.tf:311), google_compute_global_address.edge (anycast, main.tf:334) and google_compute_global_forwarding_rule.https on port 443 only (main.tf:346-352). No Cloud DNS record is wired to it: modules/dns-zone is separately built but dns_zone_domain=\"\" in every environment (ADR 0095 emptied it). var.public_edge defaults to {} (hostnames=[]) and no environment sets it (infrastructure/environments/*/terraform.tfvars), so the module plans 0 resources everywhere it is instantiated (infrastructure/terraform/main.tf:1062-1077). The portal's real, currently-deployed front door bypasses this chain entirely: infrastructure/terraform/main.tf:1079-1102 states outright the portal is reached at its Google-issued run.app URL behind Cloud Run's own IAP (module.portal_iap_run / modules/iap-run), 'which needs no domain, no registrar and no DNS delegation.'",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:1-352",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:47-80 a_declared_hostname_creates_the_whole_edge_and_nothing_on_port_80",
      "backend/crates/tests/qip-acceptance/tests/terraform_plan.rs:349 no_environment_opens_a_public_address",
      "infrastructure/terraform/main.tf:1079-1102 (portal reached via Cloud Run's own IAP, not this chain)",
      "infrastructure/terraform/modules/dns-zone/main.tf:41 (unused; dns_zone_domain=\"\" every environment)"
    ],
    "gap": "The chain is built and module-tested but 0 resources exist in any environment, has no DNS record wired to it at all, and uses google_compute_managed_ssl_certificate rather than Certificate Manager; the one surface actually deployed (the portal) reaches Cloud Run directly behind IAP with no DNS/global-LB/Armor/CDN in front of it, which is a real divergence from this requirement's ordering, not just an unused module.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl (12 runs) plus backend/crates/tests/qip-acceptance/tests/terraform_plan.rs::no_environment_opens_a_public_address; no plan has ever produced a live forwarding rule.",
    "work_item": "E14-public-edge-activation",
    "tier": "T2",
    "size": "M",
    "notes": "Certificate Manager vs google_compute_managed_ssl_certificate and the missing DNS record are both scoring divergences per the requirement's own notes, not just gaps to close by flipping a variable."
  },
  {
    "id": "API-002",
    "status": "PARTIAL",
    "current": "modules/public-edge's google_compute_backend_service.application (main.tf:222-252) is a serverless NEG (main.tf:209-219) naming var.application_backend.service_name -- built to attach exactly one Cloud Run service (the API) as the LB's dynamic backend, restricted by precondition to the public-edge/application-identity trust zones. var.public_edge.application_backend is null (unset) in every environment, so no Cloud Run service is ever attached as a backend of the global LB. qip-api itself is the only dynamic origin that exists (backend/crates/apps/qip-api/src/routes.rs:105-804, 63 routes) and it is reached instead through modules/iap-run directly, with no LB in front.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:209-252",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:82-115 the_application_identity_zone_may_be_fronted",
      "infrastructure/terraform/variables.tf:1275-1327 (application_backend optional, null default)",
      "infrastructure/terraform/modules/iap-run/main.tf:77-158 (the portal's actual front door, no LB)"
    ],
    "gap": "No environment names an application_backend, so the LB never fronts qip-api in practice; the GKE Gateway arm does not exist at all (ADR 0024 retired Kubernetes for Cloud Run, conflict C3).",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-001"],
    "blocked_by": "",
    "verification": "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl::the_application_identity_zone_may_be_fronted (plan-only, mocked provider).",
    "work_item": "E14-public-edge-activation",
    "tier": "T2",
    "size": "S",
    "notes": "The GKE Gateway half is C3 (blocked); the Cloud Run half is built and tested but unused by committed config."
  },
  {
    "id": "API-003",
    "status": "PARTIAL",
    "current": "google_compute_security_policy.edge (infrastructure/terraform/modules/public-edge/main.tf:59-136) carries a geographic-refusal rule and a rate-limit rule (rate_limit_options, checked by name in the test below) ahead of the default allow. google_compute_backend_service.application sets security_policy = google_compute_security_policy.edge[0].id (main.tf:229), attaching the policy to the API backend. google_compute_backend_bucket.static_shell (main.tf:168-196), the CDN-served static-shell backend, sets no edge_security_policy field at all -- the attribute this requirement's own verification names does not appear anywhere in the module.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:59-136,168-196,229",
      "backend/crates/tests/qip-acceptance/tests/terraform_plan.rs:254 the_public_edge_attaches_every_backend_to_the_cloud_armor_policy (asserts security_policy is set and that rate_limit_options/origin.region_code exist, but not on the bucket)"
    ],
    "gap": "google_compute_backend_bucket.static_shell has no edge_security_policy, so the one backend that would actually be reachable first by a browser (the static shell) carries no Cloud Armor policy at all if the edge were ever turned on; no test catches this because the existing assertion matches the string 'security_policy' anywhere in the file rather than on each backend.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-001"],
    "blocked_by": "",
    "verification": "backend/crates/tests/qip-acceptance/tests/terraform_plan.rs::the_public_edge_attaches_every_backend_to_the_cloud_armor_policy; a new assertion is needed for google_compute_backend_bucket.static_shell.edge_security_policy specifically.",
    "work_item": "E14-public-edge-activation",
    "tier": "T1",
    "size": "S",
    "notes": "Concrete, fixable defect independent of activation: add edge_security_policy to the backend bucket and a test that would have caught its absence."
  },
  {
    "id": "API-004",
    "status": "PARTIAL",
    "current": "modules/public-edge declares google_compute_global_forwarding_rule.https on port_range \"443\" only (main.tf:346-352) with no port-80 resource anywhere in the module -- stricter than the requirement, which only forbids serving content over plain HTTP. This chain is unused in every environment (see API-001). The path actually deployed for the portal is Cloud Run's own TLS termination behind IAP (modules/iap-run), which is also HTTPS-only by construction (Cloud Run issues no plaintext listener), so the live surface satisfies the intent of this requirement through a different mechanism than the one named.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:311-352",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:47-65 a_declared_hostname_creates_the_whole_edge_and_nothing_on_port_80",
      "infrastructure/terraform/modules/iap-run/main.tf:77-158"
    ],
    "gap": "The named mechanism (global external LB with TLS at the edge) is untested-in-production because it is never enabled; the mechanism actually live (Cloud Run's own HTTPS via IAP) is not what the requirement's verification checks for (a global forwarding rule and SSL policy).",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-001"],
    "blocked_by": "",
    "verification": "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl::a_declared_hostname_creates_the_whole_edge_and_nothing_on_port_80.",
    "work_item": "E14-public-edge-activation",
    "tier": "T2",
    "size": "S",
    "notes": ""
  },
  {
    "id": "API-005",
    "status": "PARTIAL",
    "current": "google_compute_backend_bucket.static_shell sets enable_cdn = true with a full cdn_policy (cache_mode CACHE_ALL_STATIC, negative caching, serve_while_stale) at infrastructure/terraform/modules/public-edge/main.tf:168-196; google_compute_backend_service.application sets enable_cdn = false at main.tf:240, so the authenticated API path is never cached. Both halves are asserted by the same tftest run. As with API-001, the module plans 0 resources in every environment, so this separation has never been proven against a live cache.",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:168-196,240",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:47-80 a_declared_hostname_creates_the_whole_edge_and_nothing_on_port_80 (asserts static_shell.enable_cdn == true)",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:82-115 the_application_identity_zone_may_be_fronted (asserts application.enable_cdn == false)"
    ],
    "gap": "Correct at the config level and dual-tested, but never applied anywhere -- no live cache has ever served a byte.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-001"],
    "blocked_by": "",
    "verification": "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl (both runs above).",
    "work_item": "E14-public-edge-activation",
    "tier": "T1",
    "size": "S",
    "notes": ""
  },
  {
    "id": "API-006",
    "status": "INCORRECT",
    "current": "Most of the posture holds: qip-api, qip-fastbrain and qip-deepbrain carry ingress INGRESS_TRAFFIC_INTERNAL_ONLY in every environment (infrastructure/gitops/envs/*/{api,fastbrain,deepbrain}.yaml:27, pinned by gitops.rs:2240); every 0.0.0.0/0 source range in Terraform is a deny rule (network/main.tf:51-68, trust-zones/main.tf:290-307, execution-node/main.tf:621-640, image-bake/main.tf:262-277); the execution node declares no access_config (execution-node/main.tf:354); the GitOps cluster endpoint is private (gitops-control-plane/main.tf:214-231) and the Argo CD external gateway is off (dev tfvars:521). But committed dev config opens two ingresses from the internet that do not pass the public edge chain, and the requirement's own check ('no resource outside the public-edge module has ... Cloud Run ingress all') fails on both: (1) OpenObserve, a management-zone, data-and-observability-plane store, is INGRESS_TRAFFIC_ALL with an allUsers run.invoker binding (infrastructure/gitops/envs/dev/openobserve.yaml:27, invokers.yaml:27-40), i.e. anonymously reachable on its run.app URL; ADR 0033 (authenticated before it holds telemetry) is 'accepted, not yet applied' and even applied keeps it internet-reachable behind IAP rather than behind the edge; (2) the portal is INGRESS_TRAFFIC_ALL on its run.app URL behind Cloud Run's own IAP (portal.yaml:66, ADR 0095), deliberately bypassing modules/public-edge. The test the assessor cited as proof, gitops.rs:4536 openobserve_is_deployed_..._anonymous_as_adr_0030_records..., asserts the anonymous open ingress exists, i.e. it pins the violation rather than the prohibition. Nothing currently runs (ADR 0093: the Config Connector/Argo CD path is suspended), but the committed manifests open these doors the moment the reconciler runs.",
    "evidence": [
      "infrastructure/gitops/envs/dev/openobserve.yaml:27 (ingress: INGRESS_TRAFFIC_ALL; labels qip-trust-zone: management, qip-plane: data-and-observability)",
      "infrastructure/gitops/envs/dev/invokers.yaml:27-40 (member: allUsers, roles/run.invoker on qip-dev-openobserve)",
      "infrastructure/gitops/envs/dev/portal.yaml:14-36,66 (INGRESS_TRAFFIC_ALL on run.app behind Cloud Run IAP, no public-edge chain)",
      "backend/crates/tests/qip-acceptance/tests/gitops.rs:4536 openobserve_is_deployed_at_the_reviewed_digest_anonymous_as_adr_0030_records_and_on_ephemeral_storage (asserts INGRESS_TRAFFIC_ALL + exactly one allUsers binding: pins the violation)",
      "docs/adr/0033-openobserve-becomes-authenticated-before-it-holds-telemetry.md:3 (status: accepted, not yet applied)",
      "infrastructure/gitops/envs/dev/api.yaml:27, fastbrain.yaml:27, deepbrain.yaml:27 (INGRESS_TRAFFIC_INTERNAL_ONLY -- the part that holds)",
      "backend/crates/tests/qip-acceptance/tests/gitops.rs:2240 every_run_service_holds_the_invariants_its_catalogue_entry_and_the_cloud_run_module_held (internal ingress for catalogue workloads only)",
      "infrastructure/terraform/modules/network/main.tf:51-68, trust-zones/main.tf:290-307, execution-node/main.tf:621-640 (0.0.0.0/0 only in deny rules)"
    ],
    "gap": "Two internet ingresses bypass the public edge chain in committed dev config: anonymous OpenObserve (an internal management-zone data store) and the portal's run.app URL behind IAP. Closing it needs ADR 0033 applied (drop allUsers) plus an owner decision on whether OpenObserve and the portal may stay directly internet-reachable at all, which the blueprint forbids; and a test that asserts the prohibition (no INGRESS_TRAFFIC_ALL / allUsers outside the edge) instead of pinning the exception.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "None demonstrates the prohibition. gitops.rs:2240 proves internal ingress for the three catalogue workloads only; gitops.rs:4536 asserts the opposite of this requirement for OpenObserve; console_route.rs pins the portal's INGRESS_TRAFFIC_ALL pairing.",
    "work_item": "E13-internal-plane-ingress-closure",
    "tier": "T1",
    "size": "M",
    "notes": "verify: downgraded COMPLETE -> INCORRECT. The requirement's own check forbids Cloud Run ingress 'all' outside the public-edge module; dev's committed manifests carry it twice (openobserve.yaml:27 with an allUsers invoker, portal.yaml:66), and the cited gitops.rs:4536 test asserts the anonymous OpenObserve ingress exists. All flags set false because the prohibition does not hold in committed config; deployable was also wrong on its own terms (ADR 0093: no Cloud Run service runs). Assessor note: Proven by source-text assertions over committed Terraform/manifests (the repository's own 'infra-plan' style), not by walking a live `terraform show -json`; no environment currently has a running Cloud Run instance to test against directly (GitOps control plane suspended, ADR 0093)."
  },
  {
    "id": "API-007",
    "status": "PARTIAL",
    "current": "No internal Application Load Balancer module (load_balancing_scheme INTERNAL_MANAGED) and no GKE Gateway exist anywhere in infrastructure/terraform (grep for INTERNAL_MANAGED and GKE Gateway finds nothing outside unrelated doc text). The substitute actually built and applied is Cloud Run's own network posture plus IAM: qip-api/qip-fastbrain/qip-deepbrain each carry ingress: INGRESS_TRAFFIC_INTERNAL_ONLY (infrastructure/gitops/envs/dev/{api,fastbrain,deepbrain}.yaml:27) and the portal calls the API only via a roles/run.invoker grant on the console's own service account (infrastructure/terraform/catalogue.tf:889-902).",
    "evidence": [
      "infrastructure/gitops/envs/dev/api.yaml:27, fastbrain.yaml:27, deepbrain.yaml:27",
      "infrastructure/terraform/catalogue.tf:889-902 (console_profile_claims / roles/run.invoker on the API)",
      "backend/crates/tests/qip-acceptance/tests/console_route.rs:474-511 (exactly one roles/run.invoker grant on the API, to the console)",
      "backend/crates/tests/qip-acceptance/tests/gitops.rs:2240 every_run_service_holds_the_invariants_its_catalogue_entry_and_the_cloud_run_module_held"
    ],
    "gap": "The specific regional entry the requirement names (GKE Gateway or an internal-scheme ALB) does not exist; the functioning substitute is Cloud Run's own ingress restriction plus per-caller IAM invoker grants, which is a different, untested-against-the-requirement mechanism.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test asserts an internal ALB or GKE Gateway because neither exists; the substitute mechanism is proven by backend/crates/tests/qip-acceptance/tests/console_route.rs and gitops.rs as cited.",
    "work_item": "E14-internal-alb",
    "tier": "T2",
    "size": "M",
    "notes": "The GKE Gateway arm is C3 (Kubernetes retired for Cloud Run); the internal-ALB arm is not blocked by any conflict, it is simply unbuilt."
  },
  {
    "id": "API-008",
    "status": "BLOCKED",
    "current": "No mTLS and no per-workload service policy exist for internal API entry. qip_transport's own module doc states this outright: 'Production must add mutual TLS. The realistic answer is a service mesh sidecar terminating mTLS, so peer identity comes from workload certificates' (backend/crates/libs/qip-transport/src/lib.rs:58-59), and qip-streaming/src/mesh.rs:83,229 repeat the same acknowledged gap for the data-mesh wire. The permitted substitute already applied is Cloud Run's own caller authentication: a call to qip-api without a valid identity token is refused, and the platform holds exactly one roles/run.invoker grant, on the console's service account (backend/crates/tests/qip-acceptance/tests/console_route.rs:474-511) -- workload-identity-bound authorization, but not mutual TLS with peer certificates.",
    "evidence": [
      "backend/crates/libs/qip-transport/src/lib.rs:58-59",
      "backend/crates/services/qip-streaming/src/mesh.rs:83,229",
      "backend/crates/tests/qip-acceptance/tests/console_route.rs:474-511 (the permitted substitute: exactly one IAM invoker grant, tested)"
    ],
    "gap": "True mTLS requires either an in-tree TLS stack (forbidden, ADR 0009/C2) or Cloud Service Mesh on GKE (C3, Kubernetes retired for Cloud Run); Cloud Run's built-in OIDC-identity-token caller authentication is a real, tested, different control that the requirement's own 'mutually authenticated with mTLS' language does not credit.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-007"],
    "blocked_by": "C3",
    "verification": "backend/crates/tests/qip-acceptance/tests/console_route.rs (IAM invoker binding assertions) proves the permitted substitute; no integration test makes three real HTTP calls (no cert / wrong workload / correct workload) because no live process exists to call.",
    "work_item": "E99-internal-mtls-owner-decision",
    "tier": "T3",
    "size": "L",
    "notes": "verify: status BLOCKED kept (mTLS on Cloud Run needs Cloud Service Mesh/GKE, C3, or an in-tree TLS stack, C2; an inbound Envoy sidecar cannot see client certs because Cloud Run's front end terminates TLS). deployable true -> false: the permitted substitute (the one roles/run.invoker grant) is a Config Connector manifest in invokers.yaml, and ADR 0093 records that the reconciler path is suspended and no Cloud Run service runs. The substitute's test (console_route.rs) checks the committed binding, not a refused call. Assessor note: qip-transport's mesh (edge-to-centre) carries the same acknowledged gap and is the more consequential half; see the notes on ADR 0043 in .claude/rules/architecture/00-boundaries.md."
  },
  {
    "id": "API-009",
    "status": "PARTIAL",
    "current": "qip-api (backend/crates/apps/qip-api) is the closest thing to the described category: it serves webhook/control-shaped operator routes (registration approvals, venue withdrawal/reinstatement, risk recalibration approval -- backend/crates/apps/qip-api/src/registration_views.rs, venue_views.rs, rule_views.rs) on Cloud Run with minInstanceCount: 0 (infrastructure/gitops/envs/dev/api.yaml:43), i.e. scale-to-zero, cold-start-tolerant placement. But ADR 0024 places all three warm binaries (api, fastbrain, deepbrain) on Cloud Run uniformly, not a distinguished 'burst, stateless' subset, and no service inventory records, per Cloud Run service, that it is stateless, its role (webhook/control API/other) or the cold-start/latency budget it was accepted against.",
    "evidence": [
      "infrastructure/gitops/envs/dev/api.yaml:43 (minInstanceCount: 0)",
      "backend/crates/apps/qip-api/src/registration_views.rs:227-536, venue_views.rs:100-227, rule_views.rs:33-111",
      "infrastructure/terraform/catalogue.tf (no per-service statelessness/cold-start-budget inventory field exists)"
    ],
    "gap": "No documented service inventory exists as this requirement's verification calls for; placement is broader than 'burst stateless webhook/control APIs only' because ADR 0024 runs every warm binary on Cloud Run uniformly.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists for a service inventory because none is written down; would need a new doc/test pairing recording role and cold-start budget per Cloud Run workload.",
    "work_item": "E14-service-inventory",
    "tier": "T1",
    "size": "S",
    "notes": "verify: status PARTIAL kept. deployable true -> false: Cloud Run services exist only as RunService manifests reconciled by Config Connector, and ADR 0093 suspends that path (no Cloud Run service runs in any environment). Also note qip-api is not a stateless endpoint: it holds the kernel Platform in-process (Mutex<Platform>, POST /cycle runs full cycles), so it is not an instance of the 'burst stateless' category either. Assessor note: Scored per the requirement's own note: 'This statement is met for burst endpoints; the divergence in the warm services is scored under C3' (GCP domain)."
  },
  {
    "id": "API-010",
    "status": "INCORRECT",
    "current": "qip-edge-node is kept off Cloud Run: it is absent from local.cloud_run_catalogue and from every RunService manifest, and the execution-node module's startup script runs it under systemd (infrastructure.rs:1790-1795). But a (simulated) venue-execution path is placed on Cloud Run anyway, inside the central binaries: qip-kernel's Platform builds its desk broker as SimulatedBroker ('the desk's own venue -- the one venue this process sends to (ADR 0003)', platform.rs:4366-4373), and every cycle's ACT stage sizes, signs off and releases orders through OrderManager::submit to that broker (platform.rs:13272-13326 stage_act/stage_act_orders -> submit_order platform.rs:15330-15360). qip-fastbrain (node.rs:133), qip-deepbrain (node.rs:157) and qip-api (POST /cycle, routes.rs:2259) all call platform.run_cycle, and all three are Cloud Run RunServices (infrastructure/gitops/envs/*/{fastbrain,deepbrain,api}.yaml, image: qip-*; api at minInstanceCount 0, i.e. cold starts on the order path). The requirement's own note defines venue execution here as the simulated venue, so this is the placement it forbids. The assessor's claim that qip-api 'cannot even link qip-execution-engine' is also false: api_boundary.rs's dependency_graph() reads direct non-dev edges only (cargo metadata --no-deps), and `cargo tree -p qip-api -e normal` shows qip-execution-engine, qip-capital, qip-chain, qip-capital-fabric, qip-orderbook and qip-sequencing linked transitively via qip-kernel.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:4366-4373 (desk broker = SimulatedBroker, 'the one venue this process sends to')",
      "backend/crates/runtime/qip-kernel/src/platform.rs:13272-13326 stage_act / stage_act_orders; :15330-15360 submit_order -> OrderManager::submit(order, self.broker)",
      "backend/crates/apps/qip-fastbrain/src/node.rs:133, backend/crates/apps/qip-deepbrain/src/node.rs:157, backend/crates/apps/qip-api/src/routes.rs:2259 (platform.run_cycle in Cloud Run binaries)",
      "infrastructure/gitops/envs/dev/fastbrain.yaml:50, deepbrain.yaml:50, api.yaml:43,50 (RunService images qip-fastbrain/qip-deepbrain/qip-api; api minInstanceCount 0)",
      "backend/crates/tests/qip-acceptance/tests/api_boundary.rs:98-130 dependency_graph (direct edges only; transitive links not checked)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:1790-1795 (execution-node starts qip-edge-node via systemd ExecStart -- the half that holds)",
      "backend/crates/tests/qip-acceptance/tests/manifest_wiring.rs:138-169 catalogue_workloads (a helper, not a test; no test asserts edge-node is absent from Cloud Run)"
    ],
    "gap": "The central desk's order path (ACT -> OrderManager -> SimulatedBroker) runs in three Cloud Run binaries, one at scale-to-zero. Meeting the requirement means moving desk execution to the execution-node placement (or recording, by ADR, that the central desk's simulated fills are not 'venue execution'), and adding a test that asserts no Cloud Run workload runs an execution path, including transitively.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test asserts the prohibition. infrastructure.rs checks the node's systemd unit; catalogue_workloads is a helper; api_boundary.rs's graph test checks direct edges only and passes while qip-api links qip-execution-engine through qip-kernel.",
    "work_item": "E04-desk-execution-placement",
    "tier": "T2",
    "size": "L",
    "notes": "verify: downgraded COMPLETE -> INCORRECT. The kernel's ACT stage submits orders to its SimulatedBroker desk venue inside qip-api, qip-fastbrain and qip-deepbrain, all Cloud Run services; the requirement's note says venue execution here means the simulated venue. The assessor's cited proof was a helper function (not a test) and a direct-edge graph test that misses the transitive qip-kernel -> qip-execution-engine link (confirmed with cargo tree -p qip-api -e normal). If the owner rules that central simulated fills are not venue execution, this becomes PARTIAL (no test pins edge-node off Cloud Run). Assessor note: deployable=false because execution_nodes = {} in every environment (no node exists to run the binary anywhere, Cloud Run or Compute Engine); the placement guarantee itself is structurally complete and proven."
  },
  {
    "id": "API-011",
    "status": "PARTIAL",
    "current": "frontend/portal/src/lib/server/identity-platform.ts implements the production provider (gipSignUp/gipSignIn/gipVerifyEmail/gipSendPasswordReset over Google Identity Toolkit REST, server-side only -- identity-platform.ts:96,114). infrastructure/terraform/modules/identity/main.tf:33 applies google_identity_platform_config.algorik in dev (enable_identity_platform=true, environments/dev/terraform.tfvars:451) with 3 authorized domains. But the deployed portal manifest never wires the key through: infrastructure/gitops/envs/dev/portal.yaml:94-98 states outright 'No Identity Platform key reaches this deployment -- the portal reads ALGORIK_IDENTITY_API_KEY straight from its environment... so identity.ts falls back to its own on-disk account store, which on Cloud Run is per-instance and in-memory.'",
    "evidence": [
      "frontend/portal/src/lib/server/identity-platform.ts:1-200",
      "infrastructure/terraform/modules/identity/main.tf:1-33",
      "infrastructure/environments/dev/terraform.tfvars:451-456",
      "infrastructure/gitops/envs/dev/portal.yaml:94-98 (the deployed manifest carries no Identity Platform key)",
      "frontend/portal/tests/identity-provider.spec.ts:37,74,93 (tests the half-configured refusal path only, never a real sign-in against Identity Platform)"
    ],
    "gap": "The Terraform config and the application code each exist and are each individually tested, but they are not wired together in the one manifest that would actually run them -- the deployed portal, if it ran today, would authenticate customers against a per-instance file store rather than Identity Platform.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "frontend/portal/tests/identity-provider.spec.ts (half-configured refusal only); no test signs a real customer in through Identity Platform end-to-end.",
    "work_item": "E15-identity-platform-wiring",
    "tier": "T1",
    "size": "S",
    "notes": "verify: status PARTIAL kept. tested true -> false: identity-provider.spec.ts:37,74,93 exercises only the half-configured refusal (project set, no key); no test shows a token from the project's Identity Platform tenant admitted or a foreign issuer/audience refused, which is the behaviour the requirement states. Citations checked (portal.yaml comment is at lines 96-106, not 94-98). Assessor note: CLAUDE.md names the research and risk desk as intended users, 'not external customers,' and lists retail distribution as a non-goal; this requirement's premise (a customer identity pool) is itself a scope question ADR 0099's conflict register does not cover."
  },
  {
    "id": "API-012",
    "status": "MISSING",
    "current": "infrastructure/terraform/modules/identity/main.tf:70-76 declares an mfa block whose state is var.identity_mfa_state, and the root variable defaults to \"ENABLED\" (optional MFA, not mandatory: infrastructure/terraform/variables.tf:1238-1242). No code anywhere handles a second-factor challenge from Identity Platform: identity-platform.ts and identity.ts have no mfaPendingCredential/mfaInfo/startMfaSignIn/finalizeMfaSignIn handling at all (grep for MFA across frontend/portal/src/lib/server finds nothing). Passkeys are referenced only in comments naming ADR 0038 as 'proposed, not applied' (frontend/portal/src/app/(auth)/sign-in/sign-in-form.tsx:31,162-163; src/app/api/auth/sign-in/route.ts:11-13) -- no WebAuthn/passkey code exists in the tree.",
    "evidence": [
      "infrastructure/terraform/modules/identity/main.tf:70-76",
      "infrastructure/terraform/variables.tf:1238-1242 (identity_mfa_state default \"ENABLED\", not \"MANDATORY\")",
      "frontend/portal/src/app/(auth)/sign-in/sign-in-form.tsx:31,162-163",
      "frontend/portal/src/app/api/auth/sign-in/route.ts:11-13"
    ],
    "gap": "MFA posture is optional rather than required, and no code path handles Google's MFA-challenge response even for enrolled accounts; passkeys are an unapplied, proposed ADR with no implementation. No test of any kind (Rust, Playwright or Terraform) mentions MFA, passkey or WebAuthn anywhere in the repository.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-011"],
    "blocked_by": "",
    "verification": "None exists; rg -i 'mfa|passkey|webauthn' across frontend/portal/tests and backend finds no test.",
    "work_item": "E15-customer-mfa-passkeys",
    "tier": "T2",
    "size": "L",
    "notes": "ADR 0038 (passkeys as the recorded destination) is proposed, not applied -- this requirement cannot be closed without that ADR landing first."
  },
  {
    "id": "API-013",
    "status": "PARTIAL",
    "current": "qip-api owns no Decimal-typed field and names every Mutex store it holds (api_boundary.rs:645, passes), and the ledger/treasury views (ledger_views.rs) are functions over the in-process Platform read at request time. But not every view the API returns is derived from portfolio/ledger state: the console renders cell observations from the API's own in-memory CellRegistry (cells.rs:38-117, Mutex<BTreeMap<String, CellObservation>> -- when each cell last reported and its position count, rendered by console.rs:189,338), the overview serves the last cycle's Vec<StageRow> (web.rs:69) and the stream serves a held HealthReading (stream.rs:622). The api_boundary test deliberately admits these stores; it does not prove views are recomputable. No test recomputes any API view from the event log at a position and compares it field for field (the requirement's replay check); truth_loop.rs:1193 proves the Platform's registries rebuild from the log, not the API's views.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/api_boundary.rs:645 the_api_owns_no_field_typed_as_money_and_every_store_it_holds_is_named_here (money-field proxy; admits the non-ledger stores)",
      "backend/crates/apps/qip-api/src/cells.rs:38-117 (CellRegistry, an API-held store of cell observations served in console views)",
      "backend/crates/apps/qip-api/src/console.rs:189,338 (views built from CellObservation)",
      "backend/crates/apps/qip-api/src/web.rs:69, stream.rs:622 (Vec<StageRow>, Option<HealthReading> held by the API)",
      "backend/crates/apps/qip-api/src/ledger_views.rs:289-627",
      "backend/crates/tests/qip-acceptance/tests/truth_loop.rs:1193 every_registry_the_platform_acts_on_rebuilds_from_the_event_log_alone_and_one_flipped_byte_refuses_the_replay (Platform registries, not API views)"
    ],
    "gap": "Some served views carry facts held only in the API process (cell last-report times and position counts, last stage rows, health pulse), and no replay test recomputes any API view from the log and compares field for field.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "api_boundary.rs:645 (ran: 9 passed in api_boundary) proves a proxy (no money field); the requirement's replay check -- each view at a log position recomputed from the log alone -- has no test.",
    "work_item": "E15-api-view-replay-proof",
    "tier": "T1",
    "size": "M",
    "notes": "verify: downgraded COMPLETE -> PARTIAL. The cited test proves the API keeps no money-typed field, not that every view is log-derived; the API serves views from its own CellRegistry/StageRow/HealthReading stores, and no test performs the requirement's replay comparison. tested -> false (no test demonstrates the stated behaviour); deployable -> false (ADR 0093: no Cloud Run service runs). Assessor note: e2e=false because no currently-running deployed process serves a live view to replay against; the property is proven in-process."
  },
  {
    "id": "API-014",
    "status": "COMPLETE",
    "current": "auth::Role enum (Monitor/Viewer/Analyst/Operator, backend/crates/apps/qip-api/src/auth.rs:52) and routes::ROUTES's per-route required_role (routes.rs:105-804) gate every route before a handler runs. Authenticator::authenticate is constant-time (auth.rs:367,421) to avoid a timing oracle. api_boundary.rs's route_table()-derived tests confirm every mutating route raises a typed kernel intent rather than mutating state directly (every_mutating_route_is_reviewed_here_and_each_raises_a_typed_intent, api_boundary.rs:816) and that the API calls no platform mutator it has not been explicitly allowed (api_boundary.rs:1041). A refused command provably leaves the ledger row unchanged (ledger_routes.rs:1225-1283).",
    "evidence": [
      "backend/crates/apps/qip-api/src/routes.rs:1186-1213 Api::admit (authenticate -> rate limit -> principal.require(route.required_role) before dispatch, for every ROUTES entry, REST and stream)",
      "backend/crates/apps/qip-api/src/auth.rs:52 (Role ladder)",
      "backend/crates/apps/qip-api/tests/ledger_routes.rs:1343 a_viewer_cannot_decide_a_users_eligibility_and_nothing_is_recorded (ran: 1 passed)",
      "backend/crates/apps/qip-api/tests/ledger_routes.rs:1225 an_eligibility_decision_is_refused_at_the_route_and_only_the_kernel_still_takes_one (row and replayed eligibility log unchanged)",
      "backend/crates/apps/qip-api/tests/registrations.rs:342 a_viewer_cannot_approve_a_registration",
      "backend/crates/apps/qip-api/tests/api.rs:1085 a_viewer_cannot_halt_the_platform",
      "backend/crates/apps/qip-api/tests/api.rs:651 mutating_routes_require_more_than_read_authority",
      "backend/crates/apps/qip-api/tests/api.rs:1059 an_unauthenticated_request_to_a_real_route_is_refused (GET /health only, not a command)",
      "backend/crates/tests/qip-acceptance/tests/api_boundary.rs:816 every_mutating_route_is_reviewed_here_and_each_raises_a_typed_intent",
      "frontend/portal/src/app/api/gateway/[...path]/route.ts (BFF: session + CSRF check, then forwards under the deployment's single viewer token, portal.yaml:152-158)"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-api api::; cargo test -p qip-acceptance --test api_boundary.",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "verify: status COMPLETE kept after refutation attempt: authorisation is structural (Api::admit runs before dispatch for every routed request) and named tests drive real command routes with an insufficient role and assert nothing reached the log (ledger_routes.rs:1343 ran and passed). Evidence corrected: the assessor's unauthenticated-request test hits GET /health, not a command; the no-identity case on a command endpoint is held only structurally, not by a test. Residual caveats: the portal BFF forwards every signed-in caller under one shared viewer token, so no end caller is individually authorised for a command (fail-closed today: viewer cannot mutate); qip-api's mesh publish listeners (mesh.rs CellFacing) authenticate nobody but are the edge-to-centre wire, not the public API (API-008). deployable true -> false (ADR 0093). Assessor note: No command may submit a live order (ADR 0003/0021); the command surface this proves is entirely paper-only, consistent with the platform's absolute rule."
  },
  {
    "id": "API-015",
    "status": "PARTIAL",
    "current": "What holds: qip-api and qip-web have no direct shipped Cargo edge to the ten FORBIDDEN_CRATES (api_boundary.rs:275, ran: passes); qip-api itself is INGRESS_TRAFFIC_INTERNAL_ONLY and reached publicly only through the portal gateway; the gateway redacts cells[].address on /mesh and /system/status (frontend/portal/src/lib/api/redaction.ts:70-90, proven on the wire by tests/wire.spec.ts:61,96,150). What does not: qip-api serves Reflex (edge cell) node addresses to any viewer-role caller -- GET /mesh (routes.rs:136, Role::Viewer) serialises MeshCellStatus{address} (mesh.rs:587-589) and /system/status embeds it -- so the API exposes them and only a two-entry denylist at the BFF keeps them from a browser; the 'structural' claim is overstated because dependency_graph() reads direct edges only and `cargo tree -p qip-api -e normal` shows qip-execution-engine, qip-capital, qip-chain, qip-capital-fabric, qip-orderbook and qip-sequencing linked transitively via qip-kernel; and no acceptance test enumerates every public route asserting none resolves to a ledger-store table, a Fabric broker endpoint, a Reflex node address or custody key material (the requirement's check). qip-api also hosts the mesh delta/capital inbox listeners (mesh.rs:317-380) in the same process.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/api_boundary.rs:47-58 FORBIDDEN_CRATES; :98-130 dependency_graph (cargo metadata --no-deps, direct non-dev edges only)",
      "backend/crates/tests/qip-acceptance/tests/api_boundary.rs:275 the_application_layer_depends_on_no_execution_venue_capital_or_edge_crate (ran: passes)",
      "backend/crates/apps/qip-api/src/routes.rs:134-139 (GET /mesh at Role::Viewer), :1236 (serves mesh_status_json)",
      "backend/crates/apps/qip-api/src/mesh.rs:587-589 (MeshCellStatus.address -- the cell's mesh base URL)",
      "frontend/portal/src/lib/api/redaction.ts:1-90 (BFF denylist redacting cells[].address on /mesh and /system/status)",
      "frontend/portal/tests/wire.spec.ts:61,96,150 (redaction asserted on the response body)"
    ],
    "gap": "The API serves Reflex node addresses at viewer role and relies on a BFF denylist; no route-enumeration acceptance test exists; the crate-graph guard misses transitive links to execution/capital/chain/fabric crates through qip-kernel.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "api_boundary.rs:275 (direct edges) and portal wire.spec.ts (two redacted fields) cover parts; the requirement's route enumeration over every public route does not exist.",
    "work_item": "E15-api-boundary-route-enumeration",
    "tier": "T1",
    "size": "M",
    "notes": "verify: downgraded COMPLETE -> PARTIAL. qip-api's GET /mesh and /system/status return edge-cell mesh addresses at Role::Viewer (the portal redacts them with a two-entry denylist, per its own redaction.ts); the cited graph test checks direct edges only while cargo tree shows six forbidden crates linked transitively; and the requirement's route-enumeration test does not exist. deployable -> false (ADR 0093). Assessor note: Stronger than a route-enumeration check: a Cargo-graph-level structural guarantee (compile-time), matching the repository's own 'make it structural' principle. Ledger-store-table exposure specifically cannot be tested against Spanner because Spanner is not built (C4)."
  },
  {
    "id": "API-016",
    "status": "PARTIAL",
    "current": "google_compute_backend_service.application sets protocol = \"HTTPS\" (infrastructure/terraform/modules/public-edge/main.tf:227) -- the one backend the LB would proxy to, when configured. As with API-002, no environment ever sets application_backend, so this protocol contract has never been exercised by a real request; the GKE Gateway arm the requirement also names does not exist (C3).",
    "evidence": [
      "infrastructure/terraform/modules/public-edge/main.tf:222-252",
      "infrastructure/terraform/modules/public-edge/tests/public-edge.tftest.hcl:82-115 the_application_identity_zone_may_be_fronted"
    ],
    "gap": "Correct in config and asserted by a plan test, but never applied because no environment attaches a backend to the edge.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-002"],
    "blocked_by": "",
    "verification": "No test asserts protocol = \"HTTPS\": public-edge.tftest.hcl::the_application_identity_zone_may_be_fronted asserts only the backend count and enable_cdn == false, and no terraform_plan.rs assertion reads the protocol.",
    "work_item": "E14-public-edge-activation",
    "tier": "T1",
    "size": "S",
    "notes": "verify: status PARTIAL kept. tested true -> false: the cited tftest run (public-edge.tftest.hcl:82-115) never reads google_compute_backend_service.application.protocol; nothing else asserts it. The attribute itself is at main.tf:230 (assessor cited :227)."
  },
  {
    "id": "API-017",
    "status": "MISSING",
    "current": "The portal is a single console with one internal RBAC ladder (auth::Role Monitor/Viewer/Analyst/Operator, backend/crates/apps/qip-api/src/auth.rs:52) and an admin sub-area within that one console (frontend/portal/src/app/(portal)/admin/{access,autonomy}/page.tsx). There is no investor-facing surface and no distinct general-user surface: frontend/portal/src/app/(portal)/treasury/ledger/page.tsx:138 states explicitly that 'this surface is the viewer's; it never evaluates as an investor or the desk' -- 'investor' in this codebase names a ledger-tracked customer account an operator administers, not a person who signs in.",
    "evidence": [
      "backend/crates/apps/qip-api/src/auth.rs:52 (one operator-facing Role ladder, not investor/admin/general-user)",
      "frontend/portal/src/app/(portal)/admin/access/page.tsx, admin/autonomy/page.tsx (admin is a sub-area of the one console)",
      "frontend/portal/src/app/(portal)/treasury/ledger/page.tsx:138",
      "frontend/portal/tests/mobile.spec.ts:110 (only reference to /admin/access in any test; no role-gating test exists)"
    ],
    "gap": "No investor-facing surface exists at all, and no general-user surface distinct from the operator console/admin area exists; the requirement's premise of an external-investor surface conflicts with CLAUDE.md's stated audience (the research and risk desk, 'not external customers') and its retail-distribution non-goal.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["API-011", "API-012"],
    "blocked_by": "",
    "verification": "None exists; no Playwright test signs in as an investor, an admin and a general user and asserts three distinct surfaces.",
    "work_item": "E99-portal-surface-scope-owner-decision",
    "tier": "T3",
    "size": "L",
    "notes": "This diagram-only requirement (H L99) has no supporting PDF paragraph and needs an owner decision on whether an external-investor surface is in scope at all before it is designed, per the requirement's own sourcing note."
  }
]
