[
  {
    "id": "WORLD-034",
    "status": "MISSING",
    "current": "qip-chain (backend/crates/services/qip-chain) models on-chain protocol state, AMM pricing, mempool ordering and bridges, and Platform::observe_chain (backend/crates/runtime/qip-kernel/src/platform.rs:16011) constructs a ChainState from it, but observe_chain has no caller outside tests (rg over crates/apps and qip-kernel/src finds only its definition) -- no composition root ever calls it or constructs a ChainAdapter. The specialist-society constructs a crypto brain would register under DO exist: qip_agents::governance::Roster (governance.rs:40) is the registry, AgentManifest.competencies (manifest.rs:96-100, 'the questions this agent is competent to answer; anything outside this list is a reason to defer') is each agent's declared scope, and Agent::accepts (runtime.rs:392) with ChiefInvestmentIntelligence ('decides which specialists a question belongs to', chief.rs:1-16) routes a brief every REASON stage. None of the 18 roster ids (manifests.rs:31-49) is a crypto/on-chain specialist and no agent reads qip-chain state.",
    "evidence": [
      "backend/crates/services/qip-chain/src/lib.rs:1-26",
      "backend/crates/runtime/qip-kernel/src/platform.rs:16011-16038 (observe_chain; no production caller)",
      "backend/crates/runtime/qip-kernel/tests/bridges.rs:139-236 (proves ChainState absorption, not a brain or routing)",
      "backend/crates/agents/qip-investment-agents/src/manifests.rs:31-49 (roster has no crypto entry)",
      "backend/crates/libs/qip-agents/src/governance.rs:40 (Roster: the agent registry)",
      "backend/crates/libs/qip-agents/src/manifest.rs:96-100 (competencies: declared scope)",
      "backend/crates/libs/qip-agents/src/runtime.rs:392 (Agent::accepts: per-brief routing predicate)"
    ],
    "gap": "No crypto/on-chain specialist exists on the roster and no agent reads on-chain state. The registry/scope/routing machinery exists, but routing is each agent's own accepts() predicate over the brief rather than a match of the question's topic against a declared scope, so even once a crypto agent existed the requirement's check (a fixture question on each of protocol state, token economics, bridges, liquidity, smart contracts and on-chain flows is routed to it) has no topic-routing surface to run against. qip-chain reaches no deployed process.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists for brain registration or scope-based routing. qip-chain's own contract tests (rpc_node.rs, chain.rs, pools.rs -- 69 tests) prove the on-chain model but not a brain.",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "Acting on a chain is out of scope (ADR 0099 C1); researching on-chain state is paper-safe and the underlying qip-chain crate is ready to be wired into a brain once one exists. verify: status MISSING confirmed. Corrected 'current'/'gap': the assessor said no specialist-brain registry, declared-scope type or routing mechanism exists anywhere; Roster, AgentManifest.competencies and Agent::accepts/ChiefInvestmentIntelligence are those constructs and run every REASON stage. What is missing is the crypto specialist itself and topic-based routing."
  },
  {
    "id": "WORLD-035",
    "status": "PARTIAL",
    "current": "The REASON stage allocates reasoning effort per open question and records every allocation. Platform::reason_decision_context (backend/crates/runtime/qip-kernel/src/platform.rs:10485) prices each queued opportunity as a qip_cost_router DecisionContext -- value at stake (importance x confidence), required confidence (importance capped by reasoning_confidence_bar), latency budget (time to expiry) and the regime Conditions -- and qip_cost_router's Router picks the cheapest rung able to answer or refuses one that costs more than the decision is worth. Platform::place_reason_routing (platform.rs:10768) is the only writer of the ReasonRouting record (platform.rs:2357: opportunity id, rung, rationale, whether the panel convened) and counts qip_reason_routings_total{tier,outcome}. Reached every REASON stage from qip-fastbrain/qip-deepbrain/qip-api (run_cycle). It is an allocation per question over an intelligence ladder, not across specialist brains: the result is a rung plus convene/decline for the whole panel, and which specialists run is each agent's own accepts() predicate. No 'Meta Brain' spreads a research budget across brains or models.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:2357-2430 (ReasonRouting: the per-question allocation record)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:10485-10560 (value at stake, required confidence, latency budget)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:10768-10790 (place_reason_routing: sole writer, counted)",
      "backend/crates/services/qip-cost-router/src/lib.rs:31-45 (Router::select: cheapest capable rung, unaffordable refused)",
      "backend/crates/runtime/qip-kernel/tests/kernel.rs:957 the_reason_stage_records_where_the_router_put_the_decision_and_whether_the_panel_convened"
    ],
    "gap": "Allocation is per question over a tier ladder, not across specialist brains and models: nothing assigns different questions to different brains by uncertainty and value. No platform-level test gives two open questions of differing value and asserts both allocations are recorded and move when the fixture values move; the kernel test asserts one allocation is recorded per reasoning cycle and agrees with the stage report.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test kernel -- the_reason_stage_records_where_the_router_put_the_decision_and_whether_the_panel_convened (run 2026-09-25: 1 passed).",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "qip-evolution's TrialLedger/GenerationRun allocate compute across strategy candidates, which is a different budget (strategy search, not cross-brain research attention) and does not satisfy this requirement. verify: upgraded MISSING -> PARTIAL; implemented/tested/integrated/deployable false -> true. The assessor searched for 'Meta Brain'/'research attention' and missed the cost router's REASON-stage allocation, which is a recorded per-question allocation of reasoning effort driven by value and required confidence. It is not the cross-brain allocation the requirement names, so not COMPLETE."
  },
  {
    "id": "WORLD-036",
    "status": "PARTIAL",
    "current": "Three pieces, none the whole requirement. (1) In production, per-regime reliability of a claim CLASS: Platform::score_claims_by_regime (backend/crates/runtime/qip-kernel/src/platform.rs:14597), called from the LEARN stage when theses resolve, records whether each directional claim held into a qip_evolution Scoreboard keyed (claim class, 'market/volatility' regime); claim_scores() (platform.rs:14638) is read by an operator and by nothing that decides. It keys on the regime at resolution, not at formation. (2) Per-MODEL, per-regime reliability exists but is unwired: qip_cost_router::ReputationBook (reputation.rs:306-343) keys each model's record by Conditions{asset class, region, market regime, volatility regime, horizon} (context.rs:170), shrinks a thin record to a coin flip, and scores an outcome in the regime the model was chosen in; rg finds ReputationBook only in its own crate and tests, and qip-api's missing.rs:58-62 reports 'nor any contextual reputation record'. (3) Per-BRAIN (analyst) and per-detector reliability is measured every LEARN stage by qip_learning_engine::SelfModel, keyed ComponentKey{kind, id} (self_model.rs:112) with no regime dimension, and fed back to REASON as origin factors (platform.rs:8479-8480). RegimeExperience (experience.rs:57) counts episodes per regime, not accuracy.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:14597-14640 (score_claims_by_regime, claim_scores)",
      "backend/crates/services/qip-cost-router/src/reputation.rs:1-45,306-343 (ReputationBook, keyed by Conditions)",
      "backend/crates/services/qip-cost-router/src/context.rs:170-176 (Conditions includes market and volatility regime)",
      "backend/crates/apps/qip-api/src/missing.rs:58-62 (no contextual reputation record is exposed)",
      "backend/crates/services/qip-learning-engine/src/self_model.rs:112-135 (ComponentKey has no regime field)",
      "backend/crates/libs/qip-ai/src/memory/experience.rs:57-100",
      "backend/crates/runtime/qip-kernel/tests/learning.rs:1495 a_resolved_claim_is_scored_against_the_regime_it_resolved_in",
      "backend/crates/services/qip-cost-router/tests/cost_router.rs:715 a_model_with_no_observations_in_a_regime_does_not_read_as_competent_there",
      "backend/crates/services/qip-cost-router/tests/cost_router.rs:949 a_models_score_lands_in_the_cell_it_was_chosen_in_not_the_one_its_outcome_resolved_in"
    ],
    "gap": "No measure is keyed (brain or model, regime) and reached: the production scoreboard is per claim class, the per-model regime book has no production caller, and the per-brain SelfModel has no regime key. No test replays a two-regime history in which one component is accurate in one regime and not the other and asserts its recorded reliability differs in that direction. The production scoreboard and ReputationBook also disagree on which regime an outcome belongs to (resolution vs decision time).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test learning -- a_resolved_claim_is_scored_against_the_regime_it_resolved_in (run: 1 passed); cargo test -p qip-cost-router --test cost_router -- a_model_with_no_observations_in_a_regime_does_not_read_as_competent_there a_models_score_lands_in_the_cell_it_was_chosen_in_not_the_one_its_outcome_resolved_in (run: 2 passed).",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "SelfModel's CAPABILITY_WINDOW/MINIMUM_SAMPLE machinery (self_model.rs:38-60) is the right shape to extend with a regime key; RegimeExperience already reads the episode store the same way and could supply the regime label. verify: upgraded MISSING -> PARTIAL; flags set for what exists. The assessor found SelfModel and RegimeExperience but missed ReputationBook (per-model per-regime competence, tested, unwired) and Platform::score_claims_by_regime (per-claim-class per-regime hit rates, reached every LEARN). The production scoreboard keys on the regime at resolution, which reputation.rs's module doc names as the error that makes a reputation book say the opposite of what it means; whoever closes this should pick one rule."
  },
  {
    "id": "WORLD-037",
    "status": "PARTIAL",
    "current": "Disagreement between brains (agents) is resolved for downstream use without deleting the minority. qip_reasoning_engine::engine::ReasoningEngine::reason (backend/crates/services/qip-reasoning-engine/src/engine.rs:167-257) turns every agent finding into evidence with a Supports/Contradicts stance, names every contradicting agent in ReasoningOutcome.dissenters (engine.rs:68,196), and forms the hypothesis with Hypothesis::form_with_factors, which scales each origin's evidence by the SelfModel's measured reliability for that origin (origin factors handed over every LEARN at platform.rs:8479-8480) and weighs each item by its own reliability/diagnosticity; the module doc (engine.rs:1-15) says it deliberately does not average convictions into a house view. OrganisationReport (qip-investment-agents/src/chief.rs:36-100) keeps every finding and flags is_contested, and the LEARN-stage episode keeps each analyst's stance. Reached every REASON stage through Platform::synthesise (platform.rs:12277). Not regime-weighted: the reliability factor comes from SelfModel, which has no regime key (WORLD-036), and there is no world-model plurality to arbitrate (one Upstream<WorldModel>, platform.rs:806).",
    "evidence": [
      "backend/crates/services/qip-reasoning-engine/src/engine.rs:1-15,167-257 (dissent carried as contradicting evidence, origin factors applied)",
      "backend/crates/agents/qip-investment-agents/src/chief.rs:36-100 (every finding kept; is_contested)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:8479-8480 (self-model origin factors handed to REASON each LEARN)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:12277-12360 (synthesise passes all findings)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:793-806 (single WorldModel, no plurality)",
      "backend/crates/tests/qip-acceptance/tests/stress.rs:1231 a_recommendation_its_own_organisation_contradicts_does_not_clear_the_action_bar",
      "backend/crates/runtime/qip-kernel/tests/self_model.rs:347 the_reason_factor_scales_an_origin_only_with_a_sufficient_sample_and_is_recorded_on_the_hypothesis"
    ],
    "gap": "Views are weighted by measured reliability and evidence but not by reliability in the current regime; world-model disagreement cannot arise because only one world model exists; no property test over generated disagreements asserts the resolved weights depend only on regime reliability and evidence and that every input view stays readable afterwards.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-036",
      "WORLD-041"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test stress -- a_recommendation_its_own_organisation_contradicts_does_not_clear_the_action_bar (run: 1 passed; asserts dissent is carried by name); cargo test -p qip-kernel --test self_model -- the_reason_factor_scales_an_origin_only_with_a_sufficient_sample_and_is_recorded_on_the_hypothesis (run: 1 passed).",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "This requirement is downstream of WORLD-041 (competing branches) and WORLD-036 (regime reliability); neither exists yet for it to weight against. verify: upgraded MISSING -> PARTIAL; flags set for what exists. The assessor looked only for world-model plurality, but the requirement covers brains too, and the reasoning engine already carries agent disagreement through as named dissent and contradicting evidence, weighted by measured origin reliability. Missing: the regime dimension of that weight and a world-model federation."
  },
  {
    "id": "WORLD-038",
    "status": "MISSING",
    "current": "nothing",
    "evidence": [],
    "gap": "No procedural-memory store, versioned-procedure/template/adapter/playbook type, or reuse-recording mechanism exists; rg -i 'procedural' across backend/crates matched only prose using the word 'procedural'/'procedure' in three unrelated doc comments (qip-brokers/src/lib.rs:14, qip-reasoning-engine/src/lib.rs:13, api_boundary.rs:845), none of which defines a store.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T1",
    "size": "M",
    "notes": "qip-agents::memory::ResearchMemory (backend/crates/libs/qip-agents/src/memory.rs) is the nearest existing shape -- an episode/lesson store with promotion -- but it is itself unreached in production (see qip-agents current-state gap: WriteResearchMemory is granted but no call site exercises it) and holds lessons, not versioned procedures/templates/playbooks."
  },
  {
    "id": "WORLD-039",
    "status": "PARTIAL",
    "current": "qip_twin::counterfactual::CounterfactualEngine::plan/settle/evaluate (backend/crates/services/qip-twin/src/counterfactual.rs:544,639,818) prices alternatives for both placed and declined/rejected actions. qip_twin::value::Simulated<T> (value.rs:43-69) structurally taints every counterfactual figure -- no Add against a real Decimal, no unwrapping accessor -- so a simulated result can never be reported as an observed one, which is the 'marked as an estimate' half of the requirement. qip-kernel's Platform::score_declined (platform.rs:16326) and Platform::evaluate_alternatives (platform.rs:16218) run this every LEARN stage over self.declined, and CounterfactualSet is a type distinct from the observed-outcome ledger (self.outcomes), so an estimate is never counted as observed.",
    "evidence": [
      "backend/crates/services/qip-twin/src/counterfactual.rs:48-117,370-378,494 (CounterfactualEngine),544 (plan),639 (settle),818 (evaluate)",
      "backend/crates/services/qip-twin/src/value.rs:43-69",
      "backend/crates/runtime/qip-kernel/src/platform.rs:16218,16326-16346",
      "backend/crates/runtime/qip-kernel/tests/learning.rs:1318 a_persistent_pattern_of_unfavourable_declines_on_one_instrument_narrows_only_that_instruments_sizing",
      "backend/crates/runtime/qip-kernel/tests/episodic.rs:709 a_precedent_says_what_the_platform_declined_on_the_episodes_it_recalled_and_whether_it_should_have",
      "backend/crates/tests/qip-acceptance/tests/e2e.rs:793-890 (Layer 6 builds OutcomeCapture and CounterfactualEngine by hand; it does not exercise Platform::score_declined)"
    ],
    "gap": "SimulatedOutcome (counterfactual.rs:370-378) has no field naming the pricing method that produced it -- it is always qip_simulation_engine::clock::SimulationClock::fill_price, but that is implicit in the code path rather than a recorded value, so the requirement's 'names the method' clause is not literally satisfied.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test learning -- a_persistent_pattern_of_unfavourable_declines_on_one_instrument_narrows_only_that_instruments_sizing (run 2026-09-25: 1 passed). The previous filter 'a_persistent_pattern_of_unfavourable_declines_on_one_instruments_sizing' matches no test name and ran 0 tests.",
    "work_item": "E09",
    "tier": "T0",
    "size": "S",
    "notes": "Adding an explicit `method: &'static str` field to SimulatedOutcome would close the remaining gap cheaply; the estimate-marking and never-counted-as-observed guarantees are already structural. verify: e2e true -> false. The cited e2e.rs Layer 6 constructs OutcomeCapture and CounterfactualEngine directly and never goes through Platform::score_declined, so no acceptance or cross-process test drives a declined action through the platform's own counterfactual path (the kernel learning test does, via submit_order then run_cycle). Evidence lines corrected (:494 is the struct, evaluate is :818). The row's verification filter ran 0 tests and is replaced. Status PARTIAL confirmed: DeclinedScore and SimulatedOutcome carry no field naming the method."
  },
  {
    "id": "WORLD-040",
    "status": "MISSING",
    "current": "qip_ai::registry::ModelCard (backend/crates/libs/qip-ai/src/registry.rs:56-96) is the nearest construct: it declares purpose, stage, training_datasets, evaluations, drift_score/drift_threshold and retired_at, but none of the five named fields (scope, evidence lineage, calibration, update cadence, expiry) is present -- rg -i 'scope|lineage|cadence|expiry' over registry.rs returns nothing -- and ModelCard has no constructor that refuses a card missing one of them.",
    "evidence": [
      "backend/crates/libs/qip-ai/src/registry.rs:56-96"
    ],
    "gap": "No WorldModelSpec-shaped declaration with scope/evidence-lineage/calibration/update-cadence/expiry exists, and nothing refuses a declaration missing one of the five; ModelCard's fields are a different, adjacent set.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E02",
    "tier": "T1",
    "size": "M",
    "notes": "WORLD-023 (expiry enforcement) and WORLD-024 (calibration scoring) and CONTRACT-023 are named by the catalogue as the enforcement/scoring/contract halves of this; none was found implemented either."
  },
  {
    "id": "WORLD-041",
    "status": "MISSING",
    "current": "nothing. qip_world_model::falsification (backend/crates/services/qip-world-model/src/falsification.rs) tracks a single hypothesis's Verdict (Refuted/Survived/Insufficient, enum at falsification.rs:717) against held-out data and qip_world_model::causal's EdgeStanding (causal.rs:328) retires an edge after RETIREMENT_CONSECUTIVE_FAILURES=3 (causal.rs:52) consecutive failures -- both are single-hypothesis lifecycles, not a set of competing branches updated and compared side by side until reality resolves them.",
    "evidence": [
      "backend/crates/services/qip-world-model/src/falsification.rs:717-735",
      "backend/crates/services/qip-world-model/src/causal.rs:52,169,328-343"
    ],
    "gap": "No type holds several live, competing branches at once; falsification and causal retirement each judge one hypothesis/edge in isolation, and a refuted edge is retired (its standing changes) rather than closed-with-score while the branch record stays separately readable.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-040"
    ],
    "blocked_by": "",
    "verification": "No test replays two branches predicting opposite outcomes and asserts both update until resolution.",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "M p10's Lane 3 'competing hypothesis branches' is the same placement (catalogue's own cross-reference, WORLD-027, not in this batch)."
  },
  {
    "id": "WORLD-042",
    "status": "MISSING",
    "current": "qip_ai::memory::store::EpisodicMemory (backend/crates/libs/qip-ai/src/memory/store.rs:184) enforces bounded retention -- DEFAULT_CAPACITY=4096 (store.rs:63), remember() (store.rs:350) evicts via evict_sampled() (store.rs:392) whenever the store exceeds capacity -- but this is single-episode eviction, not periodic summarisation/compression/deduplication into a compact artifact. No MemoryPack or EpisodeCluster type exists anywhere (rg found zero hits), so nothing records what a consolidation replaced.",
    "evidence": [
      "backend/crates/libs/qip-ai/src/memory/store.rs:63,184,350,392-420"
    ],
    "gap": "Bounded eviction exists; consolidation (summarise, compress, dedupe into a lineage-bearing MemoryPack/EpisodeCluster) does not. Semantic/procedural/market/failure memories have no consolidation path at all because most of those memory kinds do not exist (see WORLD-038, WORLD-046).",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists for a MemoryPack/EpisodeCluster; store.rs's own eviction tests (episodic.rs, 19 tests) prove capacity is respected, not that a summary is produced.",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "EXPAND-065 is named by the catalogue as carrying the same requirement from a different extraction; not assessed in this batch."
  },
  {
    "id": "WORLD-043",
    "status": "MISSING",
    "current": "qip_ai::memory::sampler::EpisodeSampler::victim (backend/crates/libs/qip-ai/src/memory/sampler.rs:242-261) biases eviction toward 'Calm'-graded episodes and gives 'Tail' (surprising/high-value) episodes a reserve, but the reserve is finite: once tail_held exceeds self.reserve(capacity), tail episodes are spent oldest-first exactly like calm ones (sampler.rs:249-253). There is no abstraction step, so an evicted tail episode is simply gone.",
    "evidence": [
      "backend/crates/libs/qip-ai/src/memory/sampler.rs:242-261"
    ],
    "gap": "The reserve is a soft preference, not the requirement's strict guarantee: a flagged high-value episode is not kept intact or replaced by a lineage-bearing abstraction -- once the tail reserve fills, the oldest tail episode is deleted outright.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-042"
    ],
    "blocked_by": "",
    "verification": "No test exists asserting a flagged episode survives consolidation (there is no consolidation step to test); sampler.rs's own tests prove the reserve threshold, not indefinite preservation.",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "Cannot be COMPLETE without WORLD-042's consolidation step existing first."
  },
  {
    "id": "WORLD-044",
    "status": "MISSING",
    "current": "nothing",
    "evidence": [],
    "gap": "No ReactionEpisode type or world-event-to-market-response pairing exists; rg -i 'reaction.?episode|material.?event' across backend/crates/services/qip-world-model and backend/crates/runtime/qip-kernel returned zero hits.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "Would draw on the Tick Lake (E06) for the multi-horizon, tick-level market response and on qip-world-model's absorb_news for 'material world event'; neither is wired to the other today."
  },
  {
    "id": "WORLD-045",
    "status": "MISSING",
    "current": "nothing. qip_world_model::granger::establish_temporal_precedence (backend/crates/services/qip-world-model/src/granger.rs:151-210) discovers causal edges from return-history series with a fixed policy (TEMPORAL_PRECEDENCE_LAG=1, ALPHA=0.01, granger.rs:44-104) and is reached from qip-kernel's causal_review.rs, but this is statistical discovery over price series, not an update driven by labelled reaction episodes (which do not exist -- see WORLD-044).",
    "evidence": [
      "backend/crates/services/qip-world-model/src/granger.rs:44-104,151-210"
    ],
    "gap": "No reaction-episode feed exists to update causal propagation models with, so no update-toward-observed-lag mechanism can exist either.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-044"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "Blocked in practice on WORLD-044 existing first."
  },
  {
    "id": "WORLD-046",
    "status": "PARTIAL",
    "current": "Memory grows from experience in part, reached every LEARN stage. Each resolved thesis is written to qip_ai::memory::EpisodicMemory by Platform::remember_resolved (backend/crates/runtime/qip-kernel/src/platform.rs:11538, remember at :11566) with the market state and the world model's causal context measured when it was reasoned (Episode.state, Episode.causal_context), the decision taken (including RejectedOnReview) and the outcome. Each order a control refuses is queued (self.declined), priced by the twin at its horizon and kept as a DeclinedScore naming the gate, rules and readings, then joined to recalled episodes as 'what we declined and whether we should have' (Precedent.declines). There is no distinct market memory or failure memory (rg -i 'market.?memory|failure.?memory' returns nothing); declined scores are a bounded ring (DECLINED_HISTORY), not a memory kind; market reaction episodes do not exist (WORLD-044); operational failures reach no memory; and the semantic store (qip_agents ResearchMemory) is placed on the desk empty (platform.rs:4271) behind a read-only Gated and never written in production.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:11538-11570 (remember_resolved)",
      "backend/crates/libs/qip-ai/src/memory/episode.rs:458-484 (Episode: state, causal_context, decision, outcome)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4271 (ResearchMemory::new(), never written)",
      "backend/crates/runtime/qip-kernel/tests/episodic.rs:206 an_episode_the_cycle_wrote_carries_the_market_and_world_state_measured_when_it_reasoned",
      "backend/crates/runtime/qip-kernel/tests/episodic.rs:709 a_precedent_says_what_the_platform_declined_on_the_episodes_it_recalled_and_whether_it_should_have"
    ],
    "gap": "No market-memory or failure-memory kind exists, so the requirement's check (an order rejection writes a failure-memory entry and a market reaction writes a market-memory entry, each with lineage) cannot run. A rejection is captured for counterfactual pricing and precedent joins, not as a failure memory; semantic and procedural memory do not grow at all (WORLD-038).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test episodic -- an_episode_the_cycle_wrote_carries_the_market_and_world_state_measured_when_it_reasoned a_precedent_says_what_the_platform_declined_on_the_episodes_it_recalled_and_whether_it_should_have (run: both passed).",
    "work_item": "E09",
    "tier": "T1",
    "size": "M",
    "notes": "verify: upgraded MISSING -> PARTIAL; flags set for what exists. Episodic memory does grow from resolved outcomes (one of the five named kinds) and refusals are captured with gate lineage and joined to memory; both are reached and tested. The two kinds the verification names (failure, market) do not exist."
  },
  {
    "id": "WORLD-047",
    "status": "MISSING",
    "current": "nothing",
    "evidence": [],
    "gap": "No BrainSpec/SpecialistPack type, no threshold-triggered proposal, no sandbox-only execution path, and no evaluation gate before service registration exists anywhere (rg for 'BrainSpec'/'SpecialistPack' across backend/crates returned zero hits).",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-048"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T3",
    "size": "XL",
    "notes": "qip-evolution's StrategyGenerator/ChallengerTest/TrialLedger (a champion/challenger promotion ladder for strategies, not brains) is the closest structural analogue in the codebase and could inform the design, but does not implement this requirement."
  },
  {
    "id": "WORLD-048",
    "status": "PARTIAL",
    "current": "The declaration and enforcement a specialist would carry exists for every agent: qip_agents::manifest::AgentManifest (backend/crates/libs/qip-agents/src/manifest.rs:86-116) declares role, purpose, competencies (declared scope), limitations, capabilities (the facilities and tools it may reach, i.e. its authority scope), budget, escalation and model references. AgentManifest::validate (manifest.rs:209-345) refuses a manifest with no id, purpose or owner, with an empty capability set ('is granted nothing'), or whose capabilities break separation of duties; AgentHost::run re-validates on every run through AgentManifest::authorisation (manifest.rs:338-339); and any facility call outside the declared set is refused and recorded at AgentContext::authorise (runtime.rs:331-345). There is no BrainSpec: no corpus field, no evaluations field (models names model references, not evaluations of the agent), and no creation path for a new specialist (WORLD-047).",
    "evidence": [
      "backend/crates/libs/qip-agents/src/manifest.rs:86-116 (AgentManifest fields)",
      "backend/crates/libs/qip-agents/src/manifest.rs:209-345 (validate: empty capabilities, purpose, owner, separation of duties)",
      "backend/crates/libs/qip-agents/src/manifest.rs:338-349 (authorisation calls validate on every run)",
      "backend/crates/libs/qip-agents/src/runtime.rs:331-345 (calls outside the declared set refused)",
      "backend/crates/libs/qip-agents/tests/agents.rs:195 an_agent_without_a_purpose_or_an_owner_does_not_validate",
      "backend/crates/libs/qip-agents/tests/agents.rs:455 a_gated_facility_cannot_be_reached_without_its_capability"
    ],
    "gap": "Corpus and evaluations are not declared on any agent, so a specialist missing them cannot be refused; there is no BrainSpec/SpecialistPack for a newly created specialist; no test constructs a declaration missing each of the four in turn (the empty-capability refusal is in code but no named test asserts it).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-agents --test agents -- an_agent_without_a_purpose_or_an_owner_does_not_validate a_gated_facility_cannot_be_reached_without_its_capability (run: both passed).",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "P0 per the catalogue because it is a safety invariant on authority even though creation (WORLD-047) is later Phase 9 work; qip_agents::capability::CapabilitySet::require/is_subset_of is the enforcement primitive a BrainSpec's authority scope would compose with. verify: upgraded MISSING -> PARTIAL; flags set for what exists. Two of the four declared elements (tools and authority scope, as AgentManifest.capabilities/role) are declared, refused when empty, re-validated on every run, and enforced per call -- the second half of the requirement's own check. Corpus and evaluations do not exist and there is no BrainSpec for a newly created specialist."
  },
  {
    "id": "WORLD-049",
    "status": "PARTIAL",
    "current": "Specialist brains already receive one kind of shared knowledge as a typed artifact rather than as chat: at REASON the platform recalls the nearest resolved episodes from EpisodicMemory and briefs every panel agent through the typed AgentBrief.precedent field (BriefPrecedent: digest, similarity, prior outcome, age; backend/crates/libs/qip-agents/src/finding.rs:307-314, set at platform.rs:10988), deliberately never through the free-text context. Each episode carries the previous panel's per-analyst stances, the world model's causal context and the outcome, so one cycle's resolved experience reaches every specialist in the next. The finding.rs doc says nothing that computes a conviction reads it: it informs narrative only. Findings reach the reasoning engine as validated, typed AgentFinding records. Regional cells receive only typed mesh policy frames (qip_edge::mesh) -- routing policy, sizing, region share -- not knowledge. No KnowledgeDelta/SharedEpisode type exists, no specialist can publish a discovery to another, and nothing reaches a regional cell as knowledge.",
    "evidence": [
      "backend/crates/libs/qip-agents/src/finding.rs:300-314 (typed precedent field, kept out of free-text context)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:2185,10980-10996 (brief_precedent; brief.with_precedent)",
      "backend/crates/runtime/qip-kernel/tests/episodic.rs:440 the_panel_is_briefed_on_the_recalled_precedent_through_the_typed_field_and_only_when_one_was_recalled",
      "backend/crates/services/qip-streaming/src/envelope.rs (adjacent: event envelope, not knowledge sharing)",
      "backend/crates/edge/qip-edge/src/mesh.rs (adjacent: policy frames, not knowledge sharing)"
    ],
    "gap": "No publish path from one specialist to another or to a regional cell: the shared episode is the platform's own memory recalled to the panel, not a discovery a specialist published, and it cannot move a conviction. No KnowledgeDelta type, no lineage-bearing knowledge message to a cell, and no boundary that refuses an untyped knowledge payload (the precedent test proves the typed channel is the only channel, which is the nearest thing).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-034",
      "WORLD-047"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test episodic -- the_panel_is_briefed_on_the_recalled_precedent_through_the_typed_field_and_only_when_one_was_recalled (run: 1 passed; asserts the precedent reaches the panel through the typed field and not the free-text context).",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "Same requirement as EXPAND-012/CONTRACT-002 per the catalogue's own notes; the regional-node half additionally needs MESH domain wiring not assessed here. verify: upgraded MISSING -> PARTIAL on the brain-to-brain half only: recalled episodes reach every specialist as a typed BriefPrecedent, tested to arrive through the typed channel and not as prose. It is narrative-only and recall rather than publication; the regional-node half and a specialist-published KnowledgeDelta are absent."
  },
  {
    "id": "WORLD-050",
    "status": "MISSING",
    "current": "nothing. Only one WorldModel instance exists per Platform (platform.rs:806); qip_world_model has no branching-population construct beyond the falsification/causal-edge machinery already covered under WORLD-041.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:793-806"
    ],
    "gap": "No WorldModelSpec/WorldModelBranch type, no insufficiency-threshold trigger, and no spawning mechanism exists.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-040"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T3",
    "size": "XL",
    "notes": "Same requirement as EXPAND-018 per the catalogue's own notes."
  },
  {
    "id": "WORLD-051",
    "status": "MISSING",
    "current": "nothing. qip-evolution's ChallengerTest/TrialLedger (backend/crates/services/qip-evolution/src/challenger.rs:96,350,366) is the closest existing benchmark-then-promote pattern in the workspace, applied to strategies rather than world models.",
    "evidence": [
      "backend/crates/services/qip-evolution/src/challenger.rs:96-366 (adjacent: strategy champion/challenger, not world models)"
    ],
    "gap": "Nothing to benchmark, because no spawned world model exists (WORLD-050 is MISSING).",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-050"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "Same requirement as EXPAND-019 per the catalogue's own notes; qip-evolution's promotion ladder (advance/advance_challenger, promotion.rs:39,55) is a ready-made pattern to reuse once WORLD-050 exists."
  },
  {
    "id": "WORLD-052",
    "status": "BLOCKED",
    "current": "The permitted form: qip-world-model runs in-process inside the single 'deepbrain' Cloud Run service declared in infrastructure/terraform/catalogue.tf:486-520 (min_instances=1, max_instances=1, always_on_justification given), which is long-running but is one monolithic service, not per-model versioned world-model services, holds no domain-specific store or graph of its own (WorldModel is in-memory only, held inside qip-kernel's Platform), and runs on Cloud Run rather than GKE (Kubernetes retired, ADR 0024).",
    "evidence": [
      "infrastructure/terraform/catalogue.tf:486-520 (deepbrain Cloud Run service declaration)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:793-806 (WorldModel held in-memory, no per-model store)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:5558 every_workload_that_runs_the_cycle_over_the_journal_is_pinned_to_one_warm_instance (long-running: min=max=1 with a written justification)"
    ],
    "gap": "No GKE compute pool or per-model versioned service exists (and per ADR 0024 none can, without reopening a settled decision); world-model logic is folded into one always-on Cloud Run process with no domain-specific store of its own.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-050"
    ],
    "blocked_by": "C3,C7",
    "verification": "cargo test -p qip-acceptance --test infrastructure -- every_workload_that_runs_the_cycle_over_the_journal_is_pinned_to_one_warm_instance (run: 1 passed) proves the permitted form is long-running. No terraform plan was run in this verification; no committed config declares a GKE compute pool or a per-world-model versioned service.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": "The literal requirement (GKE) conflicts with ADR 0024; the reachable form within the current architecture would be a second, separately-versioned Cloud Run service per world-model population, which is closer to but still short of the requirement's 'own domain-specific stores/graphs'. verify: blocked_by C3 -> C3,C7 (one long-running service per world model is ADR 0099's C7 'separate warm services per brain' as much as C3's GKE). Added the infrastructure test that backs tested=true for the permitted form; it had no named test. A per-model version and a world-model-owned store are buildable in-tree without C3/C7 and are absent: WorldModel lives only in Platform memory."
  },
  {
    "id": "WORLD-053",
    "status": "NEEDS-VALIDATION",
    "current": "Structurally satisfied: qip-edge (backend/crates/edge/qip-edge/Cargo.toml) declares no dependency on qip-world-model at all -- confirmed directly (grep across crates/edge/*/Cargo.toml for qip-world-model returns nothing) -- so no edge cell can reach a world-model query even transitively. qip-edge-node runs Cell::work only under QIP_VENUE_FEED=simulated (qip-edge-node/src/pass.rs:117 run_pass, called at main.rs:888), reached in production code but with execution_nodes={} in every environment, so nothing is actually deployed.",
    "evidence": [
      "backend/crates/edge/qip-edge/Cargo.toml (no qip-world-model dependency)",
      "backend/crates/apps/qip-edge-node/src/pass.rs:117 (run_pass, gated by QIP_VENUE_FEED=simulated; called at main.rs:888)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:715 no_edge_cell_can_reach_a_language_model (the shape of the missing test; it names qip-ai, not qip-world-model)"
    ],
    "gap": "No dedicated test (chaos or architecture) asserts 'no edge crate depends on qip-world-model' or 'stopping every world-model service leaves reflex latency/decisions unchanged' -- the property holds by the absence of a Cargo dependency, not by an assertion a change could break loudly.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "A test that would prove it does not exist yet; write an architecture.rs assertion mirroring no_edge_cell_can_reach_a_language_model but naming qip-world-model.",
    "work_item": "E01",
    "tier": "T0",
    "size": "S",
    "notes": "execution_nodes = {} in every environment (infrastructure/environments/*/terraform.tfvars), so this is also not deployable today regardless of the code-level guarantee. verify: status NEEDS-VALIDATION confirmed. `cargo tree -p <crate> -e normal` for all nine edge crates (qip-edge, qip-edge-node, qip-routing, qip-sequencing, qip-orderbook, qip-strategy, qip-arbitrage, qip-feature-dag, qip-protocols) shows no transitive qip-world-model, and no architecture/chaos test asserts it. Corrected the run_pass citation (main.rs:779 is stale; the call is at :888). Caveat for whoever closes this: the central fast brain (qip-fastbrain) runs the full Platform cycle, world model included, in the same synchronous loop as its paper ACT stage; it is not the reflex cell ADR 0099 maps to execution nodes, but it is the only order-producing path that is deployable today."
  },
  {
    "id": "WORLD-054",
    "status": "COMPLETE",
    "current": "qip_agents::runtime::AgentContext::authorise (backend/crates/libs/qip-agents/src/runtime.rs:331-345) is the single enforcement point: checks self.manifest.capabilities.contains(capability), records an AccessRecord (granted or not) before returning, and refuses with Error::denied naming the agent and capability when not granted. Every facility access (complete() for the language model, and Gated<T> accessors on Desk) routes through it. Reached every REASON stage in all four composition roots via Organisation::dispatch (platform.rs:10998).",
    "evidence": [
      "backend/crates/libs/qip-agents/src/runtime.rs:331-345 (authorise: check, record, refuse)",
      "backend/crates/libs/qip-agents/src/runtime.rs:74-77 (Gated::get calls authorise before any borrow)",
      "backend/crates/libs/qip-agents/src/runtime.rs:353-377 (complete: the only non-qip-ai language-model call, authorised first)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:10998 (Organisation::dispatch every REASON stage)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:11030-11046 (denials counted per agent on qip_permission_denials_total)",
      "backend/crates/libs/qip-agents/tests/agents.rs:49 a_capability_set_refuses_what_it_does_not_grant",
      "backend/crates/libs/qip-agents/tests/agents.rs:455 a_gated_facility_cannot_be_reached_without_its_capability",
      "backend/crates/libs/qip-agents/tests/agents.rs:479 a_granted_facility_is_reachable_and_charged",
      "backend/crates/tests/qip-acceptance/tests/security.rs:209 (a suborned agent's language-model call is refused and recorded)"
    ],
    "gap": "",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-agents --test agents -- a_capability_set_refuses_what_it_does_not_grant a_gated_facility_cannot_be_reached_without_its_capability a_granted_facility_is_reachable_and_charged (run 2026-09-25: 3 passed).",
    "work_item": "",
    "tier": "T0",
    "size": "S",
    "notes": "Every AccessRecord is pushed whether granted or refused, so a probing agent leaves evidence even on success. verify: status COMPLETE confirmed; e2e true -> false. authorise is the only path to a desk facility (Gated::get calls it first) and to the language model (the only complete_structured call outside qip-ai is inside AgentContext::complete); agents hold no other tool. Refusal-and-log is proven by agents.rs:455 and the permitted call carrying its grant by agents.rs:479 (capabilities_used, no denials), both run and passing; reached from qip-fastbrain/qip-deepbrain/qip-api via run_cycle. But no acceptance or cross-stage test drives a refused tool call through run_cycle: kernel.rs:1153 runs a cycle and asserts zero denials, and security.rs calls AgentHost::run directly, so e2e is not demonstrated."
  },
  {
    "id": "WORLD-055",
    "status": "PARTIAL",
    "current": "Structurally enforced in the direction the requirement asks: qip-world-model (Cargo.toml) has zero dependency on qip-execution-engine or any order-construction crate, and architecture.rs::the_agent_organisation_cannot_reach_execution_at_all (line 634) asserts qip-investment-agents (which does depend on qip-world-model) cannot reach qip-execution-engine in the dependency graph. Separation-of-duty tests (exactly_one_agent_can_submit_orders_and_it_publishes_nothing, no_research_side_agent_can_reach_the_market -- qip-investment-agents/tests) prove only ExecutionTrader can submit, and it holds no thesis-publishing capability.",
    "evidence": [
      "backend/crates/services/qip-world-model/Cargo.toml (no execution-crate dependency)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:634-670 the_agent_organisation_cannot_reach_execution_at_all",
      "backend/crates/agents/qip-investment-agents/tests/organisation.rs:116 no_research_side_agent_can_reach_the_market",
      "backend/crates/agents/qip-investment-agents/tests/organisation.rs:132 exactly_one_agent_can_submit_orders_and_it_publishes_nothing"
    ],
    "gap": "No single test traces a maximum-confidence world-model output through REASON/evaluation/authority to a blocked order the way the requirement's own verification describes; the guarantee is assembled from several separately-scoped tests (dependency graph plus agent separation of duties) rather than demonstrated end to end for a world-model-originated claim specifically.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-054"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test architecture -- the_agent_organisation_cannot_reach_execution_at_all; cargo test -p qip-investment-agents",
    "work_item": "E09",
    "tier": "T1",
    "size": "M",
    "notes": "A dedicated integration test (inject a maximal-confidence world-model claim, assert no OrderIntent exists until RiskControl/ComplianceControl have run) would close the strict-COMPLETE gap cheaply given the structural pieces already hold. verify: status PARTIAL confirmed; cited tests run and pass (architecture: 1, organisation: 2). `cargo tree -p qip-world-model -e normal` reaches qip-ai, qip-contracts, qip-core, qip-entity-resolution, qip-events, qip-financial, qip-market and qip-numerics only, and the only order types (qip-execution-engine Order, qip-routing ChildOrder) are in neither, so the structural half holds -- but no architecture test names qip-world-model as a root, which the requirement's check asks for. Evidence made precise (test file and lines)."
  },
  {
    "id": "WORLD-056",
    "status": "INCORRECT",
    "current": "qip_world_model::graph (backend/crates/services/qip-world-model/src/graph.rs) holds entities (NodeKind, graph.rs:15-27), timed facts bitemporally (valid_from/valid_to/recorded_at/retracted_at, graph.rs:66-83) and a confidence per fact (graph.rs:81); Relationship carries a source string (relationship.rs:132) and RelationshipKind::ContradictsThesis (relationship.rs:62); CausalEdge (causal.rs:169) is the causal-edge category; Thesis is a NodeKind standing in for beliefs. The write path does what the requirement's check forbids: Fact::new (graph.rs:85-94) takes no confidence and defaults it to 1.0, Fact::with_confidence (graph.rs:96-98) clamps rather than refuses (and passes NaN through, since f64::clamp returns NaN), WorldModel::relate (world.rs:234-258) writes through that clamp, and the production news path asserts ConcernsEntity facts with Fact::new and no confidence at all (world.rs:557-567), so every one is stored at confidence 1.0. Relationship::new clamps weight and accepts an empty source.",
    "evidence": [
      "backend/crates/services/qip-world-model/src/graph.rs:15-98 (schema; Fact::new defaults confidence 1.0; with_confidence clamps)",
      "backend/crates/services/qip-world-model/src/world.rs:234-258 (WorldModel::relate writes through the clamp)",
      "backend/crates/services/qip-world-model/src/world.rs:557-567 (absorb_news asserts facts with no stated confidence)",
      "backend/crates/services/qip-world-model/src/relationship.rs:62,124-150 (ContradictsThesis; Relationship::new clamps weight, any source string)",
      "backend/crates/services/qip-world-model/src/causal.rs:169",
      "backend/crates/services/qip-world-model/tests/understanding.rs:35 a_fact_is_invisible_before_it_was_known_even_if_it_was_already_true",
      "backend/crates/services/qip-world-model/tests/understanding.rs:2336 (CausalEdge::with_confidence refusal -- the causal-edge type, not the graph's Fact)"
    ],
    "gap": "A belief written without a confidence is admitted at 1.0 instead of refused, an out-of-range or NaN confidence is clamped or passed through instead of refused, and a fact with an empty source is admitted -- the opposite of the check. Contradictions are declared (ContradictsThesis) but nothing writes one (rg finds only the declaration and as_str), and SupportsThesis likewise has no writer. No test asserts the schema covers all eight categories.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-world-model --test understanding -- a_fact_is_invisible_before_it_was_known_even_if_it_was_already_true (run: passed; bitemporal facts). No test covers refusal of a belief without confidence or source, because the code does not refuse.",
    "work_item": "E09",
    "tier": "T0",
    "size": "S",
    "notes": "Closing the gap is cheap: wire a writer that actually constructs ContradictsThesis edges (e.g. from falsification's Verdict::Refuted) and add one schema-completeness test. verify: PARTIAL -> INCORRECT. The graph write path defaults a missing confidence to 1.0 and clamps a stated one (Fact::new / Fact::with_confidence / WorldModel::relate, and absorb_news in production), where the requirement's check says a belief without a confidence or source is refused -- the clamp-instead-of-refusal case the status definitions name. The assessor's 'Fact's own construction path is tested to refuse an out-of-range value elsewhere' is wrong: the cited understanding.rs:2336 test is CausalEdge::with_confidence, which was fixed; the graph's Fact was not. Flags kept for the schema that exists."
  },
  {
    "id": "WORLD-057",
    "status": "PARTIAL",
    "current": "Two structural guards, neither tested at the knowledge-graph boundary. (1) Discovery: qip_data_finder::probe::SampleManifest (backend/crates/services/qip-data-finder/src/probe.rs:109-140) replaced the PayloadSample-holding ProbeEvidence; it has no field a body can be put in, so a registered source keeps a hash, length and media type, never the text -- and registration_is_not_ingestion.rs:101 proves a registered source's JSON carries neither the sampled prose nor a quoted value. (2) The graph's own news path: qip_financial::intelligence::NewsItem (intelligence.rs:121-139) carries a SourceManifest (locator and content hash) and no article text, so WorldModel::absorb_news (world.rs:525-605) can write only the headline as an Event node label and the locator as an index attribute. The headline is a verbatim span with no declared excerpt limit, and no guard or test exists on the graph write API itself (graph.rs/world.rs accept any string in a node label or attribute).",
    "evidence": [
      "backend/crates/services/qip-data-finder/src/probe.rs:80-140 (PayloadSample vs SampleManifest)",
      "backend/crates/services/qip-data-finder/tests/registration_is_not_ingestion.rs:101 a_registered_source_carries_no_copy_of_the_text_the_probe_sampled (discovery boundary, not the graph)",
      "backend/crates/libs/qip-financial/src/intelligence.rs:121-139 (NewsItem carries a manifest, not the text)",
      "backend/crates/services/qip-world-model/src/world.rs:549-605 (headline as node label; locator, not text, indexed)"
    ],
    "gap": "No excerpt limit is declared and nothing refuses a graph write carrying a payload body: the guarantee rests on upstream types (SampleManifest, NewsItem) having no body field, and on headlines being short. No property test runs generated payloads through ingestion and asserts no graph record contains a verbatim span over a limit; the one test that asserts no retained copy is at the data-finder registration boundary, which is not the knowledge graph.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-056"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-data-finder --test registration_is_not_ingestion (run: 2 passed) covers the discovery boundary only; no test covers the graph boundary.",
    "work_item": "E09",
    "tier": "T1",
    "size": "S",
    "notes": "DATA-005/DATA-042 are named by the catalogue as carrying the platform-wide form of this rule; not assessed in this batch. verify: status PARTIAL confirmed, text corrected both ways. The assessor said no dedicated test exercises SampleManifest's exclusion of a body (registration_is_not_ingestion.rs:101 does, and passes) and that no equivalent guard exists on the graph's own write path (NewsItem structurally carries no article text, so absorb_news cannot write one). tested kept false because both the test and the guards sit upstream of the knowledge graph the requirement names."
  },
  {
    "id": "WORLD-058",
    "status": "BLOCKED",
    "current": "nothing. The Terraform tree (infrastructure/terraform) is a single flat project per environment -- rg for 'prod/world-model' or 'prod/ledger' across infrastructure/**/*.tf returns zero hits -- with no organisation/folder/project hierarchy separating a ledger workload boundary from a cognition one. Cloud Run services are trust-zoned (catalogue.tf's trust_zone field, e.g. deepbrain's 'cognition' zone) but share one GCP project.",
    "evidence": [
      "infrastructure/terraform/catalogue.tf:486-520 (single-project trust-zone model, no per-domain project)",
      "infrastructure/terraform/modules/data/main.tf:326-355 (the only Spanner declared: instance 'global' with database 'positions', enable_spanner=false in every environment)"
    ],
    "gap": "No dedicated prod/world-model project exists, so no isolation from a ledger project can exist either; the current topology is one project with trust zones, not GCP v2.1's org/folder/project model this requirement assumes.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C4",
    "verification": "grep -rn 'prod/world-model' infrastructure/ returns nothing.",
    "work_item": "E14",
    "tier": "T3",
    "size": "L",
    "notes": "Spanner itself is a new managed dependency (C4); even a project split without Spanner would only partially satisfy this requirement, which names Spanner Graph explicitly as the isolated store. verify: status BLOCKED (C4) kept; context added. The only Spanner in committed Terraform is modules/data's 'global' instance holding the 'positions' database, disabled everywhere; a world graph placed on it would share an instance with financial state, which this requirement forbids, so the placement must be a separate instance and project when C4 is lifted. The isolation half is not itself refused by ADR 0099 and is absent today: the world model and the outcome/ledger state share one Platform process in each brain."
  },
  {
    "id": "WORLD-059",
    "status": "BLOCKED",
    "current": "The permitted (in-tree) form: qip_world_model's KnowledgeGraph (graph.rs) and CausalGraph (causal.rs) hold the world and causal graph in-process, with no action-affordance graph component at all. No Spanner Graph database, schema, or client exists anywhere in the workspace.",
    "evidence": [
      "backend/crates/services/qip-world-model/src/graph.rs, causal.rs (in-tree substitute, no action-affordance graph)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:8740-8752 (absorb_news every SENSE: the in-tree graph is reached)",
      "backend/crates/services/qip-world-model/tests/understanding.rs:35 a_fact_is_invisible_before_it_was_known_even_if_it_was_already_true",
      "backend/crates/services/qip-world-model/tests/understanding.rs:321 a_shock_propagates_and_attenuates_with_each_hop",
      "infrastructure/terraform/modules/data/main.tf:326-355 (Spanner 'global'/'positions', disabled; no graph database or schema)"
    ],
    "gap": "Spanner Graph is a new managed dependency the workspace's two-dependency policy (serde/serde_json only) refuses without an ADR; even setting that aside, the in-tree graph has no action-affordance component.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-058"
    ],
    "blocked_by": "C4",
    "verification": "cargo test -p qip-world-model --test understanding -- a_fact_is_invisible_before_it_was_known_even_if_it_was_already_true a_shock_propagates_and_attenuates_with_each_hop (run: 2 passed) for the in-tree form. modules/data declares a disabled Spanner instance with a 'positions' database and no graph schema; nothing declares Spanner Graph.",
    "work_item": "E14",
    "tier": "T3",
    "size": "L",
    "notes": "A storage decision record (per the catalogue's own note) is needed before this is built, on top of the ADR the new dependency itself would require. verify: flags false -> true for the permitted in-tree form (world and causal graphs exist, are written every SENSE stage in the brains, and are tested), as the BLOCKED rule requires; e2e stays false and the action-affordance graph is absent even in-tree. Corrected the verification: Terraform does declare a real, disabled Spanner instance and database (modules/data/main.tf:326), not only boilerplate -- but no graph."
  },
  {
    "id": "WORLD-060",
    "status": "BLOCKED",
    "current": "The permitted (in-tree) form: qip_entity_resolution::Resolver (backend/crates/services/qip-entity-resolution/src/resolver.rs) normalises entity mentions across providers in-process, called from WorldModel::absorb_news (world.rs:525-537), itself reached from platform.rs:8751 every SENSE-stage news absorption -- so normalised-entity writing exists and is hardened, just entirely in-process rather than over a warm-path gRPC/SQL/GQL connection to an external graph service.",
    "evidence": [
      "backend/crates/services/qip-entity-resolution/src/resolver.rs (in-tree normalisation, hardened, reached from production)",
      "backend/crates/services/qip-world-model/src/world.rs:525-547",
      "backend/crates/runtime/qip-kernel/src/platform.rs:8745-8752"
    ],
    "gap": "No gRPC/SQL/GQL client or warm-path writer exists (a gRPC client is itself a new, refused dependency), and there is no Spanner Graph to write to (WORLD-059 is BLOCKED).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "WORLD-059"
    ],
    "blocked_by": "C2,C4",
    "verification": "cargo test -p qip-entity-resolution (tests/resolution.rs) proves in-tree normalisation; no test exercises a gRPC write because no such path exists.",
    "work_item": "E14",
    "tier": "T3",
    "size": "L",
    "notes": "EVID-029 is named by the catalogue as carrying the evidence-side half of this requirement; not assessed in this batch."
  },
  {
    "id": "WORLD-061",
    "status": "MISSING",
    "current": "nothing. No world-model plurality exists to disagree (WORLD-041/WORLD-050 are both MISSING), so no disagreement record exists to become a research signal; qip_events::topic::Topic::ResearchCampaignFlagged/ResearchCampaignClosed (backend/crates/libs/qip-events/src/topic.rs:191-199) are about qip-data-finder's fetch-campaign concentration checks, an unrelated use of 'research'.",
    "evidence": [
      "backend/crates/libs/qip-events/src/topic.rs:191-199 (unrelated: data-finder campaign events, not world-model disagreement)"
    ],
    "gap": "No world-model disagreement exists to detect, no materiality threshold is declared, and no research-task generation exists.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-037",
      "WORLD-041"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "Blocked in practice on WORLD-041 (competing branches) existing first, since there is otherwise nothing to compare."
  },
  {
    "id": "WORLD-062",
    "status": "BLOCKED",
    "current": "The permitted (in-tree) form reads none of the five named sources. qip-world-model is fed what qip-kernel's Platform absorbs each cycle (bars, news, fundamentals, macro releases, causal claims -- platform.rs:8677-8975) and holds its own in-process SearchIndex; it does not read qip_ai::memory::EpisodicMemory (rg in qip-world-model/src finds no memory import; the Platform's REASON stage, not the world model, recalls episodes). Terraform declares BigQuery (dataset 'research'), Bigtable (instance 'timeseries') and Spanner (instance 'global', database 'positions') in modules/data/main.tf:85,268,326, all disabled by enable_* = false in every environment and none wired to qip-world-model; there are no Knowledge Packs and no Spanner Graph.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:8677-8975 (what the world model actually absorbs)",
      "backend/crates/services/qip-world-model/src/world.rs:595-605 (its own in-process index)",
      "backend/crates/libs/qip-ai/src/memory/store.rs (EpisodicMemory, in-process, not read by the world model)",
      "infrastructure/terraform/modules/data/main.tf:85,268,326 (BigQuery, Bigtable, Spanner declared, all disabled)"
    ],
    "gap": "The federation reads only what the single process absorbs plus its own in-memory episodic store; four of the five named sources (Spanner Graph, Bigtable, GCS Knowledge Packs, BigQuery) do not exist as GCP resources anywhere in the tree.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "WORLD-059"
    ],
    "blocked_by": "C4",
    "verification": "rg over backend/crates/services/qip-world-model/src for EpisodicMemory/memory:: returns nothing; infrastructure/environments/*/terraform.tfvars set enable_bigquery/enable_bigtable/enable_spanner = false.",
    "work_item": "E14",
    "tier": "T3",
    "size": "L",
    "notes": "verify: status BLOCKED (C4) kept; 'current' corrected. The row said the world model reads EpisodicMemory; it does not. The Terraform check was also misdescribed as 'module scaffolding': real BigQuery/Bigtable/Spanner resources are declared and disabled. Two of the five sources are not C4-refused and are buildable now -- episodic/semantic memory (in-tree) and GCS knowledge packs (GCS is not in C4's list) -- and both are missing."
  },
  {
    "id": "WORLD-063",
    "status": "PARTIAL",
    "current": "The world model's state at decision time is written back into episodic memory every LEARN stage: Platform::remember_resolved (backend/crates/runtime/qip-kernel/src/platform.rs:11538) writes each resolved thesis as an Episode carrying causal_context -- the causal edges the world model's graph held into the instrument, with their effective transmission and whether each is established (Platform::causal_context, platform.rs:11298; episode.rs:275-299,473) -- beside the market state vector. Those fields are separate from the observed outcome (EpisodeOutcome), so a recalled episode does not present the graph's claims as the outcome. But nothing names the world-model version that produced the edges, no provenance flag marks the record model-derived versus external evidence (rg 'model.?derived' finds nothing), and the write-back is a by-product of decision episodes rather than the federation publishing its own conclusions; semantic memory receives nothing.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:11298 (causal_context read from the live graph)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:11494,11538-11570 (written into the Episode at LEARN)",
      "backend/crates/libs/qip-ai/src/memory/episode.rs:275-299,458-484 (CausalContextEdge; Episode fields)",
      "backend/crates/runtime/qip-kernel/tests/episodic.rs:626 an_episode_records_the_causal_edges_the_graph_held_into_its_instrument_when_it_was_reasoned"
    ],
    "gap": "No lineage to a world-model version and no model-derived marker on the written-back state, so nothing stops a later reader from treating a self-produced conclusion as independent evidence; no semantic-memory write-back; the write happens only when a thesis resolves, not on a federation update.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test episodic -- an_episode_records_the_causal_edges_the_graph_held_into_its_instrument_when_it_was_reasoned (run: 1 passed; asserts against the episode the production cycle wrote).",
    "work_item": "E09",
    "tier": "T1",
    "size": "M",
    "notes": "This is exactly the self-reinforcement risk the requirement's own rationale names; closing it needs a provenance field on whatever memory record type is used. verify: upgraded MISSING -> PARTIAL; flags set for what exists. The assessor said nothing writes the world model's conclusions back to memory; Episode.causal_context is filled from the live causal graph at reasoning time and tested on the episode the production cycle wrote. The model-version lineage and model-derived marker the check requires are absent, so not more than PARTIAL."
  },
  {
    "id": "WORLD-064",
    "status": "PARTIAL",
    "current": "qip_investment_agents::manifests::roster (backend/crates/agents/qip-investment-agents/src/manifests.rs:507) assembles 18 agents (ids module, manifests.rs:31-49), dispatched every REASON stage (platform.rs:10998) and tested for separation of duties (qip-investment-agents/tests: organisation.rs 39 + vocabulary.rs 4 = 43 tests). Of the ten domains this requirement names, five have a named specialist: macro (MacroAnalyst, ids::MACRO), company (EquityAnalyst/CreditAnalyst), microstructure (MicrostructureAnalyst), portfolio (PortfolioConstruction), risk (RiskControl).",
    "evidence": [
      "backend/crates/agents/qip-investment-agents/src/manifests.rs:31-49,507",
      "backend/crates/agents/qip-investment-agents/src/analysts.rs:41,218,361 (Macro/Equity/Credit)",
      "backend/crates/agents/qip-investment-agents/src/fast.rs:28 (Microstructure)",
      "backend/crates/agents/qip-investment-agents/src/construction.rs:37 (PortfolioConstruction)",
      "backend/crates/agents/qip-investment-agents/src/control.rs:21 (RiskControl)"
    ],
    "gap": "Five of the ten named domains have no specialist at all: event, supply chain, crypto, private markets, capital (confirmed absent from manifests::ids, manifests.rs:31-49). A registry exists -- qip_agents::governance::Roster (governance.rs:40), with each manifest's competencies as its declared scope -- but no test reads it and asserts domain coverage, so even the five covered domains are not checked the way the requirement's own check describes.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-investment-agents (roster/organisation tests); manual count against manifests::ids for domain coverage.",
    "work_item": "E09",
    "tier": "T2",
    "size": "L",
    "notes": "M SS9.1 (p16-17) lists 17 brains including several this catalogue places outside WORLD (Adversarial, Symbolic Reasoning, Communications & Conduct); the roster's non-domain agents (AdversarialReviewer, CausalAnalyst, SimulationAnalyst, ExecutionTrader, LearningAttribution, ComplianceControl, QuantumOptimization) are real but do not count toward the ten named domain experts. verify: status PARTIAL confirmed (five of ten domains, counted against manifests::ids). Corrected: the gap said no 'brain registry' construct exists; Roster is one. Test count corrected from 47 to 43."
  },
  {
    "id": "WORLD-065",
    "status": "PARTIAL",
    "current": "Half of it. qip_investment_agents::fast::MicrostructureAnalyst (backend/crates/agents/qip-investment-agents/src/fast.rs:28) reads the current order book off the desk's market snapshot (desk.market, the ReadMarketData gate, fast.rs:62-75) and never the world model; its manifest (manifests.rs:176-200) grants ReadMarketData, ReadReferenceData and PublishHypothesis only, so a read of desk.world would be refused at AgentContext::authorise. It is the fast brain's hosted agent (qip-fastbrain/src/roster.rs:18) and is convened every REASON stage. It does not read tick data or an internal replay window: it reads the snapshot's current book (a replayed feed reaches it only as whatever the snapshot last applied), through no replay interface, and cites no replay manifest.",
    "evidence": [
      "backend/crates/agents/qip-investment-agents/src/fast.rs:1-75 (reads desk.market's book, not desk.world)",
      "backend/crates/agents/qip-investment-agents/src/manifests.rs:176-200 (no read_world_model grant)",
      "backend/crates/agents/qip-investment-agents/src/desk.rs:69-107 (separate market and world-model gates)",
      "backend/crates/apps/qip-fastbrain/src/roster.rs:18 (FAST_PATH_AGENTS = microstructure)"
    ],
    "gap": "The tick/replay half is not implemented: no specialist reads a replay window through a replay interface or cites a replay manifest; the microstructure analyst reads the latest snapshot book. The no-world-model half is structural (capability absent from the manifest) but no test pins it: organisation.rs's the_fast_path_agent_has_no_language_model_and_a_millisecond_budget asserts no language model, not no world model.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test found. rg 'ReadWorldModel' over qip-investment-agents/tests returns nothing; the fast-path manifest test (organisation.rs:207, run: passed) covers the language model only.",
    "work_item": "E06",
    "tier": "T0",
    "size": "S",
    "notes": "'Replay' specifically (as distinct from live book state) is not separately proven; the analyst reads whatever the Desk's BookView presents, live or replayed, without distinguishing the two. verify: NEEDS-VALIDATION -> PARTIAL; work_item E01 -> E06. The requirement is that specialists read tick and internal replay data directly; the analyst reads the current snapshot book and no replay interface exists for it to read, so the code implements only the 'not through the world model' half. Also corrected: the assessor said it reads desk.book (BookView is the portfolio); it reads desk.market."
  },
  {
    "id": "WORLD-066",
    "status": "MISSING",
    "current": "nothing. No Ambient Proactive Mesh implementation exists anywhere in the workspace: every 'ambient' hit outside qip-cli's domain-description table (rg -il ambient across backend/crates) is the unrelated phrase 'ambient clock' (a rule against reading the system clock) in doc comments across qip-transport, qip-storage, qip-compliance, qip-agents, etc.",
    "evidence": [
      "backend/crates/apps/qip-cli/src/blueprint.rs (domain-description string only, no implementation)"
    ],
    "gap": "No Ambient Proactive Mesh exists to feed, so the federation-to-ambient publish path this requirement names cannot exist either.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E09",
    "tier": "T2",
    "size": "M",
    "notes": "AMBIENT-011 is named by the catalogue as covering the typed return path from the mesh back to the federation; the whole AMBIENT domain appears unimplemented, not assessed further in this WORLD-only batch."
  }
]
