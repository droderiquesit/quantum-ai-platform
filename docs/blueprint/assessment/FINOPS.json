[
  {
    "id": "FINOPS-001",
    "status": "MISSING",
    "current": "nothing — modules/execution-node declares a google_compute_instance_group_manager with no reservation_affinity block anywhere, and no google_compute_reservation resource exists under infrastructure/. The declaration itself (a reservation per zone/machine type, conditional on a node being declared, consumed by the instance template's reservation_affinity) and its plan test are buildable in-tree today at zero cost: the module already has a mocked-provider plan harness. Only provisioning it is C8.",
    "evidence": [
      "infrastructure/terraform/modules/execution-node/main.tf:305-483 (instance template and group manager, no reservation)",
      "infrastructure/environments/dev/terraform.tfvars:227, infrastructure/environments/prod/terraform.tfvars:51 (execution_nodes = {})",
      "infrastructure/terraform/modules/execution-node/tests/affinity.tftest.hcl:15-37 (mock_provider google + command = plan with a node declared — the harness shape the verification needs already exists)"
    ],
    "gap": "No capacity reservation resource or reservation_affinity exists in the module, and no plan test asserts one. Separately, and only for deployment: there is no Reflex VM to reserve capacity for — execution_nodes = {} in every environment, and provisioning one is a cost decision C8 leaves to the owner.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C8",
    "verification": "infra-plan: with an execution node declared, the plan must contain a google_compute_reservation per zone/machine-type the node group uses, sized to target_size, and the instance template's reservation_affinity must consume it. Write it as a run block beside modules/execution-node/tests/affinity.tftest.hcl, which already plans this module with mocked providers.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": "ADR 0099 C8 names the deployment gap: dedicated C4D/C4 Reflex VMs are cost-blocked and execution_nodes = {} everywhere; billing is also disabled on the dev project (external:billing) so even a trial reservation could not be applied there today. blocked_by C8 applies to provisioning only. verify: status BLOCKED -> MISSING. The requirement's own check is conditional ('with an execution node declared, the plan contains...'), and a conditional Terraform declaration plus a mocked-provider plan test costs nothing and needs no refused thing — modules/execution-node/tests/affinity.tftest.hcl already plans this module that way. Semantics buildable in-tree are MISSING with a note, not BLOCKED."
  },
  {
    "id": "FINOPS-002",
    "status": "PARTIAL",
    "current": "google_compute_instance_group_manager.node in modules/execution-node — target_size = var.node_count (a fixed value, not an autoscaler output) with lifecycle.ignore_changes = [target_size] so Terraform never reconciles a manual scale back; no google_compute_autoscaler, region autoscaler or scheduled-scaling resource exists anywhere under infrastructure/. node_count is a per-region operator input validated to 0..2 (one dedicated machine per region, per §41.4), not a value derived from any shard map.",
    "evidence": [
      "infrastructure/terraform/modules/execution-node/main.tf:434-483 (target_size = var.node_count, line 440; lifecycle.ignore_changes, lines 477-482)",
      "infrastructure/terraform/modules/execution-node/variables.tf:201-227 (node_count: 'Zero or one, per §41.4', validation 0..2; no shard map input exists)",
      "no test: grep for autoscaler|target_size|node_count across backend/crates/tests/qip-acceptance/tests/*.rs and modules/execution-node/tests/*.hcl returns nothing for this property"
    ],
    "gap": "The no-autoscaler half holds structurally but no named test asserts it, and it is unreached — execution_nodes = {} in every environment, so nothing is deployed for the property to hold true of. The second half of the requirement's check — each group's size derived from a declared venue/instrument shard map — does not exist: there is no shard map, and the unit of capacity is one machine per region, not one per shard.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-001"],
    "blocked_by": "",
    "verification": "infra-plan test asserting no autoscaler/autoscaling policy resource in the plan targets a Reflex node group, and target_size is a literal input derived from a declared shard map. Write one alongside modules/execution-node/tests/affinity.tftest.hcl.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "Vacuously true today (nothing is deployed to violate it), which is exactly the kind of untested invariant the repository's own testing rule warns against treating as protection. verify: status kept PARTIAL. Added the variables.tf evidence and the missing shard-map half to the gap — the requirement's check requires the size to be 'derived from the declared shard map', and node_count is a 0..2 per-region input with no shard concept."
  },
  {
    "id": "FINOPS-003",
    "status": "PARTIAL",
    "current": "Warm plane only. infrastructure/terraform/catalogue.tf's cloud_run_catalogue — api: min_instances = 0, max_instances = 4 (autoscales, request-serving); fastbrain and deepbrain: min_instances = 1, max_instances = 1 with a non-empty always_on_justification (single hash-chained-log writer, own-clock loop). Since ADR 0036 those bounds are applied by the RunService manifests under infrastructure/gitops/envs/<env>/ (parity-tested against the catalogue), not by Terraform. Data plane: modules/data's Bigtable instance carries autoscaling_config (min/max nodes, cpu/storage targets) but enable_bigtable = false everywhere; no BigQuery slot reservation exists. AI plane: nothing (enable_vertex_ai = false everywhere). No workload has a documented scaling model comparing measured scale-up time with its demand's rate of change.",
    "evidence": [
      "infrastructure/terraform/catalogue.tf:254-256 (api bounds, empty justification), 413-415 (fastbrain bounds + justification), 508-510 (deepbrain bounds + justification)",
      "infrastructure/gitops/envs/dev/api.yaml:43-44, dev/fastbrain.yaml:43-44, dev/deepbrain.yaml:43-44 (minInstanceCount/maxInstanceCount as applied by the manifest)",
      "infrastructure/terraform/modules/data/main.tf:268-293 (Bigtable autoscaling_config, count = enable_bigtable, false in every environment)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:5558 every_workload_that_runs_the_cycle_over_the_journal_is_pinned_to_one_warm_instance — asserts the cycle-running workloads are pinned to 1/1 with a >40-character justification and the api's ceiling exceeds 1; ran 2026-09-25: 'test result: ok. 3 passed; 0 failed' (with two other tests)",
      "backend/crates/tests/qip-acceptance/tests/gitops.rs:2240 every_run_service_holds_the_invariants_its_catalogue_entry_and_the_cloud_run_module_held (min/max parity, lines 2512-2513)",
      "rg -i 'scaling model|scale-up time|cold.start' over docs/ops, catalogue.tf and ADR 0024 returns nothing"
    ],
    "gap": "The requirement covers warm, data AND AI workloads and requires each workload's documented scaling model to compare measured scale-up time with demand's rate of change. Only the warm Cloud Run plane has bounds, and the cited test is primarily a single-writer/no-fork property (pin the brains to one instance) with one assertion that the api may scale — it does not exercise any scale-up-time judgement. The data plane's only autoscaler (Bigtable) is disabled everywhere and untested; BigQuery slot reservations and the AI plane have nothing. No scaling-model document or cold-start measurement exists for any workload.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "inspection + test: a scaling-model record per warm/data/AI workload stating whether it autoscales with its measured scale-up (cold-start) time against its demand's rate of change, and an acceptance test that every workload marked autoscaling has explicit min/max bounds in the plan or manifest (warm plane: cargo test -p qip-acceptance --test infrastructure every_workload_that_runs_the_cycle_over_the_journal_is_pinned_to_one_warm_instance covers only the pinned/api half).",
    "work_item": "E14",
    "tier": "T1",
    "size": "M",
    "notes": "deployable/e2e are false because the control-plane cluster that reconciles the RunService manifests (ADR 0036) is suspended (ADR 0093) — the bounds are parity-tested against the manifest but nothing currently applies them. The data/AI halves are gated off by C4 (Bigtable, BigQuery, Vertex are in ADR 0099's C4 row); the scaling-model record and the Cloud Run cold-start measurement are not blocked. verify: status COMPLETE -> PARTIAL; tested/integrated kept true for the warm-plane part only. The implementation covers the warm plane only (data and AI workloads are disabled or absent), no documented scaling model or measured scale-up time exists as the verification requires, and the cited test exercises the pin-to-one invariant rather than 'autoscale where scaling time fits'. Fixed the evidence line '591-592-area' (the justifications are at catalogue.tf:415 and :510) and added the manifest and gitops parity evidence."
  },
  {
    "id": "FINOPS-004",
    "status": "BLOCKED",
    "current": "No headroom figure, capacity benchmark or sizing margin is recorded anywhere for Reflex or Fabric capacity. The nearest in-tree measurement is per-operation cost: performance.rs times one edge work pass (and a pass with a fill and its drop copy) against a budget in-process, which is an input a capacity benchmark would use but says nothing about peak load, zone loss or shard-VM loss.",
    "evidence": [
      "rg -n -i 'headroom' backend/ infrastructure/ finds only capital/limit/budget headroom, no Reflex/Fabric capacity-sizing hit",
      "backend/crates/tests/qip-acceptance/tests/performance.rs:1717 an_edge_work_pass_with_a_fill_and_its_drop_copy_costs_what_the_execution_measurements_say, :1776 (per-pass cost, in-process, single cell)",
      "infrastructure/environments/*/terraform.tfvars: execution_nodes = {} in every environment"
    ],
    "gap": "There is no deployed Reflex capacity and no native Fabric to size headroom against; the benchmark this requirement's margin would be set from — peak load with one zone or shard VM removed — has not been run and cannot be without multi-zone capacity.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-001"],
    "blocked_by": "C8",
    "verification": "benchmark: at benchmark peak load with one Fabric zone or Reflex shard VM removed, remaining capacity still meets the hot-path SLO. No benchmark harness against deployed capacity exists.",
    "work_item": "E14",
    "tier": "T3",
    "size": "L",
    "notes": "The Fabric half of this requirement also depends on the native Event & Control Fabric: ADR 0100 decides an in-tree single-node first build (E03), which is not yet in the tree (no qip-storage::segment, no event_fabric module), and the multi-broker/zone form is C2/C8. verify: status BLOCKED kept — removing a zone or shard VM from deployed capacity genuinely needs C8 capacity. Added the in-process per-pass cost measurements to 'current' as the only permitted-form precursor, and corrected the headroom search claim (it does hit, but only on capital/budget headroom)."
  },
  {
    "id": "FINOPS-005",
    "status": "MISSING",
    "current": "nothing — qip-agents has no concept of an agent 'family', no cross-run concurrency ceiling, and no queue; qip_agents::budget::BudgetLedger (the nearest related code) bounds one run's own wall time/tool calls/tokens/cost, not how many runs of a family may be in flight at once",
    "evidence": [
      "backend/crates/libs/qip-agents/src/budget.rs:159-273 (BudgetLedger — per-run only)",
      "rg -n 'family|concurrency|quota|queue' backend/crates/libs/qip-agents/src/*.rs returns nothing"
    ],
    "gap": "No per-family concurrency quota exists, and no admission queue holds excess jobs until capacity frees — every run is admitted or refused independently, with no notion of a family it counts against.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "integration: submitting more jobs for one family than its quota leaves the excess queued; no such harness exists.",
    "work_item": "E08",
    "tier": "T2",
    "size": "M",
    "notes": "AI training/agent families themselves (qip_training::local::ModelFamily aside) are not modelled as first-class entities with their own quota anywhere in the tree."
  },
  {
    "id": "FINOPS-006",
    "status": "BLOCKED",
    "current": "nothing for managed training — qip_training::VertexAiProvider (backend/crates/services/qip-training/src/vertex.rs) has no client, credential or transport and reports itself unavailable by design; qip_training::local::LocalTrainingProvider fits models in-process and has no cloud-capacity concept to mark Spot/preemptible at all; modules/execution-node sets preemptible = false (correct for Reflex, not what this requirement governs)",
    "evidence": [
      "backend/crates/services/qip-training/src/job.rs:1-16 (module doc: two providers, Vertex unavailable by design)",
      "infrastructure/terraform/modules/execution-node/main.tf:389 (preemptible = false, Reflex, unrelated)",
      "infrastructure/terraform/modules/ai/main.tf (no preemptible/Spot config for any training resource; rg -i 'spot|preempt|interrupt|checkpoint' over it and qip-training/src returns nothing)"
    ],
    "gap": "Spot/preemptible training capacity requires a reachable managed training plane (Vertex AI or a GPU/Spot node pool), which is C4/C5-blocked; the only training that actually runs (LocalTrainer, in-process) has no cloud capacity to interrupt.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C4",
    "verification": "integration: preempting a Spot training job mid-run resumes from its last checkpoint; a family declared interruption-unsafe is never scheduled onto Spot. No such harness exists because no Spot capacity exists.",
    "work_item": "E10",
    "tier": "T3",
    "size": "M",
    "notes": "Framework training on Vertex/GPU pools is also outside the Rust workspace per ADR 0001/0011, which this requirement's own NON_RUST flag already anticipates. verify: status BLOCKED kept. One slice is not blocked and is also absent: the per-family interruption-safe/unsafe declaration and checkpoint-resume in the local trainer could be written in-tree (qip_training::local::ModelFamily is the natural home); it would not complete the requirement without Spot capacity, so the status stands."
  },
  {
    "id": "FINOPS-007",
    "status": "PARTIAL",
    "current": "The development-agent plane runs on the owner's desktop (ADR 0098), a separate machine and project from the GCP production runtime, so throttling it cannot touch production by construction. In-repo controls: scripts/model-gateway.mjs refuses a task once its ledger (.worker-spend.jsonl) reaches ALGORIK_WORKER_MAX_CALLS and refuses to configure at all if that value is not positive — a model-call ceiling, not a worker or build-pool concurrency quota; scripts/worker-capacity.sh reports the machine's concurrent-worker ceiling and says in its own header that it does not gate. The concurrency cap that actually binds agents is the harness's (min(16, cpus-2)) and the orchestration policy's §8 pause rules, neither of which is code in this repository.",
    "evidence": [
      "scripts/model-gateway.mjs:62 (const LEDGER), 163-166 (maxCalls config, non-positive refused), 290-303 (run(): budget-exhausted refusal, line 301)",
      "scripts/worker-capacity.sh:1-12 ('Exit 0 always: this reports, it does not gate. The gate is the policy.'), 18-25 (harness ceiling)",
      "docs/plan/algorik-orchestration-policy.md:239-266 (§8 Budgets and automatic pause — as documented policy)",
      "docs/adr/0098-the-development-factory-is-the-orchestration-policy-run-on-the-owners-desktop-with-claude-tiers-and-the-gcp-fabric-waits-on-a-measured-rung.md:27-39, decision 2 ('No packet goes to a non-Anthropic model' — the gateway carries no calls in the factory as run)"
    ],
    "gap": "No control in the repository sets the agent plane's worker or build concurrency, and none can throttle it to zero: the gateway bounds only calls to an external model provider that ADR 0098 does not route to; worker-capacity.sh reports and exits 0. The exhaustion/refusal path in run() has no named test (scripts/model-gateway.test.mjs exercises configure/probe/screenPayload/completionText, with MAX_CALLS only ever a fixture), and no test demonstrates that throttling the agent plane leaves production health, cadence and Fabric throughput unaffected — though the separation is structural.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "node --test scripts/model-gateway.test.mjs — needs a new case driving run() past ALGORIK_WORKER_MAX_CALLS via the ledger and asserting exit code 3 and the 'budget exhausted' message; plus a real concurrency control for workers/builds that can be set to zero.",
    "work_item": "E01",
    "tier": "T0",
    "size": "S",
    "notes": "The independence from production is structural (a desktop-run factory and a Node script the platform explicitly refuses to depend on), which is the strongest form of this requirement's second sentence even without a crossing integration test. verify: integrated true -> false and deployable true -> false. The gateway is dev tooling reached from no composition root and no environment (the CICD verifier reached the same conclusion for CICD-030), and ADR 0098 routes no packet to a non-Anthropic model, so it carries no calls. Added worker-capacity.sh (reports, does not gate) and ADR 0098 as evidence; the worker/build concurrency half of the requirement has no in-repo control. Status PARTIAL kept."
  },
  {
    "id": "FINOPS-008",
    "status": "MISSING",
    "current": "nothing — no google_compute_reservation or committed-use-discount resource exists under infrastructure/, so there is no commitment to have been entered early or on time",
    "evidence": [
      "rg -n 'reservation|commitment' infrastructure/terraform -g '*.tf' finds no capacity-commitment resource"
    ],
    "gap": "The constraint is unviolated only because nothing exists yet to violate it; no benchmark-sizing-stability check or commitment-introduction record exists to prove the ordering the requirement names.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-001"],
    "blocked_by": "",
    "verification": "inspection: every commitment in infrastructure history cites the benchmark sizing it covers, and that sizing was stable across preceding benchmark runs. No commitments exist to inspect.",
    "work_item": "E14",
    "tier": "T2",
    "size": "S",
    "notes": "This requirement cannot be scored against real evidence until FINOPS-001's reservation exists; it is a process gate on a resource that has no reason to exist yet."
  },
  {
    "id": "FINOPS-009",
    "status": "MISSING",
    "current": "nothing — no billing export resource or FinOps destination dataset exists in Terraform; as far as the tree records, if a BigQuery billing export exists it was enabled in the console rather than declared in code",
    "evidence": [
      "rg -n -i 'billing' infrastructure/ .github/ scripts/ finds only comments and bootstrap-deploy.sh's 'attach billing' instruction; no google_billing_* resource",
      "infrastructure/terraform/modules/data/main.tf:85-111 (google_bigquery_dataset.research — a research-data warehouse, not a billing-export destination, and itself disabled everywhere)"
    ],
    "gap": "No Terraform resource declares the FinOps destination dataset, and no recorded step enables Cloud Billing export into it; every downstream FinOps report (FINOPS-012, FINOPS-018) has no source to query.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "inspection: the billing account's BigQuery export is enabled into the platform's FinOps dataset. No such dataset or export is declared anywhere in this tree.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "verify: status MISSING kept; the note was corrected. It said billing export resources 'are declarable now'. The destination dataset is declarable. To this reviewer's knowledge (not verified here, because the provider cache is read-denied), the export itself is a billing-account setting with no google-provider resource, so 'declared in code' means the dataset plus a recorded manual step. Two constraints apply. The dataset is BigQuery, which ADR 0099 lists under C4 as a platform store, though a billing sink is arguably outside what C4 governs. And billing is disabled on the dev project (external:billing), so no cost rows would arrive there today."
  },
  {
    "id": "FINOPS-010",
    "status": "MISSING",
    "current": "nothing — no google_billing_budget resource exists anywhere under infrastructure/",
    "evidence": [
      "rg -n 'google_billing_budget' infrastructure -g '*.tf' returns nothing"
    ],
    "gap": "No spend guardrail is declared in code for any environment's project; a spend past any threshold is discovered on the invoice, not alerted.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-009"],
    "blocked_by": "",
    "verification": "infra-plan: the plan contains a billing budget per environment project, a threshold rule, and a notification target wired to FINOPS-018's anomaly path. No such resource or harness exists.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "Like FINOPS-009, this needs no dependency exception and no live venue; it is declarable independent of the C1-C8 register. verify: status kept. Added: a google_billing_budget names the billing account, which must arrive as an input (tfvars or secret), never a literal, under the no-account-identifiers rule; and with billing disabled on the dev project (external:billing) an applied budget there would have nothing to measure."
  },
  {
    "id": "FINOPS-011",
    "status": "PARTIAL",
    "current": "local.labels in infrastructure/terraform/main.tf sets platform, environment, managed_by and live_capable on every resource that reads var.labels; modules/cloudrun additionally merges qip-plane, qip-trust-zone, qip-traffic-class and qip-workload onto Cloud Run identity/service resources specifically, and the RunService manifests carry the same keys; projects themselves are not created by Terraform (bootstrap-deploy.sh leaves project creation to a person), so no project carries any of the five keys",
    "evidence": [
      "infrastructure/terraform/main.tf:106-119 (locals.labels — platform, environment, managed_by, live_capable)",
      "infrastructure/terraform/modules/cloudrun/main.tf:121-129 (qip-plane, qip-trust-zone, qip-traffic-class, qip-workload merged in)",
      "infrastructure/gitops/envs/dev/api.yaml:14-22 (manifest labels: same keys, no owner or region)",
      "scripts/bootstrap-deploy.sh:109-118 (the script never creates the project; no google_project resource exists, only data \"google_project\" at main.tf:72)",
      "rg -n '\"qip-[a-z-]+\"\\s*=' infrastructure/terraform -g '*.tf' finds exactly four keys, none of them owner or region"
    ],
    "gap": "Of the five required keys (plane, region, environment, owner, workload), only environment reaches every Terraform-labelled resource; plane and workload reach Cloud Run workloads only; owner and region reach nothing anywhere in the tree; projects carry no labels at all because Terraform does not create them. No acceptance test enforces the five-key invariant across resource types.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "infra-plan: an acceptance test over the plan fails if any labelable resource lacks a non-empty value for plane, region, environment, owner or workload. No such test exists; backend/crates/tests/qip-acceptance/tests/infrastructure.rs:2113 checks an unrelated tuple (service-account inventory).",
    "work_item": "E14",
    "tier": "T1",
    "size": "M",
    "notes": "Matches this requirement's own recorded note verbatim: 'Of the five required keys, only environment is present' at the base-label level; the cloudrun-specific merge (plane, workload) postdates that note but does not close owner or region anywhere. verify: status and flags kept. Corrected the cloudrun line reference (118-127 -> 121-129), and added two facts: the manifests carry the same keys, and projects are created outside Terraform, so the 'every project' half of the requirement has no labelling path at all today."
  },
  {
    "id": "FINOPS-012",
    "status": "MISSING",
    "current": "nothing reads a billing export or attributes cloud cost by strategy, model, data source or region. Adjacent in-tree figures are modelled, not billed. Platform::compute_spend accumulates the cost-router's per-rung price table across cycles as one platform-wide total, exported as qip_compute_spend_units with no labels. qip-cost-router's DataCosts amortises each licensed source's subscription over its observed reads per decision. That is licence cost per data source, not cloud billing.",
    "evidence": [
      "rg -ni 'unattributed|per_strategy|cost_attribution|billing_export|cost_report' backend/crates -l matches only unrelated fill-/order-attribution code (e.g. qip-execution-engine/src/broker.rs, qip-capital/src/allocation.rs), none of it cost reporting",
      "backend/crates/runtime/qip-kernel/src/platform.rs:9341 (self.compute_spend += ledger.total_cost()), 9359-9363 (COMPUTE_SPEND gauge, labels([]))",
      "backend/crates/services/qip-cost-router/src/data.rs:1-14, 164-195 (per-source licence cost per read, per decision)"
    ],
    "gap": "There is no billing-export reader, no per-dimension aggregation, and no unattributed-remainder line; this requirement has no source to draw from because FINOPS-009's export does not exist either. The modelled compute and licence charges are not a substitute: they price what the platform planned per its own table, not what GCP billed.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-009", "FINOPS-011"],
    "blocked_by": "",
    "verification": "integration: over a billing-export fixture with labelled and unlabelled rows, per-strategy/model/data-source/region totals plus an unattributed line sum exactly to the fixture total. No such code or test exists.",
    "work_item": "E08",
    "tier": "T2",
    "size": "M",
    "notes": "verify: status MISSING kept. Corrected the note, which said reading a BigQuery billing-export table from Rust 'would need a client the two-dependency rule does not currently permit'. That is wrong. The tree already has one: qip_storage::BigQueryWarehouse::query (backend/crates/libs/qip-storage/src/gcp/bigquery.rs:626) runs queries over the in-tree plaintext HTTP client behind the egress proxy. It takes a bearer token from a TokenSource (MetadataServerTokens works on Cloud Run; gcp/auth.rs:1-40). The reader is buildable in-tree with no new crate. Also added the modelled compute and licence cost code to 'current', so nobody mistakes it for this requirement."
  },
  {
    "id": "FINOPS-013",
    "status": "PARTIAL",
    "current": "infrastructure/terraform/modules/data/main.tf declares all three tiers: google_bigtable_instance \"timeseries\" (autoscaling_config, min/max nodes, cpu/storage targets) for hot replay, google_storage_bucket \"archive\" (versioned; tiered storage-class lifecycle rules at 90/365 days plus a retention_policy with is_locked = false) for immutable historical segments, and google_bigquery_dataset \"research\" for research aggregates. In the running tree, hot replay is the file-backed event log. Its in-memory index rolls replaceable retention classes by age behind a snapshot window (wired in qip-kernel config), but the JSONL file on disk is never segmented, archived or trimmed. ChainArchive deliberately does not trim either. No GCS segment tier exists in code.",
    "evidence": [
      "infrastructure/terraform/modules/data/main.tf:268-293 (Bigtable timeseries + autoscaling), 115-163 (archive bucket, tiering + retention_policy is_locked = false at 139-142), 85-111 (BigQuery research dataset)",
      "infrastructure/environments/{dev,test,stage,prod}/terraform.tfvars: enable_bigquery = enable_cloud_storage = enable_bigtable = false in every environment",
      "backend/crates/libs/qip-events/src/log.rs:67-75 (JSONL file append-only; segmenting and archiving is 'a separate change' needing an ADR)",
      "backend/crates/runtime/qip-kernel/src/config.rs:159-162 (with_snapshot_window on both log targets)",
      "backend/crates/libs/qip-storage/src/chain.rs:28-30 (ChainArchive 'does not trim')"
    ],
    "gap": "All three Terraform tiers are gated off in every environment (enable_bigtable/enable_bigquery/enable_cloud_storage = false), so none is provisioned, and no test asserts the tiering plan itself. The archive bucket's retention policy is unlocked, so it does not by itself 'prevent modification' as the check requires. In code there is no path moving sealed journal segments to GCS or aggregates to BigQuery. The hot tier's disk footprint is unbounded.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C4",
    "verification": "infra-plan: bounded retention on Fabric hot-replay topics and Bigtable tables, a GCS bucket with a modification-preventing retention policy, and BigQuery datasets for research aggregates. No test asserts this over the plan.",
    "work_item": "E06",
    "tier": "T2",
    "size": "M",
    "notes": "Bigtable and BigQuery are named explicitly in ADR 0099's C4 register, and blocked_by C4 applies to those two tiers only. The GCS tier is not C4-blocked. It is disabled alongside them by the same enable_cloud_storage flag, and it is buildable in-tree: qip_storage::CloudStorageBlobStore already speaks the GCS JSON API, and ADR 0100 decides a segment store and archive (qip-storage::segment) that is not yet built. verify: status PARTIAL kept. Fixed the Bigtable evidence line (91-111 -> 268-293; 85-111 is the BigQuery dataset). Added the unlocked retention policy to the gap, and added the in-tree hot-replay state (index rolled by age, file unbounded, no segment archive) to 'current'."
  },
  {
    "id": "FINOPS-014",
    "status": "PARTIAL",
    "current": "Terraform: google_storage_bucket \"training\" in modules/ai carries a lifecycle_rule deleting scratch after 30 days, which is bounded retention on a clearly-derived store. google_bigquery_dataset \"research\" in modules/data sets default_table_expiration_ms = null, with a comment arguing research aggregates are a source-of-truth tier rather than a derived copy. The model-artifacts GCS bucket is versioned with no lifecycle rule. All three are disabled in every environment. In code: every event-log topic declares a RetentionClass, and DerivedState (features, moments, covariance) maps to InMemoryFixed and is rolled from the in-memory index by age. The file-backed log still appends every record, derived ones included, to a JSONL file that is never trimmed.",
    "evidence": [
      "infrastructure/terraform/modules/ai/main.tf:52-87 (training bucket; 30-day Delete lifecycle_rule at 79-86)",
      "infrastructure/terraform/modules/data/main.tf:99-102 (default_table_expiration_ms = null, research dataset, deliberate)",
      "infrastructure/terraform/modules/data/main.tf:165-186 (model-artifacts bucket, versioned, no lifecycle_rule)",
      "backend/crates/libs/qip-events/src/retention.rs:171-182 (DerivedState -> InMemoryFixed), log.rs:67-75 ('a file-backed log bounds memory but not disk')"
    ],
    "gap": "Coverage is neither comprehensive nor tested. The one unambiguously derived Terraform store (training scratch) is bounded. No acceptance test scans every BigQuery table/dataset, Bigtable table and GCS bucket in the plan for an expiration or lifecycle rule, so a future derived store added without one would not be caught. In the running tree, derived-state records written to a file-backed event log are kept on disk indefinitely. The index rolls them; the file does not.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-013"],
    "blocked_by": "",
    "verification": "infra-plan: every BigQuery table/dataset, Bigtable table and GCS bucket holding derived data has an expiration, GC policy or lifecycle delete rule; only declared source-history stores lack one. No such comprehensive test exists. Plus, for the journal: a derived-state record's on-disk bytes are released once it leaves its retention window.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": "The three Terraform stores are disabled in every environment (see FINOPS-013), so on the Terraform side this is an authoring-time property, not a deployed one. verify: status PARTIAL kept. Corrected the ai/main.tf evidence range (76-84 -> 52-87). Added the in-tree journal finding: derived-state records are bounded in memory but kept forever on disk in the file-backed log. That is the deployed half of this prohibition, and the assessor's row did not mention it."
  },
  {
    "id": "FINOPS-015",
    "status": "MISSING",
    "current": "nothing — no per-family spend metering, admission gate or budget-exhaustion refusal keyed by agent/training family exists; qip_agents::budget::BudgetLedger bounds a single run's own cost_micros, not a family's cumulative spend across runs",
    "evidence": [
      "backend/crates/libs/qip-agents/src/budget.rs:118-151 (Spend, per-run), 159-273 (BudgetLedger, per-run)",
      "rg -n 'family' backend/crates/libs/qip-agents returns nothing"
    ],
    "gap": "There is no concept of an agent family or training family as a metered spend account, so nothing can be exhausted at that level and no admission decision can be made against it.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-005"],
    "blocked_by": "",
    "verification": "integration: driving one family's metered spend past its budget stops new work for that family only. No such harness exists.",
    "work_item": "E08",
    "tier": "T2",
    "size": "M",
    "notes": "The same gap as FINOPS-005 seen from the spend axis rather than the concurrency axis; both need the same missing family-scoped account."
  },
  {
    "id": "FINOPS-016",
    "status": "MISSING",
    "current": "backend/crates/runtime/qip-kernel/src/central/models.rs::register_fit records holdout_r2, holdout_rmse, holdout_correlation, in_sample_r2, in_sample_rmse and holdout_observations onto the ModelCard; qip_ai::registry::ModelCard::decision_eligibility and Roster::promote gate on evaluation presence, not on cost or latency",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/central/models.rs:100-120 (metrics inserted — no cost or latency field)",
      "backend/crates/libs/qip-ai/src/registry.rs:178 (decision_eligibility), 278-330 (promote/promote_artifact) — neither reads a cost or latency figure",
      "rg -n 'cost|latency' backend/crates/libs/qip-ai/src/registry.rs returns nothing"
    ],
    "gap": "No model evaluation record carries training/serving cost or added latency, and nothing refuses a promotion for lacking either — a model could be registered and promoted with no benefit-per-dollar or latency figure at all.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-009", "FINOPS-012"],
    "blocked_by": "",
    "verification": "acceptance: a model package cannot be registered for promotion unless its evaluation carries measured benefit against baseline, attributed training/serving cost, and added latency. No such refusal exists in ModelRegistry today.",
    "work_item": "E10",
    "tier": "T2",
    "size": "M",
    "notes": "The dollar figure this requirement wants depends on FINOPS-009/012's billing attribution existing first; the latency half could be measured and gated independently of that."
  },
  {
    "id": "FINOPS-017",
    "status": "PARTIAL",
    "current": "scripts/model-gateway.mjs enforces one model-call ceiling (ALGORIK_WORKER_MAX_CALLS) against a cumulative ledger file, with no period, refusing further calls once spent; docs/plan/algorik-orchestration-policy.md §8 documents global token/wall-clock/cost ceilings, per-worker limits and an 80%-of-budget automatic-pause rule as orchestration policy. ADR 0098 routes no packet to a non-Anthropic model, so the gateway carries no calls in the factory as it runs today.",
    "evidence": [
      "scripts/model-gateway.mjs:62, 163-166, 290-303 (the enforced call budget and its refusal)",
      "docs/plan/algorik-orchestration-policy.md:239-266 (§8 Budgets and automatic pause)",
      "docs/adr/0098-the-development-factory-is-the-orchestration-policy-run-on-the-owners-desktop-with-claude-tiers-and-the-gcp-fabric-waits-on-a-measured-rung.md decision 2 (no packet to a non-Anthropic model)"
    ],
    "gap": "Only model usage via the external-provider gateway is code-enforced, and only as one cumulative ceiling, not separate daily and monthly figures. That gateway is not on the path the factory uses. Build capacity and tool use have no code-enforced budget at all. §8's pause rule is a policy an orchestrating session is expected to follow, not a mechanism that refuses on its own. The refusal itself has no named test.",
    "implemented": true,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-007"],
    "blocked_by": "",
    "verification": "integration: an agent at its daily model budget is refused further model calls while build/tool budgets are metered separately, and the refusal lifts next period only if the monthly budget has room. No daily/monthly split or build/tool budget exists to test.",
    "work_item": "E01",
    "tier": "T1",
    "size": "M",
    "notes": "ARCH-068 (named in this requirement's own notes as the phase-9 exit test) is the place this would be pulled together; today it is one enforced ceiling plus documented, unenforced policy for the rest. verify: integrated true -> false and deployable true -> false, for the same reason as FINOPS-007. The gateway is dev tooling reached from no composition root and no environment, and ADR 0098 means it carries no calls. Clarified that the ceiling is cumulative per ledger file, with no daily or monthly period. Status PARTIAL kept."
  },
  {
    "id": "FINOPS-018",
    "status": "MISSING",
    "current": "nothing — no BigQuery/Cloud Monitoring cost-anomaly job and no bridge from a budget alert to a Fabric task or incident exists anywhere in the tree",
    "evidence": [
      "no google_billing_budget exists (FINOPS-010); grep -n 'resource \"google_monitoring_alert_policy\"' infrastructure/terraform/modules/observability/main.tf lists nine policies, none cost-related (kill_switch, live_fill, persistent_breach, permission_violation, edge_halted, edge_reconciliation_break, central_reconciliation_break, risk_figure_unevaluated, sign_off_withheld_on_liquidity)",
      "rg -il 'anomal' backend/crates finds only market/opportunity anomaly code (qip-opportunity-engine, qip-market-ingestion), none cost-related"
    ],
    "gap": "There is no anomaly-detection job and no adapter turning a detected anomaly into a journaled Fabric task or incident carrying project, labels, amount and threshold.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": ["FINOPS-009", "FINOPS-010"],
    "blocked_by": "",
    "verification": "integration: a billing-export fixture breaching a threshold produces exactly one Fabric task/incident naming project, labels and amount; replaying the same input produces no second one. No such code exists.",
    "work_item": "E12",
    "tier": "T2",
    "size": "M",
    "notes": "The requirement's own notes flag NEW_DEPENDENCY. The budget-notification path arrives on Pub/Sub, which G p23 keeps off the internal bus, so it belongs in an edge adapter. verify: status MISSING kept. Corrected the note, which said querying BigQuery from the Rust workspace 'needs a client the two-dependency rule does not permit today' and so the bridge must live outside the workspace. The query half needs no new crate: qip_storage::BigQueryWarehouse::query (backend/crates/libs/qip-storage/src/gcp/bigquery.rs:626) exists in-tree, running over the plaintext HTTP client behind the egress proxy. A scheduled anomaly query that journals a task is therefore buildable in Rust today. Only a Pub/Sub consumer is unbuilt, and its REST surface is JSON, so whether it needs a crate is itself an open question, not a given."
  },
  {
    "id": "FINOPS-019",
    "status": "PARTIAL",
    "current": "No cost-response mechanism exists (no budget, no budget action, no anomaly job). Several guards already hold part of the prohibition. (1) An acceptance test scans every .tf file and refuses any Terraform grant of a role carrying storage.objects.delete or owner/editor, so no Terraform-created identity, the agent-operated infra identity included, can delete a GCS object. (2) The evidence bucket's retention policy is locked by default and force_destroy = false, also tested. (3) The infra identity's custom storage role omits storage.objects.delete. (4) infra.yml, the one workflow that runs the agents' cost teardowns ('tear the expensive parts down again to stop the meter'), is workflow_dispatch-only, offers no prod option, and exits on prod in a step of its own. (5) The session guard hook refuses agent-issued teardowns, gcloud deletions and gsutil rm.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:4044 no_workload_identity_can_delete_from_the_evidence_bucket; :4002 the_evidence_bucket_is_versioned_and_retained — ran 2026-09-25: 'test result: ok. 3 passed; 0 failed' (with one other test)",
      "infrastructure/terraform/modules/cicd/main.tf:180-216 (the infra identity exists so agents can tear expensive parts down 'to stop the meter'; prod stays behind the human-run bootstrap), 278-300 (qipInfraStorage custom role: storage.buckets.update and storage.buckets.delete present, storage.objects.delete absent), 230-265 (infra_roles: compute/run/pubsub/logging/monitoring admin)",
      ".github/workflows/infra.yml:44-48, 122-147 (workflow_dispatch only; environment options dev/test/stage), 210-216 (explicit prod refusal)",
      ".claude/hooks/guard-dangerous-command.py:87-108 (teardown, 'gcloud ... delete', 'gsutil rm' refused)",
      "infrastructure/terraform/modules/data/main.tf:139-142 (event-archive retention_policy is_locked = false)"
    ],
    "gap": "The tested guard covers GCS object deletion only. Nothing scans for BigQuery, Bigtable or Spanner delete/truncate roles, and nothing scans for retention-update permissions. The agent-operated qip-infra-<env> identity is declared for every environment, prod included, because module cicd is unconditional. It holds storage.buckets.update, which can shorten or remove an unlocked retention policy such as the event-archive bucket's, and per-service admin roles that include deletion. The only thing keeping the agents' cost teardowns off prod is infra.yml's refusal, not the identity's grants. The guard hook matches patterns, so 'bq rm' and 'gcloud storage rm' pass it. No test asserts that a budget carries no programmatic action beyond notification; that is vacuous today because no budget exists.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": ["FINOPS-018"],
    "blocked_by": "",
    "verification": "infra-plan: extend no_workload_identity_can_delete_from_the_evidence_bucket to a scan that no identity able to run a budget action, anomaly job or agent-dispatched workflow holds delete/truncate/retention-update permission (storage.buckets.update on an unlocked retention bucket, bigquery.tables.delete/datasets.delete, bigtable.tables.delete, spanner.databases.drop) on any prod resource, and that every google_billing_budget has notification-only actions.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "P0 in the source requirement precisely because a vacuous truth here reads as protection and is not — the same lesson this repository's own MaxExpectedShortfall history names. verify: status MISSING -> PARTIAL; implemented, tested, integrated and deployable all false -> true. The assessor cited modules/cicd/main.tf:189 as 'a different domain's own no-delete-role acceptance check'. It is not a different domain. That comment describes infrastructure.rs:4044, which enforces this requirement's GCS object-deletion slice over every Terraform identity, including the one agents use for cost teardowns. The cicd module states outright that agents tear infrastructure down to stop the meter, so an agent cost action exists today. It is held off prod by infra.yml and off object deletion by the tested role scan. Its retention-update permission (storage.buckets.update) is unguarded, and that is the gap to close first."
  }
]
