[
  {
    "id": "ARCH-025",
    "status": "PARTIAL",
    "current": "Both streams are ingested and absorbed by the kernel every cycle, as separate arms of one absorption loop in Platform (backend/crates/runtime/qip-kernel/src/platform.rs): SensedRecord::News at :8745 -> world.absorb_news (:8751, entity resolution, evidence index, sentiment); SensedRecord::Tick at :8688 and Quote/Trade/Book beside it -> market snapshot, spread history and depth (absorb_depth). Ticks arrive through qip-market-ingestion's DataAdapter feeds (TapeFeed/ConnectorFeed, used from qip-api/src/feed.rs and qip-fastbrain's feed), NOT through IngestionService, which is constructed only in qip-market-ingestion's own tests. Note that a Tick is also written into the world model's own feature store as the 'close' feature (platform.rs:8688-8697), so the tick stream is partly absorbed into the world model rather than kept as a separate tick-learning store.",
    "evidence": [
      "backend/crates/runtime/qip-kernel/src/platform.rs:8688 (SensedRecord::Tick arm), :8745-8751 (SensedRecord::News -> absorb_news)",
      "backend/crates/services/qip-market-ingestion/src/adapter.rs:150 (SensedRecord: Tick/Quote/Trade/Book/News/... one enum, one feed contract)",
      "backend/crates/runtime/qip-kernel/tests/absorption.rs:186 a_news_item_lands_in_the_evidence_index_rather_than_vanishing (world stream)",
      "backend/crates/runtime/qip-kernel/tests/absorption.rs:251 a_book_feeds_the_liquidity_topology_at_the_instant_it_was_observed (tick/book stream)",
      "backend/crates/tests/qip-acceptance/tests/truth_loop.rs:679 a_record_that_fails_its_quality_gate_is_refused_by_name_and_never_reaches_the_world_model (world stream only)"
    ],
    "gap": "The two streams are ingested independently but nothing fuses them: `rg -i 'fusion|dual.stream'` across qip-world-model, qip-market-ingestion and qip-kernel returns nothing but unrelated comment hits. No fused-state type carries lineage to both a world input and a tick input; §8.4's fusion mechanics do not exist.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists for fusion; would need a new test asserting a fused-state record's lineage names at least one world-stream input and one tick-stream input.",
    "work_item": "E06",
    "tier": "T2",
    "size": "L",
    "notes": "Both required streams individually meet COMPLETE-quality bar; the requirement's actual invariant (neither substitutes for the other, proven by lineage on a fused record) has no implementation to check. verify: status unchanged (PARTIAL). Corrected 'current': the tick path is SensedRecord adapters absorbed at platform.rs:8688, not IngestionService (test-only construction, per .claude/rules/domains/observability.md and ARCH-031's own gap); recorded that ticks are folded into the world model's 'close' feature, so even the 'separate paths' half is two match arms of one loop. Added the two absorption.rs tests that actually exercise each stream; the fused-lineage invariant still has no code or test."
  },
  {
    "id": "ARCH-026",
    "status": "PARTIAL",
    "current": "The structural half holds and is tested. The Lane 0 crates (qip-edge and qip-edge-node) reach no cognitive crate: their transitive workspace dependencies (computed from cargo metadata) contain no qip-ai, qip-reasoning-engine, qip-investment-agents, qip-agents, qip-world-model, qip-training, qip-evolution, qip-lifecycle, qip-learning-engine, qip-kernel or qip-twin. architecture.rs's no_edge_cell_can_reach_a_language_model asserts qip-ai is unreachable from every edge crate, and because every one of those cognitive crates itself reaches qip-ai, that single assertion transitively excludes them all. qip-fastbrain additionally refuses at start-up to host any agent holding call_language_model (qip-fastbrain/src/roster.rs clear_agents). qip-cost-router's Determinism::Required arm (router.rs select/determined) returns a type that cannot name a model rung.",
    "evidence": [
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:715 no_edge_cell_can_reach_a_language_model (transitive; anchors on qip-reasoning-engine reaching qip-ai)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:407 no_safety_critical_engine_can_reach_a_language_model",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:439 nothing_that_decides_or_executes_names_the_language_model_interface",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:502 an_agent_that_holds_a_language_model_cannot_touch_the_market",
      "backend/crates/services/qip-cost-router/src/router.rs:441-462 (select: Determinism::Required -> DeterministicRouting)",
      "backend/crates/services/qip-cost-router/tests/cost_router.rs:307 a_decision_that_must_be_deterministic_cannot_be_routed_to_any_model_tier",
      "backend/crates/apps/qip-fastbrain/src/roster.rs clear_agents + tests an_agent_that_may_call_a_language_model_is_refused_rather_than_hosted",
      "backend/crates/libs/qip-contracts/src/degradation.rs:10-14 (the edge cell sizes every pass by DegradationState::sizing_multiplier over centre-published cognitive-capability freshness)",
      "backend/crates/apps/qip-edge-node/src/main.rs:869 (synchronous exchange with the central plane) before :888 (run_pass), same thread"
    ],
    "gap": "The requirement's second verification clause -- a chaos run with the Cognitive Core stopped leaving the hot path's decisions unchanged -- has no test, and would not pass as literally stated: by design (blueprint §6.2, qip-contracts/src/degradation.rs) the cell narrows its sizing multiplier as the centre-published causal-graph / episodic-memory / belief-state rows go stale, so stopping the centre changes sizes (never the call path). The requirement check needs clarifying ('no call' vs 'unchanged decisions') and a chaos test written for whichever is meant. Also see ARCH-043: qip-edge-node runs a blocking exchange with the central plane on the pass thread before each pass, so a stopped centre also delays passes. The architecture test proves absence of qip-ai as a proxy for 'the Cognitive Core', which is not a named crate set (ARCH-027); qip-data-finder ('request new data') and qip-opportunity-engine do not reach qip-ai and so are not covered by it. Lane 0 runs only in qip-edge-node, which is deployed nowhere (execution_nodes = {}).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-acceptance --test architecture no_edge_cell_can_reach_a_language_model; cargo test -p qip-cost-router; cargo test -p qip-fastbrain roster. Missing: a chaos test that stops the central plane during a qip-edge-node paper session and asserts the pass keeps deciding (with the §6.2 narrowing stated as the expected change).",
    "work_item": "E04",
    "tier": "T1",
    "size": "S",
    "notes": "No crate literally named a 'Cognitive Core' exists yet (that is ARCH-027's gap), so this is proven against its closest analogue, qip-ai/qip-reasoning-engine, and against qip-cost-router's type-level Determinism guarantee. No chaos run exists that 'stops the Cognitive Core' because nothing runtime-dependent exists to stop -- the structural absence of the dependency edge is a stronger guarantee than a chaos test would be, not a weaker one. qip-edge-node itself is undeployed (execution_nodes={} everywhere) but the property holds at the dependency-graph level regardless of deployment. verify: downgraded COMPLETE -> PARTIAL, deployable true -> false, work_item '' -> E04, tier T0 -> T1. The dependency-graph half is COMPLETE-quality and was confirmed (cargo metadata reachability + the transitive qip-ai test). But (1) the requirement's own chaos clause is untested and contradicted by the §6.2 degradation the edge applies to centre-fed cognitive rows; (2) the edge node serialises a blocking central-plane exchange ahead of each pass (ARCH-043); (3) Lane 0 is only qip-edge-node, which no environment deploys, so deployable cannot be true. Line refs corrected to the fn lines."
  },
  {
    "id": "ARCH-027",
    "status": "PARTIAL",
    "current": "Fragments of most capabilities exist as separate, unconnected pieces: train models (qip-training's LocalTrainer, in-tree, reached from qip-deepbrain); coordinate specialist agents (qip-investment-agents::manifests::roster, an 18-manifest fixed roster dispatched by Platform::organisation, backend/crates/agents/qip-investment-agents/src/manifests.rs:507); build causal hypotheses (backend/crates/runtime/qip-kernel/src/causal_review.rs, backend/crates/runtime/qip-kernel/src/central/causal.rs); challenge its own conclusions (backend/crates/services/qip-evolution/src/challenger.rs, qip-investment-agents::review::AdversarialReviewer); inspect evidence (backend/crates/services/qip-lifecycle/src/evidence.rs, backend/crates/services/qip-reasoning-engine/src/evidence.rs); run simulations (qip-simulation-engine, reached from production); request new data (qip-data-finder's DiscoveryDesk); produce deployable policy/model packages (qip-lifecycle's gates.rs/trials.rs/demotion.rs promotion ladder, reached from production).",
    "evidence": [
      "backend/crates/agents/qip-investment-agents/src/manifests.rs:507 roster()",
      "backend/crates/runtime/qip-kernel/src/causal_review.rs (tests e.g. an_edge_that_is_only_a_shared_driver_does_not_survive_the_control at line 635)",
      "backend/crates/services/qip-evolution/src/challenger.rs",
      "backend/crates/services/qip-lifecycle/src/{evidence.rs,gates.rs,trials.rs,demotion.rs}"
    ],
    "gap": "No goal-decomposition engine exists (no GoalSpec or equivalent anywhere in the workspace). qip-deepbrain::campaign is a fixed-cadence data-fetch-and-backtest assembly, not a self-directed campaign a system launches on its own hypothesis. No generalized tool-calling interface was found (rg for a tool-call abstraction beyond the fixed agent-capability enum returns nothing). The eleven capabilities are never composed into one flow, and no test runs a research campaign end to end into a signed, promotion-gated package.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No unified test exists; each fragment has its own crate tests (e.g. qip-evolution's, qip-investment-agents's own suites). The requirement's own e2e check -- one campaign exercising all eleven capabilities and ending in a signed, gate-passed package -- has no test.",
    "work_item": "E09",
    "tier": "T3",
    "size": "XL",
    "notes": "Policy flags NON_RUST and EXTERNAL_ACTION apply only to the parts this repository has not built at all (managed-training research runtimes, and tool calls that leave the platform); the missing goal-decomposition and self-directed-campaign machinery is ordinary in-tree Rust work, not blocked by ADR 0099's register."
  },
  {
    "id": "ARCH-028",
    "status": "PARTIAL",
    "current": "Three distinct code paths exist and share no channel: direct venue I/O (qip-edge::cell's own Placer seam); the cell's local hash-chained journal plus async Mirror::ship (backend/crates/edge/qip-edge/src/journal.rs, the closest analogue to the durable Fabric); and the cell<->centre state-up/capital-down link (backend/crates/edge/qip-edge/src/mesh.rs, over qip-transport's MeshPublisher/CapitalDownlink).",
    "evidence": [
      "backend/crates/edge/qip-edge/src/mesh.rs (module doc: \"The cell's half of the mesh spine: state up, capital down\")",
      "backend/crates/edge/qip-edge/src/journal.rs (module doc: hash-chained local record, asynchronous Mirror::ship)",
      "backend/crates/edge/qip-edge/tests/mesh.rs::a_delta_for_a_peer_that_is_not_listening_is_dead_lettered_rather_than_blocking_forever",
      "backend/crates/edge/qip-edge/tests/mesh.rs::a_second_delta_to_a_peer_that_is_down_is_refused_by_the_circuit_without_spending_a_ladder",
      "backend/crates/services/qip-streaming/src/registry.rs:1-38,60-73 (TransportKind {Local, Durable, Mesh, PubSub}; RoutingClass::path / select_for refuse a routing class on a transport whose guarantees do not fit it)",
      "backend/crates/services/qip-streaming/tests/routing.rs:26 a_hot_routed_event_never_traverses_the_durable_buffer",
      "backend/crates/services/qip-streaming/tests/registry.rs:162 a_lossy_transport_is_refused_for_an_event_that_must_not_be_lost"
    ],
    "gap": "What exists in this codebase named 'mesh' is a cell<->centre link, not the blueprint's peer-to-peer Reflex Mesh for coordinating live multi-leg opportunities across cells (`rg -i 'opportunity token|distributed reservation'` finds nothing; qip-edge's RegionTable is a per-region ledger shared in-process by the composition root, not a peer channel; no cell-to-cell network channel exists). There is no single 'Rust Event & Control Fabric' contract either -- durability is split unconsolidated across qip-events, qip-streaming::DurableLogTransport and the edge cell's own local journal. The nearest thing to the requirement's 'refuse a type on the wrong path' is qip-streaming's registry, which refuses a routing class (Hot/Warm/Cold) on a transport whose guarantees do not fit it and is tested -- but it is keyed by routing class not message type, its 'Mesh' kind is the cell<->centre link, and select_for/the registry have no caller outside qip-streaming (reached only from tests). No test registers each message type to exactly one of the three paths.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "backend/crates/edge/qip-edge/tests/mesh.rs (non-blocking uplink); no test for message-type-to-path registration exists and would need to be written.",
    "work_item": "E03",
    "tier": "T2",
    "size": "L",
    "notes": "Building the true unified Fabric (durable pub/sub, journals, replay, backpressure, control, outcomes as one native contract) is constrained by ADR 0099 C2 for anything needing an async runtime or QUIC; the separation-of-paths property itself is buildable today without a new dependency. verify: status and flags unchanged. Found a partial analogue the assessor missed (qip-streaming RoutingClass::path/select_for, tested, test-only reach) and corrected the gap text that said no refusal mechanism of any kind exists."
  },
  {
    "id": "ARCH-029",
    "status": "PARTIAL",
    "current": "Terraform defines two distinct runtime classes: modules/cloudrun for warm binaries (qip-api, qip-fastbrain, qip-deepbrain) and modules/execution-node for the reflex cell, a dedicated Compute Engine VM.",
    "evidence": [
      "infrastructure/terraform/modules/cloudrun, infrastructure/terraform/modules/execution-node",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:568 every_cloud_run_service_is_internal_and_mounts_secrets_as_files_never_as_environment",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:661 the_execution_node_has_no_external_address_and_no_container_runtime",
      "infrastructure/terraform/main.tf:623 module \"ai\" (instantiated; training_service_account = the deep brain's)",
      "infrastructure/terraform/modules/ai/main.tf (Vertex AI metadata store, endpoint, training bucket; all count-gated on enable_vertex_ai)",
      "infrastructure/environments/{dev,test,stage,prod}/terraform.tfvars: enable_vertex_ai = false"
    ],
    "gap": "execution_nodes = {} in every environment's tfvars, so the reflex lane runs on no runtime today. The training lane has no runtime of its own in any environment: modules/ai is instantiated in the root (main.tf:623), not test-only as previously stated, but it provisions Vertex AI (ADR 0099 C4) and is off (enable_vertex_ai = false) everywhere, and its own header says qip-training's Vertex port has no client. Production training (qip-training's LocalTrainer, qip-deepbrain/src/learning.rs:1316) runs in-process inside the deep brain's Cloud Run warm-service lane it is meant to be separated from.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C8",
    "verification": "terraform plan against infrastructure/environments/dev; infrastructure.rs's cloudrun/execution-node tests.",
    "work_item": "E14",
    "tier": "T2",
    "size": "M",
    "notes": "Reflex-lane placement is correct but unprovisioned (cost/billing, C8); the training-lane/warm-lane conflation is unblocked in-tree work. verify: status/flags unchanged. Corrected a false claim: modules/ai is not test-only; it is instantiated at main.tf:623 and flag-gated off (Vertex, C4) in every environment."
  },
  {
    "id": "ARCH-030",
    "status": "PARTIAL",
    "current": "Nine of the eleven named Phase 0 deliverables exist in some demonstrable form: canonical types/contracts (qip-contracts), a ledger (qip-capital/qip-financial, in-tree not Spanner), a simulation harness (qip-simulation-engine), observability (qip-observability, emits, not yet ingested), CI/CD (ci.yml/deploy.yml; GitOps suspended per ADR 0093), identity/security (WIF + Binary Authorization applied in dev), a venue adapter SDK (qip-brokers, code-complete, undeployed), a Rust SDK (qip-core and the crate boundary itself), and a benchmark harness (qip-quantum::benchmark, tests-only).",
    "evidence": [
      "docs/architecture/current-state/_summaries.json (libs-foundation, infrastructure, edge-cell groups)",
      "scripts/check-dependencies.sh, scripts/check-secrets.sh (CI gates, reached_from_production)"
    ],
    "gap": "No phase-gate acceptance test exists anywhere (`rg -i 'phase.gate|phase_gate' backend/crates/tests` returns nothing). Two named deliverables are entirely absent: a '3-zone regional quorum' (`rg -i quorum` across backend/ and infrastructure/ returns zero hits -- no consensus/quorum concept exists) and a single native Rust Event & Control Fabric contract (see ARCH-028's gap; what exists is unconsolidated).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No phase-gate test exists; one would need to be written naming a demonstrating test/run for each of the eleven deliverables.",
    "work_item": "E00",
    "tier": "T3",
    "size": "XL",
    "notes": "The regional-quorum gap needs a consensus mechanism, which ADR 0099 C2 constrains (no Raft crate without a dependency record); the Fabric-contract gap is E03's work."
  },
  {
    "id": "ARCH-031",
    "status": "PARTIAL",
    "current": "qip-edge's Cell (backend/crates/edge/qip-edge/src/cell.rs, 9,040 lines) implements the local fast path, deterministic risk/feasibility (feasibility.rs), paper-only order flow (qip-brokers' simulated venue), a hash-chained local journal drained through Mirror (journal.rs; StoreMirror in qip-edge-node/src/mirror.rs), and book reconstruction on the node's own path: qip-edge-node's SimulatedFeed publishes the simulated venue's depth as LevelSet messages into Cell::on_bytes -- decode, sequence, apply -- (qip-edge-node/src/feed.rs:25-45, :287). qip-edge has 315 #[test] functions in total (123 in src, 192 across 26 integration files); qip-edge-node runs Cell::work from its real startup path.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/cell.rs (Cell::work, Cell::on_bytes)",
      "backend/crates/apps/qip-edge-node/src/feed.rs:25-45, :287 (simulated depth through Cell::on_bytes)",
      "backend/crates/apps/qip-edge-node/tests/pass.rs:192 a_node_with_the_simulated_feed_runs_a_pass_and_the_pass_time_series_move",
      "backend/crates/edge/qip-edge/tests/cell.rs:361 the_journal_chain_catches_an_edited_decision, :398 mirror_batches_chain_onto_each_other_across_flushes",
      "backend/crates/apps/qip-edge-node/tests/mirror.rs:64 a_shipped_journal_is_readable_after_the_process_that_wrote_it_is_gone"
    ],
    "gap": "qip-edge-node (the only binary that runs Cell::work) is deployed in no environment (execution_nodes = {} in dev/test/stage/prod). No single test demonstrates the chain the requirement names -- canonical tick capture, book reconstruction and deterministic replay producing identical decisions/vetoes/ledger entries/journal records for one recorded session. qip-cli's `replay` verifies the central event log and rebuilds three registries; it does not replay an edge session. The journal flush to the mirror is synchronous on the pass thread (qip-edge-node/src/main.rs:852, before run_pass at :888) -- asynchronous with respect to Cell::work, but not to the node's loop -- and no test shows the order path unaffected by it.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-edge-node --test pass; no deterministic full-shard replay test exists and would need to be written.",
    "work_item": "E04",
    "tier": "T2",
    "size": "L",
    "notes": "Deployment itself is C8-blocked (cost/billing); the missing end-to-end deterministic-replay test is unblocked T2 work. verify: status/flags unchanged. Fixed a broken evidence path (backend/crates/edge/qip-edge/tests/journal.rs does not exist; replaced with the real journal/mirror tests), corrected the test count (315 is the total, not 315 plus 26 files), and removed a false gap claim: the node's feed does go through the cell's decode/sequence/apply book path (feed.rs:287)."
  },
  {
    "id": "ARCH-032",
    "status": "PARTIAL",
    "current": "Fragments reached from production: evidence machinery (qip-lifecycle::evidence, qip-reasoning-engine::evidence), the world model (qip-world-model, reached every cycle), episodic memory (qip-ai::memory, qip-kernel::central::episodic), a model/strategy foundry seam (qip-kernel::central::foundry, qip-evolution's promotion/challenger), and replay/digital-twin partial coverage (qip-twin's asof/capture/counterfactual/value modules).",
    "evidence": [
      "backend/crates/services/qip-lifecycle/src/evidence.rs, backend/crates/services/qip-reasoning-engine/src/evidence.rs",
      "backend/crates/runtime/qip-kernel/src/central/episodic.rs",
      "backend/crates/runtime/qip-kernel/src/central/foundry.rs",
      "docs/architecture/current-state/services-intelligence.json (qip-twin reached_from_production list)"
    ],
    "gap": "Entirely absent: a Scout Fabric (qip-data-finder has no CRAWL stage -- `follow_links`/`expand_from`/`fn crawl(` are all absent; candidates are a fixed committed list, not autonomous discovery), a Tick Lake (`rg -i tick.lake` hits only qip-cli's blueprint text), the Market Intelligence & Tick Learning Fabric / world+tick fusion (ARCH-025's gap), a World Model Federation (zero hits), and symbolic/temporal reasoning services beyond the narrow causal-review module. A model-disagreement representation exists only in an agent/evidence form -- qip-reasoning-engine records panel dissenters (engine.rs:68) and penalises confidence by net stance disagreement (hypothesis.rs:514) -- not as disagreement between world models, which is what the phase names.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-025"
    ],
    "blocked_by": "",
    "verification": "No phase-gate test exists.",
    "work_item": "E08",
    "tier": "T3",
    "size": "XL",
    "notes": "Tick Lake retention and a World Model Federation on managed stores would trigger ADR 0099 C4; the Scout Fabric's crawl stage and fusion mechanics are unblocked in-tree work. verify: status/flags unchanged. Softened 'model-disagreement representation entirely absent' to what is true: an agent/evidence disagreement representation exists in qip-reasoning-engine; world-model disagreement does not."
  },
  {
    "id": "ARCH-033",
    "status": "PARTIAL",
    "current": "An asset registry exists: AssetClass has 13 variants (backend/crates/libs/qip-financial/src/asset_class.rs:13-27) and the kernel holds an AssetClassRegistry (qip-kernel/src/asset_class_registry.rs:365); sizing runs through qip-capital, qip-risk-engine and qip-portfolio-engine, wired into Platform. Treasury/custody corridors (qip-capital-fabric's destination registry, corridor lifecycle and seven-check TransferGate) ARE reached from a composition root: qip-api loads an operator declaration named by QIP_CAPITAL_FABRIC_PATH (qip-api/src/main.rs:313 load_fabric_declaration; qip-api/src/fabric.rs) and feeds its Destination/Corridor/Gate commands to Platform::decide_fabric (platform.rs:7583 re-derives a gate command's corridor ruling); Terraform can mount that declaration (variables.tf:535 capital_fabric_file, catalogue.tf:133-138).",
    "evidence": [
      "backend/crates/libs/qip-financial/src/asset_class.rs:13 (AssetClass, 13 variants)",
      "backend/crates/apps/qip-api/src/main.rs:313 load_fabric_declaration; backend/crates/apps/qip-api/src/fabric.rs (module doc: Destination/Corridor/Gate now reach production)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:7583 (FabricCommand::Gate handling in decide_fabric)",
      "backend/crates/runtime/qip-kernel/tests/ledger.rs:879 a_corridor_funding_a_strategy_that_holds_no_capital_is_suspended_and_the_gate_vetoes_it, :931 a_gate_command_stating_a_ruling_the_policy_did_not_derive_is_refused_before_the_gate_sees_it",
      "backend/crates/apps/qip-api/tests/fabric.rs:93 a_declared_destination_reaches_the_chain_as_a_record, :120 a_corridor_declared_before_its_destination_is_refused_by_the_control_and_the_refusal_is_a_record",
      "infrastructure/terraform/variables.tf:535 capital_fabric_file (null in every environment; dev tfvars:344 commented out)",
      "backend/crates/services/qip-capital-fabric/src/{custody.rs,destination.rs,assessment.rs}"
    ],
    "gap": "No phase-gate test exists. The gate is veto-only: an Approved verdict has no consumer and there is no transfer engine in the workspace (qip-api/src/ledger_views.rs GATE_NOTE; fabric.rs 'An admitted verdict carries no way to execute'), so no treasury or custody-corridor movement is ever executed, even against simulated balances, which is what the requirement's check asks for. No environment sets capital_fabric_file, so a deployed qip-api assesses nothing. No test drives the gate from the declaration file (qip-api/tests/fabric.rs covers destination and corridor commands only). 'Asset/Capital/Risk brains' are not named components, and no test sizes positions in two asset classes as the phase gate requires.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-kernel --test ledger; cargo test -p qip-api --test fabric; qip-capital-fabric's own tests. A phase-gate test sizing two asset classes and executing a corridor movement against simulated balances would need to be written.",
    "work_item": "E07",
    "tier": "T2",
    "size": "M",
    "notes": "Live transfer is BLOCKED (C1, ADR 0021) regardless; the gap scored here is that even the paper/simulated transfer path is unwired from any composition root, which is a smaller, unblocked fix. verify: integrated false -> true; status stays PARTIAL. The assessor's 'no code outside tests constructs a GateCommand' misread pre_positioning.rs:22-31, which says only that the pre-positioning plan does not reach the gate. qip-api's capital-fabric declaration (QIP_CAPITAL_FABRIC_PATH) reaches decide_fabric including Gate commands in production code. Real gap restated: no transfer engine consumes an approval (so nothing moves even in simulation), no environment mounts a declaration, no phase-gate test."
  },
  {
    "id": "ARCH-034",
    "status": "BLOCKED",
    "current": "No three-region deployment exists (execution_nodes = {} everywhere; no NCC, no peer Reflex Mesh, no cross-region tick alignment or lead/lag learning, no distributed opportunity replay, no selective cross-region Fabric mirroring, no game-day). The permitted in-tree form does cover part of the phase's failure semantics: a cell reads which other regions have gone dark (qip-edge/src/region.rs, blueprint §36.3 -- 'mirrors involving it suspend, and nothing else changes'), wired in qip-edge-node (src/dark.rs, and the region_wire poll in main.rs); §31.1 cross-region mirror arrangements (qip-edge/src/mirror.rs, qip-edge-node/src/cross_region.rs); and per-region capital reservation tables shared by a region's cells (qip-edge/src/reservation.rs) with region shares from the centre (ADR 0039).",
    "evidence": [
      "infrastructure/environments/{dev,test,stage,prod}/terraform.tfvars: execution_nodes = {} in all four",
      "rg -i 'network.connectivity.center|NCC' infrastructure/ -> zero hits",
      "backend/crates/edge/qip-edge/src/region.rs (RegionOutlook, §36.3), backend/crates/apps/qip-edge-node/src/dark.rs",
      "backend/crates/edge/qip-edge/tests/telemetry.rs:1059 a_cell_told_a_region_it_mirrors_into_is_dark_publishes_it_before_its_first_pass_and_until_it_returns",
      "backend/crates/edge/qip-edge/tests/reservation.rs:249 a_second_strategy_is_refused_once_the_region_allocation_is_spent_even_though_its_own_envelope_would_admit_it",
      "backend/crates/edge/qip-edge/tests/cross_region.rs"
    ],
    "gap": "The entire multi-region layer -- three regional cells, the direct Reflex Mesh, cross-region tick alignment/lead-lag learning, selective Fabric mirroring, and game-day failover -- is unbuilt.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-028",
      "ARCH-031"
    ],
    "blocked_by": "C8",
    "verification": "No test exists.",
    "work_item": "E11",
    "tier": "T4",
    "size": "XL",
    "notes": "Also touches C3 (GKE for warm services) if that placement choice were taken. Externally blocked too: billing is disabled on the dev project. verify: status BLOCKED (C8) kept -- three concurrent regions genuinely need the multi-region/cost footprint. Per the BLOCKED rule, set implemented/tested/integrated true for the permitted in-tree form, which the row had recorded as nothing: region-darkness suspension of mirrors (§36.3), §31.1 cross-region mirror discipline and per-region reservation tables, all tested and wired into qip-edge-node. deployable stays false (no node deployed). The peer Reflex Mesh, tick alignment and lead/lag learning are in-tree semantics that are simply unbuilt, not C8-blocked."
  },
  {
    "id": "ARCH-035",
    "status": "PARTIAL",
    "current": "Graph optimization (backend/crates/edge/qip-arbitrage/src/graph.rs), a quote engine (backend/crates/edge/qip-edge/src/quoting.rs, reached_from_production), and a cross-class hedge map (backend/crates/runtime/qip-kernel/src/hedge_review.rs) all exist and are wired into the cycle.",
    "evidence": [
      "backend/crates/edge/qip-arbitrage/src/graph.rs",
      "backend/crates/edge/qip-edge/src/quoting.rs (docs/architecture/current-state/edge-cell.json reached_from_production list)",
      "backend/crates/runtime/qip-kernel/src/hedge_review.rs",
      "backend/crates/services/qip-execution-engine/src/multileg.rs:1-43 (LegGroup: 'a group that cannot complete is unwound, never abandoned'; 24 unit tests)",
      "backend/crates/libs/qip-contracts/src/intent.rs:104-108 ('LegGroup still has no call site, deliberately')",
      "backend/crates/edge/qip-edge/src/decomposition.rs:20-24, backend/crates/edge/qip-edge/src/cell.rs:5955-5967",
      "backend/crates/runtime/qip-kernel/src/pre_positioning.rs (DECIDE plans, LEARN scores; plans never become transfer intents)"
    ],
    "gap": "Saga-style recovery is built but unwired. qip-execution-engine's multileg::LegGroup decides unwinds for a half-filled group ('unwound, never abandoned') and has 24 unit tests, but no production call site (its only caller outside its crate is qip-acceptance's performance.rs; qip-contracts/src/intent.rs:104 says so explicitly). The edge cell, where cycles actually run, cannot use it: decomposition.rs:20-24 -- 'The cell cannot unwind -- its Placer can withdraw a resting order where the gateway has a cancel path, and cannot send the compensating order that would reverse a leg already filled' -- so a broken cycle is sized down / journaled and halts the cell instead. Pre-positioned inventory exists only as a plan: qip-kernel's pre_positioning module plans and scores moves but deliberately never turns them into transfer intents, and no transfer engine exists (ARCH-033). No phase-gate replay forces a leg failure through recovery and the hedge map.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-033"
    ],
    "blocked_by": "",
    "verification": "backend/crates/edge/qip-arbitrage/tests/*; no saga-recovery test exists because no saga mechanism exists.",
    "work_item": "E11",
    "tier": "T3",
    "size": "L",
    "notes": "Quoting runs only against the simulated venue, matching the requirement's own note. verify: status/flags unchanged. Corrected 'no saga mechanism exists': LegGroup in qip-execution-engine is a tested saga-style unwind decider with no production caller. Fixed a misquote of decomposition.rs (the original says the Placer can withdraw a resting order and CANNOT send a compensating order). Corrected the pre-positioning claim (it is a planned-and-scored forecast, not an unreached TransferGate path)."
  },
  {
    "id": "ARCH-036",
    "status": "PARTIAL",
    "current": "qip-quantum implements ClassicalSolver, QuantumInspiredSolver and IbmQuantumSolver behind a SolverBenchmark (src/{solver.rs,benchmark.rs,provider.rs,qaoa.rs,statevector.rs}), tested in qip-quantum's hosted.rs/quantum.rs/solvers.rs and in qip-acceptance e2e.rs section 9. The kernel links qip-quantum and attaches a SimulatedProvider to the ComputeRouter only when PlatformConfig::quantum_enabled is true (platform.rs:4323-4326).",
    "evidence": [
      "backend/crates/libs/qip-quantum/src/{solver.rs,benchmark.rs,provider.rs,qaoa.rs,statevector.rs}",
      "backend/crates/libs/qip-quantum/tests/{hosted.rs,quantum.rs,solvers.rs}",
      "backend/crates/runtime/qip-kernel/src/config.rs:534 (quantum_enabled: false), :573 with_quantum (zero callers anywhere, tests included)",
      "backend/crates/runtime/qip-kernel/src/platform.rs:4323-4326"
    ],
    "gap": "No process can run a quantum plug-in: PlatformConfig::with_quantum has zero callers anywhere (not even tests), no composition root reads a quantum setting, and SolverBenchmark has no caller outside tests. No QML research plug-in exists (no QML/quantum-kernel code in the workspace). No symbolic/combinatorial-search or world-model/ensemble-selection benchmark experiment was found. No phase-gate test runs each plug-in against its classical counterpart.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-quantum (tests hosted, quantum, solvers); cargo test -p qip-optimization-engine --test optimization. There is no `benchmark` test target in qip-quantum.",
    "work_item": "E10",
    "tier": "T2",
    "size": "L",
    "notes": "The symbolic-search and ensemble-selection experiments are entirely unbuilt, not merely unreached. verify: integrated true -> false and deployable true -> false: the row's own gap says the solver path never runs in a deployed process, and PlatformConfig::with_quantum has no caller at all, so no composition root reaches a quantum plug-in. Fixed the verification command (no `benchmark` test target exists) and added the missing QML plug-in to the gap."
  },
  {
    "id": "ARCH-037",
    "status": "PARTIAL",
    "current": "Two structural halves exist. (1) qip_quantum::benchmark::BenchmarkReport carries classical_baseline as a non-optional field (backend/crates/libs/qip-quantum/src/benchmark.rs:303-321), and measured_advantage/claim (around :345-393) compare entrants against it. (2) At run time, qip-optimization-engine's ComputeRouter::solve always computes the classical baseline first and uses a quantum answer only when it is feasible and better by more than policy.quantum_margin, classical winning ties (router.rs:1-17, :289-360); the router is reached in production through qip-portfolio-engine's construction.rs:366. Its quantum branch never engages in any process, because nothing sets quantum_enabled (ARCH-036).",
    "evidence": [
      "backend/crates/libs/qip-quantum/src/benchmark.rs:303-393 (BenchmarkReport, measured_advantage, claim)",
      "backend/crates/services/qip-optimization-engine/src/router.rs:1-17, :289-360 (baseline always solved; quantum used only above margin)",
      "backend/crates/services/qip-optimization-engine/tests/optimization.rs:202 the_classical_baseline_is_always_computed, :389 a_quantum_answer_that_ties_loses_to_the_classical_baseline",
      "backend/crates/tests/qip-acceptance/tests/e2e.rs:907-936 (calls SolverBenchmark directly inside the test; asserts the baseline is present and classical)"
    ],
    "gap": "No promotion gate consults a quantum/classical comparison: qip-evolution's promotion.rs and qip-lifecycle's gates contain no quantum or classical-baseline reference, so nothing refuses to promote a quantum-backed capability that failed to beat its baseline -- the requirement's check. The per-solve rule in ComputeRouter is the nearest runtime analogue, but it selects an answer per problem; it is not a capability promotion gate, and its quantum branch is never taken in any process.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "ARCH-036"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-optimization-engine --test optimization; cargo test -p qip-acceptance --test e2e (section 9). A promotion-gate test refusing a quantum-backed candidate without a recorded, beaten classical baseline would need to be written.",
    "work_item": "E10",
    "tier": "T1",
    "size": "S",
    "notes": "This is ADR 0006, already a standing decision; the structural half is solid, the enforcement-at-promotion half is the gap. verify: e2e true -> false: e2e.rs section 9 calls SolverBenchmark directly inside the test, crossing no process or stage, and does not exercise the requirement's promotion refusal. Corrected the method name (there is no beats_baseline; it is measured_advantage) and added the ComputeRouter per-solve margin rule the assessor missed."
  },
  {
    "id": "ARCH-038",
    "status": "PARTIAL",
    "current": "qip-prediction's event-market engine (books, oracle settlement, adapters, arbitrage, pricing, cross-market comparison) is fully implemented and tested, and qip-financial/src/physical.rs (1,072 lines: PhysicalArbitrage, LandedCost, Customs, Spoilage, LogisticsTerms, 11 embedded tests) is likewise complete.",
    "evidence": [
      "backend/crates/services/qip-prediction/src/{market.rs,oracle.rs,adapter.rs,arbitrage.rs,cross.rs,pricing.rs,scoring.rs}",
      "backend/crates/libs/qip-financial/src/physical.rs",
      "backend/crates/services/qip-prediction/src/adapter.rs:11 (VenueApiAdapter always returns Error::Unavailable by design)"
    ],
    "gap": "Roughly 98% of qip-prediction's src is reached only from its own tests; qip-financial's physical.rs has zero callers anywhere outside its own file. No market-creation workflow and no logistics adapter are wired to any composition root. The kernel does link qip-prediction, but only its resolution module (hypothesis falsification, qip-kernel/src/falsification.rs:62, platform.rs:171) -- not the event-market engine.",
    "implemented": true,
    "tested": true,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "Each module's own crate tests; no production caller to demonstrate integration.",
    "work_item": "E17",
    "tier": "T2",
    "size": "L",
    "notes": "Beyond simulation, market creation and physical commerce are BLOCKED under ADR 0021/C1; the gap scored here is that even the simulated/paper form is unwired. verify: deployable true -> false: with integrated false (no composition root reaches the event-market engine or physical.rs), nothing here could run from committed config. Noted that the kernel's qip-prediction use is resolution-only."
  },
  {
    "id": "ARCH-039",
    "status": "PARTIAL",
    "current": "An autonomous model/strategy lifecycle exists and is reached from production: qip-lifecycle's gates.rs/trials.rs/demotion.rs promotion ladder, gated by AutonomyLevel (backend/crates/services/qip-risk-engine/src/autonomy.rs:30-43).",
    "evidence": [
      "backend/crates/services/qip-lifecycle/src/{gates.rs,trials.rs,demotion.rs}",
      "backend/crates/services/qip-risk-engine/src/autonomy.rs:30-43"
    ],
    "gap": "The phase's headline additions do not exist at all: `rg -i 'Ambient Model Mesh|Attention Router'` across backend/ returns zero hits. qip-deepbrain::campaign runs on a fixed learning cadence, not launched 'without prompts' by an ambient signal. The 18-agent roster (backend/crates/agents/qip-investment-agents/src/manifests.rs:507) is a fixed manifest, not a dynamically coordinated specialist-agent society.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "ARCH-027"
    ],
    "blocked_by": "",
    "verification": "No test exists for an ambient/unprompted trigger; would need to be written once the Ambient Model Mesh exists.",
    "work_item": "E09",
    "tier": "T3",
    "size": "XL",
    "notes": "Broader tool use and external research-campaign action beyond read-only stays BLOCKED under ADR 0021 regardless of what gets built."
  },
  {
    "id": "ARCH-040",
    "status": "PARTIAL",
    "current": "Fragments of three of the phase's deliverables exist and are reached from production: (a) registries -- a versioned model registry (qip-ai/src/registry.rs:220 ModelRegistry; cards keyed name@version, :165-167; governed by qip-kernel/src/central/models.rs) and a source registry (qip-data-finder/src/registration.rs:265 RegistrationRegistry, held by Platform at platform.rs:4542); (b) continuous benchmark-gated promotion -- models need a passing evaluation before promotion (qip-ai), and qip-deepbrain's SuccessionDesk runs champion-vs-challenger rounds that crown a challenger only on a gated win (qip-deepbrain/src/succession.rs, constructed at evolution.rs:392); (c) retirement -- qip-lifecycle's demotion ladder. qip-agents::Capability is a fixed enum, not a capability registry.",
    "evidence": [
      "backend/crates/libs/qip-ai/src/registry.rs:220 ModelRegistry; tests backend/crates/libs/qip-ai/tests/model_infrastructure.rs:523 a_model_needs_a_passing_evaluation_before_promotion, :618 the_registry_finds_the_current_production_version",
      "backend/crates/services/qip-data-finder/src/registration.rs:265 RegistrationRegistry; backend/crates/runtime/qip-kernel/src/platform.rs:4542; tests backend/crates/services/qip-data-finder/tests/lifecycle.rs:39",
      "backend/crates/apps/qip-deepbrain/src/succession.rs (SuccessionDesk; tests :548 a_crowning_names_the_new_champion_and_the_round_it_won, :665 a_regime_with_an_established_losing_record_holds_back_a_win_until_it_repeats); evolution.rs:392",
      "backend/crates/services/qip-lifecycle/src/demotion.rs",
      "rg -i 'curriculum' backend/crates -> only qip-cli/src/blueprint.rs text; rg -i 'ontology|active.learning' -> zero hits"
    ],
    "gap": "The phase's headline machinery is unbuilt: no Curriculum Engine (no novelty- or live-vs-replay-residual-driven curriculum items), no ontology/tool/brain registries and no versioned capability registry, no sandboxed tool or agent creation, no active learning, no memory consolidation, no regional-to-global learning loop, no governed spawning/retirement of world models, no symbolic theory/rule growth. No phase-gate test.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "ARCH-027",
      "ARCH-039"
    ],
    "blocked_by": "",
    "verification": "cargo test -p qip-ai --test model_infrastructure; cargo test -p qip-deepbrain succession; cargo test -p qip-data-finder --test lifecycle. The phase-gate test (curriculum item -> sandboxed creation -> benchmark-gated promotion with versioned registries) would need to be written.",
    "work_item": "E16",
    "tier": "T4",
    "size": "XL",
    "notes": "Master Phase 9 maps to P3, the lowest priority in this batch; nothing here is urgent relative to the earlier phases that are themselves incomplete. verify: upgraded MISSING -> PARTIAL with flags implemented/tested/integrated/deployable true for the fragments: a versioned model registry, a source registry held by Platform, and benchmark-gated promotion (model evaluation gate; deep brain champion/challenger SuccessionDesk) all exist in non-test code, are reached from qip-kernel/qip-deepbrain, and have named tests. 'Nothing implements it' was not true; the Curriculum Engine and sandboxed creation remain entirely absent."
  },
  {
    "id": "ARCH-041",
    "status": "MISSING",
    "current": "nothing. No GoalSpec/action-affordance graph, Intervention Planner, authorized action adapter, communications/conduct gate, causal-effect-attribution machinery, experiment/no-action baseline, or Action Outcome Memory exists anywhere in the workspace.",
    "evidence": [
      "rg 'GoalSpec' backend/ -> zero hits",
      "rg -i 'Intervention Planner|Action Outcome Memory' backend/ -> zero hits"
    ],
    "gap": "The entire capability, including its shadow-only entry form, is unbuilt.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E16",
    "tier": "T4",
    "size": "XL",
    "notes": "The requirement's own shadow-only starting form is not BLOCKED by ADR 0021 to build (a shadow planner that acts on nothing needs no live-action path); it is simply not started."
  },
  {
    "id": "ARCH-042",
    "status": "MISSING",
    "current": "nothing implements causal-agency rollout specifically, because causal agency itself does not exist (ARCH-041). The general strategy-graduation ladder (qip_contracts::gate::GateStage::{Paper,Shadow,Pilot,...}, backend/crates/libs/qip-contracts/src/gate.rs:23-78, 'no path that skips shadow') and qip-risk-engine::AutonomyLevel are the structural mechanisms any future agency rollout would have to route through, but neither is wired to a causal-agency capability today.",
    "evidence": [
      "backend/crates/libs/qip-contracts/src/gate.rs:23-78",
      "backend/crates/services/qip-risk-engine/src/autonomy.rs:30-43"
    ],
    "gap": "There is no causal-agency capability to gate.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-041"
    ],
    "blocked_by": "",
    "verification": "No test exists.",
    "work_item": "E16",
    "tier": "T4",
    "size": "XL",
    "notes": "Under ADR 0021 every external action stays shadow-form in this repository regardless, so even once ARCH-041 exists, the second and third rollout stages named here stay BLOCKED (C1). verify: status unchanged (MISSING confirmed: no GoalSpec, affordance, Intervention Planner or Action Outcome Memory anywhere). Corrected the enum name: it is GateStage, not Gate."
  },
  {
    "id": "ARCH-043",
    "status": "INCORRECT",
    "current": "At the crate level the separation holds: qip-edge-node's dependency closure contains no warm/global-service crate (no qip-kernel, qip-api, qip-ai; computed from cargo metadata). At the composition level it does not. qip-edge-node's probe loop runs, on one thread and in series: the journal flush (main.rs:852), a blocking exchange with the central plane via link.exchange_with_installers (main.rs:869), and only then the pass (run_pass, main.rs:888). The uplink retries with a real ThreadSleeper ('a deployment actually waits', qip-edge-node/src/mesh.rs:233-242) over qip-transport's HTTP client (2 s connect and 15 s read timeouts, http.rs:73-74) and a three-attempt ladder before dead-lettering. qip-edge's own mesh test says it outright: 'the cell's work loop is the thread doing the waiting' (qip-edge/tests/mesh.rs:601-605). The node's comment at main.rs:861 concedes that a production cell would run the exchange on a timer.",
    "evidence": [
      "backend/crates/apps/qip-edge-node/src/main.rs:852 (cell.flush), :869 (link.exchange_with_installers), :888 (run_pass) -- same loop, same thread",
      "backend/crates/apps/qip-edge-node/src/mesh.rs:233-242 (ThreadSleeper; 'a deployment actually waits')",
      "backend/crates/libs/qip-transport/src/http.rs:73-74 (connect_timeout 2 s, read_timeout 15 s)",
      "backend/crates/edge/qip-edge/tests/mesh.rs:601 a_delta_for_a_peer_that_is_not_listening_is_dead_lettered_rather_than_blocking_forever (comment: 'the cell's work loop is the thread doing the waiting'; 3 attempts spent)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:715 no_edge_cell_can_reach_a_language_model (crate-level separation only)"
    ],
    "gap": "A partitioned or slow central plane delays the next pass by the uplink's connect/read timeouts and retry backoff until the breaker opens (and again on each half-open probe), so tick-to-order latency is NOT unchanged -- a synchronous warm-lane call sits in series with the local decision loop, which is the form the requirement forbids. The fix is in-tree: move the exchange (and the journal flush) off the pass thread onto a timer/thread with a bounded hand-off, then write the chaos test the requirement names (partition the centre, measure pass latency and assert no synchronous global call on an order's trace). Nothing can be observed live: qip-edge-node is deployed nowhere.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-031"
    ],
    "blocked_by": "",
    "verification": "Read backend/crates/apps/qip-edge-node/src/main.rs:840-890. A chaos test that partitions the central plane during a simulated-feed session and asserts unchanged pass latency does not exist and would currently fail.",
    "work_item": "E04",
    "tier": "T2",
    "size": "M",
    "notes": "verify: NEEDS-VALIDATION -> INCORRECT (tier T1->T2, size S->M). The row reasoned only from Cargo.toml; the node's loop runs a blocking, retrying central-plane exchange on the pass thread immediately before each pass, which a qip-edge test comment itself acknowledges. That is a synchronous global/warm dependency on the local hot path's cadence -- the INCORRECT example in the status rules."
  },
  {
    "id": "ARCH-044",
    "status": "PARTIAL",
    "current": "Cell::work and Cell::on_bytes write nothing to a file (qip-edge/src/journal.rs module doc); decisions are appended to an in-memory hash-chained journal and drained through Mirror later. In qip-edge-node that drain (cell.flush into a KeyValueStore -- engine target on the execution node, startup.sh.tftpl:184) runs synchronously on the same thread immediately before the pass (main.rs:852 then :888); a failed flush is reported and entries stay held and chained for the next flush (main.rs:848-857). There is no separate Fabric broker: the only journal destination is local storage.",
    "evidence": [
      "backend/crates/edge/qip-edge/src/journal.rs (module doc; MemoryMirror :608, FileMirror :627)",
      "backend/crates/apps/qip-edge-node/src/main.rs:848-857 (flush failure reported, node keeps serving), :888 (run_pass after flush)",
      "backend/crates/apps/qip-edge-node/src/mirror.rs:144 (StoreMirror)",
      "backend/crates/edge/qip-edge/tests/cell.rs:398 mirror_batches_chain_onto_each_other_across_flushes (normal flushing only)"
    ],
    "gap": "There is no distinct 'Fabric' separate from the cell's local journal and store (see ARCH-028), so the requirement's chaos check -- stop every Fabric broker, observe unchanged latency, observe the journal buffer and catch up -- cannot be performed as stated. No test exercises a failing mirror while passes continue, nor measures pass latency with the journal destination stalled; the flush is synchronous on the pass thread, so a stalled store would stall the next pass. The mesh tests previously cited here concern the cell<->centre link, not the Fabric, and they show bounded blocking of the work-loop thread, not non-blocking behaviour.",
    "implemented": true,
    "tested": false,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-028"
    ],
    "blocked_by": "",
    "verification": "No named test demonstrates the property. Needed: a qip-edge-node test with a store that fails or stalls, asserting passes keep running at unchanged latency and that the held journal ships once the store returns.",
    "work_item": "E03",
    "tier": "T2",
    "size": "M",
    "notes": "verify: tested true -> false. The only cited tests were mesh-uplink tests, which are about a different path and demonstrate the uplink BLOCKS the work loop (bounded), contradicting the row's 'mesh uplink is likewise non-blocking'. No test shows the hot path surviving a journal-destination outage. Recorded that the node's flush is synchronous before each pass."
  },
  {
    "id": "ARCH-045",
    "status": "PARTIAL",
    "current": "At run time the reflex node cannot synchronously depend on a managed service through its store: the execution node's startup script fixes QIP_STORAGE_TARGET=engine (infrastructure/terraform/modules/execution-node/templates/startup.sh.tftpl:184), and every managed target refuses key-value construction at StorageSettings::preflight, so a misconfigured node stops rather than reaching BigQuery/Spanner/Bigtable/AlloyDB/Memorystore (qip-storage/src/settings.rs:219-240). No third-party client crate exists workspace-wide (serde family only), and no edge crate reaches qip-ai (so no AI endpoint). qip-edge-node's source never touches a blob store or warehouse. (infrastructure/terraform/variables.tf:365-383 storage_target governs the central Cloud Run workloads, not the node.)",
    "evidence": [
      "infrastructure/terraform/modules/execution-node/templates/startup.sh.tftpl:184 (QIP_STORAGE_TARGET=engine)",
      "backend/crates/libs/qip-storage/tests/settings.rs:173 a_managed_target_fails_preflight_naming_the_configuration_it_needs",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:715 no_edge_cell_can_reach_a_language_model",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1485 no_crate_declares_a_third_party_dependency_beyond_the_two_permitted",
      "backend/crates/libs/qip-storage/src/gcp/mod.rs:1-16 (in-tree CloudStorageBlobStore and BigQueryWarehouse over HTTP/1.1, linked into qip-edge-node via qip-storage)"
    ],
    "gap": "The requirement's dependency check of the reflex binary would find clients: qip-edge-node depends on qip-storage, whose gcp module holds real in-tree HTTP adapters for Cloud Storage and BigQuery (plus the refusing managed ports). They are unreachable from the node's key-value path, but they are in the binary, and no test pins the node's dependency closure against them. No chaos run with Spanner/Bigtable/BigQuery/Redis/Cloud Service Mesh/AI endpoints unreachable exists, and none can be observed live because no node is deployed. Separating the managed adapters out of the edge node's closure (or a test asserting the node never constructs them) is in-tree work.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "",
    "verification": "cargo test -p qip-storage --test settings; cargo test -p qip-acceptance --test architecture. Missing: a dependency-closure test for qip-edge-node that excludes qip-storage::gcp/managed adapters, and the chaos run.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "verify: NEEDS-VALIDATION -> PARTIAL, tested false -> true. Constituent tests exist (managed targets refuse at preflight; no edge crate reaches qip-ai; no third-party crate), so 'no test' was wrong; but the requirement is not wholly met, because the reflex binary links in-tree BigQuery and Cloud Storage clients through qip-storage::gcp. Corrected the evidence: storage_target (variables.tf:365-383) is the central workloads' setting; the node's target is fixed at engine in its startup template."
  },
  {
    "id": "ARCH-046",
    "status": "PARTIAL",
    "current": "infrastructure/terraform/modules/execution-node provisions a single dedicated (non-shared) Compute Engine VM holding direct venue sessions, with no external address and no container runtime.",
    "evidence": [
      "infrastructure/terraform/modules/execution-node/variables.tf:118-146 (machine_type)",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:661 the_execution_node_has_no_external_address_and_no_container_runtime",
      "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:1654 (machine_type test)"
    ],
    "gap": "The machine_type variable's validation set is exactly c3-highcpu-8, c3-highcpu-22, c3d-highcpu-8, c3d-highcpu-16 -- it refuses C4D/C4 entirely, which is the machine class v2.1 (the more specific, governing document for this requirement) names. execution_nodes = {} in every environment, so no instance of any class currently runs.",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": false,
    "e2e": false,
    "depends_on": [],
    "blocked_by": "C8",
    "verification": "backend/crates/tests/qip-acceptance/tests/infrastructure.rs:1654 and :661.",
    "work_item": "E14",
    "tier": "T1",
    "size": "S",
    "notes": "The placement pattern (dedicated, non-shared, direct venue sessions) is correct; only the machine-family whitelist needs widening, pending the venue-level benchmark the requirement's own source calls for. Actually running one is C8-blocked (cost/billing)."
  },
  {
    "id": "ARCH-047",
    "status": "PARTIAL",
    "current": "No Pub/Sub or Kafka client crate exists anywhere in the workspace: scripts/check-dependencies.sh permits only serde/serde_json and their transitives, enforced by architecture.rs's third-party-dependency test. qip-streaming's pubsub module (PubSubBinding/PubSubTransport) is an in-tree port that returns Error::Unavailable and never falls back; its only production reference is TransportKind::PubSub in the stream registry's config (registry.rs:60-73), which refuses at construction. No Kafka appears anywhere in backend/, infrastructure/terraform or .github.",
    "evidence": [
      "scripts/check-dependencies.sh output ('all permitted', serde-family only)",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1485 no_crate_declares_a_third_party_dependency_beyond_the_two_permitted",
      "backend/crates/tests/qip-acceptance/tests/architecture.rs:1533 the_decision_core_named_by_adr_0009_is_the_set_actually_held_to_two",
      "backend/crates/services/qip-streaming/src/pubsub.rs (PubSubBinding/PubSubTransport, refusing port); src/registry.rs:60-73 (TransportKind::PubSub)",
      "infrastructure/terraform/modules/secrets/main.tf:78 google_pubsub_topic.rotation (Secret Manager -> operator rotation notice)",
      "infrastructure/terraform/modules/trust-zones/main.tf:587-609 google_pubsub_topic_iam_member fabric_publish/fabric_attach on var.control_fabric_topic; infrastructure/terraform/main.tf:466-493 (passed null)"
    ],
    "gap": "The requirement's Terraform scan is not clean and nothing tests it. Terraform creates one Pub/Sub topic (google_pubsub_topic.rotation, a Secret Manager rotation notification to an operator -- not internal messaging, but a scan will find it and the allowance should be recorded), and the trust-zones module still carries publish/attach bindings for a Pub/Sub 'control fabric' topic, passed null at main.tf:493 with a comment that 'whether it is Pub/Sub at all is open' -- which ADR 0099's adoption of this prohibition should now close. qip-streaming still offers PubSub as a selectable internal TransportKind (refusing). No acceptance test scans Terraform/workflows for Pub/Sub or Kafka used for internal messaging. The second clause -- 'every internal asynchronous contract is a Fabric schema' -- does not hold: internal async contracts are split across qip-events, qip-transport and qip-streaming's DurableLogTransport (ARCH-028).",
    "implemented": true,
    "tested": true,
    "integrated": true,
    "deployable": true,
    "e2e": false,
    "depends_on": [
      "ARCH-028"
    ],
    "blocked_by": "",
    "verification": "scripts/check-dependencies.sh; cargo test -p qip-acceptance --test architecture. Missing: an infrastructure acceptance test refusing google_pubsub_*/Kafka resources other than the recorded rotation-notice exception.",
    "work_item": "E03",
    "tier": "T2",
    "size": "M",
    "notes": "The 'no Pub/Sub or Kafka client' half is solid and COMPLETE-quality on its own; scored PARTIAL because the requirement bundles it with the unified-schema claim. verify: status/flags unchanged. The row's scan claim was incomplete: Terraform has a Pub/Sub rotation topic and a latent Pub/Sub control-fabric binding path in trust-zones, and qip-streaming keeps PubSub as a selectable transport kind; none is used for internal messaging today, but the Terraform half of the check is untested. Fixed the garbled type name ('qip_streaming::pubsub::n')."
  },
  {
    "id": "ARCH-048",
    "status": "MISSING",
    "current": "nothing. No partner has been named, and no bridge code, module or rule exists.",
    "evidence": [
      "rg -i 'partner.*bridge|bridge.*kafka|bridge.*pub.?sub' backend/ infrastructure/ -> no relevant hits"
    ],
    "gap": "If a partner bridge were added today, nothing specifically enforces 'stateless and at the external boundary only' -- the only applicable control is the generic new-dependency ADR gate (ADR 0002/0009) that any Kafka/Pub/Sub client crate would have to pass regardless of where it sits.",
    "implemented": false,
    "tested": false,
    "integrated": false,
    "deployable": false,
    "e2e": false,
    "depends_on": [
      "ARCH-047"
    ],
    "blocked_by": "",
    "verification": "No test exists; not applicable until a partner need arises.",
    "work_item": "E03",
    "tier": "T0",
    "size": "S",
    "notes": "Conditional requirement with no phase and P2 priority; nothing to build until a partner integration is proposed, at which point a dependency record (ADR 0002/0009) is required in any case."
  }
]
