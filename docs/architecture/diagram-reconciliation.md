# Superseded reconciliation — every earlier-diagram id scored against the tree

> **Correction, 2026-09-02 — the runtime rows describe a runtime that no
> longer exists.** T.7 (GKE), N.2, N.3 and N.4, BB.1 and L2.9, and backlog item
> 2 cite `infrastructure/terraform/modules/cluster/`,
> `infrastructure/kubernetes/base/{api,edge-cell,journal-storage}.yaml`, a PDB,
> an HPA and an edge-cell StatefulSet. All of that was removed under ADR 0024:
> the cluster's Terraform in `808ca32`, the manifests in `67b3e92` and
> `7d79161`. The runtime in `infrastructure/terraform/main.tf` is now Cloud Run
> services from `catalogue.tf` and one Compute Engine execution node per region
> from `modules/execution-node`, never applied. Backlog item 2's action — set
> `QIP_MESH_CELLS` and `QIP_MESH_PEER` in the manifests — has no manifest to
> land in; ADR 0024 records the centre-to-node path as unwired on the new
> runtime and names the reason (`catalogue.tf:21-27`). The rows are left as
> scored at `9854ec7`, for the reason the note below gives.
>
> **This scores a superseded reference.** ADR 0022 makes the Algorik Master
> Blueprint v10.1-4 the architecture of record; the live scorecard is
> [`algorik-blueprint-traceability.md`](algorik-blueprint-traceability.md).
>
> The 104 component ids below belong to
> [`canonical-platform.md`](canonical-platform.md), the earlier diagram.
> **Retained for history, not deleted:** this is a real scoring pass whose
> findings — which controls could not fire, which crates no binary composed —
> are still the only written record of several defects and of why they were
> fixed. Deleting it would destroy evidence in order to tidy a directory.
>
> A status in this file describes alignment with the *old* picture. It neither
> confirms nor contradicts a row in the live scorecard, and the two must not be
> merged: a component ALIGNED here can be MISSING against the blueprint, and
> collapsing that loses the finding.


Scored against the working tree at `9854ec7` (branch `claude/autonomous-investment-platform-76gt4y`).
Every row was re-derived by grep/read of the current source; the prior audits were used only as leads.

**Method.** A row is *Complete and verified* only where both an implementation `path:line` and a
**named test that passed in this session** exist. Where a type is constructed but no binary in
`backend/crates/apps/` invokes it, the ceiling is *Implemented but unverified*, whatever its tests say.

**Suites executed here** (`cargo test --offline`, prebuilt target): `qip-kernel`, `qip-api`, `qip-risk`,
`qip-routing`, `qip-edge`, `qip-mesh`, `qip-world-model`, `qip-cost-router`, `qip-confidential`,
`qip-training`, `qip-fastbrain`, `qip-deepbrain`, `qip-edge-node`, `qip-acceptance` — all green.
(Mid-session, `qip-acceptance::documentation::no_document_promises_something_the_platform_does_not_do`
failed on an overclaim in the then-untracked `docs/architecture/repo-inventory.md`; another agent
corrected that file and the test passes. This document introduces no such claim.)

**Scope boundary — work in flight.** Scoring covers the committed tree only. While this was being
written, other agents left uncommitted work in the checkout that contradicts two premises this
reconciliation was handed: an untracked `frontend/` (Next.js, Playwright, `package.json`) and an
untracked `backend/crates/apps/qip-api/src/stream.rs` whose own header opens "Live streams, as server-sent
events", plus edits to `qip-api/src/{http,lib,routes,web}.rs` and a new
`qip-market-ingestion/src/connector/`. None of it is scored here — it is not committed and was not
built or tested in this session — but "there is no frontend and no SSE" is on its way to being false,
and the next pass over this document must re-check the L1.1 / N.6 / operator-console rows against it.

**Standing context applied, not rediscovered.** ADR 0011 replaced Pub/Sub with the in-tree `qip-transport`
mesh; six managed-data flags are deliberately `false` pending a TLS egress proxy that does not exist;
the committed tree has no frontend framework and no WebSocket/SSE (see the scope boundary above);
the platform is paper-trading only, so "execute" means "execute against the simulator".

| id | Diagram capability | Expected behavior | Repository evidence (path:line) | Runtime evidence | Tests | Status | Gap | Required action |
|---|---|---|---|---|---|---|---|---|
| L1.1 | Market data, all asset classes | Live prices for every class reach the loop | `backend/crates/runtime/qip-kernel/src/platform.rs:1049` absorbs Bar/Trade/Tick/Quote/Book; `backend/crates/services/qip-market-ingestion/src/rest.rs` live adapter | fastbrain polls a feed each tick (`backend/crates/apps/qip-fastbrain/src/node.rs:107`) but `feed.rs:49-51` can only open Synthetic or Replay | `acceptance.rs::a_market_observation_travels_the_whole_loop` | Partially implemented | Every deployment's data is synthetic or replayed; the REST adapter is reached only from `e2e_live.rs`; no non-equity class is exercised | Wire `RestMarketDataAdapter` into `feed.rs` behind the existing credential gate |
| L1.2 | Order books L1/L2/L3 | Depth maintained and priced | `backend/crates/edge/qip-orderbook/src/{l2,auction,venue}.rs`; `platform.rs:1114-1127` folds books into depth; L3 at `qip-orderbook/src/l3.rs:95` | L1/L2 absorbed every cycle and drive the cell's venue state | `absorption.rs::a_book_feeds_the_liquidity_topology_at_the_instant_it_was_observed`; `performance.rs::book_apply_costs_what_the_budget_says` | Partially implemented | `L3Book` and `QueuePosition` have zero consumers outside their own crate — order-by-order depth is never built | Wire `L3Book` into the cell's venue state, or drop "Level 3" from the canonical list |
| L1.3 | News & social events | Items resolved to entities and scored | `platform.rs:1129-1138` → `WorldModel::absorb_news` + `MarketEvent::from_news` | Runs in `observe` on every cycle | `absorption.rs::a_news_item_lands_in_the_evidence_index_rather_than_vanishing` | Partially implemented | No live news or social adapter exists; only synthetic narratives feed it | Add one live news source behind the licensing gate |
| L1.4 | Company data — filings, earnings | Fundamentals with surprise vs consensus | `platform.rs:1140-1153`; surprise series keyed for the detector | Absorbed each cycle; feeds `FundamentalSurprise` detection | `absorption.rs::a_fundamental_that_previously_vanished_now_lands_with_its_own_two_instants` | Partially implemented | No live filings/earnings source | Add a filings adapter; reuse the tri-temporal provenance already required |
| L1.5 | Macro & econ indicators | Releases absorbed with knowability | `platform.rs:1154-1158` `absorb_macro` + `MarketEvent::from_macro` | Runs in `observe` | `qip-world-model/tests/understanding.rs:752` exercises `absorb_macro`; no kernel-level test names the macro arm | Partially implemented | No live macro source; no kernel test pins the macro arm of `observe` | Add a macro adapter and one absorption test mirroring the fundamental one |
| L1.6 | On-chain / DeFi — NFTs, wallets | Chain state, reorgs, NFT and wallet views | `platform.rs:2679` `observe_chain`; `backend/crates/services/qip-chain/src/{amm,block,bridge,finality,gas,mempool,rpc,state}.rs` | Chain summarised in `stage_understand`; `rpc.rs` reaches a real node | `platform_services.rs::chain_state_cannot_be_read_shallower_than_the_configured_depth`; `::a_block_from_another_chain_is_refused_without_losing_the_batch` | Partially implemented | Zero occurrences of `nft` or `wallet` in `qip-chain/src` — both named boxes have no code | Either build wallet/NFT views or delete them from the canonical list |
| L1.7 | Alt data — satellite, IoT, mobility, web | Panels as bitemporal features | `platform.rs:1181-1203` records `alt/{dataset}/{metric}`; `backend/crates/services/qip-market-ingestion/src/alternative.rs` | Runs in `observe` | No named test asserts the alternative-data arm of `observe` | Partially implemented | Untested at the kernel seam; no live alt-data source | Add an absorption test for the alt arm; wire one panel source |
| L1.8 | Reference data — prices, FX, rates, ids, corp actions | Effective-dated identity and fields | `platform.rs:1204-1243` (reference), `:1159-1180` (corporate actions) | Both arms run in `observe`; reference values are readable only from their effective instant | `absorption.rs::a_reference_change_is_readable_only_from_its_effective_instant` | Partially implemented | No FX or rates source anywhere; the corporate-action arm has no named test | Add an FX rate source (also unblocks L4.5) and a corp-action absorption test |
| L1.9 | AI feed discovery agents | Continuously find and qualify new sources | `platform.rs:2614` `assess_sources`; `backend/crates/services/qip-data-finder/src/{probe,scoring,quality,robots,legal,replacement}.rs` | No binary calls `assess_sources`; `/data-sources` answers `NO_DATA_FINDER` (`qip-api/src/routes.rs:449`) | `platform_services.rs::a_source_the_finder_registers_appears_in_the_mesh_catalogue` | Implemented but unverified | Discovery never runs outside tests | Drive a discovery round from deepbrain's research cadence, as evolution already is |
| L1.10 | Continuous collectors (Rust) | Always-on polling into the loop | `backend/crates/apps/qip-fastbrain/src/node.rs:107`; `backend/crates/apps/qip-fastbrain/src/feed.rs:89` | Polls every tick in the deployed fastbrain | `qip-fastbrain/src/node.rs` inline cycle tests (`platform.cycle_count() == 3`) | Partially implemented | Collector loop is real; the only adapters it can open are synthetic and replay | Same action as L1.1 |
| L1.11 | Dedup, normalization, time sync, enrichment | One canonical, deduplicated, enriched stream | Dedup `backend/crates/libs/qip-events/src/bus.rs:262`, `backend/crates/services/qip-streaming/src/envelope.rs:399`; normalization `backend/crates/services/qip-normalization/`; sequencing `backend/crates/edge/qip-sequencing/` | Dedup and sequencing run; `qip_normalization` is imported only by `truth_loop.rs` and `performance.rs` | `truth_loop.rs::one_market_event_travels_all_seven_stages_of_the_truth_loop`; `resilience.rs::an_out_of_order_and_duplicated_feed_is_absorbed_without_loss_of_legibility` | Partially implemented | The normalizer is not in any runtime path — `observe` takes adapter records directly | Put `Normalizer` between the adapter and `Platform::observe` in `feed.rs` |
| L1.12 | Source scoring & quality | Continuous per-source quality scores | `backend/crates/services/qip-data-finder/src/{scoring,quality,health}.rs` | Reachable only through the test-only `assess_sources` | `platform_services.rs::a_source_licensed_only_for_research_never_reaches_the_catalogue` | Implemented but unverified | No scores are produced in any deployment | Follows L1.9 |
| L1.13 | Event fingerprinting | One id for one real-world event across feeds | Contract fingerprints `backend/crates/libs/qip-events/src/registry.rs:26,66`; envelope dedup keys `qip-streaming/src/envelope.rs:399` | Both run each cycle via the journal publish | `qip-events/tests/backbone.rs` (registry fingerprints) | Partially implemented | What exists is *schema* fingerprinting and per-body idempotency; cross-source event identity does not exist | Build an event-identity key over (subject, class, knowable instant) and dedupe `MarketEvent` on it |
| BB.1 | Global streaming backbone | One durable, ordered, deduplicated spine | `backend/crates/libs/qip-transport/`, `backend/crates/services/qip-mesh/src/{spine,delta}.rs`, `backend/crates/apps/qip-api/src/mesh.rs` | API serves the mesh when `QIP_MESH_CELLS` is set (`qip-api/src/main.rs:94-160`); deltas drain, capital dispatches through the durable spool | `qip-api/tests/mesh.rs::a_delta_published_twice_by_a_restarted_cell_is_one_ingestion`; `::a_dispatched_envelope_survives_the_spool_and_verifies_at_a_real_downlink` | Implemented differently | Pub/Sub replaced by the in-tree mesh per ADR 0011. But **no shipped manifest sets `QIP_MESH_CELLS` or `QIP_MESH_PEER`** (`infrastructure/kubernetes/base/{api,edge-cell}.yaml`), so the deployed API answers `MESH_NOT_SERVED` and cells publish nowhere | Set the two variables in the manifests; delete the dead Pub/Sub port |
| L2.1 | Market understanding | Coverage and state reported per cycle | `platform.rs:1520` `stage_understand` | Runs every cycle in fastbrain, deepbrain and `/cycle` | `absorption.rs::the_understand_stage_reports_absorbed_state_rather_than_the_price_series_length` | Complete and verified | — | — |
| L2.2 | Anomaly detection | Named anomalies each cycle | `platform.rs:1613` `opportunities.scan`; 13 kinds at `qip-opportunity-engine/src/detector.rs:17-47` | Runs every cycle | `kernel.rs::the_discover_stage_finds_something_in_a_series_with_a_jump` | Complete and verified | — | — |
| L2.3 | Liquidity & impact | Venue depth and impact priced per decision | `platform.rs:1279` `absorb_depth` into `LiquidityTopology`; impact `backend/crates/libs/qip-financial/src/costs.rs:162` | Topology is fed each cycle; nothing reads it back into a decision | `absorption.rs::a_book_feeds_the_liquidity_topology_at_the_instant_it_was_observed` | Partially implemented | `Platform::liquidity()` has no consumer — depth informs no sizing, routing or refusal | Feed usable depth into `construct_from`'s capacity bound |
| L2.4 | Local alpha & arbitrage | Regional arbitrage found and acted on | `backend/crates/edge/qip-arbitrage/src/scan.rs:173`; cell seam `backend/crates/edge/qip-edge/src/seam.rs:14` | No strategy is deployed into a running cell (`qip-edge-node/src/main.rs:461` prints a count that is always 0); `/arbitrage` answers `NO_ARBITRAGE_ENGINE` | `stress.rs::a_broker_outage_mid_arbitrage_stops_the_plan_and_names_the_exposure_it_stranded` | Partially implemented | The engine is dormant in every deployment | Give `qip-edge-node` a way to receive and deploy a strategy (see L7.6) |
| L2.5 | Cash & inventory | Per-region cash and inventory tracked | `platform.rs:485-591` `CapitalBook`; envelopes `qip-kernel/src/central/plane.rs` | Book updates on every fill; envelopes issued and recalled centrally | `capital.rs::equity_starts_at_the_configured_book_and_an_opening_fill_costs_exactly_its_costs` | Partially implemented | No inventory module exists at the cell; balances are single-location | Add a cell inventory ledger reported on the state delta |
| L2.6 | Risk & limits | Pre-trade checks and limit refusals | `platform.rs:2336` submit path; `backend/crates/libs/qip-risk/src/limits.rs:357` `LimitSet::check` | Every submission passes the checker; refusals are captured on the chain | `kernel.rs::an_order_that_would_breach_a_limit_is_refused_by_the_assembled_platform`; `acceptance.rs::the_risk_limits_the_platform_runs_under_all_state_a_rationale` | Complete and verified | — | — |
| L2.7 | Ultra-fast decisioning | A hot path that cannot stall | `backend/crates/apps/qip-fastbrain/src/roster.rs:18,24` — `FAST_PATH_AGENTS`, 50 ms ceiling checked before assembly | Enforced at fastbrain start-up | `roster.rs::an_agent_whose_budget_exceeds_the_ceiling_is_refused_even_with_no_model_capability` | Complete and verified | — | — |
| L2.8 | Rust engine + local models | Compiled models running in-region | Engine: the whole workspace. Models: `backend/crates/edge/qip-strategy/src/model.rs:54` `DistilledModel`; wired LM is the deterministic stand-in `backend/crates/libs/qip-ai/src/language.rs` | No distilled model is ever loaded by a running cell; the hosted LLM adapter refuses (no TLS) | `qip-strategy/tests/strategy.rs` (model evaluation) | Partially implemented | "Local models" is aspirational: nothing produces or ships one | Follows L7.5 and L7.6 |
| L2.9 | Publish regional state deltas | Opportunities, confidence, liquidity, capital, risk upward | `backend/crates/apps/qip-edge-node/src/mesh.rs` uplink; decode `backend/crates/services/qip-mesh/src/delta.rs` | Cell publishes each tick; API drains into `Platform::ingest_cell_report` | `qip-api/tests/mesh.rs::a_delta_published_twice_by_a_restarted_cell_is_one_ingestion` | Partially implemented | The code path is complete and tested in-process; `QIP_MESH_PEER` is set in no manifest, so no deployed cell publishes | Configure the mesh variables in the Kubernetes base |
| L3.1 | Global knowledge graph | Assets, currencies, derivatives, companies, venues, cash, collateral, regions | `backend/crates/services/qip-world-model/src/world.rs:39-47`; fed by `platform.rs:1049-1250` | Graph, causal graph, features and evidence index are written every cycle | `absorption.rs` (four named absorption tests) | Partially implemented | Only instruments, news entities, fundamentals, macro and reference fields are nodes — currencies, derivatives, collateral, venues and regions are not | Add the missing node classes, or narrow the canonical wording |
| L3.2 | Cross-market correlation | Correlation structure across markets | `qip-opportunity-engine/src/detector.rs:29` `CorrelationBreakdown`; covariance in `platform.rs:1778` `construct_from`; `backend/crates/libs/qip-risk/src/factor.rs` | Detector runs each cycle; covariance estimated from observed closes at sizing time | `platform.rs::too_little_shared_history_is_a_named_refusal_and_not_a_guessed_covariance` | Partially implemented | One asset class, one venue set; no cross-market surface | Broaden the universe before claiming this |
| L3.3 | Multi-leg arbitrage engine (2/3/N-leg) | Ranked multi-leg paths with net edge | `backend/crates/edge/qip-arbitrage/src/{scan.rs:173,netedge.rs,graph.rs}` | No central caller; `/arbitrage` refuses | `e2e.rs::the_platform_walks_from_a_discovered_source_to_a_learned_lesson` (triangular fixture, `:1085`); `stress.rs::an_arbitrage_leg_the_book_cannot_fill_is_refused_rather_than_part_executed` | Implemented but unverified | Reachable only from the dormant cell seam and tests | Wire the scanner into `stage_discover` as a second opportunity source |
| L3.4 | Event impact & catalyst detection | Moves linked to knowable events | `backend/crates/services/qip-opportunity-engine/src/catalyst.rs`; `platform.rs:1309` `push_market_event` from news/fundamental/macro/corp actions | Catalyst links are formed each cycle from the event buffer | `qip-opportunity-engine/tests/catalyst.rs::an_event_known_before_the_move_explains_it_with_the_lag_recorded` | Complete and verified | — | — |
| L3.5 | Structural mispricing detection | A pricing model that says what is mispriced | `detector.rs:25` `StructuralBreak`; hypothesis mechanism `platform.rs:1896` `synthesise` | Detector runs; the mechanism formed is a single displacement-reversion claim | `kernel.rs::the_reason_stage_forms_a_reviewed_hypothesis_when_a_mechanism_is_implied` | Partially implemented | No structural pricing model — "mispricing" is a statistical break, not a model residual | Add one relative-value model whose residual is the claim |
| L3.6 | Strategy discovery (statistical + AI) | A search that proposes and scores candidates | `backend/crates/apps/qip-deepbrain/src/evolution.rs:250` `turn`; foundry `qip-kernel/src/central/foundry.rs` | Runs on deepbrain's research cadence (`qip-deepbrain/src/main.rs:192`, `node.rs:261`) | `qip-deepbrain/src/evolution.rs::a_turn_registers_scored_candidates_with_the_platform_factory` | Partially implemented | The statistical half runs on cadence; the "AI" half does not — the only wired language model is the deterministic stand-in `qip-ai/src/language.rs`, and the hosted adapter refuses for want of TLS | Either drop "+ AI" from the box or resolve the TLS egress question (backlog 20) |
| L3.7 | Liquidity topology understanding | Usable-vs-total depth across venues | `backend/crates/services/qip-world-model/src/liquidity.rs`; field at `platform.rs:215` | Fed by every Quote and Book | `qip-world-model/tests/liquidity.rs`; `absorption.rs:226` | Partially implemented | Written but never read by any decision | Same action as L2.3 |
| L3.8 | Regime & cycle detection | A standing regime state feeding decisions | `detector.rs:27` `RegimeChange`; scenarios `backend/crates/services/qip-simulation-engine/src/scenario.rs` | Detector fires per cycle | `kernel.rs::the_discover_stage_finds_something_in_a_series_with_a_jump` (shared) | Partially implemented | Regime is an event, not a state — nothing carries a current regime into sizing | Add a regime field to the world model and let the mandate read it |
| L3.9 | Outputs: ranked opportunities, legs, edge, decay, complexity, capital | A ranked, costed queue | `backend/crates/services/qip-opportunity-engine/src/opportunity.rs:68-89` (score ÷ √cost); TTL retain in `stage_discover` | Queue ranked and expired every cycle | `kernel.rs::the_discover_stage_finds_something_in_a_series_with_a_jump` | Partially implemented | Required legs, execution complexity and required capital are not attached to an opportunity | Extend `Opportunity` with the three missing fields at construction |
| L4.1 | Opportunity scoring | Cost-adjusted ranking | `opportunity.rs:68-89`; `platform.rs:1583` `stage_discover` | Runs every cycle | `kernel.rs::the_discover_stage_finds_something_in_a_series_with_a_jump` | Complete and verified | — | — |
| L4.2 | Expected alpha calculation | Nine-deduction net edge | `backend/crates/libs/qip-contracts/src/edge.rs`; kernel computes two at `platform.rs:2993` | Two of nine deductions charged per cycle; the full nine only on the dormant cell path | `platform_outcomes.rs::the_cycle_produces_the_two_deductions_the_platform_charges_itself` | Partially implemented | Seven of nine deductions are never computed centrally | Compute slippage, impact and latency decay in `construct_from` |
| L4.3 | Position sizing & capital allocation | Approved theses become sized legs | `platform.rs:2066` `stage_decide` drains `pending_theses` → `construct_from:1778` → `PortfolioConstructor::construct` | Runs every cycle; sizes against tracked equity | `platform.rs::an_approved_thesis_becomes_a_draft_proposal_with_legs_and_is_drained`; `capital.rs::the_decide_stage_sizes_against_the_tracked_equity_not_a_constant` | Complete and verified | — | — |
| L4.4 | Cash, collateral & margin management | Funding known ahead of the trade | `backend/crates/services/qip-capital-fabric/src/{forecast.rs,plan.rs,transfer.rs}`; `platform.rs:2898` `forecast_capital_demand` | Demand recorded from fills; lanes reported in `stage_decide` | `platform_outcomes.rs::a_fill_is_the_demand_observation_the_forecaster_is_fitted_on`; `::the_decide_stage_reports_where_capital_will_be_needed` | Partially implemented | Transfers are plan types with no executor; **no margin model exists anywhere** | Add a margin model; give `transfer.rs` an executor behind the kill switch |
| L4.5 | FX & multi-currency exposure | Exposure converted, bucketed and hedged | `qip-capital-fabric` `FxRates`; currency-bucket checks in `qip-kernel/src/central/plane.rs` | Composed portfolio is single-currency USD (`platform.rs:2080` `Currency::USD`) | `stress.rs::an_amount_in_a_currency_nobody_can_convert_is_refused_rather_than_summed_at_par` | Partially implemented | No FX rate source; no currency exposure hedging action | Depends on L1.8's FX source |
| L4.6 | Risk engine — real-time VaR / CVaR | VaR and CVaR on the live book each cycle | `backend/crates/libs/qip-risk/src/metrics.rs:24,37,52`; limit kind `limits.rs:97`; monitor `backend/crates/services/qip-risk-engine/src/monitor.rs:121` | Monitor runs every cycle over a real book — but `platform.rs:2306` `risk_state()` leaves `expected_shortfall` at `RiskState::default()`, so **no VaR/CVaR is ever computed for the running book** | `qip-risk/tests/risk.rs::historical_var_is_the_empirical_quantile`; `capital.rs::closing_a_position_above_its_entry_realises_a_gain_the_monitor_can_see` | Partially implemented | The `MaxExpectedShortfall` limit can never fire: its input map is always empty | Populate `RiskState::expected_shortfall` from the position return history in `risk_state()` |
| L4.7 | Hedge & inventory optimization | Residual exposure hedged, inventory optimised | `backend/crates/libs/qip-risk/src/hedge.rs` `HedgeEngine` (proposals only, never orders) | Zero callers outside `qip-risk/tests/hedge.rs`; no inventory optimizer exists | `qip-risk/tests/hedge.rs::rounding_half_up_would_over_hedge_and_the_engine_rounds_down_through_that_case` (16 passing) | Partially implemented | The hedge engine runs nowhere; inventory optimisation has no code at all | Call `HedgeEngine` from `stage_decide` on the constructed proposal |
| L4.8 | Trade / no-trade decision | A governed yes or no per cycle | `platform.rs:2066` decide; gates `platform.rs:2336` submit; refusal reasons captured | Proposals are produced and marked releasable; `stage_act:2146` counts releasable proposals and **stops there** | `acceptance.rs::a_proposal_cannot_reach_execution_without_both_controls` | Partially implemented | Nothing converts a releasable proposal into an order in any binary — `submit_order` is called only by tests | Add a governed release step in `stage_act` that submits approved proposal legs |
| L4.9 | Identity: return − costs − … = usable alpha | The full deduction chain per opportunity | `backend/crates/libs/qip-contracts/src/edge.rs`; `backend/crates/edge/qip-arbitrage/src/netedge.rs` | Computed in full only on the dormant cell path | `qip-arbitrage` net-edge tests | Partially implemented | Same as L4.2 | Same as L4.2 |
| L4.10 | Actions emitted: trade, size, region, venue, strategy, hedge, cash, TTL | A complete action record | Proposal legs + TTL retain; envelopes with expiry in `plane.rs` | Legs and TTL emitted; region, venue, hedge and cash-to-reserve are not | `platform_outcomes.rs::the_decide_stage_reports_where_capital_will_be_needed` | Partially implemented | Four of eight action fields are never produced | Extend the proposal type once L4.7 and L4.8 land |
| L5.1 | Smart order routing | Marginal-cost routing across venues | `backend/crates/edge/qip-routing/src/router.rs:263` | No caller constructs a `RoutingRequest` outside the crate's own tests | `qip-routing/tests/routing.rs::routing_picks_the_best_net_cost_and_not_the_best_quote` (26 passing) | Implemented but unverified | The router is a well-tested island | Call it from the cell gateway before placing |
| L5.2 | Venue selection | Health-scored dynamic selection | `backend/crates/apps/qip-edge-node/src/venue.rs:180` `from_env`; scoring `qip-routing/src/{venue,health}.rs` | Deployment picks the **first** venue in `QIP_VENUES` and opens one gateway | `qip-routing/tests/routing.rs` (exclusion reasons) | Partially implemented | Selection is static config; health scoring is unwired | Follows L5.1 |
| L5.3 | Smart order slicing | Parent split into child orders | `qip-routing/src/router.rs` `RouteSlice`, `RouterSettings::slices`; `children.rs` | Same unwired crate | `qip-routing/tests/routing.rs` (conservation of sliced quantity) | Implemented but unverified | No parent order is ever sliced in a deployment | Follows L5.1 |
| L5.4 | Fill optimization | Best achievable fill given the book | `backend/crates/services/qip-brokers/src/{exchange.rs:600,matching.rs}` price-time matching, lot/tick admission, commissions | Wired via `qip-edge-node/src/gateway.rs`; also `SimulatedBroker` in the kernel | `qip-brokers/tests/exchange.rs` (price-time, lot/tick, duplicate refusal) | Partially implemented | Single-venue only; cross-venue optimisation is the unwired router; the `Repricer` (`qip-routing/src/reprice.rs`, 18 passing tests) has no caller | Wire `Repricer` into the gateway's resting-order loop |
| L5.5 | Hedging | Execution-side hedge legs | `backend/crates/libs/qip-risk/src/hedge.rs` | No caller | `qip-risk/tests/hedge.rs::the_residual_never_flips_to_the_other_side_of_the_target` | Implemented but unverified | Same island as L4.7 | Same as L4.7 |
| L5.6 | Inventory | Per-cell inventory position and limits | None — zero inventory code in `backend/crates/edge/qip-edge/src` | None | None | Missing | The box has no implementation | Build a cell inventory ledger reported on the state delta |
| L5.7 | Failover | Trading survives a failed feed, peer or venue | Feed A/B arbitration `backend/crates/edge/qip-sequencing/`; breaker/retry/spool `backend/crates/libs/qip-transport/`, `qip-mesh/src/spine.rs`; partition tolerance `qip-edge-node/src/mesh.rs` | Cells keep trading inside their envelope through a partition | `stress.rs::a_cell_cut_off_from_the_centre_spends_only_its_grant_and_then_stops`; `chaos.rs::randomly_injected_faults_never_break_the_five_invariants` | Partially implemented | Venue failover does not exist — one gateway per node, first venue only | Allow a second gateway and fail over on venue rejection |
| L6.1 | Fills & P&L | Realised P&L from actual fills | `platform.rs:503` `apply_fill`, `:968` `realised_pnl`; capture `:2372` | Book updates on every fill; but the only caller of the fill path is `submit_order`, which no binary calls | `capital.rs::closing_a_position_above_its_entry_realises_a_gain_the_monitor_can_see` | Partially implemented | No fills are produced in any deployment | Follows L4.8 |
| L6.2 | Slippage & costs | Signed slippage per fill | `platform.rs:2372` `capture_submission` computes signed slippage bps; costs `qip-financial/src/costs.rs` | Same reachability limit as L6.1 | `platform_outcomes.rs::a_fill_and_a_refusal_land_on_the_same_chain` | Partially implemented | No producer in a deployment | Follows L4.8 |
| L6.3 | Partial fills | Remaining quantity tracked and reconciled | `backend/crates/services/qip-execution-engine/src/broker.rs` `remaining_quantity`; drop-copy `backend/crates/edge/qip-edge/src/dropcopy.rs` | Cell gateway drains fills on the independent channel; no strategy generates any | `qip-edge/tests/cell.rs` (fill reconciliation) | Implemented but unverified | No partial fill occurs in a deployment | Follows L2.4 / L7.6 |
| L6.4 | Missed opportunities | A refusal recorded as a priced miss | `backend/crates/services/qip-twin/src/capture.rs:83-186` `Action::MissedOpportunity` | Its only constructor call is `e2e.rs:814`; expired queue entries are counted, never captured | `e2e.rs::the_platform_walks_from_a_discovered_source_to_a_learned_lesson` | Implemented but unverified | Expiring opportunities are dropped rather than captured as misses | Capture a miss in `stage_discover` when an opportunity expires unacted |
| L6.5 | Market impact | Realised impact measured against the model | Model `qip-financial/src/costs.rs:162` (square-root law); twin `backend/crates/services/qip-twin/src/asof.rs` | Impact is modelled, never measured | `qip-twin/tests/twin.rs` | Partially implemented | No realised-vs-modelled impact comparison exists | Add an impact residual to the attribution decomposition |
| L6.6 | Strategy attribution | Exact decomposition that closes | `backend/crates/services/qip-learning-engine/src/attribution.rs:299`; called `platform.rs:2231` | Runs every cycle — on periods with zero spread, zero impact, zero realised P&L and no hypotheses (`platform.rs:2245-2270`) | `acceptance.rs::an_order_travels_the_control_path_and_produces_an_exact_attribution` | Partially implemented | The machinery is exact; its inputs in the running loop are degenerate. `/attribution` answers `NO_ATTRIBUTION` | Populate `PositionPeriod` from the position lots, not from single fills |
| L6.7 | Exposures over time | A time series of exposure | `AggregateExposure` in `backend/crates/libs/qip-capital/` is point-in-time only | None | None | Missing | No exposure history is kept anywhere | Snapshot `AggregateExposure` per cycle into the journal |
| L6.8 | Risk & limit utilization | How close the book is to each limit | `qip-risk/src/limits.rs:357` check; monitor each cycle | Runs each cycle; `/limits` answers `NO_LIMIT_UTILISATION` | `qip-risk/tests/risk.rs` (limit checks) | Partially implemented | Utilisation is computed but not surfaced, and the shortfall inputs are empty (L4.6) | Serve utilisation on `/risk` once L4.6 lands |
| L7.1 | Counterfactual digital twin | Every decision priced against its alternatives | `platform.rs:2809` `evaluate_alternatives`; `backend/crates/services/qip-twin/src/{counterfactual.rs,asof.rs}` | No caller outside `qip-kernel/tests/platform_outcomes.rs` | `platform_outcomes.rs::the_alternatives_to_a_trade_are_priced_and_stay_simulated` | Implemented but unverified | Never runs in a deployment | Call it in `stage_learn` for each captured fill |
| L7.2 | Model training (Vertex AI) | Models fitted and registered | In-tree substitute `backend/crates/services/qip-training/src/{fit,distill}.rs`; Vertex port `qip-training/src/vertex.rs:40-65` refuses (no TLS stack) | Nothing fits a model at runtime; `central/models.rs register_fit` is called only by `qip-kernel/tests/models.rs`; `/training` refuses | `models.rs::a_model_fitted_on_noise_cannot_be_registered_as_having_skill`; `::a_model_with_real_out_of_sample_skill_is_registered_as_having_it` | Partially implemented | The ADR 0011 substitute is real and tested but has no runtime driver | Add a training round to deepbrain's cadence beside the evolution round |
| L7.3 | Strategy evolution engine | Generate, mutate, score, promote | `backend/crates/services/qip-evolution/src/{grammar,generate,mutate,scoring,promotion,challenger}.rs`; driver `qip-deepbrain/src/evolution.rs:250` | Runs on deepbrain's research cadence | `qip-deepbrain/src/evolution.rs::a_turn_registers_scored_candidates_with_the_platform_factory`; `foundry.rs::the_trial_count_that_reaches_the_gate_is_the_whole_search_not_the_last_round` | Complete and verified | — | — |
| L7.4 | Model validation & backtesting | Holdout, purged folds, leakage audit before promotion | `backend/crates/services/qip-simulation-engine/src/{backtest.rs,harness.rs}`; gates `backend/crates/libs/qip-lifecycle/src/gates.rs`; evaluated at `qip-deepbrain/src/evolution.rs:333` | Every candidate is scored on observed history before registration; the ladder refuses rung skips | `qip-deepbrain/src/evolution.rs::a_turn_registers_scored_candidates_with_the_platform_factory`; `central.rs::a_candidate_with_perfect_evidence_but_no_dual_approval_never_yields_an_envelope` | Complete and verified | — | — |
| L7.5 | Policy distillation (small models) | A teacher distilled into a hot-path model | `backend/crates/services/qip-training/src/distill.rs` (fidelity re-checked on every `approved_student`) | Nothing produces a distilled model at runtime | `qip-training` distillation tests | Implemented but unverified | No teacher is ever fitted (L7.2), so nothing is ever distilled | Follows L7.2 |
| L7.6 | Deploy to all regions | Distilled policy reaches every cell | Pieces exist: `DistilledModel`, DNA sealing `qip-kernel/src/central/dna.rs`, `Cell::deploy` | **No channel**: `qip-mesh/src/delta.rs` and the downlink carry capital envelopes and utilisation only; `Cell::deploy` is called only from tests | `central.rs::a_tampered_dna_fails_verification_naming_the_section_that_changed` | Missing | There is no path by which any model or strategy reaches a running cell | Add a sealed-DNA frame to the mesh downlink and a deploy handler in `qip-edge-node` |
| L7.7 | IBM Quantum optimization (offline/nearline) | Quantum-assisted portfolio, N-leg, allocation, scenario search | `backend/crates/libs/qip-quantum/src/provider.rs` (hosted, refuses without TLS proxy); `solver.rs:911` reports unavailable; `SimulatedProvider` attached at `platform.rs:671` when `quantum_enabled` | Flag defaults `false` (`qip-kernel/src/config.rs:260`); the classical baseline always solves via `ComputeRouter` inside `PortfolioConstructor` | `qip-quantum` solver tests; `e2e.rs` QUBO fixture (`:1161`) | Partially implemented | IBM is unreachable from this build; of the four named uses only portfolio optimisation runs, classically | Stand up the TLS egress proxy ADR 0011 names, or state the classical baseline as the answer |
| X.A | Counterfactual digital twin (cross-cutting) | Same capability as L7.1 | Same as L7.1 | Same as L7.1 | Same as L7.1 | Obsolete or duplicated | The diagram draws this box twice (L7.1 and X.A) | Merge X.A into L7.1 in `canonical-platform.md` |
| X.B | Contextual model router | Right model per decision context | `backend/crates/services/qip-cost-router/src/{router,context,reputation}.rs` (`Router::select`, determinism type-gate); metering `platform.rs:1391` `charge_cycle` | **Metering runs, routing does not** — the kernel imports only `{ComputeLedger, CostEngine, DataCostModel, DataReads, IntelligenceTier}` (`platform.rs:62`); `Router::select` has zero callers outside its crate | `qip-cost-router/tests/cost_router.rs`; `platform_outcomes.rs::a_busier_cycle_costs_more_than_a_quiet_one` | Partially implemented | No decision is ever assigned a model tier by context | Route `stage_reason` through `Router::select` with a `Determinism` requirement |
| X.C | Predictive capital fabric | Pre-position cash where opportunity will be | Forecast `platform.rs:2898`; plan `:2918` `pre_position`; planner `backend/crates/services/qip-capital-fabric/src/plan.rs` | Forecasting runs in `stage_decide`; `pre_position` is called only by `platform_outcomes.rs:397` | `platform_outcomes.rs::a_pre_positioning_plan_is_scored_against_what_the_world_needed` | Partially implemented | Plans are never made or executed in a deployment; `transfer.rs` has no executor | Call `pre_position` each cycle and record the plan; executor follows L4.4 |
| X.D | Confidential global intelligence | Share insight without raw data | `backend/crates/libs/qip-confidential/src/{noise,query,release,budget}.rs`; `qip-kernel/src/central/insights.rs` | `Platform::insights_mut` (`platform.rs:817`) is called only by `qip-kernel/tests/insights.rs`; no route or binary calls it | `insights.rs::an_insight_over_too_few_cells_is_refused_and_zero_is_not_substituted`; `::the_same_seed_reproduces_a_release_exactly_and_a_repeat_spends_nothing` | Implemented but unverified | The gate stands with nobody walking through it | Serve one released statistic on the console's cell view |
| X.E | Quantum-centric learning fabric | Quantum + HPC + AI on hard problems | `qip-quantum` + `backend/crates/services/qip-optimization-engine/src/router.rs` | Simulated provider only, behind a default-off flag; nothing connects quantum output to strategy discovery | `qip-quantum` solver tests | Partially implemented | The "learning fabric" link does not exist — evolution never asks the solver anything | Let the foundry pose one N-leg search as a QUBO |
| G.1 | Global risk policies | One limit set, stated with rationale | `qip-risk/src/limits.rs:330`; concentration limits in `qip-kernel/src/central/plane.rs` | Attached to the Platform and to the desk; enforced each cycle | `acceptance.rs::the_risk_limits_the_platform_runs_under_all_state_a_rationale`; `central.rs::three_cells_holding_one_name_produce_a_concentration_recall` | Complete and verified | — | — |
| G.2 | Regulatory controls | Controls a regulator could inspect | `backend/crates/libs/qip-compliance/src/{incident.rs,model_risk.rs}`; `plane.rs:487` `compliance_report` | `Platform::compliance_report` (`platform.rs:832`) has **no caller in any binary or route** | `compliance_proof.rs::the_report_covers_all_six_controls_and_names_a_real_mechanism_for_each`; `::the_report_round_trips_through_json_so_it_can_be_filed_as_evidence` | Partially implemented | The report is produced only in tests; no operator can obtain it | Add a `GET /compliance` route behind the Approver role |
| G.3 | Audit trail (immutable) | Hash-chained, restart-surviving record | `backend/crates/libs/qip-events/src/log.rs` `verify_chain`; journal restore `platform.rs:1450`, `qip-deepbrain/src/node.rs:162` | Written every cycle; chain continues across restart | `acceptance.rs::the_event_log_hash_chain_survives_a_full_run`; `kernel.rs::a_second_platform_over_the_same_file_continues_the_chain_instead_of_starting_a_new_one` | Complete and verified | — | — |
| G.4 | Kill switch | One control that stops everything | `qip-api/src/routes.rs:499,517`; enforcement `platform.rs:2154` | Trips from the API, from the risk monitor and from a cell reconciliation break | `kernel.rs::a_tripped_kill_switch_stops_the_assembled_platform`; `qip-api/tests/mesh.rs::a_reconciliation_break_crossing_the_wire_halts_that_cell_and_only_that_cell` | Complete and verified | — | — |
| G.5 | Model governance | Gated promotion with evidence | Ladder `backend/crates/libs/qip-contracts/src/gate.rs`; factory `qip-kernel/src/central/factory.rs`; registry `backend/crates/libs/qip-ai/src/registry.rs:186` | Ladder and factory run (evolution registers on the bottom rung); the model registry is test-only, `/models` answers `NO_MODEL_REGISTRY` | `central.rs::a_demotion_revokes_nothing_retroactively_but_the_next_issuance_refuses`; `models.rs::a_stricter_policy_refuses_what_the_default_admits` | Partially implemented | Strategy governance is live; model governance is not attached to anything running | Attach `ModelRegistry` to the Platform once L7.2 produces a model |
| G.6 | Data lineage | Every number traces to its source | `Lineage`/`CorrelationId` threaded from `platform.rs:1322` through every capture; catalog lineage `backend/crates/services/qip-mesh/src/catalog.rs` | One correlation id per cycle, journaled | `platform_services.rs::one_correlation_id_reconstructs_a_cycle_from_the_journal`; `qip-mesh/tests/catalog.rs::lineage_resolves_to_the_roots_a_dataset_was_built_from` | Complete and verified | — | — |
| G.7 | Compliance rules | Licensing and usage refusals that bind | `qip-mesh/src/catalog.rs` entitlements; refusal path through the truth loop | Enforced at the catalogue before a dataset can be spent on a trade | `truth_loop.rs::a_dataset_licensed_for_research_cannot_be_spent_on_a_trade`; `compliance_proof.rs::a_dataset_the_catalogue_licenses_for_research_is_refused_a_trade_by_the_named_mechanism` | Complete and verified | — | — |
| G.8 | Stress testing | Scenarios run against the live configuration | `backend/crates/services/qip-simulation-engine/src/scenario.rs`; gates consume scenario evidence `qip-lifecycle/src/gates.rs`; suites `stress.rs` (16), `chaos.rs` | Runs in CI, not in the platform; `Platform::learn_from_cells` (`platform.rs:865`) has **zero callers anywhere, tests included** | `stress.rs::slippage_ten_times_wider_turns_a_profitable_path_into_a_recorded_refusal`; `chaos.rs::randomly_injected_faults_never_break_the_five_invariants` | Partially implemented | No scheduled stress run against the deployed book; the cell-learning edge is dead code | Run the scenario library from deepbrain and feed the result to `learn_from_cells` |
| N.1 | Ultra-low latency | Bounded wire-to-order latency | Per-operation ceilings `backend/crates/tests/qip-acceptance/tests/performance.rs:94-108`; 50 ms fast-path budget `qip-fastbrain/src/roster.rs:24` | Ceilings asserted in CI | `performance.rs::order_construction_costs_what_the_budget_says`; `::the_budgets_document_says_what_is_measured_and_what_is_not` | Partially implemented | The suite states it plainly: wire-to-wire and tick-to-order are not measured | Add one end-to-end latency measurement over the loopback path |
| N.2 | Massive scalability | Horizontal scale under load | HPA for the API `infrastructure/kubernetes/base/api.yaml`; wide-universe cycle test | One `Platform` per node; HPA on the API only | `resilience.rs::a_wide_universe_completes_a_cycle_with_every_stage_intact`; `stress.rs::a_very_high_synthetic_event_rate_is_absorbed_without_losing_an_event` | Partially implemented | No load evidence beyond the suites; brains do not scale horizontally | Define and measure one scale target before claiming this |
| N.3 | High availability | Survives node and peer loss | PDB + HPA (`base/api.yaml`), edge-cell StatefulSet ×2, partition tolerance in `qip-edge-node/src/mesh.rs` | Cells keep trading inside their envelope through a partition | `stress.rs::a_region_that_goes_completely_offline_is_halted_by_scope_and_the_others_keep_trading` | Partially implemented | Single brain replica; no leader election | Decide whether the brains are singletons by design and write it down |
| N.4 | Disaster recovery | Restore from durable state | Journal + restore-on-start `qip-deepbrain/src/node.rs:162,319`; `infrastructure/kubernetes/base/journal-storage.yaml`; `docs/operations/disaster-recovery.md` | Deepbrain restores its chain on start | `kernel.rs::a_second_platform_over_the_same_file_continues_the_chain_instead_of_starting_a_new_one`; `infrastructure.rs::the_evidence_bucket_is_versioned_and_retained` | Partially implemented | No cross-region restore; never rehearsed against a real cluster | Rehearse the runbook once and record the result |
| N.5 | Multi-region active | Seven regions live at once | `infrastructure/terraform/modules/edge-cell/`; `edge_cells` map in `infrastructure/environments/*/terraform.tfvars` | Only `london-1` is enabled in test/stage/prod; six regions are commented out; the kernel region is the constant `HOME_REGION = "home"` (`platform.rs:306`) | `infrastructure.rs::the_edge_cells_are_one_module_rather_than_seven_copies`; `::a_cell_gets_its_own_subnet_its_own_identity_and_its_own_binding` | Partially implemented | The module generalises to seven; the configuration enables one, and the brain has no region concept | Make the kernel region configurable, then enable a second cell |
| N.6 | Observability | Metrics and traces from the running loop | `backend/crates/libs/qip-observability/`; `/health` and `/metrics` at `qip-api/src/routes.rs:405,426` | **The kernel writes nothing into telemetry** — no `.counter(`, `.gauge(` or `.observe(` call on `Telemetry` exists in `qip-kernel/src` or any app | `qip-observability` unit tests | Partially implemented | `/metrics` serves a surface with no instrumentation behind it; `missing.rs` is the real status page | Instrument the eight stages: duration, records absorbed, refusals by gate |
| N.7 | Cost efficiency | Every cycle priced, spend monotone | `platform.rs:1391` `charge_cycle`, `:2971` `compute_spend`, `:2993` `cost_deductions` | Charged every cycle in every binary | `platform_outcomes.rs::a_cycle_charges_what_it_consumed_and_the_total_only_grows`; `::a_busier_cycle_costs_more_than_a_quiet_one` | Complete and verified | — | — |
| T.1 | Google Cloud (multi-region) | Multi-region GCP footprint | `infrastructure/terraform/` (15 modules), `infrastructure/environments/{dev,test,stage,prod}/` | One cluster region per environment plus one cell region | `infrastructure.rs` (61 passing) | Partially implemented | Multi-region is configured for, not configured | Follows N.5 |
| T.2 | Pub/Sub | Managed streaming backbone | Replaced by `qip-transport` + `qip-mesh`; refusing residual `qip-streaming/src/pubsub.rs` | The mesh runs in `qip-api`, `qip-edge-node` and `qip demo --live` | `qip-api/tests/mesh.rs` (5 passing) | Implemented differently | ADR 0011 | Delete the dead Pub/Sub port |
| T.3 | Spanner | Managed relational store | No adapter; `enable_spanner = false`; `qip-storage/src/provider.rs` names Spanner as a refusing provider kind | None | `qip-mesh/tests/managed_providers.rs::an_unavailable_managed_adapter_errors_at_first_use_on_every_port` | Missing | Deliberate: the embedded engine + WAL is the ADR 0011 substitute | None — remove T.3 from the canonical list or mark it as superseded |
| T.4 | Vertex AI | Managed training | `infrastructure/terraform/modules/ai/main.tf` (gated on `enable_vertex_ai`, false); port `qip-training/src/vertex.rs` refuses without a TLS proxy | Neither Vertex nor the in-tree substitute runs | `qip-training` tests; `infrastructure.rs` module scans | Partially implemented | ADR 0011's replacement exists but nothing drives it (L7.2) | Follows L7.2 |
| T.5 | BigQuery | Managed analytics | `backend/crates/libs/qip-storage/src/gcp/bigquery.rs` (real REST adapter that refuses without TLS egress); `enable_bigquery = false` | Journal + `qip-mesh` local lakehouse serve instead | `qip-mesh/tests/local_adapters.rs::the_lakehouse_time_travels_to_any_committed_version` | Partially implemented | Deliberately off pending the TLS egress proxy | Stand up the proxy or record the substitution as final |
| T.6 | Dataflow | Managed stream processing | Replaced by `backend/crates/services/qip-streaming/` (envelopes, sequencing, processing, durable journal) | Journal publish runs every cycle (`platform.rs:1450`) | `platform_services.rs::every_cycle_reaches_the_durable_log_and_comes_back_unchanged` | Implemented differently | ADR 0011 | None |
| T.7 | GKE | Cluster the platform runs on | `infrastructure/terraform/modules/cluster/`, `infrastructure/kubernetes/base/` | No evidence in-tree of a live cluster; the last commits delete and rebuild one | `infrastructure.rs::every_deployable_binary_in_the_workspace_has_a_deployment`; `::no_container_in_any_workload_runs_as_root_or_can_escalate` | Implemented but unverified | Manifests and Terraform are test-verified; a running cluster is not demonstrated here | Record one successful apply and a pod listing |
| T.8 | Cloud Storage | Managed object store | `qip-storage/src/gcp/storage.rs` refuses without TLS egress; `enable_cloud_storage = false` | Local evidence store serves instead | `qip-mesh/tests/evidence_store.rs::a_second_write_of_different_bytes_is_refused_and_names_the_conflict` | Partially implemented | Deliberately off pending the TLS egress proxy | Same as T.5 |
| T.9 | Security Center | Org-level threat findings | `infrastructure/terraform/modules/scc/` + its own `ORGANISATION-SCOPED.md`; `enable_security_command_center = false` | Project-scoped only; no `org_id` exists in this configuration | `infrastructure.rs` module scans | Partially implemented | Cannot be activated without organisation-level access nobody has | Escalate for org activation or drop T.9 |
| T.10 | Confidential VMs | Confidential compute nodes | `infrastructure/terraform/modules/cluster/main.tf:264` dynamic `confidential_nodes`; `enable_confidential_nodes = false` | Off; would force node-pool replacement | `infrastructure.rs::the_node_pool_does_not_use_the_default_service_account` | Partially implemented | Declared, never enabled; note `qip-confidential` is disclosure control, not confidential computing | Enable in one environment and measure the cost |
| F.1 | L1 → BB | Normalized events onto the backbone | `platform.rs:1450` `journal_cycle` publishes stream envelopes each cycle | Runs in every binary | `platform_services.rs::every_cycle_reaches_the_durable_log_and_comes_back_unchanged`; `::the_journal_is_replayable_as_of_an_instant` | Complete and verified | — | — |
| F.2 | BB → L2 | Each region consumes from the backbone | No fan-out exists: `Platform::observe` reads its adapter directly (`qip-fastbrain/src/node.rs:107`); the journal is written, not subscribed to | Journal is read only on restart | `platform_services.rs::one_correlation_id_reconstructs_a_cycle_from_the_journal` | Partially implemented | The backbone is downstream of L1, not between L1 and L2 — nothing consumes it live | Add a journal subscriber, or redraw the arrow |
| F.3 | L2 → L3 | Regional state deltas travel up | `qip-edge-node/src/mesh.rs` uplink → `qip-api/src/mesh.rs` drain → `platform.rs:845` `ingest_cell_report` | Runs when `QIP_MESH_CELLS` is set | `qip-api/tests/mesh.rs::a_redelivery_the_inbox_has_forgotten_is_still_one_ingestion_at_the_drain` | Partially implemented | Runs only when `QIP_MESH_CELLS` is set, which no manifest does | Same as BB.1 |
| F.4 | L3 → L4 | Ranked opportunities into capital allocation | `stage_discover:1583` → `stage_reason:1639` → `pending_theses` → `stage_decide:2066` | Runs every cycle | `platform.rs::an_approved_thesis_becomes_a_draft_proposal_with_legs_and_is_drained` | Complete and verified | — | — |
| F.5 | L4 → L5 | Actions down to regional execution | Capital envelopes dispatch down the mesh (`qip-mesh/src/spine.rs`, `qip-api/src/mesh.rs`) | Envelopes travel; no proposal, order or strategy does | `qip-api/tests/mesh.rs::a_dispatched_envelope_survives_the_spool_and_verifies_at_a_real_downlink` | Partially implemented | Only capital flows down, and only when the mesh is configured (it is not, in any manifest); the action itself has no channel at all | Follows BB.1, L4.8 and L7.6 |
| F.6 | L5 → L6 | Execution outcomes captured | `platform.rs:2372` `capture_submission` — fill and refusal on one hash chain | No deployed producer of fills | `platform_outcomes.rs::the_capture_chain_verifies_across_every_kind_of_record` | Partially implemented | The capture side is complete; the producing side is not | Follows L4.8 |
| F.7 | L6 → L7 | Outcomes feed learning | `stage_learn:2206` → `attribute:2231`; `learn_from_cells:865` | Attribution runs on degenerate periods; `learn_from_cells` has zero callers | `platform_outcomes.rs::the_learn_stage_reports_what_was_captured` | Partially implemented | Evolution scores against observed bars, not against realised outcomes | Feed realised attribution into the foundry's challenger |
| F.8 | L7 → L2 | Distilled policy deployed to all regions | None — the downlink carries capital only | None | None | Missing | Same absence as L7.6 | Same as L7.6 |
| F.9 | X.* ↔ all layers | Cross-cutting fabrics touch every layer | B metering `platform.rs:1391`, C forecasting `:2898`, D `:817`, A `:2809`, E `:671` | Only B's metering and C's forecasting run in the loop | `platform_outcomes.rs::the_decide_stage_reports_where_capital_will_be_needed` | Partially implemented | Three of five fabrics are fields nothing calls | Follows X.A–X.E |
| F.10 | G.* ↔ all layers | Governance overlays every layer | Kill switch, autonomy, pre-trade gates, hash-chained audit and lineage thread through every stage | Enforced at every entry point in every binary | `acceptance.rs::a_halt_stops_the_assembled_platform_at_every_entry_point`; `::every_number_the_organisation_produces_carries_a_provenance` | Complete and verified | — | — |

## Coverage summary

**The brief for this reconciliation said 79 ids; `canonical-platform.md` actually declares 104**
(L1 ×13, BB ×1, L2 ×9, L3 ×9, L4 ×10, L5 ×7, L6 ×8, L7 ×7, X ×5, G ×8, N ×7, T ×10, F ×10).
Every one of the 104 is scored above. The percentage below uses the real denominator; over the
briefed 79 it would read higher for no reason other than a smaller divisor.

| Status | Count |
|---|---|
| Complete and verified | 18 |
| Implemented differently | 3 |
| Implemented but unverified | 12 |
| Partially implemented | 65 |
| Stub or mock only | 0 |
| Missing | 5 |
| Obsolete or duplicated | 1 |
| **Total** | **104** |

**Diagram coverage = (18 + 3) / 104 = 20.2%.**

Read that number with its shape in mind: only five ids have no implementation at all. The dominant
failure is not absence — it is *reachability*. Twelve well-tested subsystems have zero callers in any
binary, and most of the 65 partial rows are partial because the code exists and nothing drives it.
The single highest-leverage change is not new code; it is closing the release path from a sized
proposal to a simulated order, which converts nine rows at once.

**No row reads "Stub or mock only".** That is not generosity: the workspace contains no `todo!()`,
no `unimplemented!()`, and no placeholder function bodies. What it contains instead are honest
refusing ports (`qip-storage/src/gcp/*`, `qip-training/src/vertex.rs`, `qip-quantum/src/provider.rs`)
and complete subsystems nobody calls. Those are scored as *Partially implemented* and *Implemented
but unverified* respectively, which is the harsher reading, not the softer one.

**Where the prior audits are wrong.** `diagram-gap-audit.md` was written before five commits that
landed the same day (`81f0428`, `117b815`, `c6fd78a`, `4ffa9c0`, `a6f991a`). Its top five ranked
findings are stale: `stage_decide` is no longer hardwired to `nothing_to_do`; the world model *is*
written by `observe`; the mesh centre *does* run in `qip-api`; the evolution brain *does* turn in
`qip-deepbrain`; and `risk_state()` reads the tracked book rather than a constant. Hedging and
repricing now exist in code (unwired). `current-state-audit.md` and `repo-inventory.md` are older still.

## Implemented but not in the diagram

| Capability | Where | Verdict | Reasoning |
|---|---|---|---|
| Server-rendered operator console — 9 investment surfaces + 9 console views, no JavaScript | `backend/crates/apps/qip-web/`, served from `qip-api/src/console.rs` | Merge into canonical | The diagram has no operator surface at all; this is how a human sees every layer, and `Panel::absent` is what keeps "no data" from reading as "zero" |
| Honest-absence catalogue — 12 named refusals served instead of empty results | `backend/crates/apps/qip-api/src/missing.rs` | Retain | It is the platform's real status page and the reason this reconciliation could be written at all |
| Falsifiable predictions — every hypothesis becomes a claim scored against what a source published | `platform.rs:2725,2737`; `backend/crates/services/qip-prediction/` | Merge into canonical | This is the platform's only self-correction mechanism and belongs beside L7 |
| Governed agent organisation — capability grants, budgets, owners, expiry, audit per agent | `backend/crates/libs/qip-agents/`, `backend/crates/agents/qip-investment-agents/` | Merge into canonical | The diagram says "AI brains"; the tree implements a governed org with hard capability separation. It is the substance behind L2/L3 |
| Architecture rules enforced as tests — 18 dependency and composition invariants | `backend/crates/tests/qip-acceptance/tests/architecture.rs` | Retain | Keeps the layering the diagram draws from eroding; not a diagram box |
| Autonomy ladder + paper-trading ceiling | `AutonomyController`, `qip-edge-node/src/venue.rs:180` | Merge into canonical | The standing deviation depends on it; it should be a named governance box, not a footnote |
| Entity resolution and feature DAG | `backend/crates/services/qip-entity-resolution/`, `backend/crates/edge/qip-feature-dag/` | Merge into canonical | Both are real L1.11 enrichment mechanisms the diagram lumps into one word |
| Native venue protocol decoders (FIX / ITCH / SBE / JSON) | `backend/crates/edge/qip-protocols/` | Retain | Implementation detail beneath L1.2 |
| `qip demo --live` bounded loopback | `backend/crates/apps/qip-cli/src/demo/` | Retain | A demonstration harness, not a platform capability |
| Prediction/event-market instruments | `backend/crates/services/qip-prediction/` | Remove or justify | No diagram id claims this asset class and no deployment trades it; carrying it costs maintenance for an untaken bet |

## Dependency-ordered backlog

Each item is independently assignable and unblocked by the ones above it. Vertical slices first.

1. **Release an approved proposal into the simulator.** (L4.8, L6.1, L6.2, F.5, F.6) — **M** — `backend/crates/runtime/qip-kernel/src/platform.rs` (`stage_act`, `order_from`, `submit_order`). Turn releasable proposal legs into orders through the existing gate chain. Everything below it starves without this. Add a test that a released proposal produces a captured fill *and* one that a halted platform releases nothing.
2. **Configure the mesh in the shipped manifests.** (BB.1, L2.9, F.3, F.5) — **S** — `infrastructure/kubernetes/base/{api,edge-cell}.yaml`, `backend/crates/apps/qip-api/src/mesh.rs:88`. `QIP_MESH_CELLS` and `QIP_MESH_PEER` appear in no manifest, so the whole backbone — drained deltas, dispatched capital, cell-scoped kill switch — is dead in every deployment despite being complete and tested in-process. Cheapest large win in this list.
3. **Populate `RiskState::expected_shortfall`.** (L4.6, L6.8) — **S** — `platform.rs:2306` `risk_state()`, `backend/crates/libs/qip-risk/src/metrics.rs`. Compute the return series from the tracked book and fill the map, so `MaxExpectedShortfall` can fire. Independent of item 1 but doubles in value after it.
4. **Instrument the eight stages.** (N.6) — **S** — `backend/crates/runtime/qip-kernel/src/platform.rs`, `backend/crates/libs/qip-observability/`. Stage duration, records absorbed, refusals by gate, opportunities live. `/metrics` currently serves nothing real.
5. **Attribute position periods rather than single fills.** (L6.6, L6.5, F.7) — **M** — `platform.rs:2231` `attribute`, `backend/crates/services/qip-learning-engine/src/attribution.rs`. Build `PositionPeriod` from the lots in `CapitalBook`, with real spread, impact and realised P&L. Requires item 1 to have fills.
6. **A sealed-DNA frame on the mesh downlink, and a deploy handler in the cell.** (L7.6, F.8, L2.4, L2.8, L6.3) — **L** — `backend/crates/services/qip-mesh/src/delta.rs`, `backend/crates/apps/qip-api/src/mesh.rs`, `backend/crates/apps/qip-edge-node/src/{mesh.rs,main.rs}`, `backend/crates/edge/qip-edge/src/cell.rs`. The only path by which any strategy reaches a running cell; unblocks the whole dormant edge half.
7. **Wire the router and repricer into the cell gateway.** (L5.1, L5.2, L5.3, L5.4) — **M** — `backend/crates/apps/qip-edge-node/src/gateway.rs`, `backend/crates/edge/qip-routing/src/{router.rs,reprice.rs}`. Two fully tested crates with zero callers; needs item 5 so there is order flow to route.
8. **Call `HedgeEngine` from the decide stage.** (L4.7, L5.5) — **M** — `backend/crates/libs/qip-risk/src/hedge.rs`, `platform.rs` `stage_decide`. Proposals only, never orders; the hedge leg joins the same proposal item 1 releases.
9. **Put the normalizer in the ingest path.** (L1.11, L1.5, L1.7) — **S** — `backend/crates/apps/qip-fastbrain/src/feed.rs`, `backend/crates/services/qip-normalization/`. Also add the two missing absorption tests (macro, alternative data).
10. **Drive source discovery from deepbrain's cadence.** (L1.9, L1.12) — **M** — `backend/crates/apps/qip-deepbrain/src/{main.rs,node.rs}`, `platform.rs:2614`, `backend/crates/apps/qip-api/src/routes.rs:449`. Mirror the evolution round; retire `NO_DATA_FINDER`.
11. **Add a training round beside the evolution round.** (L7.2, L7.5, G.5) — **M** — `backend/crates/apps/qip-deepbrain/src/evolution.rs`, `backend/crates/services/qip-training/`, `qip-kernel/src/central/models.rs`. Fit a teacher, distil a student, register it. Feeds item 5 with something worth deploying.
12. **Route decisions through the contextual model router.** (X.B) — **M** — `backend/crates/services/qip-cost-router/src/router.rs`, `platform.rs:62` imports and `stage_reason`. Metering already runs; add `Router::select` with the `Determinism` gate.
13. **Feed usable depth into sizing.** (L2.3, L3.7) — **S** — `platform.rs` `construct_from`, `backend/crates/services/qip-world-model/src/liquidity.rs`. The topology is written every cycle and read by nobody.
14. **Capture expiring opportunities as priced misses.** (L6.4) — **S** — `platform.rs:1583` `stage_discover`, `backend/crates/services/qip-twin/src/capture.rs`. One call site; makes the refusal ledger complete.
15. **Snapshot exposure per cycle.** (L6.7) — **S** — `backend/crates/libs/qip-capital/`, `platform.rs:1450` `journal_cycle`. A time series where there is currently only a point.
16. **Serve `/compliance` and one released confidential statistic.** (G.2, X.D) — **S** — `backend/crates/apps/qip-api/src/{routes.rs,console.rs}`, `platform.rs:832,817`. Two accessors with no caller; both are one route each.
17. **Make the kernel region configurable and enable a second cell.** (N.5, T.1, L2 ×7) — **M** — `platform.rs:306` `HOME_REGION`, `infrastructure/environments/*/terraform.tfvars`. Until this lands, "seven regions" is one region named `home`.
18. **Run the scenario library against the deployed configuration and feed `learn_from_cells`.** (G.8, F.7) — **M** — `backend/crates/apps/qip-deepbrain/src/`, `backend/crates/services/qip-simulation-engine/src/scenario.rs`, `platform.rs:865`. `learn_from_cells` has zero callers in the entire tree, tests included.
19. **Cross-source event fingerprinting.** (L1.13) — **M** — `backend/crates/libs/qip-events/src/registry.rs`, `backend/crates/services/qip-normalization/`. An identity key over (subject, class, knowable instant) so one real-world event is one event.
20. **Decide the TLS egress proxy question.** (T.4, T.5, T.8, L7.7, X.E) — **L** — `backend/crates/libs/qip-transport/src/http.rs`, `infrastructure/terraform/modules/`, six `enable_*` flags. Either provision the proxy or record the in-tree substitutions as final and delete the refusing adapters. Five ids are blocked on one infrastructure decision.
21. **Remove or build the empty boxes.** (L1.6 NFTs/wallets, L1.2 Level 3, L5.6 inventory, X.A duplication, T.3) — **S** — `docs/architecture/canonical-platform.md` plus, if kept, `backend/crates/services/qip-chain/` and `backend/crates/edge/qip-edge/`. A diagram box with no code and no intent should stop being scored.
