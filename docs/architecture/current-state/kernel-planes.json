[
  {
    "name": "qip-kernel (crate root)",
    "path": "backend/crates/runtime/qip-kernel/src/lib.rs",
    "kind": "runtime",
    "purpose": "Declares every module in the composition root and re-exports the small public surface (Platform, PlatformConfig, CentralPlane, CycleReport/Stage/StageOutcome, asset-class and valuation types) that the four deployable binaries actually import.",
    "key_contracts": [
      "pub mod {adaptive_cadence,adversary_review,asset_class_registry,blueprint_objectives,capacity_review,causal_review,central,config,counterfactual_trial,cross_margin,cycle,exploration,fabric_declaration,falsification,family_review,feature_statistics,hedge_review,model_serving,platform,pre_positioning,precedent_declines,quote_loop,references,regime_allocation,regime_transition,rule_review,session_replay,shared_cause,sizing_review,valuation,venue_admission,venue_measurement,venue_review}",
      "pub use asset_class_registry::{AssetClassRecord, AssetClassRegistry, ValuationEngine}",
      "pub use central::{CellReport, CentralPlane, StrategyDna, StrategyFactory}",
      "pub use config::{EventLogDestination, PlatformConfig}",
      "pub use cycle::{CycleReport, Stage, StageOutcome}",
      "pub use model_serving::{DistilledManifestEntry, ModelManifestIssue, ModelPromotion}",
      "pub use platform::{CapitalGrantEntry, ChainAbsorption, CycleJournalEntry, Platform, RecordedPrediction, SourceAssessment, UniverseAssembled}",
      "pub use valuation::CreditRegister"
    ],
    "workspace_deps": ["all 41 workspace crates listed in Cargo.toml — see Cargo.toml [dependencies]"],
    "reached_from_production": "yes",
    "production_evidence": "backend/crates/apps/qip-api/src/main.rs:30, qip-cli/src/main.rs:32, qip-deepbrain/src/main.rs:51, qip-fastbrain/src/main.rs:42 all `use qip_kernel::{Platform, PlatformConfig}`.",
    "io_and_state": "None itself; pure module declaration and re-export.",
    "tests": {"count": 0, "notable": []},
    "status": ["exists", "hardened"],
    "status_reason": "Stable composition-root surface consumed by all four deployable binaries.",
    "blueprint_capabilities": ["ARCH"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/lib.rs:36-68", "backend/crates/apps/qip-api/src/main.rs:30"]
  },
  {
    "name": "config",
    "path": "backend/crates/runtime/qip-kernel/src/config.rs",
    "kind": "runtime",
    "purpose": "Everything that varies between a test, a backtest and a deployment: autonomy ceiling (defaults to paper trading), event-log destination and durability, user mandates/eligibility, routing/monitor/hedge/review policy. PlatformConfig::default() is structurally incapable of live trading.",
    "key_contracts": [
      "pub enum EventLogDestination { InMemory, File { path, durability } }",
      "pub struct UserMandate", "pub struct UserEligibility",
      "pub struct PlatformConfig { autonomy_ceiling, event_log, hedge_policy, .. }"
    ],
    "workspace_deps": ["qip_capital", "qip_core", "qip_data_finder", "qip_events", "qip_optimization_engine", "qip_portfolio_engine", "qip_reasoning_engine", "qip_risk_engine"],
    "reached_from_production": "yes",
    "production_evidence": "Every app builds a PlatformConfig before calling Platform::new/run_cycle; qip-api/src/main.rs:30, qip-cli/src/main.rs:32, qip-deepbrain/src/main.rs:51, qip-fastbrain/src/main.rs:42.",
    "io_and_state": "Declares the EventLogDestination::File{path,durability} that Platform assembly (platform.rs) uses to open qip_events::log::EventLog on disk (JSONL, appended, read back at assembly to continue the hash chain). config.rs itself performs no I/O.",
    "tests": {"count": 8, "notable": ["a_platform_configured_by_nobody_writes_its_event_log_nowhere", "an_os_buffered_file_survives_the_process_and_does_not_survive_the_machine", "a_stored_configuration_without_an_initial_equity_deserialises_to_the_old_ten_million"]},
    "status": ["exists", "hardened"],
    "status_reason": "Fail-closed default (paper ceiling, in-memory log) is tested; durability defaulting and back-compat deserialisation are tested.",
    "blueprint_capabilities": ["ARCH", "SEC", "GOV", "LEDGER"],
    "gaps_observed": ["config.rs:273,316,480 note the $10M initial equity was a hardcoded constant before this field existed — now a configurable default with back-compat deserialisation, not a live gap."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/config.rs:1-60", "backend/crates/runtime/qip-kernel/src/config.rs:223"]
  },
  {
    "name": "adaptive_cadence",
    "path": "backend/crates/runtime/qip-kernel/src/adaptive_cadence.rs",
    "kind": "runtime",
    "purpose": "Blueprint §23.6 adaptive cadence: decides which periodic work (tiering, reinvestment) actually needs to run this cycle versus 'nothing changed, do not run,' while recomputing the regime-narrowing control every cycle regardless because a control must never be skipped for a saving.",
    "key_contracts": [
      "pub const TIERING_CADENCE_CYCLES: u64 = 12", "pub const REINVESTMENT_CADENCE_CYCLES: u64 = 12",
      "pub enum Work", "pub struct CadenceSignals", "pub struct CadencePlan",
      "pub fn plan(signals, policy) -> CadencePlan", "pub fn review(platform) -> (Option<String>, Vec<String>)"
    ],
    "workspace_deps": ["qip_capital", "qip_core", "qip_optimization_engine"],
    "reached_from_production": "yes",
    "production_evidence": "crate::adaptive_cadence::review(self, now) called at platform.rs:14126 inside the LEARN-stage review sequence, reached via Platform::run_cycle from all four apps.",
    "io_and_state": "None; pure arithmetic over Platform state and caller-supplied Timestamp.",
    "tests": {"count": 3, "notable": ["a_cycle_where_nothing_has_a_subject_runs_no_work_at_all", "work_with_a_subject_still_waits_for_its_cadence", "a_book_with_profit_and_no_strategies_runs_only_the_item_that_has_a_subject"]},
    "status": ["exists", "hardened"],
    "status_reason": "Small, tested, wired into the LEARN stage every cycle.",
    "blueprint_capabilities": ["CAPITAL", "REFLEX"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/adaptive_cadence.rs:82-288", "backend/crates/runtime/qip-kernel/src/platform.rs:14126"]
  },
  {
    "name": "adversary_review",
    "path": "backend/crates/runtime/qip-kernel/src/adversary_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §15.2 adversary monitor: measures whether a venue's fill quality relative to the twin's modelled cost is deteriorating or adapting. Explicitly answers only one of the section's five questions; the module doc names the other four as unbuilt rather than approximating them.",
    "key_contracts": [
      "pub enum AdversaryPosture", "pub struct VenueAdversaryProfile",
      "pub fn assess(scores) -> BTreeMap<String, VenueAdversaryProfile>",
      "pub fn slot(scores, now) -> Slot<AdversaryProfiles>",
      "pub struct AdversaryPostureChanged", "pub fn review(platform, now) -> (Option<String>, Vec<String>)"
    ],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_events", "qip_financial", "qip_streaming"],
    "reached_from_production": "yes",
    "production_evidence": "crate::adversary_review::review(self, now) at platform.rs:14107 (LEARN stage); also directly imported by qip-api/src/mesh.rs:997 (qip_kernel::adversary_review::slot(platform.fill_scores(), now)) for a console/API surface.",
    "io_and_state": "None; reads FillScore history the platform already holds.",
    "tests": {"count": 14, "notable": ["a_venue_filling_materially_worse_than_modelled_reads_deteriorating_and_not_adapting", "a_non_finite_twin_error_is_dropped_rather_than_carried_into_the_slot", "the_window_keeps_only_the_most_recent_scores_so_the_working_set_is_bounded"]},
    "status": ["exists", "hardened"],
    "status_reason": "Deliberately partial implementation of §15.2, tested and reached from both the LEARN stage and a direct API call site.",
    "blueprint_capabilities": ["EXEC", "RISK"],
    "gaps_observed": ["Module doc (lines 39-46) states 4 of 5 blueprint §15.2 questions and all 4 named responses are unbuilt by design, not by omission."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/adversary_review.rs:174-590", "backend/crates/runtime/qip-kernel/src/platform.rs:14107", "backend/crates/apps/qip-api/src/mesh.rs:997"]
  },
  {
    "name": "asset_class_registry",
    "path": "backend/crates/runtime/qip-kernel/src/asset_class_registry.rs",
    "kind": "runtime",
    "purpose": "Blueprint §17.7 asset-class registry: one record per class naming its valuation engine, settlement convention, trading calendar and eligible strategy families — the gate between architecturally reachable and actually supported. Platform::new refuses to assemble over a universe holding an instrument in an unregistered class.",
    "key_contracts": [
      "pub enum ValuationEngine", "pub enum ClassSettlement", "pub enum GridRule",
      "pub enum TradingCalendar", "pub enum TaxTreatment",
      "pub struct AssetClassRecord", "pub struct AssetClassRegistry"
    ],
    "workspace_deps": ["qip_capital", "qip_capital_fabric", "qip_core", "qip_financial", "qip_optimization_engine"],
    "reached_from_production": "yes",
    "production_evidence": "crate::asset_class_registry::AssetClassRegistry::shipped() called at platform.rs:4091 during Platform assembly; re-exported from lib.rs and used as a refusal gate in assembly.",
    "io_and_state": "None; in-memory registry built from a shipped constant table.",
    "tests": {"count": 3, "notable": ["the_shipped_registry_holds_nine_classes_and_names_the_four_it_refuses", "a_record_naming_no_eligible_family_is_refused_and_one_with_no_engine_cannot_be_written", "every_hedge_class_the_shipped_table_names_is_itself_registered"]},
    "status": ["exists", "hardened"],
    "status_reason": "Assembly-time refusal gate, tested, called on every Platform::new.",
    "blueprint_capabilities": ["ASSET"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/asset_class_registry.rs:82-365", "backend/crates/runtime/qip-kernel/src/platform.rs:4091"]
  },
  {
    "name": "blueprint_objectives",
    "path": "backend/crates/runtime/qip-kernel/src/blueprint_objectives.rs",
    "kind": "runtime",
    "purpose": "Evaluates qip_observability::slo::blueprint_slos (§49.1's 15 SLO rows) against what this process actually observed, rather than leaving them declared but never read — explicitly built to avoid the MaxExpectedShortfall failure mode of a reader that reports every objective met because nothing fed it.",
    "key_contracts": [
      "pub const RECONCILIATION_BREAKS_ZERO/NETTING_RATIO/FED_OBJECTIVES",
      "pub enum Unmeasured", "pub fn unmeasured(name) -> Option<(Unmeasured, &str)>",
      "pub enum ObjectiveStanding", "pub struct ObjectiveReview", "pub struct ObjectiveLedger",
      "pub fn assess(ledger, now) -> ObjectiveReview", "pub fn review(..)", "pub fn fold(outcome, ledger, now) -> StageOutcome",
      "pub fn netting_ratio_of(orders) -> Option<f64>"
    ],
    "workspace_deps": ["qip_core", "qip_mesh", "qip_observability"],
    "reached_from_production": "yes",
    "production_evidence": "crate::blueprint_objectives::{netting_ratio_of, fold, assess} called at platform.rs:5500,5539,5552,5563,5567,8195,14259 across multiple stages; ObjectiveLedger constructed at platform.rs:4474 during assembly.",
    "io_and_state": "None; reads Metrics/Telemetry snapshots and order reports already held by Platform.",
    "tests": {"count": 0, "notable": ["10 tests in tests/blueprint_objectives.rs exercise this module externally (0 internal #[test])"]},
    "status": ["exists", "hardened"],
    "status_reason": "Heavily used across the cycle (12 call sites in platform.rs); tested via a dedicated integration suite rather than inline unit tests.",
    "blueprint_capabilities": ["OBS", "GOV"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/blueprint_objectives.rs:61-449", "backend/crates/runtime/qip-kernel/src/platform.rs:5500", "backend/crates/runtime/qip-kernel/tests/blueprint_objectives.rs"]
  },
  {
    "name": "capacity_review",
    "path": "backend/crates/runtime/qip-kernel/src/capacity_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §20.1's sixth control: estimates the capital level at which an edge decays, from book depth, an impact law and each proposed leg's own stated estimated_cost_bps rather than an invented desk-wide constant.",
    "key_contracts": [
      "pub const MIN_ASSESSABLE_SPREAD_BPS: f64 = 0.01",
      "pub struct LegCapacity", "pub struct CapacityReview", "pub fn measure(..)"
    ],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_portfolio_engine", "qip_world_model"],
    "reached_from_production": "unknown",
    "production_evidence": "NONE FOUND. `rg -rn 'capacity_review|CapacityReview|LegCapacity'` across the entire backend/ tree returns only this file's own definitions and the `pub mod capacity_review;` declaration in lib.rs:38 — no caller in platform.rs, no caller in any crates/apps/*/src, and no caller in any tests/*.rs file outside its own `#[cfg(test)] mod tests`.",
    "io_and_state": "None; would be pure arithmetic over ProposalLeg/order-book depth if called.",
    "tests": {"count": 4, "notable": ["a_leg_asking_for_more_depth_than_its_own_cost_assumption_supports_is_a_finding", "a_leg_whose_assumed_cost_does_not_cover_half_the_spread_has_no_capacity_at_all", "a_leg_with_no_readable_depth_is_named_with_its_reason_rather_than_treated_as_roomy"]},
    "status": ["exists", "unused"],
    "status_reason": "A complete, internally-tested 686-line module implementing blueprint §20.1's sixth control that is never called from anywhere else in the workspace — not from Platform::run_cycle, not from any app, not from any integration test. It is the same shape of defect the repo's own architecture rules warn about (a control that cannot fire reads as protection and is not), except here the control cannot even execute.",
    "blueprint_capabilities": ["RISK", "CAPITAL"],
    "gaps_observed": ["Zero production or integration-test callers anywhere in the backend tree; only its own 4 unit tests exercise it. lib.rs:38 declares the module but nothing consumes it."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/lib.rs:38", "backend/crates/runtime/qip-kernel/src/capacity_review.rs:87-315"]
  },
  {
    "name": "causal_review",
    "path": "backend/crates/runtime/qip-kernel/src/causal_review.rs",
    "kind": "runtime",
    "purpose": "Applies blueprint §9.2's confound-control qualifier to the causal edges Platform::discover_temporal_precedence already writes, and §8.2's concentration query over the same graph — closing the gap that temporal-precedence edges were written uncontrolled.",
    "key_contracts": [
      "pub const MIN_FACTOR_CONSTITUENTS/MAX_EDGES_AUDITED_PER_PASS/CROSS_SECTIONAL_FACTOR",
      "pub struct UnsupportedEdge", "pub struct ControlAudit", "pub fn audit_controls(..)",
      "pub fn concentration(..)", "pub struct CausalReview", "pub fn review<F>(..)",
      "pub fn concentration_rests_on_unaudited_edges(report) -> bool", "pub fn is_established(standing) -> bool"
    ],
    "workspace_deps": ["qip_core", "qip_world_model"],
    "reached_from_production": "yes",
    "production_evidence": "fn causal_review(&self, now) at platform.rs:10045 calls crate::causal_review::review(..) at platform.rs:10056; that private method is a thin assembly wired into the cycle per its own doc comment.",
    "io_and_state": "None; reads WorldModel's CausalGraph and price_history already held by Platform.",
    "tests": {"count": 6, "notable": ["an_edge_that_is_only_a_shared_driver_does_not_survive_the_control", "an_edge_not_yet_knowable_is_not_audited_at_an_earlier_instant", "a_universe_too_thin_to_average_yields_no_factor_rather_than_a_thin_one"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closes a real gap (uncontrolled causal-edge writes) named in its own module doc; tested and wired in.",
    "blueprint_capabilities": ["WORLD", "RISK"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/causal_review.rs:76-554", "backend/crates/runtime/qip-kernel/src/platform.rs:10045-10056"]
  },
  {
    "name": "counterfactual_trial",
    "path": "backend/crates/runtime/qip-kernel/src/counterfactual_trial.rs",
    "kind": "runtime",
    "purpose": "Blueprint §12.4's third guardrail: replaces ADR 0055's fixed ten-observation/three-quarters threshold with a statistical hypothesis test (one-sided binomial p-value against a multiple-comparisons-corrected alpha), since the fixed bar lets the platform run as many free looks at the same hypothesis shape as it likes.",
    "key_contracts": [
      "pub const COUNTERFACTUAL_FAMILY_WISE_ALPHA: f64 = 0.05",
      "pub mod outcome", "pub struct CounterfactualTrial",
      "pub fn one_sided_p(sample, supporting) -> Result<f64>",
      "pub fn corrected_alpha(looks) -> f64", "pub fn verdict(..)"
    ],
    "workspace_deps": ["qip_core", "qip_lifecycle", "qip_numerics"],
    "reached_from_production": "yes",
    "production_evidence": "crate::counterfactual_trial::{CounterfactualTrial, outcome as trial_outcome} imported at platform.rs:44 and used 4x; rule_review, sizing_review and venue_review (all cycle-reached modules) consult its bar per module docs.",
    "io_and_state": "None; pure statistics over sample counts.",
    "tests": {"count": 5, "notable": ["a_unanimous_sample_at_the_fixed_bar_clears_the_first_look", "the_same_unanimous_sample_stops_clearing_once_the_platform_has_looked_a_hundred_times", "the_corrected_bar_never_divides_by_a_look_count_of_zero"]},
    "status": ["exists", "hardened"],
    "status_reason": "Replaces a previously fixed, gameable threshold with a proper multiple-comparisons-corrected test; tested including the divide-by-zero edge case.",
    "blueprint_capabilities": ["EVID", "REASON"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/counterfactual_trial.rs:97-232", "backend/crates/runtime/qip-kernel/src/platform.rs:44"]
  },
  {
    "name": "cross_margin",
    "path": "backend/crates/runtime/qip-kernel/src/cross_margin.rs",
    "kind": "runtime",
    "purpose": "Blueprint §25.6's collateral graph, built from the only two facts this process actually knows: per-venue counterparty exposure (the risk aggregate's counterparty axis) and each venue's collateral statement, joined nowhere else because a lib may not compose another lib's domain.",
    "key_contracts": [
      "pub const NON_CASH_HAIRCUT: Decimal", "pub const HOLDING_DRIVER: &str = \"security\"",
      "pub struct UnobservedExposure", "pub struct CrossMarginReview", "pub fn review(..)",
      "pub struct CrossMarginFinding"
    ],
    "workspace_deps": ["qip_capital", "qip_capital_fabric", "qip_contracts", "qip_core", "qip_events", "qip_risk"],
    "reached_from_production": "yes",
    "production_evidence": "crate::cross_margin::review(..) called at platform.rs:15007 inside the LEARN-stage review sequence; CrossMarginFinding::of(..) journals a record at platform.rs:15060.",
    "io_and_state": "None; joins qip_risk::limits::COUNTERPARTY_AXIS running gross with qip_capital::collateral statements already held.",
    "tests": {"count": 6, "notable": ["a_haircut_on_securities_can_turn_cover_that_looks_sufficient_into_a_call", "a_debit_balance_is_charged_as_an_obligation_and_never_netted_against_the_securities", "the_journal_record_carries_the_venues_rather_than_only_the_totals"]},
    "status": ["exists", "hardened"],
    "status_reason": "Composes two libs at the only layer permitted to; wired into LEARN and tested including the netting/haircut edge cases.",
    "blueprint_capabilities": ["CAPITAL", "RISK"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/cross_margin.rs:108-371", "backend/crates/runtime/qip-kernel/src/platform.rs:15007-15060"]
  },
  {
    "name": "exploration",
    "path": "backend/crates/runtime/qip-kernel/src/exploration.rs",
    "kind": "runtime",
    "purpose": "Blueprint §13.2's exploration budget as the cycle runs it: reads mandate share and the platform's own ignorance measures, holds the budget out of return-seeking capital via a ReservationLedger hold (so it is subtracted from deployable capital, not merely reported), and reports what it did for the DECIDE stage.",
    "key_contracts": [
      "pub const HOLD_ID/PROBE_VALIDITY/RESOLUTION_VALUE_BPS/CAPACITY_SAMPLE",
      "pub struct ExplorationDesk", "pub fn review(..)"
    ],
    "workspace_deps": ["qip_capital", "qip_core", "qip_learning_engine", "qip_observability"],
    "reached_from_production": "yes",
    "production_evidence": "crate::exploration::review(..) called at platform.rs:13100 (DECIDE stage); ExplorationDesk constructed during assembly at platform.rs:4539; Platform::exploration() public accessor at platform.rs:8252 documented as 'DECIDE stage, through crate::exploration::review'.",
    "io_and_state": "Holds capital under one ReservationLedger id (HOLD_ID) rather than merely reporting a number; no direct file/network I/O.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/exploration.rs (4 tests) and tests/regime_boundary.rs (1 test) as integration coverage"]},
    "status": ["exists", "hardened"],
    "status_reason": "6 call sites in platform.rs across assembly, DECIDE and public accessor; internal tests are absent but integration coverage exists in a dedicated test file.",
    "blueprint_capabilities": ["CAPITAL", "REASON"],
    "gaps_observed": ["No #[test] inside exploration.rs itself; all direct verification lives in tests/exploration.rs and tests/regime_boundary.rs."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/exploration.rs:105-168", "backend/crates/runtime/qip-kernel/src/platform.rs:13100", "backend/crates/runtime/qip-kernel/tests/exploration.rs"]
  },
  {
    "name": "fabric_declaration",
    "path": "backend/crates/runtime/qip-kernel/src/fabric_declaration.rs",
    "kind": "runtime",
    "purpose": "Parses an operator's capital-fabric declaration file into the command vocabulary the journal writes (§37.1/§37.3/§38.4). Lives in the kernel rather than qip-api because api_boundary.rs forbids qip-api a dependency edge to qip-capital-fabric or anything that can name an order constructor or venue adapter.",
    "key_contracts": [
      "pub const MAX_FABRIC_COMMANDS: usize = 1024",
      "pub const FABRIC_PATH_VARIABLE: &str = \"QIP_CAPITAL_FABRIC_PATH\"",
      "pub struct Declaration { pub fn parse(text) -> Result<Declaration> }"
    ],
    "workspace_deps": ["qip_capital_fabric", "qip_core"],
    "reached_from_production": "yes",
    "production_evidence": "Not called from platform.rs at all (0 references) — instead used directly by the qip-api composition root: qip-api/src/fabric.rs:110 `pub use qip_kernel::fabric_declaration::{Declaration, FABRIC_PATH_VARIABLE, MAX_FABRIC_COMMANDS};`, and fabric.rs:307 `fn read(path) -> Result<(Fingerprint, Declaration)>` which is wired into main.rs:473 (FabricRefresh::new) and main.rs:1065 (FabricFeed::from_env) — a live server-startup path, not through Platform::run_cycle.",
    "io_and_state": "None itself; the file path is read by qip-api's fabric.rs, which qip-kernel does not touch (env reading is forbidden outside crates/apps).",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via crates/apps/qip-api/tests/fabric.rs (13 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Production-reached via a different composition root (qip-api) than most kernel modules (which go through Platform::run_cycle); this is the intended seam per its own module doc.",
    "blueprint_capabilities": ["FABRIC", "CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/fabric_declaration.rs:48-67", "backend/crates/apps/qip-api/src/fabric.rs:110,307", "backend/crates/apps/qip-api/src/main.rs:473,1065"]
  },
  {
    "name": "falsification",
    "path": "backend/crates/runtime/qip-kernel/src/falsification.rs",
    "kind": "runtime",
    "purpose": "Blueprint §14.3's Testing gate: evaluates the falsifiers a thesis was formed with against what actually happened, closing a gap where ThesisOutcome::falsifiers_triggered was always constructed as Vec::new() and so silently read as though every falsifier had cleared.",
    "key_contracts": ["pub const FALSIFIER_MIN_OBSERVATIONS: usize = 5", "pub struct Book { pub fn review(..) }"],
    "workspace_deps": ["qip_core", "qip_prediction", "qip_world_model"],
    "reached_from_production": "yes",
    "production_evidence": "Book::review(..) called from Platform::stage_learn per module doc; Book field wired at platform.rs:434 and 4455 (assembly), and Platform::falsification() accessor at platform.rs:14650.",
    "io_and_state": "None; evaluates falsifiers against price_history/world-model state already held.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/falsification.rs (5 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closes a previously-silent gap (falsifiers nothing evaluated); wired into LEARN and covered by a dedicated integration suite.",
    "blueprint_capabilities": ["EVID", "REASON"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/falsification.rs:76-147", "backend/crates/runtime/qip-kernel/src/platform.rs:434,4455,14650"]
  },
  {
    "name": "family_review",
    "path": "backend/crates/runtime/qip-kernel/src/family_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §12.3's fifth row, measurement half only (ADR 0064 deliberately withholds the second half — revising weights): which strategy families are earning their funding, producing a Misallocation finding when a funded family's evidence is dominated by an unfunded one by more than a margin.",
    "key_contracts": [
      "pub const FAMILY_REVIEW_MIN_MEMBERS/FAMILY_REVIEW_MARGIN/FAMILY_FINDING_PROPOSED/FAMILY_FINDING_WITHDRAWN",
      "pub struct FamilyMember<'a>", "pub struct FamilyStanding",
      "pub fn standings(members) -> BTreeMap<String, FamilyStanding>",
      "pub struct Misallocation", "pub fn misallocation(standings) -> Option<Misallocation>",
      "pub fn record_standings(metrics, standings)", "pub struct FamilyAllocationReview", "pub struct MisallocationFinding"
    ],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_events", "qip_lifecycle", "qip_numerics", "qip_observability", "qip_simulation_engine"],
    "reached_from_production": "yes",
    "production_evidence": "crate::family_review::standings(&members) called at platform.rs:5938; also imported by qip_kernel::quote_loop internally (src/quote_loop.rs) for family-scoped sizing.",
    "io_and_state": "None; pure arithmetic. record_standings writes to the Metrics registry (in-memory) passed in.",
    "tests": {"count": 10, "notable": ["a_family_whose_evidence_beats_every_funded_family_moves_no_weight_anywhere", "a_gap_of_exactly_the_margin_is_a_finding_and_one_ulp_below_it_is_not", "two_families_whose_names_differ_only_after_a_colon_cannot_exist_so_the_finding_key_is_injective"]},
    "status": ["exists", "hardened"],
    "status_reason": "Deliberately half-implemented per ADR 0064 (measures but never revises weights); the absence is enforced by a review discipline (REVIEWED_WEIGHT_MOVERS in qip-acceptance security.rs) rather than a structural guarantee, per its own module doc.",
    "blueprint_capabilities": ["CAPITAL", "REASON"],
    "gaps_observed": ["Module doc (lines 320-335) states weight revision is deliberately not built (ADR 0064) and the guarantee is a review tripwire, not a structural one — an independent review defeated 3 rounds of a scan meant to catch a weight-mover being compiled in."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/family_review.rs:185-563", "backend/crates/runtime/qip-kernel/src/platform.rs:5938"]
  },
  {
    "name": "feature_statistics",
    "path": "backend/crates/runtime/qip-kernel/src/feature_statistics.rs",
    "kind": "runtime",
    "purpose": "Blueprint §21.1/§22.2's streaming half over live-sensed series: t-digest quantiles, HyperLogLog cardinality and weighted-reservoir samples per instrument, plus the first production caller anywhere of qip_numerics::stats::RunningStats (Welford's accumulator).",
    "key_contracts": [
      "pub const FEATURE_STATISTICS_MIN_RETURNS: usize = 200", "pub const FEATURE_STATISTICS_SEED",
      "pub struct InstrumentStatistics", "pub struct DistributionShift",
      "pub struct FeatureStatisticsReview", "pub fn measure(..)"
    ],
    "workspace_deps": ["qip_core", "qip_events", "qip_training"],
    "reached_from_production": "yes",
    "production_evidence": "crate::feature_statistics::measure(&self.price_history, self.cycle, now) called at platform.rs:9954, with a comment at platform.rs:9932 noting the module 'was built, tested and declared' before this caller existed.",
    "io_and_state": "None; streams over price_history already held in memory, with bounded working sets (t-digest/HLL/reservoir), consistent with the bounded-retention product decision.",
    "tests": {"count": 4, "notable": ["an_instrument_whose_recent_returns_changed_scale_is_reported_and_a_calm_one_is_not", "a_price_feed_repeating_three_values_is_named_rather_than_summarised_as_a_distribution", "the_reported_memory_is_the_summarys_and_does_not_move_with_the_series"]},
    "status": ["exists", "hardened"],
    "status_reason": "Was built with no caller for a period (per its own comment at platform.rs:9932) and now has one; tested and bounded.",
    "blueprint_capabilities": ["DATA", "TICK"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/feature_statistics.rs:68-211", "backend/crates/runtime/qip-kernel/src/platform.rs:9932,9954"]
  },
  {
    "name": "hedge_review",
    "path": "backend/crates/runtime/qip-kernel/src/hedge_review.rs",
    "kind": "runtime",
    "purpose": "The only production caller of qip_risk::hedge (blueprint §31.4 hedge sizing): feeds the engine the instrument catalogue, actual risk-state exposures and current price snapshot, refusing rather than defaulting when a policy names an instrument the catalogue does not hold.",
    "key_contracts": [
      "pub struct HedgePolicyDeclaration", "pub struct HedgeReview", "pub struct HedgeSurveyed"
    ],
    "workspace_deps": ["qip_core", "qip_events", "qip_financial", "qip_market", "qip_portfolio", "qip_risk"],
    "reached_from_production": "yes",
    "production_evidence": "fn review_hedges(&mut self, now) at platform.rs:14901 calls crate::hedge_review::review(..) at platform.rs:14905, and HedgeSurveyed::of(..) journals a record at platform.rs:14920 — wired into the cycle.",
    "io_and_state": "None; reads RiskState exposures and market snapshot already held.",
    "tests": {"count": 6, "notable": ["a_review_with_no_declared_policy_says_so_rather_than_falling_silent", "a_stated_loading_that_is_not_a_number_refuses_every_policy_and_not_only_the_factor_axis", "a_policy_naming_an_instrument_the_catalogue_does_not_hold_is_refused_not_defaulted"]},
    "status": ["exists", "hardened"],
    "status_reason": "Only production feeder of qip-risk's hedge engine; refuses invalid policies rather than defaulting; wired and tested.",
    "blueprint_capabilities": ["RISK", "EXEC"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/hedge_review.rs:94-439", "backend/crates/runtime/qip-kernel/src/platform.rs:14901-14954"]
  },
  {
    "name": "model_serving",
    "path": "backend/crates/runtime/qip-kernel/src/model_serving.rs",
    "kind": "runtime",
    "purpose": "ADR 0083: where a trained model is promoted, served to the platform's provider and named to a cell. Platform::promote_model asks the serving provider to accept the artifact before promoting a scratch copy of the registry, and journals the promotion before the live registry adopts it, so the log never lacks a promotion being acted on.",
    "key_contracts": [
      "pub const MODEL_PROMOTION_ORIGIN: &str = \"kernel/model-promotion\"",
      "pub struct DistilledManifestEntry", "pub struct ModelPromotion", "pub struct ModelManifestIssue",
      "pub(crate) fn resume_model_promotions(log) -> Result<BTreeMap<String, ModelPromotion>>",
      "impl Platform { pub fn promote_model(..) }"
    ],
    "workspace_deps": ["qip_ai", "qip_contracts", "qip_core", "qip_events", "qip_strategy", "qip_streaming"],
    "reached_from_production": "yes",
    "production_evidence": "Platform::promote_model called at crates/apps/qip-deepbrain/src/learning.rs:929 (non-test code); resume_model_promotions called at platform.rs:4446 during assembly to replay journalled promotions.",
    "io_and_state": "Journals a ModelPromotion event before the in-memory registry adopts it (order-of-operations invariant); no direct file I/O (goes through the platform's EventLog).",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/serving.rs (5 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Reached from qip-deepbrain's production learning path; re-exported from lib.rs; journal-before-adopt ordering is a named invariant.",
    "blueprint_capabilities": ["MODEL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/model_serving.rs:57-210", "backend/crates/apps/qip-deepbrain/src/learning.rs:929", "backend/crates/runtime/qip-kernel/src/platform.rs:4446"]
  },
  {
    "name": "pre_positioning",
    "path": "backend/crates/runtime/qip-kernel/src/pre_positioning.rs",
    "kind": "runtime",
    "purpose": "Blueprint §25.2's movement half: how much capital to pre-position and where (cheapest signed corridor arriving in time), planned in DECIDE via Platform::pre_position and scored in LEARN via Platform::evaluate_pre_positioning against demand actually recorded inside the plan's horizon window.",
    "key_contracts": [
      "pub struct PrePositioningPlanned", "pub struct PrePositioningScored", "pub struct PrePositioningJournal",
      "pub fn window_closes_at(planned_at, horizon) -> Timestamp",
      "pub fn realised_between(..)"
    ],
    "workspace_deps": ["qip_capital_fabric", "qip_contracts", "qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "PrePositioningJournal/PrePositioningPlanned/PrePositioningScored imported at platform.rs:50 and used across 4 sites; per module doc, Platform::pre_position (DECIDE) and Platform::evaluate_pre_positioning (LEARN) both existed with only test callers until this module, now wired into both stages.",
    "io_and_state": "None; the journal retains one plan per horizon window in memory (bounded by horizon elapsing).",
    "tests": {"count": 2, "notable": ["realised_demand_counts_only_the_observations_inside_the_window_and_both_ends_of_it", "a_window_closes_at_the_plan_instant_plus_the_horizon_and_never_before_it"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed a gap where DECIDE forecast capital movement and nothing ever scored whether the forecast held; now wired into both DECIDE and LEARN.",
    "blueprint_capabilities": ["CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/pre_positioning.rs:63-192", "backend/crates/runtime/qip-kernel/src/platform.rs:50,2240"]
  },
  {
    "name": "precedent_declines",
    "path": "backend/crates/runtime/qip-kernel/src/precedent_declines.rs",
    "kind": "runtime",
    "purpose": "Blueprint §10.3's last query — 'what did we decline in situations like this, and should we have?' — joins episodes REASON recalled to the counterfactual DeclinedScore the twin already priced, so the situation-scoped form of the question can be asked, not just the per-rule aggregate rule_review already answers.",
    "key_contracts": [
      "pub fn hypothesis_of(episode_id) -> Option<&str>",
      "pub struct GateDeclines", "pub struct PrecedentDeclines", "pub fn join<F>(..)"
    ],
    "workspace_deps": ["qip_ai", "qip_core", "qip_twin"],
    "reached_from_production": "yes",
    "production_evidence": "crate::precedent_declines::join(&recall.nearest, &self.declined_scores, ..) called at platform.rs:11460; pub declines: crate::precedent_declines::PrecedentDeclines field on Platform at platform.rs:2240.",
    "io_and_state": "None; joins in-memory episode recall to already-scored DeclinedScore records.",
    "tests": {"count": 4, "notable": ["a_refusal_scored_on_a_recalled_episodes_hypothesis_is_joined_to_it_and_charged_to_its_gate", "an_episode_id_this_platform_did_not_write_joins_nothing_however_well_the_rest_matches", "one_refusal_naming_two_recalled_episodes_is_one_refusal_against_two_matched_analogues"]},
    "status": ["exists", "hardened"],
    "status_reason": "Fills a named gap (no code joined an episode to a score); wired into a REASON-adjacent path and tested.",
    "blueprint_capabilities": ["REASON", "EVID"],
    "gaps_observed": ["Module doc (precedent_declines.rs:172) notes 'everything but the id is a well-typed placeholder' for a field in the join result, held intentionally minimal rather than invented."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/precedent_declines.rs:54-112", "backend/crates/runtime/qip-kernel/src/platform.rs:11460,2240"]
  },
  {
    "name": "quote_loop",
    "path": "backend/crates/runtime/qip-kernel/src/quote_loop.rs",
    "kind": "runtime",
    "purpose": "Blueprint §29.1's composition point: what the platform would quote, priced against its own book (market view + order manager + equity), and explicitly sent nowhere — the module doc restates the paper-trading boundary because a quote loop is structurally the most dangerous thing to compose (in a real venue, quoting is order submission).",
    "key_contracts": [
      "pub const QUOTE_MIN_BARS/QUOTE_PASS_LIMIT/QUOTE_INVENTORY_FRACTION/QUOTE_BUDGET_FRACTION",
      "pub fn default_policy() -> QuotePolicy", "pub struct QuoteLoopReview",
      "pub fn review(platform, now, outcome) -> StageOutcome"
    ],
    "workspace_deps": ["qip_core", "qip_execution_engine", "qip_financial", "qip_market", "qip_market_ingestion", "qip_observability", "qip_risk"],
    "reached_from_production": "yes",
    "production_evidence": "crate::quote_loop::review(self, now, outcome) called at platform.rs:13584 and platform.rs:13791.",
    "io_and_state": "None; computed quotes are held in an in-memory review record, never submitted to any venue adapter (structurally paper-only per module doc).",
    "tests": {"count": 5, "notable": ["a_platform_with_an_observed_book_prices_a_two_sided_quote_and_sends_nothing", "a_position_the_platform_actually_holds_skews_the_quote_it_would_show", "a_working_limit_order_is_located_in_the_book_and_sizes_the_quote_down_behind_the_queue"]},
    "status": ["exists", "hardened"],
    "status_reason": "Explicitly the highest-risk-shaped module in the kernel by its own doc comment; wired into the cycle twice and tested that it sends nothing.",
    "blueprint_capabilities": ["EXEC"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/quote_loop.rs:117-309", "backend/crates/runtime/qip-kernel/src/platform.rs:13584,13791"]
  },
  {
    "name": "references",
    "path": "backend/crates/runtime/qip-kernel/src/references.rs",
    "kind": "runtime",
    "purpose": "Gives a data-reference revision detected by qip-data-finder's ReferenceLedger actual consequence (§22.3): 'the backtest that used the original is flagged, not silently invalidated.' Refuses a fetch digest from a source the composition root did not admit through the licensing gate.",
    "key_contracts": [
      "pub struct DataReferenceRecorded", "pub struct SourceRevisionDetected", "pub struct RecordedReference",
      "pub struct ResearchCampaignClosed", "pub struct ResearchCampaignFlagged"
    ],
    "workspace_deps": ["qip_core", "qip_data_finder", "qip_events", "qip_financial", "qip_market_ingestion", "qip_observability", "qip_risk", "qip_streaming"],
    "reached_from_production": "yes",
    "production_evidence": "Not called from platform.rs (0 references) — reached directly from qip-deepbrain's production campaign path: crates/apps/qip-deepbrain/src/campaign.rs:88 `use qip_kernel::references::ResearchCampaignClosed;` and campaign.rs:1141 `.decode::<qip_kernel::references::ResearchCampaignFlagged>()?`.",
    "io_and_state": "None itself; consumes FetchDigest and AdmittedSource types a composition root already produced.",
    "tests": {"count": 2, "notable": ["a_revision_restores_the_revising_reference_after_its_own_record_was_evicted", "the_ledger_rebuilds_from_a_log_that_evicted_an_interior_record"]},
    "status": ["exists", "hardened"],
    "status_reason": "Reached via qip-deepbrain rather than Platform::run_cycle, matching fabric_declaration's pattern of a kernel module used directly by an app's composition root.",
    "blueprint_capabilities": ["DATA", "EVID"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/references.rs:89-287", "backend/crates/apps/qip-deepbrain/src/campaign.rs:88,1141"]
  },
  {
    "name": "regime_allocation",
    "path": "backend/crates/runtime/qip-kernel/src/regime_allocation.rs",
    "kind": "runtime",
    "purpose": "Blueprint §23.3's production seam: reads the regime Platform::market_regime already classifies (long-lived for the cost router) and narrows sizing bounds by it — the only place in the kernel where a regime reaches a weight. Classifies nothing itself and holds no history.",
    "key_contracts": [
      "pub const fn allocation_regime(regime: MarketRegime) -> AllocationRegime",
      "pub fn narrow<F>(..)"
    ],
    "workspace_deps": ["qip_core", "qip_cost_router", "qip_optimization_engine", "qip_portfolio_engine"],
    "reached_from_production": "yes",
    "production_evidence": "crate::regime_allocation::narrow(&mut caps, &mut capped, theses, ..) called at platform.rs:12125.",
    "io_and_state": "None; exhaustive match over an already-classified MarketRegime.",
    "tests": {"count": 4, "notable": ["every_regime_the_classifier_can_return_maps_to_one_the_allocator_reads", "a_regime_narrows_a_bound_and_compounds_with_the_fill_record_cap_rather_than_replacing_it", "a_directional_regime_narrows_less_than_one_the_platform_cannot_name"]},
    "status": ["exists", "hardened"],
    "status_reason": "Single, exhaustive, tested seam between an existing classifier and sizing.",
    "blueprint_capabilities": ["RISK", "CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/regime_allocation.rs:51-79", "backend/crates/runtime/qip-kernel/src/platform.rs:12125"]
  },
  {
    "name": "regime_transition",
    "path": "backend/crates/runtime/qip-kernel/src/regime_transition.rs",
    "kind": "runtime",
    "purpose": "Blueprint §9.4's exploration handling for regime boundaries: marks when a subject's regime label crosses, feeding qip_capital::exploration::ProbeKind::RegimeBoundary, which previously sat declared and unfed because nothing marked a transition (only the regime in force).",
    "key_contracts": [
      "pub const MARKER_WINDOW: Duration = 48h", "pub const SUBJECT_PREFIX/MAX_SUBJECTS",
      "pub fn untested_share<I>(standings) -> Option<f64>",
      "pub struct RegimeTransitions", "pub struct Crossing"
    ],
    "workspace_deps": ["qip_core", "qip_world_model"],
    "reached_from_production": "yes",
    "production_evidence": "regime_transitions: crate::regime_transition::RegimeTransitions field constructed at platform.rs:262,4540; crate::regime_transition::untested_share(standings) called at platform.rs:10150; crate::regime_transition::SUBJECT_PREFIX used at platform.rs:10152.",
    "io_and_state": "Bounded, evicting set (MAX_SUBJECTS=1024) held in Platform state; no external I/O.",
    "tests": {"count": 10, "notable": ["the_boundary_uncertainty_is_the_share_of_edges_untested_in_the_regime_entered", "the_remembered_set_is_bounded_and_evicts_the_least_recently_changed", "an_evicted_subject_seen_again_re_enters_as_a_first_sighting_rather_than_a_crossing"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closes a named gap (RegimeBoundary probe kind previously unfed); bounded working set; heavily tested (10 tests).",
    "blueprint_capabilities": ["WORLD", "REASON"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/regime_transition.rs:59-127", "backend/crates/runtime/qip-kernel/src/platform.rs:262,10150"]
  },
  {
    "name": "rule_review",
    "path": "backend/crates/runtime/qip-kernel/src/rule_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §12.3's per-rule accumulation: what the twin's counterfactual scores say about each risk rule (too tight / earning its place / dead weight), closing a gap where a refusal was charged only to the control ('pre-trade-risk') and never to the specific rule.",
    "key_contracts": [
      "pub struct RuleRegret", "pub fn regret_by_rule(scores) -> BTreeMap<String, RuleRegret>",
      "pub struct RegretEvidence", "pub const PROPOSAL_PROPOSED/WITHDRAWN/ENACTED",
      "pub struct RecalibrationProposal", "pub struct RuleDefence", "pub struct RuleDormant",
      "pub struct RuleReviewJournal"
    ],
    "workspace_deps": ["qip_core", "qip_events", "qip_risk", "qip_twin"],
    "reached_from_production": "yes",
    "production_evidence": "RuleRegret/RecalibrationProposal/RuleDefence/RuleDormant/RuleReviewJournal imported at platform.rs:51 (8 uses total); directly re-used by qip-api/src/rule_views.rs:15 `use qip_kernel::rule_review::RecalibrationProposal;` for an API surface.",
    "io_and_state": "None; pure arithmetic over &[DeclinedScore], BTreeMap throughout for replay-stable iteration order.",
    "tests": {"count": 2, "notable": ["a_proposal_cannot_tighten_a_bound", "a_proposal_with_a_non_positive_floor_is_refused_rather_than_journaled_forever"]},
    "status": ["exists", "hardened"],
    "status_reason": "Wired into the cycle and directly exposed through qip-api's console surface; BTreeMap discipline for replay stability.",
    "blueprint_capabilities": ["RISK", "GOV"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/rule_review.rs:63-498", "backend/crates/runtime/qip-kernel/src/platform.rs:51", "backend/crates/apps/qip-api/src/rule_views.rs:15"]
  },
  {
    "name": "session_replay",
    "path": "backend/crates/runtime/qip-kernel/src/session_replay.rs",
    "kind": "runtime",
    "purpose": "Blueprint §34.4's simulated rung producer: qip_lifecycle::venue_ladder::SimulatedGate needed a SimulationEvidence (replayed sessions, reconciliation breaks, clip cost) that nothing ever constructed, so the gate could only ever refuse and every venue stopped at the observed rung.",
    "key_contracts": [
      "pub const BREAK_UNSENT_FILL/OVERFILLED/MISROUTED_FILL/FILL_BEFORE_INSTRUCTION/OUT_OF_SEQUENCE/UNPRICED_FILL",
      "pub struct ReplayOutcome", "pub fn replay(sessions, venue) -> ReplayOutcome"
    ],
    "workspace_deps": ["qip_core", "qip_execution_engine", "qip_lifecycle"],
    "reached_from_production": "yes",
    "production_evidence": "crate::session_replay::replay(&self.orders.sessions(&broker_name), &broker_name) called at platform.rs:14048.",
    "io_and_state": "None; replays RecordedSession data the order manager already holds in memory.",
    "tests": {"count": 9, "notable": ["a_fill_beyond_what_the_desk_instructed_is_a_reconciliation_break", "a_saturated_session_is_not_replayed_because_it_cannot_show_the_absence_of_a_break", "the_reference_clip_is_the_largest_clip_the_venue_actually_absorbed"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closes a named gap where the venue ladder's ceiling rung could never be reached from production; wired into the cycle and well tested.",
    "blueprint_capabilities": ["EXEC", "EVID"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/session_replay.rs:82-119", "backend/crates/runtime/qip-kernel/src/platform.rs:14048"]
  },
  {
    "name": "shared_cause",
    "path": "backend/crates/runtime/qip-kernel/src/shared_cause.rs",
    "kind": "runtime",
    "purpose": "Produces blueprint §25.3's three exposure levels no per-instrument record carries: per family, per factor and per causal driver — joining qip-risk's SharedCauseExposure shape with the world model's causal graph, the platform's own tape and the factory's strategy registry, none of which can reach qip-risk directly.",
    "key_contracts": ["pub fn observe(world, price_history, state) -> SharedCauseExposure (approx signature)"],
    "workspace_deps": ["qip_agents", "qip_core", "qip_risk", "qip_world_model"],
    "reached_from_production": "yes",
    "production_evidence": "let state = crate::shared_cause::observe(&self.world, &self.price_history, state) called at platform.rs:15141.",
    "io_and_state": "None; joins three in-memory sources (causal graph, tape, strategy registry) already held by Platform.",
    "tests": {"count": 4, "notable": ["a_causal_edge_whose_transmission_is_not_a_number_refuses_every_level_rather_than_dropping_the_name", "a_causal_edge_below_the_offered_floor_is_left_out_without_refusing_the_level", "a_well_formed_graph_and_tape_still_charge_both_fed_levels"]},
    "status": ["exists", "hardened"],
    "status_reason": "The only composer of qip-risk's SharedCauseExposure; wired into the cycle; refuses malformed inputs rather than dropping/defaulting.",
    "blueprint_capabilities": ["RISK", "WORLD"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/shared_cause.rs", "backend/crates/runtime/qip-kernel/src/platform.rs:15141"]
  },
  {
    "name": "sizing_review",
    "path": "backend/crates/runtime/qip-kernel/src/sizing_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §12.3's last row, executed-order half: reads FillScores the LEARN stage writes for every fill and produces a cap_multiplier (armed when >=3/4 of >=10 scored fills would have done better smaller) and a LargerSizeFinding for the opposite pattern.",
    "key_contracts": [
      "pub const SIZING_CAP_MIN_SAMPLE/SIZING_CAP_FAVOUR_FRACTION/SIZING_CAP_MULTIPLIER",
      "pub struct SizeRegret", "pub fn size_regret(scores, object_id) -> SizeRegret",
      "pub fn cap_multiplier(scores, object_id) -> Decimal",
      "pub struct LargerSizeFinding", "pub fn larger_size_finding(..)",
      "pub struct SizingProposal", "pub struct SizingCapEntry"
    ],
    "workspace_deps": ["qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "crate::sizing_review::cap_multiplier(&self.fill_scores, object_id) called at platform.rs:16850; imported at platform.rs:55 (7 uses total).",
    "io_and_state": "None; pure arithmetic over &[FillScore] the platform already holds.",
    "tests": {"count": 2, "notable": ["below_the_sample_the_cap_is_one", "a_pattern_of_fills_that_would_have_done_better_larger_never_raises_the_cap_above_one"]},
    "status": ["exists", "hardened"],
    "status_reason": "A bound rather than a second budget multiplier by design (can only narrow, per its own module doc); wired into sizing and tested.",
    "blueprint_capabilities": ["RISK", "CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/sizing_review.rs:37-192", "backend/crates/runtime/qip-kernel/src/platform.rs:16850"]
  },
  {
    "name": "valuation",
    "path": "backend/crates/runtime/qip-kernel/src/valuation.rs",
    "kind": "runtime",
    "purpose": "The valuation plane's composition point: joins qip-market's TermStructure (discounting) and qip-financial's CreditProfile (default probability, recovery, covenant state), which no lib may compose across itself. Closed a gap where TermStructure had no production caller anywhere and credit was two bare f64 fields.",
    "key_contracts": ["pub struct CreditRegister { pub fn from_universe(universe, now) -> CreditRegister }"],
    "workspace_deps": ["qip_core", "qip_financial", "qip_market"],
    "reached_from_production": "yes",
    "production_evidence": "crate::valuation::CreditRegister::from_universe(&universe, now) called at platform.rs:4221 during assembly; credit: crate::valuation::CreditRegister field at platform.rs:721; Platform::credit_register() accessor at platform.rs:11651; re-exported from lib.rs.",
    "io_and_state": "None; built once from the Universe at assembly time and held as Platform state.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/valuation_plane.rs (8), tests/valuation_seam.rs (7), tests/valuation_credit.rs (7) = 22 integration tests"]},
    "status": ["exists", "hardened"],
    "status_reason": "8 call sites in platform.rs including assembly-time construction and a public accessor; 22 integration tests across three dedicated test files.",
    "blueprint_capabilities": ["ASSET"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/valuation.rs:78", "backend/crates/runtime/qip-kernel/src/platform.rs:721,4221,11651"]
  },
  {
    "name": "venue_admission",
    "path": "backend/crates/runtime/qip-kernel/src/venue_admission.rs",
    "kind": "runtime",
    "purpose": "Blueprint §34.4 at the kernel: reports which reachable venues have actually earned the simulator rung on the ladder versus which are used on their own documentation. Structurally report-only — no function here returns a venue to add or withdraw.",
    "key_contracts": [
      "pub struct VenueStanding", "pub fn standings(ladder, reachable) -> Vec<VenueStanding>", "pub fn review(..)"
    ],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_lifecycle"],
    "reached_from_production": "yes",
    "production_evidence": "crate::venue_admission::review(&self.venue_ladder, &reachable, &measured.unmeasured) called at platform.rs:14075, immediately after venue_measurement's measure() call.",
    "io_and_state": "None; reads the VenueLadder and reachable-venue set already held.",
    "tests": {"count": 7, "notable": ["a_reachable_venue_the_ladder_never_heard_of_is_reported_as_never_registered", "a_mixed_estate_reports_one_problem_per_unadmitted_venue_in_a_replayable_order", "a_venue_the_platform_cannot_reach_is_not_reviewed_however_it_stands"]},
    "status": ["exists", "hardened"],
    "status_reason": "Report-only by structural design (no enable/withdraw function exists); wired into LEARN immediately after venue_measurement.",
    "blueprint_capabilities": ["EXEC", "GOV"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/venue_admission.rs:66-116", "backend/crates/runtime/qip-kernel/src/platform.rs:14075"]
  },
  {
    "name": "venue_measurement",
    "path": "backend/crates/runtime/qip-kernel/src/venue_measurement.rs",
    "kind": "runtime",
    "purpose": "Blueprint §34.4's measurement seam that qip_lifecycle::venue_ladder::attempt_promotion never had: reads a VenueObservation from the broker port, turns it into a VenueDeclaration and VenueMeasurement, seats the venue at the registered rung, and asks whether it can walk further.",
    "key_contracts": ["pub struct VenueMeasurementOutcome", "pub fn measure(..)"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_execution_engine", "qip_lifecycle"],
    "reached_from_production": "yes",
    "production_evidence": "let measured = crate::venue_measurement::measure(..) called at platform.rs:14054, immediately before venue_admission's review call.",
    "io_and_state": "None; reads broker-adapter VenueObservation data already surfaced to Platform.",
    "tests": {"count": 6, "notable": ["the_measurement_seam_cannot_walk_a_venue_past_the_simulator_on_any_number_of_cycles", "the_desks_own_simulated_broker_crosses_the_port_and_is_refused_on_its_untimed_latency", "a_venue_short_of_evidence_it_could_supply_is_a_problem_rather_than_an_excused_gap"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closes a named gap (attempt_promotion had no production feeder); paired with venue_admission in the LEARN stage; the paper-trading boundary is reinforced by a test that the simulator ceiling cannot be walked past.",
    "blueprint_capabilities": ["EXEC", "EVID"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/venue_measurement.rs:78-100", "backend/crates/runtime/qip-kernel/src/platform.rs:14054"]
  },
  {
    "name": "venue_review",
    "path": "backend/crates/runtime/qip-kernel/src/venue_review.rs",
    "kind": "runtime",
    "purpose": "Blueprint §12.3's fourth row: 'feasibility rejections cluster on one venue' -> venue withdrawn. Keeps one bounded window of refusals from both the desk's order manager and cells' reports, each naming venue and constraint, and answers whether one venue dominates recent refusals — a finding only, never a mutation.",
    "key_contracts": [
      "pub const VENUE_WITHDRAWAL_MIN_SAMPLE/VENUE_WITHDRAWAL_SHARE/VENUE_WITHDRAWAL_MIN_CELLS",
      "pub enum FeasibilitySeam", "pub struct FeasibilityRefusal", "pub enum RefusalStanding",
      "pub fn pardon(window, venue) -> usize", "pub struct VenueCluster",
      "pub fn assess(window, withdrawn) -> Option<VenueCluster>",
      "pub struct VenueWithdrawal", "pub const REINSTATEMENT_AWAITING/REINSTATED/REINSTATEMENT_REFUSED",
      "pub struct VenueReinstatementEntry"
    ],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_events", "qip_execution_engine"],
    "reached_from_production": "yes",
    "production_evidence": "Imported at platform.rs:59 (central plane's own feasibility-refusal admission uses this shape too, per FEASIBILITY_REFUSALS in platform.rs and qip-contracts::feasibility); also directly re-used by qip-api/src/venue_views.rs:27 `use qip_kernel::venue_review::{VenueReinstatementEntry, VenueWithdrawal};` for a console API.",
    "io_and_state": "None; assess/pardon are pure functions over a bounded in-memory window of FeasibilityRefusal.",
    "tests": {"count": 14, "notable": ["ten_refusals_from_two_distinct_cells_do_clear_the_bar", "an_echo_can_sustain_a_withdrawn_venues_weight_and_never_amplify_it", "a_pardoned_refusal_holds_the_denominator_for_every_venue_but_the_one_it_names"]},
    "status": ["exists", "hardened"],
    "status_reason": "Densely tested (14 tests) including the echo/pardon edge cases the observability rules file flags as a load-bearing invariant elsewhere in this codebase; reached both from the cycle and directly from qip-api's console.",
    "blueprint_capabilities": ["RISK", "EXEC"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/venue_review.rs:69-563", "backend/crates/runtime/qip-kernel/src/platform.rs:59", "backend/crates/apps/qip-api/src/venue_views.rs:27"]
  },
  {
    "name": "central (mod)",
    "path": "backend/crates/runtime/qip-kernel/src/central/mod.rs",
    "kind": "runtime",
    "purpose": "Module-doc composition root for the central plane, the ADR-0008 half allowed to be slow: candidate-to-scaled promotion, the capital that sizes a promotion, aggregate exposure no single cell can see, and six governance controls. Declares the 16 submodules and their flat pub-use re-export surface.",
    "key_contracts": [
      "pub mod {belief,causal,darkness,dna,episodic,factory,foundry,horizon,insights,learning,models,plane,realised,regions,structure,whitelist}",
      "pub use belief::{BeliefIssue, BeliefOutcome}", "pub use causal::{CausalIssue, CausalOutcome}",
      "pub use plane::{CentralPlane, CentralConfig, CellReport, ReconciliationBreak, BreakOrigin, BreakDirection, IssuedCapital, AbsorbedFill, CellIngestion, capital_subject}",
      "pub use learning::{CellOutcome, LearningReport, LearningVerdict, ...}",
      "pub use regions::{RegionMembership, RegionShare, RegionShares, GrantManifests, ManifestDecision}",
      "pub use whitelist::{ArbitragePolicy, WhitelistIssue, WhitelistOutcome, WhitelistedMarket, WhitelistedVenue}"
    ],
    "workspace_deps": ["none directly; re-exports children which each depend on qip_contracts/qip_core/etc."],
    "reached_from_production": "yes",
    "production_evidence": "lib.rs:69 `pub use central::{CellReport, CentralPlane, StrategyDna, StrategyFactory};` re-exports this module's surface to all 4 apps.",
    "io_and_state": "None; pure declaration.",
    "tests": {"count": 0, "notable": []},
    "status": ["exists", "hardened"],
    "status_reason": "Stable re-export surface; note insights::CellInsights, foundry::StrategyFoundry, models::{ModelRegistration,register_fit} are NOT re-exported here (apps reach them via full crate::central::<submodule> paths).",
    "blueprint_capabilities": ["ARCH", "GOV"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/mod.rs:50-86"]
  },
  {
    "name": "central::plane",
    "path": "backend/crates/runtime/qip-kernel/src/central/plane.rs",
    "kind": "runtime",
    "purpose": "The central plane itself: composes a StrategyFactory, CapitalAllocator, EnvelopeIssuer, CompliancePlane and AggregateExposure into one thing. Sizes capital only for a strategy standing at a capital-holding rung, feeds cell reports into aggregate exposure crowding checks, and stops a cell on any reconciliation break so the rest of the platform's risk numbers stay non-fictional.",
    "key_contracts": [
      "pub struct CentralConfig", "pub struct CentralPlane",
      "pub fn capital_subject(strategy, cell) -> String",
      "pub struct ReconciliationBreak", "pub enum BreakOrigin", "pub enum BreakDirection",
      "pub struct CellReport", "pub struct CellIngestion", "pub struct Settlement",
      "pub struct AbsorbedFill", "pub struct CostAccrual", "pub struct StrategyLot", "pub struct IssuedCapital",
      "impl CentralPlane { fn ingest, fn ship, fn verify_dna, fn region_shares, fn family_structure, fn learn_from_cells-target learn, fn live_outcomes, .. }"
    ],
    "workspace_deps": ["qip_capital", "qip_compliance", "qip_contracts", "qip_core", "qip_financial", "qip_learning_engine", "qip_lifecycle", "qip_mesh", "qip_observability", "qip_risk_engine"],
    "reached_from_production": "yes",
    "production_evidence": "Platform holds a `central: CentralPlane` field; CentralPlane methods reached from Platform::run_cycle: self.central.region_transitions (platform.rs:5068), self.central.cycle_whitelist_for (platform.rs:5165), self.central.family_structure (platform.rs:14190), self.central.arm_horizons (platform.rs:11905), self.central.learn (via Platform::learn_from_cells, platform.rs:8114, doc-confirmed 'the stage's call is the one a deployed process makes'). Also constructed directly: qip-api/src/main.rs:30 `use qip_kernel::central::{ArbitragePolicy, CentralConfig, CentralPlane};`, qip-deepbrain/src/main.rs:51, qip-api/src/trust.rs:29, qip-fastbrain/src/trust.rs:22, qip-deepbrain/src/trust.rs:22.",
    "io_and_state": "None directly; holds all central-plane state (factory ledger, capital envelopes, reconciliation break history) in memory, persisted only via the Platform's event log through journalled records.",
    "tests": {"count": 3, "notable": ["3 internal #[test]; the bulk of coverage is tests/central.rs (80 tests) and tests/central_feasibility.rs (5)"]},
    "status": ["exists", "hardened", "partial"],
    "status_reason": "The composition is heavily used and tested overall, but two of its own public methods — ship() and verify_dna() — have zero production callers anywhere in crates/apps (see central::dna finding below); everything else on CentralPlane that platform.rs or an app calls is production-reached.",
    "blueprint_capabilities": ["GOV", "CAPITAL", "RISK", "MESH"],
    "gaps_observed": ["CentralPlane::ship (plane.rs:1985) and CentralPlane::verify_dna (plane.rs:2010) have no caller in any crates/apps/*/src file (grep confirmed empty); only exercised in tests/central.rs:524-525,1234-1235."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/plane.rs:73-818", "backend/crates/runtime/qip-kernel/src/platform.rs:5068,5165,8114,11905,14190", "backend/crates/apps/qip-api/src/main.rs:29-30"]
  },
  {
    "name": "central::belief",
    "path": "backend/crates/runtime/qip-kernel/src/central/belief.rs",
    "kind": "runtime",
    "purpose": "Producer of blueprint §41.5's shipping-payload slot 3 (belief priors) as the centre can honestly state it: BeliefState only ever recorded when/how-many, never the beliefs themselves, so this holds a belief per subject a cell trades, retained past the cycle that formed it.",
    "key_contracts": ["pub enum BeliefOutcome", "pub struct BeliefIssue { pub fn derive(drafts, last, now) -> Result<BeliefIssue> }"],
    "workspace_deps": ["qip_ai", "qip_contracts", "qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "BeliefIssue::derive(..) called at platform.rs:5277, reached via Platform::run_cycle from all four apps.",
    "io_and_state": "None; derives from Platform::pending_episodes drafts already held.",
    "tests": {"count": 7, "notable": ["a_fresh_prior_is_what_stops_a_cell_halving_its_size_and_silence_restores_the_halving", "a_confidence_that_is_not_a_probability_is_refused_rather_than_signed", "every_belief_older_than_the_slots_own_window_is_left_out_and_counted"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed a slot the whitelist audit had named as permanently unfillable ('the kernel holds no belief engine'); wired and tested.",
    "blueprint_capabilities": ["REASON", "MESH"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/belief.rs:96-270", "backend/crates/runtime/qip-kernel/src/platform.rs:5277"]
  },
  {
    "name": "central::causal",
    "path": "backend/crates/runtime/qip-kernel/src/central/causal.rs",
    "kind": "runtime",
    "purpose": "Producer of blueprint §8.3's causal digest (shipping-payload slot) — the compact digest of relationships that currently matter, as the centre's own graph can honestly state it, after ADR 0054 gave WorldModel::claim_causal a real writer.",
    "key_contracts": ["pub enum CausalOutcome", "pub struct CausalIssue { pub fn derive(edges, last, now) -> Result<CausalIssue> }"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_events", "qip_world_model"],
    "reached_from_production": "yes",
    "production_evidence": "CausalIssue::derive(causal.edges().iter(), causal.last_updated(), now)? called at platform.rs:5335.",
    "io_and_state": "None; derives from the WorldModel causal graph already held.",
    "tests": {"count": 5, "notable": ["a_graph_that_has_absorbed_nothing_produces_no_slot_and_says_so", "a_graph_whose_record_contradicts_itself_or_its_clock_is_refused_rather_than_stamped", "a_fresh_digest_is_what_lifts_a_cells_causal_narrowing_and_silence_restores_it"]},
    "status": ["exists", "hardened"],
    "status_reason": "Second of the three slots the whitelist audit had named unfillable, now closed and wired.",
    "blueprint_capabilities": ["WORLD", "MESH"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/causal.rs:99-236", "backend/crates/runtime/qip-kernel/src/platform.rs:5335"]
  },
  {
    "name": "central::darkness",
    "path": "backend/crates/runtime/qip-kernel/src/central/darkness.rs",
    "kind": "runtime",
    "purpose": "ADR 0079: a dark region is the centre's derived word for silence. Never a claim a cell can make (a crashed node publishes nothing); derived on every read from the instant the centre last heard from each cell versus CentralConfig::region_dark_after, and never stored as a flag — only the transition is journalled.",
    "key_contracts": ["pub struct LastHeard", "pub struct RegionDarkness", "pub enum RegionTransition", "pub struct RegionWentDark", "pub struct RegionSpokeAgain"],
    "workspace_deps": ["qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "use crate::central::darkness::{RegionSpokeAgain, RegionTransition, RegionWentDark} at platform.rs:37; CentralPlane::region_darkness/dark_regions/region_transitions (plane.rs:1141,1178,1203) called from platform.rs:5068 during the cycle.",
    "io_and_state": "None; purely derived from LastHeard timestamps already recorded per cell report; journalled transitions only (not the derived state itself).",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/central.rs's darkness section (functions dark_set, plane_with_dark_window, a_region_dark_window_is_refused_at_zero_and_past_the_envelope_ceiling_and_admitted_between, a_region_silent_past_the_window_is_dark_on_the_next_read_and_not_before, the_platform_journals_region_dark_from_the_act_stage_and_region_lit_at_the_report_that_cleared_it)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Derived-not-stored design verified by tests/central.rs; wired into the cycle and into qip-api's web.rs BreakDirection surface indirectly.",
    "blueprint_capabilities": ["MESH", "OBS"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/darkness.rs:39-160", "backend/crates/runtime/qip-kernel/src/platform.rs:37,5068", "backend/crates/runtime/qip-kernel/tests/central.rs:6034-6283"]
  },
  {
    "name": "central::dna",
    "path": "backend/crates/runtime/qip-kernel/src/central/dna.rs",
    "kind": "runtime",
    "purpose": "The sealed bundle (compiled program, features, model/evidence provenance, commit bound, stop conditions) the centre ships to a cell so a partitioned cell can keep trading correctly. StrategyDna::seal is the only constructor and refuses a stage that qip_contracts::gate::GateStage::holds_capital says cannot hold capital.",
    "key_contracts": ["pub struct DnaPayload", "pub struct StrategyDna { pub fn seal(..) -> Result<StrategyDna>, pub fn verify(&self, key, now) -> Result<()> }"],
    "workspace_deps": ["qip_compliance", "qip_contracts", "qip_core", "qip_lifecycle", "qip_strategy"],
    "reached_from_production": "tests-only",
    "production_evidence": "NONE FOUND for the transport path. CentralPlane::ship (plane.rs:1985, calls StrategyDna::seal) and CentralPlane::verify_dna (plane.rs:2010, calls dna.verify) have zero callers across all of crates/apps/*/src — `rg -rn '\\.ship\\(|verify_dna' crates/runtime/qip-kernel/src/platform.rs crates/apps` (excluding tests) returns nothing. StrategyDna and DnaPayload are re-exported from lib.rs (via central::mod.rs) but no deployed binary constructs or verifies one. Note separately: crates/apps/qip-edge-node (the actual cell process that would receive a DNA) does not depend on qip-kernel at all (confirmed via `grep -rl qip_kernel crates/apps` — qip-edge-node/src is absent from the result list), so even architecturally there is no wired transport from CentralPlane::ship to an edge cell today.",
    "io_and_state": "None; a pure signed/serialisable bundle (qip_contracts::governance::Provenance) with no I/O of its own.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised only in tests/central.rs:485-551,1234-1235 (seal/ship/verify_dna/tamper-detection tests)"]},
    "status": ["exists", "tests-only", "requires-migration"],
    "status_reason": "A structurally-enforced, type-safe sealing/verification mechanism (cannot construct below pilot stage) that is fully tested but has no production caller and no wired transport to qip-edge-node, which does not even depend on qip-kernel. This is the DNA-shipping half of ADR 0008 'cells decide alone' described as built in module docs, but it is not reachable from any deployed process today.",
    "blueprint_capabilities": ["MESH", "SEC", "CONTRACT"],
    "gaps_observed": ["central/plane.rs:1985 `pub fn ship` and :2010 `pub fn verify_dna` have zero non-test callers anywhere in the workspace.", "qip-edge-node (the process that would consume a StrategyDna) does not depend on qip-kernel at all, so there is no code path — wired or not — by which a sealed DNA reaches a cell in any deployed binary."],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/dna.rs:78-136", "backend/crates/runtime/qip-kernel/src/central/plane.rs:1985,2010", "backend/crates/runtime/qip-kernel/tests/central.rs:524-525,1234-1235"]
  },
  {
    "name": "central::episodic",
    "path": "backend/crates/runtime/qip-kernel/src/central/episodic.rs",
    "kind": "runtime",
    "purpose": "Producer of blueprint §41.5's shipping-payload slot 4 (episodic digest), the third of the whitelist audit's three named-unfillable slots to gain a producer — over Platform::pending_episodes' open drafts.",
    "key_contracts": ["pub enum EpisodicOutcome", "pub struct EpisodicIssue { pub fn derive(episodes, len, now) -> Result<EpisodicIssue> }"],
    "workspace_deps": ["qip_ai", "qip_contracts", "qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "EpisodicIssue::derive(self.episodes.episodes(now), self.episodes.len(), now)? called at platform.rs:5223; EpisodicIssue also directly imported by qip-api/src/web.rs:19.",
    "io_and_state": "None; derives from the episodic memory Platform already holds.",
    "tests": {"count": 5, "notable": ["a_process_that_has_formed_no_belief_ships_no_prior_however_many_drafts_are_open (belief-pattern test reused for episodic per module family)", "the closest true episodic tests are internal to this file"]},
    "status": ["exists", "hardened"],
    "status_reason": "Third slot closed; wired into the cycle and exposed to qip-api directly.",
    "blueprint_capabilities": ["REASON", "MESH"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/episodic.rs:76-97", "backend/crates/runtime/qip-kernel/src/platform.rs:5223", "backend/crates/apps/qip-api/src/web.rs:19"]
  },
  {
    "name": "central::factory",
    "path": "backend/crates/runtime/qip-kernel/src/central/factory.rs",
    "kind": "runtime",
    "purpose": "Registers strategy candidates and walks them up the promotion ladder using qip_lifecycle primitives it owns none of the rules for: AuthorisedPromotion for whether a promotion may happen, the rung's own gate for whether evidence is enough, DemotionMonitor for pushing a live strategy back down.",
    "key_contracts": ["pub struct StrategyCandidate", "pub struct StrategyReview", "pub struct StrategyFactory { fn register, fn candidate, fn promote, fn stage_of, fn ledger, fn candidates }"],
    "workspace_deps": ["qip_ai", "qip_contracts", "qip_core", "qip_lifecycle", "qip_observability", "qip_optimization_engine", "qip_strategy"],
    "reached_from_production": "yes",
    "production_evidence": "StrategyFactory::promote called at crates/apps/qip-deepbrain/src/evolution.rs:925 (before the file's #[cfg(test)] boundary at line 1523); factory() accessor used at qip-api/src/mesh.rs:615,910, qip-api/src/console.rs:411, qip-api/src/routes.rs:3148,3204,3859, qip-api/src/ledger_views.rs:297.",
    "io_and_state": "None; owns a LifecycleLedger and DemotionMonitor state in memory, journalled through the platform event log by its callers.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/foundry.rs (11) and tests/central.rs"]},
    "status": ["exists", "hardened"],
    "status_reason": "Reached directly from qip-deepbrain's production evolution/promotion path and from multiple qip-api read surfaces.",
    "blueprint_capabilities": ["GOV", "CAPITAL", "EXPAND"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/factory.rs:51-243", "backend/crates/apps/qip-deepbrain/src/evolution.rs:925", "backend/crates/apps/qip-api/src/mesh.rs:615"]
  },
  {
    "name": "central::foundry",
    "path": "backend/crates/runtime/qip-kernel/src/central/foundry.rs",
    "kind": "runtime",
    "purpose": "Joins qip-evolution's generated strategies to central::factory's promotion ladder — before this module existed, both crates were dependencies of qip-kernel but neither was named by a line of its code, so a search could run for a thousand strategies without any reaching a gate. Preserves the trial count across the handoff so multiple-comparisons accounting is not lost.",
    "key_contracts": ["pub struct FoundryRound", "pub struct StrategyFoundry", "pub struct HoldoutInputs", "pub fn recorded_manifest(subject, bars) -> Result<DatasetManifest>"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_evolution", "qip_lifecycle", "qip_market", "qip_strategy"],
    "reached_from_production": "yes",
    "production_evidence": "Directly imported and used in qip-deepbrain: succession.rs:67 `use qip_kernel::central::foundry::StrategyFoundry;`, evolution.rs:48 `use qip_kernel::central::foundry::{HoldoutInputs, StrategyFoundry, recorded_manifest};`.",
    "io_and_state": "None; joins two in-memory sources.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/foundry.rs (11 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed a real join gap (two composed crates never actually meeting in code); reached from qip-deepbrain's evolution and succession paths.",
    "blueprint_capabilities": ["EXPAND", "GOV"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/foundry.rs:84-363", "backend/crates/apps/qip-deepbrain/src/succession.rs:67", "backend/crates/apps/qip-deepbrain/src/evolution.rs:48"]
  },
  {
    "name": "central::horizon",
    "path": "backend/crates/runtime/qip-kernel/src/central/horizon.rs",
    "kind": "runtime",
    "purpose": "The seam between qip-lifecycle's capital veto on rungs that hold capital and qip_optimization_engine::horizons' §23.4 pool arithmetic (four pools summing to one total exactly once, unfunded commitment liability charged to the reserved pool). Lives in the runtime because two services need introducing and only the runtime may do that.",
    "key_contracts": ["pub struct HorizonClaim", "pub struct HorizonPolicy", "pub struct HorizonArming", "pub struct PoolReconciler", "pub struct UnarmedHorizons"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_lifecycle", "qip_optimization_engine"],
    "reached_from_production": "yes",
    "production_evidence": "self.central.arm_horizons(liability, drawdown, now) called at platform.rs:11905; HorizonPolicy directly imported at qip-deepbrain/src/main.rs:50 `use qip_kernel::central::{CentralConfig, HorizonPolicy};`.",
    "io_and_state": "None; pool arithmetic over in-memory grant/liability state.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/horizon_seam.rs (9 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Wired into the cycle (arm_horizons) and into qip-deepbrain's composition root directly.",
    "blueprint_capabilities": ["CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/horizon.rs:92-566", "backend/crates/runtime/qip-kernel/src/platform.rs:11905", "backend/crates/apps/qip-deepbrain/src/main.rs:50"]
  },
  {
    "name": "central::insights",
    "path": "backend/crates/runtime/qip-kernel/src/central/insights.rs",
    "kind": "runtime",
    "purpose": "Cross-cell insight released only through qip-confidential's aggregate gate, or not at all. Before this module qip-confidential (54 tests) was reachable from nothing in the kernel. Rule: no method here ever returns a per-cell breakdown.",
    "key_contracts": ["pub struct CellInsights { pub fn new(seed) -> CellInsights, .. }"],
    "workspace_deps": ["qip_confidential", "qip_core"],
    "reached_from_production": "yes",
    "production_evidence": "insights: crate::central::insights::CellInsights::new(config.seed) constructed at platform.rs:4441 during Platform assembly; used at platform.rs 3 call sites.",
    "io_and_state": "None; wraps qip-confidential's aggregate-release-gate arithmetic over in-memory cell state.",
    "tests": {"count": 0, "notable": ["0 internal #[test] in this file; the underlying qip-confidential crate carries its own 54 tests per module doc"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed the 'reachable from nothing' gap named in its own module doc; constructed at assembly time on every Platform.",
    "blueprint_capabilities": ["SEC", "OBS"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/insights.rs:47", "backend/crates/runtime/qip-kernel/src/platform.rs:4441"]
  },
  {
    "name": "central::learning",
    "path": "backend/crates/runtime/qip-kernel/src/central/learning.rs",
    "kind": "runtime",
    "purpose": "The learn edge: what a strategy was promoted on versus what it realised, using assess_overfitting (pilot baseline as in-sample, live returns as out-of-sample) and qip_lifecycle::DemotionMonitor to turn a bad answer into a demotion with no human in the call. Uncertainty ratchets up, never down, in the allocator's standard error.",
    "key_contracts": [
      "pub struct CellOutcome", "pub enum LearningVerdict", "pub struct StrategyLearning",
      "pub enum DispositionInstruction", "pub struct DispositionedPosition", "pub struct RetirementDisposition",
      "pub struct PositionDiscrepancy", "pub struct DispositionRefused", "pub enum DispositionOutcome",
      "pub struct LearningReport"
    ],
    "workspace_deps": ["qip_ai", "qip_contracts", "qip_core", "qip_events", "qip_lifecycle", "qip_simulation_engine"],
    "reached_from_production": "yes",
    "production_evidence": "Reached every cycle from LEARN via Platform::learn_from_cells (platform.rs:8109-8114), whose doc comment states plainly: 'the stage's call is the one a deployed process makes.' CentralPlane::live_outcomes (plane.rs:1504) feeds it the CellOutcome slice.",
    "io_and_state": "None directly; StrategyLearning/LearningReport results are journalled by the caller through the platform event log.",
    "tests": {"count": 1, "notable": ["a_review_the_ledger_retired_is_reported_as_retire_and_not_adapt", "further coverage in tests/central.rs and tests/ledger.rs"]},
    "status": ["exists", "hardened"],
    "status_reason": "Production doc comment explicitly names this as the deployed-process path (distinct from the tests-only pattern seen in central::dna).",
    "blueprint_capabilities": ["GOV", "CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/learning.rs:75-353", "backend/crates/runtime/qip-kernel/src/platform.rs:8097-8114"]
  },
  {
    "name": "central::models",
    "path": "backend/crates/runtime/qip-kernel/src/central/models.rs",
    "kind": "runtime",
    "purpose": "Where a fitted model (qip-training) becomes a model the ModelRegistry may inform a decision with. A fit with no skill must not become decision-eligible: FitDiagnostics::skill_verdict exists because in-sample error is not evidence on a boosted ensemble, only held-out tail error is.",
    "key_contracts": ["pub struct ModelRegistration", "pub fn register_fit(..) -> Result<ModelRegistration>"],
    "workspace_deps": ["qip_ai", "qip_core", "qip_training"],
    "reached_from_production": "yes",
    "production_evidence": "Directly imported and called from qip-deepbrain: learning.rs:130 `use qip_kernel::central::models::{ModelRegistration, register_fit};` (non-test code, per module doc 'writes the fit's own holdout').",
    "io_and_state": "None; registers a fit's skill verdict into the in-memory ModelRegistry.",
    "tests": {"count": 0, "notable": ["0 internal #[test]; exercised via tests/models.rs (5 tests) and tests/serving.rs (5 tests)"]},
    "status": ["exists", "hardened"],
    "status_reason": "Companion gap-closer to central::foundry (fits and factory candidates were both islands before); reached directly from qip-deepbrain's production learning path.",
    "blueprint_capabilities": ["MODEL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/models.rs:49-83", "backend/crates/apps/qip-deepbrain/src/learning.rs:130"]
  },
  {
    "name": "central::realised",
    "path": "backend/crates/runtime/qip-kernel/src/central/realised.rs",
    "kind": "runtime",
    "purpose": "What each strategy realised at each cell, session by session, built solely from the centre's own settled attribution (never the cell's self-reported Utilisation figure, because two claims about the same P&L will disagree and a demotion argued from the cell's own figure would be circular). Feeds the demotion monitor's live-return series.",
    "key_contracts": [
      "pub const REALISED_SESSIONS: usize = 252", "pub struct RealisedSession", "pub struct RealisedSeries",
      "pub struct GrantedDay", "pub struct RealisedCalendar"
    ],
    "workspace_deps": ["qip_contracts", "qip_core"],
    "reached_from_production": "yes",
    "production_evidence": "CentralPlane::realised_loss_by_cell (plane.rs:1314) and CentralPlane::realised_calendar (plane.rs:1484) are called from Platform::run_cycle's family_structure path (platform.rs:14190, self.central.family_structure) and from realised_loss_by_cell consumers elsewhere in platform.rs.",
    "io_and_state": "Bounded to REALISED_SESSIONS=252 (one trading year), evicting the oldest — in-memory, journalled by callers.",
    "tests": {"count": 13, "notable": ["drawdown_is_measured_from_the_equity_high_water_mark", "the_series_keeps_a_trading_year_and_evicts_the_oldest", "a_strategys_day_sums_the_cells_that_granted_it"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed a gap where the demotion trigger's input was never computed in a deployed process; bounded working set; densely tested (13 tests).",
    "blueprint_capabilities": ["CAPITAL", "LEDGER"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/realised.rs:57-427", "backend/crates/runtime/qip-kernel/src/central/plane.rs:1314,1484", "backend/crates/runtime/qip-kernel/src/platform.rs:14190"]
  },
  {
    "name": "central::regions",
    "path": "backend/crates/runtime/qip-kernel/src/central/regions.rs",
    "kind": "runtime",
    "purpose": "ADR 0039 option (a): partitions a region's grant into disjoint per-cell shares before anything ships, refusing (never scaling) a plan whose cells' shares would together exceed the region's grant — closing the gap where two nodes under one regional grant held two amounts nothing summed.",
    "key_contracts": ["pub struct RegionMembership", "pub struct RegionShare", "pub struct RegionShares", "pub enum ManifestDecision", "pub struct GrantManifests", "pub fn partition(plan, membership, envelopes, frozen, now) -> Result<RegionShares>"],
    "workspace_deps": ["qip_capital", "qip_contracts", "qip_core"],
    "reached_from_production": "yes",
    "production_evidence": "partition(..) called at plane.rs:1784 inside CentralPlane::region_shares, itself called from Platform::issue_region_shares (platform.rs:5153) called from qip-api/src/mesh.rs:815 (platform.issue_region_shares(cells.iter().map(String::as_str), membership, now)); RegionMembership::parse used directly at qip-api/src/mesh.rs:225.",
    "io_and_state": "None; refuses rather than scales an over-committed plan (structural guarantee, not operator discipline).",
    "tests": {"count": 2, "notable": ["a_membership_is_read_from_a_committed_declaration_and_refuses_what_it_cannot_file", "a_refused_plan_withholds_every_cell_with_the_refusal_and_no_cell_gets_a_default_region"]},
    "status": ["exists", "hardened"],
    "status_reason": "Reached from a live qip-api endpoint (mesh.rs) all the way through to the partition arithmetic; refusal-not-scaling verified by test.",
    "blueprint_capabilities": ["CAPITAL", "MESH"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/regions.rs:73-418", "backend/crates/runtime/qip-kernel/src/central/plane.rs:1764-1784", "backend/crates/apps/qip-api/src/mesh.rs:72,225,815"]
  },
  {
    "name": "central::structure",
    "path": "backend/crates/runtime/qip-kernel/src/central/structure.rs",
    "kind": "runtime",
    "purpose": "Blueprint §23.1 LEVEL 1 measured on this platform's own realised returns: qip_optimization_engine::families clusters strategies on correlation inside a stated stress window (refusing full-sample clustering, since independence in calm markets can hide co-movement in drawdowns) — was complete and tested with no caller until central::realised gave it one series per strategy per calendar.",
    "key_contracts": ["pub const CLUSTERING_WINDOW: usize = 120", "pub const STRESS_QUANTILE: f64 = 0.10", "pub struct FamilyStructureJournal", "pub fn measure(calendar) -> Result<Option<FamilyStructureJournal>>"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_numerics", "qip_optimization_engine"],
    "reached_from_production": "yes",
    "production_evidence": "CentralPlane::family_structure (structure.rs:214, an impl on CentralPlane) called at platform.rs:14190 `self.central.family_structure(now)` inside Platform::run_cycle; FamilyStructureJournal held as Platform state at platform.rs:590,2484.",
    "io_and_state": "None; measures over the RealisedCalendar central::realised already assembles in memory.",
    "tests": {"count": 6, "notable": ["a_corpus_without_an_aligned_window_is_measured_as_an_absence_not_a_problem", "a_population_over_the_clustering_bound_is_refused_before_the_merge", "a_strategy_flat_through_the_stress_window_is_excluded_and_counted"]},
    "status": ["exists", "hardened"],
    "status_reason": "Was complete-but-uncalled ('had no caller' per its own module doc) until central::realised closed the gap; now wired into every cycle's LEARN stage.",
    "blueprint_capabilities": ["RISK", "CAPITAL"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/structure.rs:107-224", "backend/crates/runtime/qip-kernel/src/platform.rs:590,14190"]
  },
  {
    "name": "central::whitelist",
    "path": "backend/crates/runtime/qip-kernel/src/central/whitelist.rs",
    "kind": "runtime",
    "purpose": "Producer of blueprint §41.5's shipping-payload slot 8 (cycle whitelist), derived from single-instrument opportunities DISCOVER/REASON carry rather than any native cycle/path/pair type (the centre has no pair list) — turned into structured conversions/start_sizes the edge node's arbitrage.rs consumes.",
    "key_contracts": ["pub const MAX_MARKETS: usize = 128", "pub struct WhitelistedVenue", "pub struct WhitelistedMarket", "pub struct ArbitragePolicy", "pub enum WhitelistOutcome", "pub struct WhitelistIssue"],
    "workspace_deps": ["qip_contracts", "qip_core", "qip_events"],
    "reached_from_production": "yes",
    "production_evidence": "self.central.cycle_whitelist_for(cell, now)? called at platform.rs:5165; ArbitragePolicy directly imported at qip-api/src/main.rs:29 `use qip_kernel::central::{ArbitragePolicy, CentralConfig, CentralPlane};`.",
    "io_and_state": "None; MAX_MARKETS=128 bounds the whitelist size; no I/O.",
    "tests": {"count": 6, "notable": ["a_market_yields_one_conversion_out_of_each_of_its_instruments", "a_policy_that_sizes_its_own_funding_instrument_is_refused", "a_grant_for_another_strategy_cannot_size_the_desk"]},
    "status": ["exists", "hardened"],
    "status_reason": "Closed a gap where every deployed payload shipped this slot unproduced; wired into the cycle and into qip-api's composition root.",
    "blueprint_capabilities": ["MESH", "EXEC"],
    "gaps_observed": [],
    "evidence": ["backend/crates/runtime/qip-kernel/src/central/whitelist.rs:358-663", "backend/crates/runtime/qip-kernel/src/platform.rs:5165", "backend/crates/apps/qip-api/src/main.rs:29"]
  }
]
