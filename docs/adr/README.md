# Architecture decisions

> **The registers these decisions cite were consolidated on 2026-09-07.**
> Nineteen status documents — `completion-plan.md`, `PROJECT-PLAN.md`,
> `gap-matrix.md`, `algorik-blueprint-traceability.md`,
> `integration-truth-pass.md` and fourteen others — were replaced by the single
> `../DELIVERY-STATUS.md`, which scores all 181 numbered blueprint sections.
>
> **The ADR bodies below still name the old files, deliberately.** An ADR
> records a decision made at a time, on the evidence available then; rewriting
> its citations to point at a document that did not exist would be
> anachronistic, and in at least one case the mechanical substitution produced
> a sentence asserting what the new document says about a line — a claim nobody
> had checked. Read a citation to a deleted register as "the register that said
> this when the decision was taken", and read current status only in
> `../DELIVERY-STATUS.md`.

Each record states what was decided, what it costs, and what would make it
wrong. A decision with no stated cost has not been thought about, and one with
no stated reversal condition cannot be revisited honestly.

| # | Decision |
|---|---|
| [0001](0001-rust-everywhere.md) | Rust for everything, including the web interface |
| [0002](0002-two-dependencies.md) | Two third-party dependencies |
| [0003](0003-paper-trading-by-default.md) | Paper trading by default, with a deployment ceiling |
| [0004](0004-capability-gated-agents.md) | Agents reach facilities only through capability gates |
| [0005](0005-confidence-is-arithmetic.md) | Confidence is computed, never assigned |
| [0006](0006-classical-baseline-always.md) | A classical baseline for every quantum result |
| [0007](0007-exact-attribution.md) | Attribution must reconcile exactly |
| [0008](0008-edge-cells-decide-alone.md) | Edge cells decide alone, on capital granted in advance |
| [0009](0009-tiered-dependency-policy.md) | A tiered dependency policy, so the core stays at two |
| [0010](0010-what-gets-deployed.md) | Four of the six application crates are deployed, and the other two are not |
| [0011](0011-everything-in-rust-on-kubernetes.md) | Everything is Rust on Kubernetes; IBM Quantum is the only integration — *Kubernetes half superseded by 0024; the Cloud Run runtime is applied in `dev` and no execution node exists anywhere, and the GKE cluster that now exists is 0036's control plane, which runs no trading binary* |
| [0012](0012-where-a-library-earns-its-place.md) | A dependency is admitted only where getting it wrong is silent, the problem is specialist, and a maintained implementation exists |
| [0013](0013-identity-verification-earns-a-dependency.md) | Verifying an identity token earns a dependency; issuing sessions does not |
| [0014](0014-one-design-system-four-surfaces.md) | One design system, four surfaces, no second router |
| [0015](0015-licensed-templates-are-the-visual-source-of-truth.md) | The licensed templates are the visual source of truth |
| [0016](0016-repository-layout.md) | One layout: backend, frontend, data, infrastructure — each top-level directory answers one question |
| [0017](0017-gitops-delivery.md) | Delivery becomes GitOps: a Helm chart, Argo CD sync, Kargo promotion — *superseded by 0024, then re-taken by 0036 on a control-plane cluster without the chart; the text stays as the record of the first attempt. `deploy.yml` no longer moves a service either — the old status line's claim that it does was corrected on 2026-09-05, so neither the pull path nor the push path runs today* |
| [0018](0018-the-console-reaches-the-platform-over-the-vpc.md) | The console reaches the platform over the VPC, on an internal load balancer, as viewer |
| [0019](0019-identity-platform-is-the-only-identity-store.md) | Identity Platform is the only identity store, and the session is a sealed cookie |
| [0020](0020-two-runtime-topologies-and-the-order-to-resolve-them.md) | Two runtime topologies exist; neither is deleted, and the order to resolve them is fixed — *direction decided by 0022; the code executed under 0024, and the `dev` apply authorised by 0040 and done — `test`, `stage` and `prod` are untouched, and step 3's execution node still does not exist anywhere, so this record's reversal condition has not fired* |
| [0021](0021-the-blueprint-expects-live-capital-and-this-platform-refuses-it.md) | The blueprint expects live capital; the paper-trading boundary wins and the structure is built without it |
| [0022](0022-the-algorik-blueprint-is-the-architecture-of-record.md) | The Algorik blueprint is the architecture of record; Kubernetes and Next.js become transitional, and no live-capital path is authorised by that — *item 4 amended by 0081 on 2026-09-19: Leptos is retired as a non-goal and the TypeScript surfaces are no longer transitional; the body stays as the record of what was decided when* |
| [0023](0023-real-trading-is-the-destination-and-the-opening-is-gated.md) | Real trading is the destination and paper trading the harness; the opening is a ten-step gated sequence that this record does not authorise |
| [0024](0024-the-blueprint-runtime-is-provisioned-in-code-and-the-gitops-runtime-is-retired.md) | The blueprint runtime is provisioned in code and the GitOps runtime is retired; nothing was applied *by the commits this record describes*, and the `dev` apply has since happened under 0040 — this record's closing "the platform is not running anywhere" is corrected in place, and what remains unobserved is any scrape and any execution node — *partially superseded by 0036, for the controllers only: Argo CD and Kargo return on a control-plane cluster, the runtime and the paper-trading layers stand* |
| [0025](0025-the-rust-frontend-boundary-and-the-leptos-dependency.md) | *Half decided.* **Decided:** Leptos is a reversal of 0001 and 0012 and not a dependency request under 0009's edge tier, admissible only against 0001's own reversal condition and only by a record that supersedes both — accepted because it takes nothing and only narrows. **Still the owner's:** whether `qip-web` replaces the fifty-four TypeScript pages, which is weeks of work, re-scores §40 by name, and in its alternative would amend the boundaries rule, which is not an agent's file. Corrected 2026-09-05: both TypeScript surfaces *are* deployed and observed serving, so the surface to be retired is the one that runs and the surface replacing it is the one nobody outside the VPC can reach — **the open half is closed by 0081 on 2026-09-19**, on authority the owner delegated to the policy lane, by this record's own Option D; the decided half stands verbatim and is now 0081's reversal mechanism |
| [0026](0026-telemetry-export-bounded-opentelemetry-versus-prometheus-exposition.md) | **Resolved, and not by itself.** An OpenTelemetry crate stays rejected and none was added. Its Option (a) recommendation was superseded by 0028 (OpenObserve over OTLP, by owner instruction) and then refused on its own terms — the managed-Prometheus sidecar carries a CRITICAL `x/crypto` finding with no published tag to move to, twice scanned — so its own first reversal condition fired and Option (b)'s push shape became the only path off a Cloud Run service, taken by 0032. The drain is applied in the three central roots and inert everywhere (no deployment sets `QIP_OPENOBSERVE_URL`); the open half is a *producer* — nothing starts a span, so rule 74 is unmet and the ring the drain posts is empty |
| [0027](0027-concentration-limits-are-a-share-of-what.md) | A concentration cap is a share of **equity**, not of gross — option (a) taken and applied at `eca7ebb`: `LimitKind::MaxAxisWeight` replaces the two share-of-gross entries in `LimitSet::conservative_default` on the same axes at 0.35 and 0.60, so the first order into a fed book is admitted and a sector past the cap is still refused; `MaxConcentration` stays in the type, out of the default set, for §28.1's family cap |
| [0028](0028-openobserve-is-adopted-as-a-deliberate-deviation-from-section-2-1.md) | OpenObserve is adopted as the observability backend over OTLP, on ephemeral storage — a named, deliberate deviation from §2.1's Google Cloud/IBM-only managed-services rule, superseding ADR 0026's recommendation by explicit owner instruction; applied in `dev` and observed serving, which turns its `ignore_changes` exposure from predicted to live — the pinned image will not move on a plain apply and nothing is scheduled to `-replace` it |
| [0029](0029-the-normaliser-is-removed-rather-than-recorded-as-research-only.md) | `qip-normalization` is deleted rather than recorded research-only: nothing constructed it, its unmapped-symbol guard could not fire, and four documents cited it as a control — the gap it leaves is named here rather than disguised; applied: the crate, its workspace and dev-dependency lines and its benchmark are gone, the truth-loop suite owns its stage-4 identity table, and the workspace is 58 crates |
| [0030](0030-openobserve-is-reachable-anonymously-on-the-owners-instruction.md) | OpenObserve's own `run.app` URL answers the internet unauthenticated, on explicit repeated owner instruction, amending ADR 0028 decision 5 — the `allUsers` refusal becomes conditional rather than absent so no *other* workload can become anonymous by accident, the order path stays structurally unpublishable, and the cost is zero until the OTLP drain is wired, which is the trigger to revisit — corrected 2026-09-05: the service now exists and was observed serving anonymously, but no deployment sets `QIP_OPENOBSERVE_URL`, so it is still empty and the trigger has still not fired in fact; 0033 has decided what happens when it does and is not applied |
| [0031](0031-a-vendored-workload-may-take-a-secret-as-an-environment-value.md) | `modules/cloudrun` gains `secret_env`, refused unless `image_source` is `vendored` — OpenObserve's image carries no shell and no `_FILE` symbol for its credential, so the file mount satisfied the rule and was inert; the value still reaches no committed file, plan or state, and the crash-dump exposure it does leave is named rather than solved |
| [0032](0032-telemetry-reaches-a-collector-inside-the-vpc-not-a-service-on-the-internet.md) | Telemetry drains to a vendored, attested collector on a private address, not to a service on the internet — the fast brain is the forcing case, having no egress proxy by design because port 9102 routes to a model API and nothing on the fast path may consult one (ADR 0008); one image closes the fast-brain route, A6's collector and the pull path together, and stops operational history crossing the public internet |
| [0033](0033-openobserve-becomes-authenticated-before-it-holds-telemetry.md) | OpenObserve moves from anonymous to authenticated external access before the first byte of telemetry lands — firing the condition ADR 0030 set for itself; it stays reachable from the internet and stops being reachable by anyone, because an anonymous invoker is anonymous in both directions and a store anyone can write to cannot be evidence for a gate |
| [0034](0034-the-first-market-data-and-prediction-sources.md) | Coinbase for crypto first (keyless, so the transport is proven before any contract), then Alpaca for equities (its market data and its paper brokerage are one account, so vendor expectation and platform behaviour are the same sentence), then Kalshi for prediction — candidates only: `qip-data-finder`'s licensing gate decides, and these terms have not been read against a contract |
| [0035](0035-one-execution-node-in-shadow-mode.md) | One execution node, `us-east4`, shadow mode, dev only — the whole edge plane runs today only under `cargo test`, and one probe answers what is actually open where seven multiplies every first-deployment surprise by seven; partition behaviour is explicitly not tested by it — **accepted and not applied**: `execution_nodes = {}` in all four environments including `dev`, because no boot image exists and nobody has chosen the node's capital allocation, and both are a person's to supply. Corrected 2026-09-06: this row said the boot image was one "nothing in this repository builds", which stopped being true at `5ccaea9` — `.github/workflows/image.yml`, `infrastructure/images/execution-node/` and `modules/image-bake/` are the bake. The blocker is unchanged and reads the same way, so do not take the correction for progress: the workflow has never been dispatched (`actions/workflows/image.yml/runs` answers `total_count: 0`), `image_bake_subnet_cidr` is commented out so the module creates nothing, and `boot_image` has no value in any environment |
| [0036](0036-argo-cd-and-kargo-return-on-a-control-plane-cluster.md) | Argo CD and Kargo return, by owner instruction, on a GKE Autopilot control-plane cluster per environment that runs no trading binary: Config Connector `RunService` manifests replace `modules/cloudrun`'s service resource, Terraform keeps the foundation and releases the services with `removed {}`, Kargo promotes by commit with prod refused by policy, `deploy.yml` keeps build/scan/sign/attest and loses the rollout; the Cloud Run and Compute Engine runtime and the three paper-trading layers are untouched. Corrected 2026-09-05: **partly applied in `dev`** — the control plane's foundation exists (82 resources, run 34), the services were released from state undestroyed, and the cluster exists **tainted** because its node could not reach its own private endpoint through the zone's deny (run 37); no controller runs, no bootstrap has executed, no `RunService` has been reconciled and no promotion has happened, and clearing the taint is a person's act under 0040 |
| [0037](0037-hugging-face-is-the-first-hosted-language-model-provider.md) | Hugging Face Inference Providers is the first hosted language-model provider, by owner instruction: applied for the development lane (`scripts/model-gateway.mjs` preset, token from a file, payload screened, keyless probe) and *proposed* for the platform lane — a `HuggingFaceModel` behind `NumericGuard` through the egress proxy, constructible by the deep brain only, never the fast brain (ADR 0008, 0032); no environment sets it and no secret exists, so every reasoning run still narrates through templates |
| [0038](0038-passkeys-are-the-only-credential-and-passwords-leave-the-portal.md) | **Accepted with conditions, 2026-09-19** — and still with **no check run**: the deciding lane was told it had web-fetch, web-search and the Microsoft Learn tools and had none (four tool errors quoted verbatim in the record), and held no credential to the dev project. What it decided is that the four checks gate decision 2 alone — which surface verifies the passkey — so decisions 1, 3, 4, 5, 6 and every refusal are accepted on the tree's evidence, and decision 2 is accepted as a **table with one activation step**: one `passkeyEnrollment:start` probe against `algorik-dev`, three outcomes (`credentialCreationOptions` → Shape A; a refusing code → Shape B under amendment (ii), which also requires ADR 0042 applied; "works but preview" → stay on the deprecated password path, no fall-through), with its HTTP status and code — and nothing else — quoted into the status line. Two things earlier passes left open are decided: under Shape B a credential record is an event-log row with the signature counter derived by replay, never the claims blob; and the recovery code's HMAC lives as a field of `StoredProfileClaims` behind the one writer, single-use and **single-attempt**, which removes the counter nothing could hold. `qip-api`'s only gain is `Presence::Attested`, downstream of ADR 0042. The implementation brief for both lanes is `docs/architecture/adr-0038-implementation-brief.md`. *Previously proposed* — passkeys (WebAuthn) become the portal's only end-user credential and the password, forgot-password and reset-password flows are retired; Identity Platform verifies the passkey natively if four named checks pass (no dependency anywhere), otherwise the portal is the relying party and earns one npm dependency by ADR 0012's test, never a Rust crate; the session stays ADR 0019's sealed cookie; recovery is a second passkey or an operator-issued one-time re-enrolment code that opens only the enrolment ceremony, never a password and never TOTP; applied so far: sign-in copy and four deprecation comments only. **Stays proposed deliberately** — the shape turns on four calls against Identity Platform on the dev project that no reading of this tree can answer, and choosing between Shape A and Shape B without them would pick a credential design on no evidence; the four checks, run by the owner with their outputs quoted into the status line, are the only thing that closes it. **Re-examined 2026-09-06 and still proposed**: no documentation-search, web-fetch or web-search tool was reachable, so not one check is answered with a vendor citation and the two tool errors are quoted rather than summarised. What the tree settled instead decides the shape anyway — **Shape B as written is not viable**, because a credential list in the profile claim is replaced whole by `gipWriteProfile`'s `accounts:update` and the "two passkeys" cap is a byte budget whose largest term is the authenticator's own credential-ID length; decision 5's five-attempt counter has no store under either shape (ADR 0019 removed the console's); a Rust relying party is impossible until ADR 0043's CSPRNG gap is closed; and a credential does not survive a change of relying-party ID, so enrolling before the algorik.ai hostname buys a re-enrolment of every user. **Shape A is recommended**, with "exists, preview only" named as a third answer the record has no shape for and which does not fall through to Shape B by default **Check 1's documentation half was answered from the primary source later on 2026-09-19 and selects the table's third row**: the Identity Toolkit discovery documents (v1 and v2, revision 20260911) contain no passkey method or schema, the v2 REST reference and the release notes name none, yet the gateway routes all four methods (403/400 in step with a documented method; a fabricated method gets 404) — routed but undocumented, so the platform stays on the deprecated password path, does not fall through to Shape B, and the shape-independent work proceeds; what reopens it is the methods appearing in the discovery document. |
| [0039](0039-a-regions-grant-is-shared-across-its-cells-over-the-mesh.md) | **Accepted and applied in-process; not deployed** (corrected 2026-09-05 — it was marked *proposed* with three slices of applied work under it, which is itself the drift) — a region's grant is shared across its cells, one per process, as disjoint per-cell shares the centre computes from the allocation plan it already produces and signs into the `capital_grants` slot each cell already verifies; `RegionTable` becomes the cell's private view of its own share, opened unfunded and re-based only by a higher sequence, so two nodes under one grant cannot together exceed it and a partitioned cell keeps spending within its last share until its envelopes expire (ADR 0008 kept whole); a gossiped per-region ledger rejected as a consensus problem the blueprint does not ask for, an on-request re-balance absorbed into the payload cadence; costs idle share between payloads and a fresh node that sends nothing until first granted to. Applied across `qip-kernel`, `qip-edge`, `qip-edge-node` and `qip-api` with one stated deviation — the share travels **by reference**, as the manifest of grants whose gross the centre has already checked against the share, rather than as an explicit `RegionShare` field in `qip-contracts` — and proven end to end by five tests in `qip-acceptance/tests/region_share.rs`, one of which asserts a funded cell is still assembled paper-only; three of the four owner decisions are answered in the record beside the question, and only the payload cadence is open. Nothing is deployed: `execution_nodes = {}` everywhere, so no cell of any funding state runs in any project |
| [0040](0040-the-owner-authorises-the-agent-to-apply-dev-and-what-that-authorisation-cannot-reach.md) | The owner's instruction of 2026-09-05 to "deliver the entire thing", split into the part a record can carry and the part it cannot: the agent may dispatch `infra.yml` `dev` `up` once, after reading the plan and finding it destroys nothing beyond an IAM binding's replacement, amending the "a person applies" clauses of 0036 and 0037; the paper-trading boundary, ADR 0021's refusal of any signing or withdrawal path, and the reading of a vendor's terms are named as what no agent's record can move; amended the same day on the instruction's third repetition: dev may be re-dispatched after each fix until green, test and stage on the same terms once it is, prod and the rules-file boundary unchanged; amended 2026-09-13 on the fourth: dev `plan` and `up` are standing (`down` is not), a destroy is covered when a commit explains it *and* the owner has seen it listed, a tainted cluster is cleared by a person with state access and never by flipping the `deletion_protection` literal or making it an input |
| [0041](0041-venue-registration-is-one-operators-attributed-click-and-never-anonymous.md) | Two owner instructions of 2026-09-05 answered differently: the anonymous self-registering scraper is refused — a venue binds a key to an identity that accepted its terms, this repository puts licensing posture before use, and 0040 leaves the reading of a vendor's terms with the owner — and the approval-only flow under the company's own identity is accepted; a source needing an account is refused until an operator-attributed record exists, the record has one constructor that deserialisation goes through, the operator's click is the attestation journaled with the authenticated subject and replayable from the log, the signup job types only the mechanical form under a fresh approval and stops without retry at captchas, identity documents, tax identifiers, second factors, unapproved consent boxes and unknown fields, credentials reach Secret Manager and never a file, stdout, a response or a screenshot, and no flag enables anonymous account creation; the Alpaca and Kalshi terms, their true requirement level, the registrations mount and the first watched run stay the owner's |
| [0042](0042-the-console-proves-who-clicked-with-a-keyed-assertion-the-api-verifies.md) | *Proposed* — the console proves which human clicked by minting a per-request HMAC assertion the API verifies, closing ADR 0041's amendment gap in which every console approval is journaled as `operator@env`: one Secret Manager slot per environment mounted as a file to both processes, a domain-separated length-prefixed tuple over `{nonce, subject, authenticated_at, issued_at, method, path, body hash}` signed with Node's built-in `createHmac` and verified with `qip_core::hash::hmac_sha256` and `constant_time_eq` — no crate, no npm package, no new primitive; the assertion changes attribution only and never authority, the role still comes from the bearer credential and the kernel keeps its single `OperatorIdentity` door; two reversals of the sketch, a nonce with a bounded fixed-window seen-set because a body hash does not stop the same body being sent twice, and a requirement *derived from the key* rather than configured, so an unverifiable assertion is refused and never ignored and a deployment holding a key can never accept an unattributed approval; an unauthenticated header, asymmetric signatures, cookie verification at the API, ID-token verification, log correlation and per-human bearer tokens each rejected with reasons; nothing applied — no key, no slot, no code, and the forgery test that is the point of the mechanism has not been run |
| [0043](0043-the-cryptography-this-platform-has-and-the-three-gaps-no-crate-closes.md) | *Proposed* — no cryptography crate is admitted, and DEC-D2's question is answered by rejecting it: ADR 0009 never forbade in-tree cryptography (that prohibition exists only as one mis-cited line in a rules file), ADR 0002 authorises SHA-256 and HMAC by name, and the tree's primitives are real rather than placeholders — a complete SHA-256 and RFC 2104 HMAC in `qip-core/src/hash.rs`, verified against FIPS 180-4 and RFC 4231 vectors, with domain separation, length-prefixed signing strings, key-id binding, constant-time comparison, a 32-byte floor that refuses, and a start-up refusal of a live ceiling on a seed-derived key. What the contradiction hid is three unrelated gaps with three different answers: **no asymmetric signature** (every signer is also a verifier, so any cell holding the envelope key can mint capital for every region) — routed to Cloud KMS as a service call, the shape Binary Authorization already uses, blocked on the unapplied egress path rather than on the dependency policy, with cell-side public-key verification the one place a crate is the right answer and the day a node is deployed the day to ask; **no CSPRNG** (`Xoshiro256` is deterministic by design and there is no other source, so no Rust process here can mint a nonce) — answered by `/dev/urandom` through `std::fs` in a composition root, no crate and no primitive, with an explicit refusal to ever use `Xoshiro256` for a value that must be unguessable; and **an unanchored event-log chain** (unkeyed SHA-256, so a writer with file access recomputes it end to end and `verify_chain` returns `Ok`) — a custody problem needing a witness, not a primitive, and its own record. `sha2`+`hmac`, `ring`/`aws-lc-rs`, `ed25519-dalek` alone, hand-rolled Ed25519, the KMS `MacSign` API on the order path, and leaving DEC-D2 open each rejected with reasons; ADR 0002 gains a second reversal condition and a scope statement saying the affirmation covers a fixed-output primitive with published vectors and generalises to no signature scheme, cipher, key exchange or protocol parser. Costs named rather than hidden: a compromised cell can mint capital, no signature names a signer, and the log is tamper-evident against an outsider and not against its writer. Nothing applied — no crate, no lockfile change, no code; `constant_time_eq` still exists twice, and the gates were not run because no file a gate reads was changed and the session had no shell |
| [0044](0044-adr-0039s-four-open-decisions-on-cross-process-region-shares-are-taken.md) | *Proposed* — ADR 0039's four open decisions on cross-process region shares are taken, and three of them are ratifications of what a commit already decided rather than what an owner chose, which is the gap this record closes: **(1)** option (a) accepted as applied and the explicit `RegionShare` field in `qip-contracts` **declined rather than deferred** — a signed amount beside a manifest whose envelope gross the centre already checked against the same share is the "second signed path to the cell's bound" ADR 0039's own reversal conditions name, and the cost of declining is stated (the centre has two narrowing moves, renew smaller or withhold to zero, and no third in between); **(2)** unfunded at boot ratified, because ceiling-at-boot fails exactly during first boot, restart and partition-recovery — the only window anybody has planned to watch — and `QIP_REGION_ALLOCATION` is fixed as a ceiling that never funds, which is what makes ADR 0045's number a bound on a share rather than a balance; a bootstrap fraction rejected as an amount nobody computed and refuse-to-boot-without-a-share rejected as ADR 0008 consequence 2 inverted; **(3)** `QIP_MESH_REGIONS` at the API root ratified and ADR 0039's own `CentralConfig` recommendation overruled — membership and the served cell set are two halves of one invariant that `RegionMembership::covering` checks before the backbone opens, and configuration is read at composition roots and only there; deriving the grant from treasury on hand, and deriving membership from what cells claim about themselves, both refused on record; **(4)** the payload cadence is **not the free number the question assumed** — the share rides the payload by design, and the payload is pinned from below at sixty seconds by the fastest slot the cell actually reads, the cycle whitelist, which `Cell::cycle_whitelist` treats as absent the instant it is stale; the `capital_grants` slot's own hourly TTL is specifically the wrong clock. The genuinely open item is a **driver, not a value**: nothing schedules `POST /cycle` (no `google_cloud_scheduler` resource exists anywhere in `infrastructure/`) and the node's own loop runs one pass per health-port connection, so this platform has no cadence in the sense the question meant. Whether sixty seconds is *fast enough* is left to the ADR 0035 probe and is the half deliberately not taken. Nothing applied and nothing authorised: decisions 1–3 change no file, `QIP_MESH_REGIONS` is set nowhere, and `execution_nodes = {}` stays `{}` |
| [0045](0045-the-first-execution-nodes-capital-ceiling.md) | *Proposed* — `region_allocation = "1000000"` for the one `us-east4` node ADR 0035 authorises: one tenth of `PlatformConfig::initial_equity`, whose default is `Decimal::from_int(10_000_000)` and which no deployment overrides, chosen so seven equal ceilings sum to 70% of the desk's book rather than to all of it. It is a **ceiling and never a funding** — `apply_bound` is `share.min(self.ceiling)`, so it can only narrow the share ADR 0039's manifest derives — and it sits below the centre's `default_per_cell` (half the book, so it *can* fire) and below `per_strategy` (a quarter, so it *will*, which is argued rather than excused: a quarter of the desk in one region is the aggregate concentration ADR 0008 exists to prevent, and `bound` and `ceiling` are published as separate fields so an operator can tell which one refused). Rejected: the whole book (a control that cannot fire), `default_per_cell` (a second claim about a fact the centre holds, and it can only fire after the limit it backs), the book divided by seven (a non-terminating fraction, and seven equal sevenths admit the whole desk), and the module README's `"250000"` (a sample, and it would make the backstop rather than the envelope the binding constraint the probe observes). The failure it is the last defence against is verified and live: with `QIP_MESH_REGIONS` unset — and no deployment sets it — `qip-api` ships every live grant to every cell, so a cell's derived share is the whole grant book. Scalar per node entry, not a per-region table: `var.execution_nodes` is already the table and ADR 0039 already partitions regions at the centre. **Nothing is applied** — `execution_nodes = {}`, no tfvars edited, and ADR 0035's *other* blocker is untouched: the bake has never been dispatched and `boot_image` has no value anywhere, so this record does not by itself deploy a node. 0043 and 0044 were left free deliberately for records being written in parallel |
| [0046](0046-the-internal-crossing-cap-is-measured-over-sixty-seconds-of-wall-time.md) | *Proposed* — DEC-D3/PHASE-B13 answered: §27.1's forty percent internal-crossing cap is measured over `CrossingInterval::Span(60s)` of wall time, per instrument, set at `qip-edge-node`'s composition root. **`Passes(n)` is rejected on the shape of the guarantee**: the node has no scheduler and `run_pass` runs once per accepted TCP connection on its health port, whose only committed periodic caller is the Ops Agent at `scrape_interval: 30s`, so a `Passes` window's wall-clock length would be owned by a monitoring config — a risk parameter changeable by someone who does not know they are changing it. The failure the cap prevents, named from the code rather than from the blueprint: `book_cross` moves `strategy_positions` and `strategy_cash` between two of the platform's own strategies at a mid no venue confirmed and deliberately leaves `positions` alone, so a crossed price is adjudicated by nobody — the drop-copy reconciliation compares fills and a cross is not a fill — while the diverging strategy books are the ones the LEARN stage scores. Sixty is fixed by four converging bounds: it is the cycle whitelist's TTL, the fastest clock the cell reads, so the window spans one strategy population rather than two; twelve times the five-second book staleness past which the cell's own prices stop counting; three orders of magnitude clear of `MAX_CROSSING_WINDOW_SAMPLES = 1_024`, above which the cap becomes a refuse-all under `internal_cross_window` that reads in the log like a ratio firing; and it lands the realised crossing ratio at 33.3% — inside §27.1's stated 20–40% band — by continuing the arithmetic of the `Passes(3)` test trace, which is **a derivation and not a measurement and is labelled as such**. Today's unset default is safe by refusing §27.1's flagship case (a full cancellation is over the cap by arithmetic, so two strategies that disagree are never settled against each other), which is less than the blueprint asks for. Leaving it unset, any `Passes(n)`, an hour-long span matching the `capital_grants` TTL, a five-second span, a per-instrument table, and a runtime-derived interval each rejected with reasons. **The gap that limits it, stated rather than buried:** no published series carries the realised crossed volume — `qip_edge_internal_crosses_total` is a count by venue, `qip_edge_netting_ratio` has a different denominator — so the parameter would be tunable only by refusal counts, and the series belongs in the same change. Nothing applied: no root sets an interval, and `execution_nodes = {}`. Drafted as 0043 and renumbered after a parallel record took that number; the void stub this entry said "should be deleted" no longer exists (checked 2026-09-06 — `ls docs/adr/0043*` returns only the cryptography record), so the instruction is withdrawn |
| [0047](0047-the-reasoning-bar-caps-the-consequence-of-a-decision-not-the-belief-in-it.md) | *Proposed* — a **ratification**, in ADR 0044's shape: the wiring of `PlatformConfig::reasoning_confidence_bar` is already in the tree and the commit left open whether it needed a record. **Half of it did, and not the half a reader would guess.** Making a documented control readable repairs the `MaxExpectedShortfall` shape and takes no new position; choosing *which number* the requirement is does, and that choice was argued in a seventeen-line call-site comment while three files in the tree disagreed about the same field — its own documentation described an operator control, `reason_decision_context` passed `rank.confidence` under a comment arguing for that, and a fastbrain test asserted the field governed a stage it never touched. Decided: the requirement is the decision's **consequence** (`rank.importance`), not the platform's belief in it, because credibility is already inside `value_at_stake` (`share = importance * confidence`) and counting it twice lowered the budget and the bar together, while `Incapable` is the only verdict that climbs so a requirement can never decline an incredible observation; the default stays 0.90 as the panel's own resolving power; a bar outside `(0, 1]` stops the decision and names the field rather than being swallowed by `min`. **The spend did not change and the record says so**: the panel convenes regardless of the placement whenever it is affordable, and the ledger charges `convened` rather than `tier`, so the 750-fold cost gap between `tiny_model` and `multi_agent_reasoning` never reached the ledger. What did change is a measurement — the gap between the rung placed and the rung run is documented as "the measured case for building the cheaper rung", and capping the requirement at the panel's own resolving power narrows the population in which that gap can appear. Uncapped importance (unroutable at exactly the most consequential observations), keeping `confidence`, clamping, deleting the field, and writing nothing each rejected with reasons; no register row existed for a change that had already landed, and one was created on 2026-09-06 as **DEC-D14** in `../plan/PROJECT-PLAN.md`, with that file's row count and awk tally re-run (57 → 58) rather than left to disagree with itself; accepting or rejecting this record stays the owner's |
| [0048](0048-adr-0023-carries-both-the-permission-and-the-prohibition-for-step-3.md) | *Proposed*, and **deliberately deciding nothing** — DEC-D10's conflict is **inside ADR 0023**, not between the record and the tree: the same accepted record says steps 1–4 "are buildable today", orders step 3 after a Phase 2 gate that has never been attempted, names execution infrastructure built before that gate as its own worst reversal condition, and requires recorded approval naming each step before it begins, which nothing satisfies. Step 3's components are built with a register row and a commit each (feasibility gate, attribution join, inventory reservation, risk aggregates, netting, the cell's hot path) and **step 3's own evidence clause is not met** — "shadow mode running against the simulator and reconciling" reaches no process, because `execution_nodes = {}` everywhere — so "overtaken in practice" overstates it in the direction that matters. Three readings set out with their costs (the permission governs, and the strongest sentence stops refusing anything; the prohibition governs, and an uncurable violation trains readers to skip findings while the approval clause is discredited exactly where steps 5–8 need it; harmonise by scope and supply the missing approval record); the choice changes a decision rather than a description, so it is left to the owner. Proposes an annotation to ADR 0023 that asserts only facts. **Unverified and named as such**: whether ADR 0023 pre-dates or post-dates the step 3 commits, which the session had no shell to establish and which decides whether D10 is a decision or a description fix |
| [0049](0049-the-blueprint-s-section-48-toolchain-is-scored-as-three-rows-and-adopting-any-of-it-is-a-separate-decision.md) | *Proposed* — DEC-D11 was two questions in one row, and the scoring half is answerable from the tree today. The inconsistency is **between two registers carrying the same id**: `deployed-vs-blueprint.md:648` records an *assumption* ("Transitional … `gcloud run deploy` and a MIG update stand in for Cloud Deploy's rollout") in a table headed "Decisions this document does not make", while the project plan carries D11 as blocked-external — and the assumption's factual half is now false, because `deploy.yml:397-432` says the rollout "belongs to the reconciler now" and the job "no longer commits anything". Three rows drafted for the matrix owner and, on 2026-09-06, **written into `docs/architecture/deployed-vs-blueprint.md`** — replacing the single `ABSENT` line and correcting D11's stale assumption in place; `algorik-blueprint-traceability.md` still has no §48 rows and that half stays the matrix owner's. Each scores the **tool** and the **property** separately: rule 77's OpenTofu is TOOL-SUBSTITUTED with every property met (plan reviewed before apply, `backend "gcs"` state, GCP-only providers); §48's Cloud Build is **PARTIAL and not ABSENT** — it builds the portal and the landing through `scripts/deploy-frontends.sh`, which the existing "ABSENT" score hides; Cloud Deploy is absent and its guarantee is **not reproduced** by ADR 0036's Kargo/Argo CD, which proves a revision before traffic and measures no error rate and rolls nothing back — and none of it runs. Adoption of all three left open with costs, and the useful finding is that **Cloud Deploy is blocked on observability rather than on a decision**: automatic rollback on an error rate needs an error rate, which needs the ingestion ALIGN-A6 is blocked on. Scoring the lines CONTRADICTS, scoring them ALIGNED, copying the stale assumption, and one row instead of three each rejected |
| [0050](0050-what-an-option-quote-source-must-satisfy-before-the-volatility-surface-has-a-caller.md) | *Proposed*, **naming no vendor and accepting nobody's terms** — the volatility surface (§16.1, engine 13) is the one valuation engine reached by nothing, and that is a data source with a licensing evaluation ahead of it rather than neglect. Sets out the gate every source meets (`admission.rs`: an entry or refusal by name, manifest/catalogue class agreement, **both** `Usage::Derive` and `Usage::Trade` — stricter than ADR 0034's own "research-only may be sufficient here" sentence, because ADR 0023's destination means a ceiling change would promote such a source onto live trading — membership of `KNOWN_SOURCES`, and ADR 0041's attributed registration), then six requirements specific to options: the chain reference and the quote are two records; **implied volatility is a model output and must be the platform's own**, because a vendor's IV carries a forward, rate and dividend assumption that do not arrive with it and cannot be reproduced from the log; **synchrony across the strike chain**, because `VolatilitySurface::new` takes one `as_of` and one `forward` for every point and cannot detect a smear; an honest `publication_delay_ms`, on the Frankfurter precedent; a class that constrains the console before a page is written; and never `Synthetic`, whose `allows_production_decisions` is false. Five classes of source with what each costs — venue of record, broker-bundled, redistributor, delayed/end-of-day published series, and the platform's own model — with the recommendation that the first candidate evaluated be a **published end-of-day series**, for the reason `api.frankfurter.dev` is the only source ever proven end to end here: it had no licensing barrier, so the transport, the manifest, the stamps and the gate are provable before any commercial conversation, and an end-of-day surface is a smaller claim than the engine can carry. Wiring the surface to synthetic vols, deleting it on ADR 0029's precedent (rejected on the distinction 0029 drew: nothing cites this engine as a control), accepting a research-only options licence, taking vendor greeks, naming a vendor now, and building the connector before the evaluation each rejected |
| [0051](0051-the-two-unbound-custody-attestations-are-bound-to-a-content-digest-and-a-policy-fingerprint.md) | *Accepted* — §37.4 requires three enforcement points to agree before capital leaves a venue, and `EnforcementPoints::all_agree` proved only that three **spoke**. An `Attestation::reference` was validated for being non-empty and read by nothing, except the venue allowlist's, which an earlier change bound to the destination. So an approval could name three identities as having agreed to a movement while two of them had agreed to nothing in particular — the shape `risk-and-execution.md` names by `MaxExpectedShortfall`: a control that cannot fire reads as protection and is not. Both remaining references are bound. The transfer gate's binds to a new `assessment::AssessmentId`, the SHA-256 of the corridor, source, destination, amount and instant — **a content digest, because the record's `EventId` is minted by `FabricJournal::decide` after `TransferGate::assess` has returned and so does not exist at the seam that would check it**; balances, history, breaker and switch are deliberately excluded, because an attestation invalidated by a moving balance is a control nobody can satisfy. The custody policy's binds to `CustodyPolicy::fingerprint`, a canonical `BTreeMap`/`BTreeSet` digest of the whole table — **derived, not a stored `version` field, because a hand-set version is a second claim that will disagree with the table it certifies, and because a derived digest changes no wire shape on the hash-chained log**. `FabricRecord::SCHEMA_VERSION` goes to 3 and a version 2 record is refused by name: unlike version 1, it decodes perfectly, so re-judging it would file `gate_attestation_names_another_assessment` — a veto, which reads as a movement the gate declined rather than as a record this build cannot judge. Every existing fixture's reference strings are invalidated and derived instead; nothing is deployed, so no sealed version 2 record exists to migrate. The `EventId`, the prior chain head, a caller-supplied intent id, a `version` field on `CustodyPolicy`, and documenting the limitation each rejected. **Only refusals are added; the paper-trading boundary is untouched.** |
| [0052](0052-blueprint-rule-17-is-corrected-a-deterministic-gate-must-be-able-to-approve.md) | *Proposed* — §56.2 rule 17 said "neither gate ever returns approval"; `qip-risk-engine::pretrade::PreTradeDecision` has `Approved` and a `Reduced` arm, and `DELIVERY-STATUS.md` filed it as a contradiction. It is the rule that does not survive contact with its own section: a deterministic pre-trade check that never lets a compliant order through cannot do its job, §33's own belief-freshness row already resizes ("reduce to conservative multiplier") rather than only vetoing, and `Approved` is the structural, exhaustively-matched form of the "silence is permission" rule the blueprint states elsewhere. `Reduced` is computed by exact `Decimal` bisection (never a guess), off unless a composition root calls `.allowing_reduction()` (the one production root never does, so it is built, tested and dormant), and every uncomputed figure or `Err` — including the checker's own validation — refuses rather than approves by default, which is rule 17's "errors and timeouts are vetoes" clause held exactly as written. Rule 17 is corrected in place to state the property that actually holds; §56.2's row moves from contradicted to held. Scoring it as a code defect, reading the rule as forbidding only `Reduced`, deleting the `Reduced` arm, and leaving the disagreement open each rejected — the second and third are also out of this record's reach, since it changes no Rust source |
| [0053](0053-declared-and-gated-managed-service-scaffolding-does-not-contradict-not-used.md) | *Proposed* — §44.1 says Vertex AI, Bigtable and AlloyDB (the latter two by implication only — neither name appears in the blueprint source at all) are "not used"; `DELIVERY-STATUS.md` called Terraform's disabled `google_vertex_ai_metadata_store`/`endpoint`, `google_bigtable_instance.timeseries` and `google_alloydb_cluster`/`instance` a contradiction, "inert, not removed". Read against the module's own variable descriptions, it is not one: `count = var.enable_X ? 1 : 0` with the flag `false` in all four environments means **zero** instances of any of the five resources exist anywhere, and each variable names the protocol or credential obstacle that means flipping the flag would not make the platform use the service either — no REST data plane for AlloyDB, gRPC-only for Bigtable, no client or egress path for Vertex AI's port in `qip-training`. "Not used" and "declared, disabled, no adapter" are the same fact from two directions. Ratifies the pattern under three conditions (default `false` and every environment agreeing, no adapter capable of using it if flipped, and the obstacle named where the flag is declared) rather than writing the fact into the blueprint itself, on the reasoning ADR 0049 used for the OpenTofu row: a second copy of one fact in two documents is a fact that can disagree with itself. Removing the Terraform scaffolding, duplicating the caveat into the blueprint text, and scoring the row CONTRADICTS each rejected |
| [0054](0054-temporal-precedence-is-the-first-real-causal-edge-establishment-method.md) | *Accepted* — the causal graph was empty in every process: `seed_demo_world` was the only writer of a `CausalEdge` and had no production caller, and none of §9.2's six establishment methods had a line of code behind it. Implements exactly one — Granger-style temporal precedence — as `qip_numerics::stats::granger_causality` (a nested-OLS F-test, reusing the incomplete-beta machinery `student_t_cdf` already proves, no new dependency) and `qip_world_model::granger::establish_temporal_precedence` (the domain wrapper, refusing below `p < 0.01` and a 0.02 partial-R² floor, confidence capped at 0.5 because precedence is not a mechanism), reached from `Platform::discover_temporal_precedence` on the UNDERSTAND stage against real `price_history`/`bar_history`, bounded at 200 pairs a cycle. The other five methods are rejected for this pass by name — natural experiments and instrumental variables need an exogeneity assumption this platform cannot verify, structural constraints are a certainty rather than an estimate and need their own record, the platform's own order flow has no fill history to learn from while `execution_nodes = {}` everywhere, and hypothesis-plus-falsification is only as sourced as `falsifiers_triggered`, which is built and inert. Two new `Mechanism` variants (`TemporalPrecedence`, `InverseTemporalPrecedence`) name that no economic channel is proposed. Widening the threshold to the conventional 0.05, an unbounded pairwise scan, guessing a mechanism from sector or name, and wiring the writer into `stage_discover` instead each rejected |
| [0055](0055-counterfactual-sizing-discipline-is-the-first-consequence-of-declined-scores.md) | *Accepted* — §12.3 ("What It Changes") was `ABSENT`: `score_declined` accumulated `DeclinedScore` and nothing read it beyond a test. Of the table's four named consequences (a rule recalibrated, a venue dropped, an allocator objective revised, a sizing function adjusted), only the last is built, because `DeclinedScore` carries an instrument and a gate and nothing else — no family, no venue — so the other three would need an upstream signal nothing produces today, including the task's own starting candidate (a strategy-family discount). `Platform::counterfactual_sizing_multiplier(object_id)` reads the already-bounded `declined_scores` history and narrows `sizing_confidence` — never widens it, by construction: below ten scored observations on an instrument it is `Decimal::ONE` unconditionally (recalibrating on noise), and even at or above ten it returns more than `Decimal::ONE` on no branch at all, which is how §12.4's "never loosened automatically" guardrail is held structurally rather than by a check someone could later drop. At or above the floor, an instrument whose declines were at least three-quarters correctly declined (`!regret`) is halved (`COUNTERFACTUAL_SIZING_DISCOUNT`), read live every cycle at the same seam DELIVERY-STATUS §11.2 already scores as production — `sizing_confidence` → `sizeable_theses` → `construct_from` → `stage_decide`. No risk-engine, execution, or autonomy path touched; no new dependency; no new instrument-labelled telemetry. Dropping a venue and revising the allocator's objective rejected on the same missing-attribution grounds; recalibrating the risk gate itself rejected outright as the exact automatic loosening §12.4 forbids; a discount curve instead of one constant, and writing the finding during LEARN into a second mutable field instead of reading `declined_scores` live, both rejected. §12.3 moves `ABSENT → PARTIAL`; the other three named consequences stay open |
| [0056](0056-source-categories-data-references-and-a-bounded-fetch-campaign.md) | *Accepted* — §7.6.1, §22.3 and §22.4 were `ABSENT` and form one dependency chain: a data reference needs to say what kind of source it came from, and a fetch campaign reasons about concentration and licensing per category, so they are built in that order. `SourceCategory` (the blueprint's eight-variant table) is classified by `SourceCategory::classify` from a declared `ContentSignal`, refusing a candidate with none and refusing the three shapes §7.4's own Classify question names that none of the eight admits (general news, unspecialised discussion, a leak forum) rather than force-fitting one; wired into `DataFinder::assess_one` on the same production chain §7.4/§7.5/§7.6 already established. `DataReference` records the source's category, a locator, symbols, a period, its `SourceSchema` shape, and a content hash computed with `qip_core::sha256_hex` — the exact mechanism `SourceManifest` already uses for §7.2, not a second scheme — and refuses a source with no recorded category, an empty locator or symbol set, or empty bytes; `DataReference::of` takes `&RegisteredSource`, whose only constructor runs after the licensing gate, so a reference cannot be built around it. `FetchCampaign` holds a TTL- and entry-bounded `ResearchCache`, verifies a re-fetch's hash against what it already cached and flags every manifest entry naming that locator on a mismatch, and returns only its `CampaignManifest` on close — the cache is dropped. `assess_concentration` closes the concentration-risk gap by name: fewer than `MINIMUM_VIABLE_SOURCES = 2` independently viable sources holds a class back. Of §22.4's five named mitigations, three are built in full, one is partial (the three-year bar-level fallback is §22.1's still-absent retention taxonomy), and one is not built (no sketch exists anywhere in this codebase for an error bound to apply to). §7.6.1 moves `ABSENT → REACHED`; §22.3 and §22.4 move `ABSENT → PARTIAL`, each explicitly not rounded up because neither has a non-test caller — nothing in this build fetches real bytes for either to describe, since no HTTP transport is linked in (ADR 0009). Classifying by reading a sampled payload, making `ContentSignal` a required constructor argument on `SourceCandidate`, storing fetched bytes inside `DataReference` itself, and folding `assess_concentration` into `DataFinder` as a method were each considered and rejected |
| [0057](0057-a-catalogue-door-into-the-source-registry-and-the-research-campaign-that-uses-it.md) | *Accepted* — ADR 0056's `DataReference` and `FetchCampaign` had no production caller, and `bbf31c8` showed that wiring alone could not give them one honestly: `DataReference::of` needed a `RegisteredSource`, which only the discovery pipeline produces. This record makes the second of the two designs that commit named. A catalogue-admitted connector enters as an `AdmittedSource` — a distinct type built from the licensing gate's own `LicensingDecision` (now unforgeable: a private `GatePassed` field leaves one construction site) plus the category its shipped manifest declares (`SourceCategory` moved down to `qip-financial` so a manifest can name it; Kalshi is `marketplace`, not `resolution_source`, on the blueprint's own definition) — and every `DataReference` carries a `SourceOrigin` (`Discovered`, `CatalogueAdmitted`, `Generated` for the platform's own synthetic streams, on `SourceManifest::generated`'s precedent). `ConnectorRuntime::ingest` digests exactly the bytes a vendor served; the kernel builds the reference from the digest, keeps it in a `ReferenceLedger` bounded by count on both axes, and makes a revision cost something — a `SourceRevisionDetected` record on the hash-chained log, a metric, and a `revision_covering` flag a research run reads. §22.1's nine `RetentionClass`es and a daily-bar `FallbackSeries` under three stated bounds (three years, 1,100 bars, 512 instruments) and §22.2's one sketch — a count-min sketch built from a declared `(ε, δ)` — are the foundations. §22.4's caller is the deep brain's learning round, assembled through `qip_deepbrain::campaign::assemble`: door → ledger → TTL cache the fit reads back from → sketch with its bound on the manifest and a refusal past tolerance → concentration → manifest journaled under its own permanently retained topic, and nowhere else. Rule 31 gates promotion past validation over vendor doors only: a subject with fewer than two vendors has its candidates held at `Candidate`, unjudged; the shipped synthetic-only deep brain counts for no vendor, no shipped subject has two, and promotion is held everywhere — the rule working, and stated as the cost. Amended in place after two reviews of the implementing commits: the records have their own topics and idempotency keys, the ledger is rebuilt from the log on restart, the reference is taken between the poll and the checkpoint commit so a refusal re-fetches rather than drops, the closed campaign that used the original is the one flagged, a door refusal is a round's outcome rather than the node's end, a replay may be admitted under the connector it was recorded from, and the gate's types do not deserialise. Building a `RegisteredSource` for a connector, narrowing `DataReference::of` to an ungated id and category, one poll per campaign, a minute-bar fallback, gating the fit rather than promotion, and admitting the source in each root's `main.rs` each rejected |
| [0058](0058-the-market-factor-is-the-equal-weighted-return-of-the-committed-universe.md) | *Proposed* — `qip_risk::metrics::beta` was built, tested and called by nothing outside tests, and the consequence was not local: `factor_betas` was constructed empty at both sites the kernel builds a position period, so the platform had **no factor model at all** and three capabilities executed while measuring nothing — §19.1's `effective_bets` over contributions nobody populated, §23.7's `StressTester::apply` pushing every position onto its `unmodelled` list, and the factor half of the learning engine's attribution. Three rows in the delivery status, one missing wire. The wire needed a benchmark, and choosing one settles what every factor number the platform reports is measured against, so it is decided here rather than inside the patch. **The market factor is the equal-weighted arithmetic mean of the per-period simple returns of the instruments on the platform's own price tape** — its own tape because an index fetched from outside is a number a replay cannot check, and equal weight because the platform observes no share counts and no free float, so a capitalisation weight would have to be estimated from something it does not see. Two refusals carry the value: below `MINIMUM_OVERLAP` observations an instrument carries **no beta**, and a factor whose variance is below a floor produces **no betas at all** — because `metrics::beta` answers `0.0` when it cannot divide, and `0.0` is indistinguishable downstream from a genuine zero beta, which reads as immunity to a market that simply never moved. That second floor was found by a test whose fixture grew at a constant rate and got a confident zero for an instrument that was the entire factor. Named `market` in `factor_betas`, and the name is what the movement is, not a claim to be an index: an equal-weighted mean of five synthetic instruments is a factor in the arithmetic sense and no proxy anybody should trade against. Explicitly not a performance benchmark, not a claim one factor suffices — rates and credit shocks keep reporting `unmodelled`, correctly and visibly — and void the moment any caller substitutes zero for a missing beta, which is the whole difference this record exists to keep. **Corrected 2026-09-08 in the record itself:** this ADR originally asserted that `market` already matched the name the standard scenario library gives its equity-style shock. It does not — the library shocks `equity` — so every position would have been filed under a key the stress tester never looks up and reported `unmodelled`. The mapping is the constant `EQUITY_SHOCK`, and because neither crate depends on the other an acceptance test in `architecture.rs` holds the two names together |
| [0059](0059-a-candidates-edge-is-re-measured-against-reacting-counterparties.md) | *Proposed* — `qip-simulation-engine` builds five counterparty behaviours and a market that reacts to them, and `with_agents` was called by nothing outside `tests/agents.rs`. Meanwhile the deep brain judged every candidate on a tape where **nothing else is trading**, so a candidate whose apparent edge consists entirely of being the only participant on one side scored exactly like one whose edge would survive company, and the loop could not tell them apart — blueprint §15.3's crowding stress, and the gap that makes a backtest most flattering. **After a candidate produces holdout evidence its own tape is replayed twice — same bars, same book shapes, same seed, same costs, same program, the only difference being whether the standard counterparty panel is attached — and the difference is recorded.** The panel is sized off the volume the **tape** recorded and never off the liquidity profile's claim about it — the book is filled from the bars, so the two are one fact with two claimants and the panel is the louder; written the wrong way round first and found by a fixture whose profile overclaimed tenfold and produced a crowded run losing half the capital. The historical replay rather than a fresh synthetic path, because a candidate's edge is specific to the series it was found on and measuring it against a different random walk would report its disappearance and blame the counterparties. The load-bearing half is the second decision: **the measurement may not refuse a candidate.** The panel is not calibrated and the crate already says so in one place — `FlowCalibration` has exactly one arm, `NotCalibrated`, carried by every run bearing agent flow — so gating on it would be a promotion decision made on parameters nobody measured. This repository's standing complaint is `MaxExpectedShortfall`, a control that could never fire; **the opposite defect is a control that fires confidently on invented inputs, and it is worse, because the first fails open and visibly while the second fails closed and silently.** What would change it is named rather than left to judgement: a second `FlowCalibration` arm fitted to observed flow, which is what the enum exists to force. Costs stated — two extra simulator runs per candidate that produced evidence (which is why the check sits after evaluation, not inside it), a number that invites over-reading, and an asymmetry that is not a bug. **And the check reports, on today's tape, that it measures nothing** — the panel places thousands of orders a round and every comparison comes back with a cost of zero to the last digit, because the book's displayed depth at the touch far outlasts the panel's clips. That is ADR 0059's own "the panel being so small it changes nothing", observed rather than reasoned about, and the response is to count it (`crowding_unmoved` beside `crowding_uncontested`, with a test asserting the two counts still agree) rather than to enlarge the panel until the number moves — which would be tuning an uncalibrated model until it spoke, the same defect as gating on it by a friendlier route. Wiring it to the gate now, sweeping the panel for a capacity number, enlarging the panel until it bites, and calling the panel realistic each rejected |
| [0060](0060-a-source-is-probed-only-where-a-reviewed-egress-route-already-exists.md) | *Proposed* — `DataFinder::assess` evaluates a candidate source's licensing, tier, robots policy, freshness and schema, and is reached from no application; three delivery rows (§7.5, §7.6, §7.6.2) are scored `UNREACHED` on that one fact and have been read as a coding gap. **They are not.** The obvious blocker — `NetworkProbe` being a stub that refuses for want of an HTTP transport — is not the real one, because `qip_transport::http` exists and already carries every outbound call here. The real blocker is a property somebody chose on purpose and wrote down in `egress/envoy.yaml`: the client emits neither `CONNECT` nor an absolute-form URI, so **a process cannot reach a host by asking for it, because there is no field in the request in which a host can be asked for**, and the destination is a property of the loopback port it connected to. A source-discovery crawler is definitionally a process that reaches hosts it names at runtime; the two cannot both be true. **Decided: a candidate is probed only where a reviewed egress route already exists**, `NetworkProbe` takes a base URL like every other adapter, and the candidate catalogue names the route each source is reached through so a host with no route is refused at load rather than at the socket — adding a source is three reviewed files, not something a running process can do. A `CONNECT` proxy with a host allowlist is rejected explicitly and for a reason, not on preference: it is **strictly weaker**, because the allowlist filters a value the client sends, the same class of control `endpoint.rs` documents a permissive parser guessing past. The cost is stated plainly, including the one that will bite — the per-source cost scales badly, and the pressure for a forward proxy will come from that number rather than from an argument. **What this decision declines is §7.6's own claim**: the platform will assess a list somebody listed, not find sources nobody had, so the row can be `PARTIAL` and cannot be more, and a later reader taking it for a half-built crawler would build the other half. First candidate is `api.frankfurter.dev`, because it already has a cluster and the only end-to-end proof any source here has |
| [0061](0061-rule-regret-is-a-proposal-a-defence-or-a-dormancy-finding-and-only-a-signed-file-moves-a-bound.md) | *Proposed* — Blueprint §12.3 has three rows about risk rules — a rule that vetoes mostly profitable paths is too tight, one that vetoes mostly losing paths is earning its place, one that almost never fires is dead weight — and the platform could key none of them, because a refusal was charged to the *control*: `RiskRejected` carried the sentence pre-trade risk wrote, the OMS dropped the checker's own record of which limits bound, and every limit in the set was one bar on one chart. **A refusal is now charged to the rule by the breach the checker wrote, never by a word in the sentence**, and from the per-rule accumulation the LEARN stage journals three findings at the two bars sizing already uses: a *defence* (a record, with the simulated loss the rule avoided), a *dormancy finding* (a record: no fire for a hundred cycles across a hundred accepted orders, both bars because a rule nothing asked is not a rule that cannot fire), and a *recalibration proposal* — the only shape the first row may take. The proposal's one constructor refuses a bound that does not loosen; two operator signatures, cloned line for line from the promotion approval, produce a **file** — the running set with exactly one bound replaced — and the process keeps the limits it booted with. The only door is `QIP_RISK_LIMITS_PATH`, mounted from `risk_limits_file` on all three central roots because each assembles its own platform on its own set; the file **may move a bound and never remove a control**, and the acceptance suite refuses a `&mut self` method naming a limit on any type that holds the set, so the automatic direction §12.4 forbids is not merely unimplemented — there is no code that could take it. The `Decimal` crossing is one function and says so; the hundred/hundred constants are stated bars and named under "what would make this wrong". §12.3 stays `PARTIAL`: three rule rows built as findings with a governed manual enactment, the sizing row as ADR 0055 left it, the venue and allocator rows open |
| [0062](0062-a-venue-is-withdrawn-on-feasibility-evidence-and-reinstated-only-by-two-signatures.md) | *Proposed* — Blueprint §12.3's fourth row — "feasibility rejections cluster on one venue" → "venue withdrawn" — was `ABSENT` because the evidence had no key: a feasibility refusal was counted by its *gate* on both planes and by its venue on neither, and a cell's refusal reached the centre as a count on the delta sink. **A feasibility refusal now names its venue at both seams** — the desk broker's name on the desk, the intent's venue joined by index onto the delta at the cell, `CellReport.refusals` and one line in the sink carrying it — and lands in one bounded rate window under `qip_feasibility_refusals_total{venue,constraint}`; a cell's refusal is admitted only when its gate is one of the eight `qip_contracts::feasibility::EDGE_GATES` (declared once, aliased by the edge, pinned by a kernel test on the desk) **and** its venue is one the arbitrage policy or a live grant names, else counted under `unknown`/`other` and admitted to nothing. **A venue that accounts for three in four of a window of ten — ADR 0055's bars by reference — is withdrawn at both seams by omission only, after the `venue.withdrawn` record is in the log**: the order manager refuses under the existing `VenueUnavailable` reason at a new step independent of `is_simulated` (because `is_available()` lives inside the live-venue arm and the only broker the kernel constructs answers true, so a withdrawal read there could never fire), after the kill switch so a halt still reads as a halt; the whitelist `retain`s by parsed venue and its journaled outcome names the omission, `AllWithdrawn` when nothing survives. The share is over the whole window including a withdrawn venue's entries, so withdrawing one cannot make the runner-up a cluster of the remainder and cascade. Reinstatement is cloned from the promotion approval — fresh credential, two distinct people, a first signature stale after a day, every signature journaled under `venue.reinstated` before anything changes — and can only remove a name from a subtractive set; a venue `QIP_VENUES`, the policy and the grant do not name is not made reachable by any signature, and the fill-error diagnostic is read by nothing that withdraws. The sole-venue consequence is chosen: a cluster on the desk's one broker stops desk orders loudly until two people reinstate. The edge limit was stated, not hidden: an installed desk kept its graph until restart, because `install_arbitrage` refuses a second desk and slot 11 had no producer. **Amended 2026-09-14: the limit is closed and the row is `REACHED`** — slot 11 is produced from `CentralPlane::feasibility_constraints` carrying `withdrawn_venues` and three empty grids, and `qip_edge::feasibility::assess` refuses a withdrawn venue under a ninth `EDGE_GATES` literal that the centre charts and deliberately keeps out of the window, since a withdrawal's own echo would evict the evidence it came from. **All three follow-ons named by the original record are done, 2026-09-14, and this row said two of them were still open until the same day** — the ADR's own Consequences bullet was struck through and amended by one lane while this index was amended by another, which is exactly the two-registers-disagreeing failure the record itself is about: `POST /api/v1/venues/:venue/reinstatements` exists in `qip-api`'s routes over `Platform::reinstate_venue`, and `RefusalReason::Infeasible { venue, gate, detail }` replaced the `Malformed` prefix the desk used to carry a feasibility gate in. Nothing here is deployed; the ADR is still *Proposed* |
| [0063](0063-a-sizing-cap-is-the-second-consequence-of-counterfactual-scores-and-a-larger-size-finding-is-only-a-proposal.md) | *Proposed* — Blueprint §12.3's last row has two halves and ADR 0055 built the declined one; the executed half — whether the orders a venue actually filled should have been smaller or larger — was scored by nobody, although the twin's `smaller_size` and `larger_size` arms exist for exactly that. The shared foundation now prices every fill in LEARN under the one per-cycle cap, and two facts found by inspection shape the consequence: the size bits compare the size arms with the twin's own `trade` arm rather than with a realised outcome an opening fill books at zero (which would have called every winning direction "smaller favoured" and "larger favoured" at once), and the constructor's budget equality makes a bare bound cut infeasible on every one-name cycle. **A pattern of fills that would mostly have done better smaller arms a cap on that instrument's weight bound — one half, floored at the minimum position so a shrink can never become a silent drop, with the budget equality lowered by what the cap took so the shortfall is recorded and not reallocated — and a pattern that would mostly have done better larger is a journaled proposal and nothing else.** A bound rather than a second budget multiplier because a budget cannot name the instrument and a bound can, and the proposal's `compromises` then say which name, from what to what, and what the gross gave up. `construct_capped` refuses a cap outside `(0, 1]` or on an absent name rather than clamping; `cap_multiplier` has no branch above one; `larger_size_finding` returns a type with no multiplier on it; ADR 0055's bars by reference. Compounding is stated and tested — half the budget times half the bound, a quarter, on disjoint evidence — as the position sized rather than the traded notional, because the reference cycle's own ACT stage fills its proposal. §12.3's last row moves to `REACHED` for the executed half with the loosening direction as a proposal only; §11.2 gains a bound beside its four narrowings |
| [0064](0064-a-family-s-funding-standing-is-measured-and-the-allocator-s-weights-are-not-revised.md) | *Proposed* — Blueprint §12.3's fifth row ("a strategy family consistently underperforms" → "allocator objective revised") is the last row of that table with nothing behind it, and it **cannot honestly be reached**, so this record builds the measurement and writes the refusal down. ADR 0055's argument stands verbatim: `DeclinedScore` carries an order, an instrument, a gate and a venue and no strategy, so the evidence the other rows are keyed on cannot be grouped by family at all. The half ADR 0055 did not reach is that *underperforms* need not be measured from declined paths — every candidate the foundry registers carries the family whose sweep produced it, its holdout evidence, and a ledger rung. `family_review.rs` groups the registered population by family and reports, per family, members, members at a capital-holding rung, members whose evidence could be read, members refused, and the member mean of `observed - expected_maximum` — the gate's own deflation and deliberately **not** `DeflatedSharpe::observed`, which is the *undeflated* Sharpe and would rank a family that tried ten thousand configurations level with one that tried ten. **No weight moves, and the guarantee is the absence of the code path rather than a check.** Three findings from inspection carry it: every quantity a family finding could narrow sits behind a writer with no production caller (`CentralPlane::issue`, `set_proposal`, `family_horizons`, and the correlation calendar that depends on the first — each located by `grep`, none by line); `approve_promotion` governs the rung and not the money, so "funded" means *standing where capital may be held* and nothing in any deployment holds any; and — found while implementing, the one that would have shipped a dead control — `HoldoutGate::deflated` resolves its trial count from `StrategyEvidence::trial_account`, which **no candidate in this platform ever carries**, because `charge_holdout_trials` charges it into a `Cow` that is dropped and `submit_evidence` has no caller, so a review built the obvious way would have refused every member of every family for ever while reading like a working comparison. A `FamilyCap` today would be the `MaxExpectedShortfall` shape rebuilt inside the lane whose subject is what counterfactual evidence may change. `MisallocationFinding` carries two names, two counts, an outcome and a cycle — no margin, no ratio, no `Decimal` — because the magnitude on the *finding* is the one field "size the reallocation by how far ahead it is" would reach for; the module exports no function returning a multiplier; and `security.rs::every_shipped_function_that_moves_a_capital_weight_is_one_of_the_reviewed_ones` walks every shipped `fn` and refuses any weight-mover not on a reviewed list, failing closed. `qip_family_standings{standing}` is written every cycle **including when both arms are zero**, the `qip_rule_dormant` discipline, and here that is the deliverable: a `funded` arm reading zero cycle after cycle is "this row has never had a subject" as a fact somebody can chart, where an absent series reads identically to a review that never ran, and it is the series in which the day the row becomes buildable is visible. §12.3's R5 keeps its `absent` clause verbatim and gains a measured half; §12.3 stays `PARTIAL`, §23.1's LEVEL 2 stays `ABSENT`, and no verdict in the shape table moves. Building a family cap, discounting a family's allocator notional, inventing a declined-path-to-family link, and putting the excess on the finding each rejected |
| [0065](0065-a-standing-secret-has-no-authentication-instant-and-a-freshness-gate-refuses-one.md) | *Proposed* — the fifteen-minute operator-freshness window on every signature-gated route measured **process uptime**, not authentication. `qip-api/src/main.rs::run` binds `let now = clock.now()` once at start-up and stamps every `Credential` it mints from `QIP_TOKEN_*` with it, so `Principal::issued_at` — the value all seven `OperatorIdentity::verified` call sites passed as `authenticated_at` — was the boot instant for every caller for the life of the process. Both halves were wrong and the admitting half was the dangerous one: a six-week-old copy of `QIP_TOKEN_OPERATOR` posting at 09:05 to a process started at 09:00 computed an age of five minutes and passed, while the operator actually at the keyboard was refused from 09:15 onward with nothing in the platform to re-authenticate against. The `MaxExpectedShortfall` shape, fifth instance. Neither guarding test could catch it — `compliance_proof.rs` supplies a synthetic `authenticated_at` and proves the two windows agree, `security.rs` walks the *first* argument of `OperatorIdentity::verified` and nothing walked the third — and the earlier fix that replaced `now` with `issued_at` swapped one fabricated instant for another and shipped a test proving the fabrication worked. **Decision: a standing bearer token has no authentication instant and the composition root stops inventing one.** `qip_api::auth::Presence` has one variant, `Unattested { credential_minted_at }`, whose `attested_at()` is `None`; `Principal::authentication_instant(action)` is the one seam a route may ask through and it refuses with a message naming the act, the minting instant a reader might have mistaken for an authentication, and what would be required instead. Seven routes now refuse every caller (403) — the two ledger decisions, the three signatures, the registration approval and `DELETE /kill-switch`. The cost is stated unflattened: three of the seven were **already** uncompletable because the operator credential is minted per *role* (`format!("{}@env", …)`) so two humans present one subject (ADR 0062 Amendment B); four genuinely worked inside the uptime window and now do not; tripping the kill switch is unchanged and only clearing refuses, which is the fail-closed direction of a control whose safe state is "stopped". A real fix needs a per-request proof of recency (asymmetric primitive, ADR 0043) or an interactive authentication step with a **per-person** subject, and neither is built here |
| [0066](0066-a-regime-narrows-an-allocation-and-never-favours-it-and-a-cadence-may-skip-a-record-but-never-a-control.md) | *Proposed* — §23.3, §23.6, §19.2 and §18.4 share two seams rather than needing four mechanisms: the §19 alpha-family taxonomy (a third thing, distinct from `families::FamilyId`'s correlation cluster and `qip_lifecycle::StrategyFamily`'s sweep key, and its module doc says which of the three a reader holds) and one cadence question — *does this run now?* — asked of three subjects. `Stance::multiplier` has **no arm above 1.0**: a regime narrows an allocation and the favouring half leaves as a set of names carrying no number, so nothing here can raise a bound on a model's say-so. Production reads `unattributed_multiplier`, the minimum over the table, because no path attributes a family today and treating "unattributed" as unaffected would have made the reader invisible in the only state a deployment reaches — the `MaxExpectedShortfall` shape. A cadence may skip work that produces a **record** and never a **control**, which is why the regime narrowing is recomputed every construction and is deliberately not in the plan |
| [0067](0067-a-quote-is-a-priced-intent-with-no-path-to-a-venue-and-an-originated-market-is-gated-by-a-type.md) | *Proposed* — Blueprint §29.1 ("The Quote Loop") and §29.3 ("Market Creation") were both `ABSENT`, and §29.1 is the most dangerous row in the register to close, because **in every real venue quoting is order submission**: a quote loop built carelessly is a live-order path with a research name on it. **A `QuotePair` carries an object, a fair value, a bid, an ask, a size and the decomposition of the half spread, and carries no venue, no side, no client id and no time in force** — there is nothing on the type a venue adapter could act on, and no function in the workspace turns one into an `Order`. The arithmetic is `qip-execution-engine`'s (`quoting`, `origination`); the composition of market view, order manager and equity is `qip-kernel`'s `quote_loop`, for the reason `valuation` and the four `*_review` modules are kernel modules. Every component §29.1's table names is present **with the input that makes it bind**, because this workspace has a register of controls that read as protection and could never fire: the skew reverses with the sign of the deviation and the position limit halts quoting rather than skewing harder; the volatility and adverse-selection terms are charged separately so deleting either coefficient fails a test the other would have passed; **toxic flow requires one-sided direction *and* a moving reference**, since either alone would halt trading on every trending day; belief weights the signal, scales the size and below its bar withholds; the requote threshold is asserted inside and outside. **Queue position value is `Unknown` or `Measured` and never an invented average** — it is not derivable from a depth snapshot alone, only from a snapshot plus a resting order of one's own — and a test asserts the unknown arm really is the smaller size, a fail-closed default that is not actually smaller being a comment rather than a control. A half spread past the ceiling **withholds rather than narrowing to it**, and `QuotePolicy::validate` refuses any policy whose terms could total one whole, which is what makes a non-positive bid arithmetically impossible so that no arm carries an unreachable defensive branch. §29.3's five gates are a type with private fields, one constructor and **no `Deserialize`**, so a mandate cannot be decoded out of a payload; its `ORIGINATION_MAX_EXPOSURE` is a constant in its own file because the blueprint's fourth gate is the word "hard-coded", and a larger request is refused rather than lowered. The mandate is then load-bearing on §29.1 — it anchors the fair value of an instrument with no observable market and its remaining headroom bounds the size on every pass — rather than a document filed once. Two limits are stated rather than discovered later: adverse selection is a **bar-resolution proxy** and not the realised-minus-effective decomposition, and the `Measured` queue arm is unreached by today's cycle because `order_type_for` never returns `Limit`. Nothing sends a quote and nothing is built that could; the three layers of the paper-trading boundary are untouched |
| [0068](0068-a-cycle-is-assigned-one-of-eight-execution-paths-or-refused-and-the-preference-is-not-the-routers.md) | *Proposed* — blueprint §30.2's path router, from `ABSENT` to built: §30's six edge classes and §30.2's eight execution paths in `qip-routing::path`, assigned from a validated `Composition` and, for a mirror edge, from inventory, a local hedge, resting support or a firm quote beyond a measured round trip. Three things are the record rather than the code. **The preference is the caller's** — §30.2 says assignment is "a policy decision made globally with full cost and risk information, not a local heuristic", so `PathPolicy` arrives from outside and a ranking that omits a path is refused, because an unranked path is one the router can find eligible and can never assign: the `MaxExpectedShortfall` shape as a blueprint row the platform would claim to support and would not. **Settlement is refused, not guessed** — it is one of §30's six classes and §30.2 gives it no row, and treating it as a conversion would price a lagged leg as an instant one; the acceptance suite asserts that asymmetry against the blueprint text so the refusal is revisited rather than outliving its reason. **The two facts the graph cannot hold are supplied and refused when absent** — a venue's region, without which `EdgeKind::Transfer` cannot tell row 2's single-process dispatch from a leg crossing an ocean, and whether a synthetic is a basis or an equivalence, which is a carry check against a Greeks gate. `ExecutionPath` is deliberately not `#[non_exhaustive]` so §33.1's per-path check cannot silently default a path nobody considered. It is at the edge and not in the kernel because `runtime -> edge` is not a dependency direction this workspace has, and both follow-on sections (§31.1, §33.1) are edge-side. No `Decimal` and no `f64`: cost is the optimiser's and a second claim about it here would disagree. The paper boundary is untouched at all three layers and the router names no venue class, asserted by a doc-comment-stripped source scan carrying a vacuity guard. **As recorded it has no production caller, so §30.2 is not `REACHED`** — stated in the record itself so nobody reads it as shipping. |
| [0069](0069-an-infrastructure-gate-is-proven-by-a-plan-and-capability-nothing-consumes-is-not-declared.md) | *Proposed* — the infrastructure gates in this repository were asserted only as text, by Rust tests reading the HCL, and text cannot tell a rule that works from a rule that cannot run. `terraform test` with `mock_provider` and `command = plan` needs no credential, reaches no project, creates nothing and does not touch the GCS backend — so it runs in CI and against the environments ADR 0040 tore down. The first plans ever run found two controls that could not fire: `modules/network`'s prefix check on `console_egress_cidr` handed a null to `split`, killing the plan in the three environments where the value is null, while a Rust test asserted it "both fires and admits" by re-implementing the arithmetic in Rust; and `modules/trust-zones`' Cloud NAT precondition counted on a list its own failure case empties, so a precondition on an unplanned resource was never evaluated in exactly the case it was written for. Every harness must prove a refusal **and** an admission, enforced by `qip-acceptance`'s `terraform_plan` suite, because a gate proven only to refuse may refuse everything — which is what the first of those was doing. The second decision is which absent §45.1 rows to write: the public edge (Cloud Armor, global HTTPS load balancer, Cloud CDN) is declared and switched off in all four environments because its content is refusals that can be argued about and planned today — §40.5's "customer traffic and trading traffic never share a load balancer" becomes a plan that stops; Cloud Workflows, Cloud HSM and a spot GPU pool are **not** declared, because each is capability whose consumer does not exist and which carries no refusal to prove, and the reversal condition for each is written down. No §49.1 SLO is declared: availability, cycle completion and arrival dispersion have no descriptor any binary records, and naming one would be a policy that reads in the console as a project being watched and evaluates nothing. |
| [0070](0070-a-venue-is-promoted-to-the-simulator-and-no-further-and-the-mev-estimate-rides-the-minimum-notional.md) | *Proposed* — Blueprint §34.3 and §34.4 were both `ABSENT` and are one subject from two ends: what must be known about a decentralised venue, and what must be *measured* before any venue is used. Three decisions. **The venue ladder is `qip_contracts::gate::GateStage` and its ceiling is `Paper`** — registered→`Candidate`, observed→`Holdout`, simulated→`Paper`, and shadow, capped live and full are refused by name, because `qip_brokers::AdapterClass` has two variants and no third and those three rungs describe a connection this build cannot make. `attempt_promotion` *computes* its target from `GateStage::next`, so there is no argument that names a rung and no policy field that raises the ceiling; promotion here means eligible for the **simulator** and `VenueLadder` has no method answering "may real capital". `Holdout` for "observed" is exact rather than convenient: both rungs refuse a number checking itself. **The MEV estimate rides the minimum notional rather than a new gate literal** — `gas × 10000 / (budget − slippage − extractable)`, so a venue offering more headroom to the mempool demands a larger ticket, refusals land under `feasibility_minimum_notional` which both planes and the centre already know, and a literal invented in a service crate would have reached the centre as `other`. A venue whose headroom exhausts the budget is **refused** rather than given a threshold no order reaches. **Contract risk is its own axis** (`contract`, not `counterparty`), because netting code that cannot be called against dealers that can would let a book held through one unaudited contract read as diversified. Honest limits, stated: the four §34.3 pieces are a model and not a connection — no chain client, no signer, and `DexVenue` deliberately does not implement `VenueAdapter`; the MEV figure is a bound and not a forecast; and `qip_kernel::venue_admission::review` **reports and does not withdraw**, because withdrawing every unadmitted venue would empty a first deployment's whole venue list, and nothing calls it yet because `Platform` holds no `VenueLadder`. The paper boundary is unchanged: Terraform's plan-time refusal, `AutonomyLevel::deployable` at the roots, and the `Cell`/`Determinism::Required` types all stand, and `OrderManager::submit`'s `LiveVenueBelowLiveAutonomy` step is untouched and does not consult the ladder |
| [0071](0071-granger-style-lead-lag-is-run-with-controls-and-an-unobservable-confounder-lowers-a-ceiling-rather-than-attenuating-an-edge.md) | *Proposed* — the causal graph already had a production writer (`Platform::discover_temporal_precedence`, ADR 0054), and it ran an **uncontrolled** pairwise Granger test. §9.2 names "Granger-style lead-lag **with controls**", and the difference is not cosmetic: `an_uncontrolled_pairwise_scan_of_a_book_with_one_common_driver_writes_edges_a_control_removes` builds a book in which no lagged relationship between any two names exists — only a shared persistent driver — and the uncontrolled scan writes edges anyway while the controlled test removes them. Every one of those edges is spurious, and they are spurious *together*, which is verbatim the failure §9's opening paragraph exists to answer. So the graph's density was the artefact, and building §8.2's queries on top without saying so would have been the `MaxExpectedShortfall` shape inverted: a control firing confidently on noise. An unobservable confounder lowers a confidence **ceiling** rather than attenuating an edge, and the two ceilings are stated as assumptions nobody estimated — what is asserted is their *ordering*, held by a `const` assertion that fails the build |
| [0072](0072-a-collateral-graph-refuses-the-arrangements-it-cannot-cost-and-a-physical-rate-carries-its-source.md) | *Proposed* — blueprint §25.6 ("The Cross-Margin Model") and §17.4 ("Physical"), both `ABSENT`, taken as far as the platform's own evidence honestly reaches and no further. `qip_capital::collateral::CollateralGraph` is what a scalar margin requirement cannot be — which asset stands behind which obligation, at which venue, under which regime — and it refuses the arrangements that make a collateral balance larger than the collateral: an asset pledged beyond its mark, a haircut outside `[0, 1)`, maintenance above initial, a re-pledge out of a domain that forbids it or into one that ring-fences. Isolated margin and forbidden rehypothecation are the `Default`, because portfolio margin is a term somebody signed. **A re-pledge chain deeper than one link is refused**, and the argument is the load-bearing one: which of A's securities B passed to C is a fact B knows and this platform does not, so modelling it means choosing an attribution rule and presenting it as a measurement. The cascade terminates because a domain is closed out at most once, not because of an iteration cap that would hide a real cascade. `qip_kernel::cross_margin::review` joins the counterparty axis every fill already writes to the statements `Platform::observe_statement` already takes, and **an unobserved account is reported as unobserved and never as empty** — the two are arithmetically identical and call for opposite actions. Reuse and a multi-step cascade are library-only and are deliberately *not* fields on the review, because a field structurally empty on every production path is the `MaxExpectedShortfall` shape and an always-`None` `Option` is the same thing wearing a type. For §17.4, `qip_financial::physical` costs a consignment through freight, customs, storage, spoilage, returns and marketplace fees, compounding the quantity loss so a long route never delivers a negative quantity and charging duty and storage on what survives rather than on what shipped; `LogisticsTerms` has no `Default` and **a `Spoilage` rate is refused without a source**, because nothing here measures shrinkage and an unsourced figure is quoted forward for ever. §17.4's auction row is untouched and said to be. **The kernel review has no production call site as recorded**, so §25.6 is not `REACHED`; the paper boundary is untouched at all three layers and the acceptance suite asserts it rather than stating it. |
| [0073](0073-a-region-gates-its-own-side-of-a-mirror-and-the-band-never-arrives-on-the-wire.md) | *Proposed* — blueprint §31.1 and §33.1, and the one line that was keeping §30.2's rows 3 to 6 out of reach: `Cell::install_arbitrage` put **every** venue a cell may trade in the cell's own region, so a transfer was always a transport edge and a cross-region cycle could not be composed at all. Four things are the record rather than the code. **The band never arrives on the wire** — `InventoryTargets` carries targets and reference prices and no band field, and that is the design: the payload is a wire that may subtract and never add (`FeasibilityConstraints::withdrawn_venues` argues the same rule in the safe direction), so the centre says where a region should be and the operator says how far off it may drift. **A cell asserts a setup fact, never a measurement it cannot make** — `both_sides_at_target` is a claim about two regions and a cell can see one, so `MirrorFacts` gains `established_mirror` beside it rather than having it redefined underneath every caller; §31.1's construction is that neither side needs the other's holding. **The hard band is evaluated first**, against the table's own printed order, or a holding far past it matches "above target" and row 4 never fires — the `MaxExpectedShortfall` shape. **"Either direction, reduced size" is refused rather than traded at full size**, because nothing in the gate can make a scanned cycle smaller, which is stated as a deliberate narrowing of the blueprint and not as delivery. §33.1's extension is a check with no return value that can widen anything, its `match` names all eight paths, and a missing fact is a refusal. What this opens is row 3, reachable from a cell and proven by a pass; rows 4, 5 and 6 stay out of reach because a cell measures no hedge depth, no resting support and no firm quote, and each `false` says so at the line. **Nothing reaches a deployment: `qip-edge-node` annotates no venue's region and nothing produces policy slot 10**, both stated in the record so nobody reads the row as shipping. |
| [0074](0074-a-streaming-estimator-declares-a-bound-that-can-be-exceeded-and-is-implemented-in-tree.md) | *Proposed* — Blueprint §22.2 asks for seven streaming methods and the workspace had two and a half; §21.1 states the condition they all rest on, that "every estimator declares an error bound, and one that drifts past it marks every model depending on it as degraded". **A t-digest, a HyperLogLog and a weighted reservoir are written in-tree**, beside the count-min sketch ADR 0057 built and sharing its hash rather than growing a second one, on the ADR 0002 test that made the in-tree hashing defensible: each has a stated mathematical guarantee and each is proven against a distribution whose answer is known independently of the implementation — the digest against a hundred thousand points laid exactly on the unit interval *and* exactly on the unit exponential, the estimator against streams of known cardinality including ten keys repeated a hundred thousand times. None faces an adversary, which is why this is not permission to widen ADR 0043's three gaps. **The load-bearing half is the second decision: a declared bound is worth nothing unless it can be exceeded.** `StreamingDrift` computes the population stability index from two digests with neither sample retained — so a node measuring drift against a training window need no longer keep that window, which is the archive §21.1 exists to remove — and carries a `floor`, the index the two digests' own declared rank errors could manufacture between two draws of *one* distribution; `is_material` is the index exceeding the floor and nothing else. Both failure modes either side read in code like a working comparison: a floor of zero fires on every feature every cycle, and a floor above every achievable index fires on none — and **this lane shipped the second in a first draft**, by sizing the estimators to fit two hundred features inside §21.1's "~320 KB" when §22.2 assigns that figure to the covariance row alone (`200² × 8` bytes to the byte) and budgets quantiles separately at "few KB per distribution". The bound chose the configuration, not the other way round. Four memory ceilings, each named, each refused at construction and each admitting a configuration below it, checked together because a gate that refuses everything is not a working gate. **§20.1's capacity estimate invents no edge**: it asks at what size the impact the observed depth implies exceeds the cost the leg's *own sizing* assumed, both figures the platform's own, under a one-line uniform ladder that is conservative and reaches zero on its own when a leg's assumed cost does not cover half the quoted spread. Nothing moves a weight, a size or a limit. **§20.1's live canary is deliberately not built and the reason is a finding**: its only possible subject is a strategy at `GateStage::Pilot` with a baseline, `StrategyFactory::set_baseline` has no production caller at all, and the deep brain's evolution turn promotes one rung and says in its own comment that Pilot is not reachable from there — so a canary on `live_outcomes` today would be a control whose input is structurally empty, the `MaxExpectedShortfall` shape inside the very record that names it. Taking three crates for the algorithms, sizing the digests to the wrong blueprint row, gating drift on a fixed threshold, and building the canary anyway each rejected |
| [0075](0075-the-capital-issuance-route-is-authorised-in-shape-and-refused-in-fact-and-a-family-may-never-revise-a-share.md) | *Proposed* — the decision ADR 0064's first reversal condition asks for, taken **before** the code rather than after it. `CentralPlane::issue` is the only writer of a capital envelope and has no `src/` caller, which holds four things dead: `retain_grants` retains no day, so §23.1 LEVEL 1's family measurement returns `None` on every cycle of every deployment however much the cells settle; LEVEL 2 has no subject; `cycle_whitelist_for` answers `NoLiveGrant`; `recall_for` has nothing to recall. **The finding is that the missing route is not the binding constraint.** `issue` refuses before anything else unless the strategy stands at a capital-holding rung, the only path to one is `approve_promotion`'s two signatures, `qip-api` mints the operator credential per *role* (`format!("{}@env", …)`) so two humans present one subject, and since ADR 0065 `Principal::authentication_instant` refuses every signature-gated route outright — so a capital route built today would call nothing, and `retain_grants`' baseline filter is gated on the same promotion twice over (`set_baseline` has no caller at all; the only writer with one is `baseline_for`, inside `approve_promotion`). The order is therefore **Gate A** a per-person operator identity carrying an authentication instant, **Gate B** a strategy at `Pilot` in a deployment, **Gate C** the route; the route is *authorised in shape and refused in fact* until A closes, because building it first is the `MaxExpectedShortfall` shape built by a lane that had been shown the list. Four decisions taken now: **the route originates the proposal and nothing else may** — `share = score / total_score`, so a cycle stage writing a proposal is a cycle stage reducing every already-approved strategy's capital on the same write, and `CapitalRequest::subject()` is `capital:{strategy}@{cell}` and names no amount, so a machine-chosen size is a number nobody signed for; **three family notions, not two** (provenance, correlation, and ADR 0066's alpha taxonomy) with no conversion between any pair, only the correlation family ever permitted to bound an allocation because it is the only one that is a claim about risk, and neither permitted to name the other's key in a journaled record; **a family finding may lower a ceiling and may never move a share**, since in a normalised allocator every narrowing is somebody else's raise and ADR 0066's rule cannot be obtained by lowering a term — the residual is left unallocated, ADR 0063's discipline; and **a live envelope is revoked only by `MAXIMUM_ENVELOPE_VALIDITY` or `recall_for`**, with the proposal book frozen for a strategy whose request is pending, because `ApprovalChain::check` needs every credential fresh at the instant of grant and that fifteen-minute window is exactly the interval in which a revision could change what the signatures buy. The design is handed over in full — three requests, the sized limits quoted at step 1 and re-checked at the last signature, twelve refusals each with the admission that proves the gate is not refusing everything, and the two tests an implementer would not think to write. No new dependency; the paper-trading boundary is untouched at all three layers and an envelope is a bound and never a permission. No verdict moves |
| [0076](0076-a-per-person-operator-identity-is-the-consoles-asserted-subject-and-a-two-person-rule-needs-a-credential-this-platform-cannot-issue.md) | *Proposed* — ADR 0075's **Gate A**, decided before the code and decided partly as a refusal. Gate A is three problems wearing one name and they fail independently: **the subject** (`format!("{}@env", role.as_str())`, so two humans present one), **the instant** (ADR 0065's refusal, which stands), and **the carrier** — the one nobody had named and the finding here. Every route in ADR 0065's table is `Role::Operator`, and the console holds `qip-token-viewer` and no other platform credential by ADR 0018's decision, so **ADR 0042's mechanism attaches an assertion to routes the asserting process cannot call**: its own decision 5 puts the role check before the assertion, and the console is refused before the MAC is computed. This record amends ADR 0042 by supplying the carrier decision rather than changing its mechanism. Decided: **(1)** a per-person operator identity is a per-person bearer credential minted in the composition root from a mounted directory — file name is the subject, contents are the token, `BTreeMap`-ordered, refusing a subject ending `@env`, a duplicate, a short token, an empty directory, and a deployment holding both this door and `QIP_TOKEN_OPERATOR`, because two doors into one role let `operator@env` back in beside the named people; a variable per person and a single JSON blob both rejected with reasons; **(2)** no second `Presence` variant and no fabricated instant — possession is not presence, so **all seven routes stay refused after this lands**, and a lane reporting "Gate A closed" on decision 1 has reported a false statement about the system; **(3)** the console stays `viewer` — mounting the operator token into the largest attack surface in the platform to obtain an audit fact trades a control for a label, and both directions of the kill switch sit behind it; **(4)** the rest of Gate A needs a step-up against Identity Platform (no dependency; `authenticatedAt` is stamped once at sign-in against a twelve-hour `SESSION_TTL_MS`, so the fifteen-minute window is satisfiable only in the first quarter-hour), a carrier for console-verified presence (an identity terminator in front of `qip-api` recommended — ADR 0013's own reversal condition — a two-part ceremony named as the no-dependency fallback with its two-namespace defect stated, console-as-operator refused), and, for the **two-person rule only**, per-person key material the platform cannot itself mint: under one shared assertion key one holder mints two distinct subjects and the kernel compares strings, which is `MaxExpectedShortfall` one layer up. **So Gate A cannot be completed within the two-dependency rule** — its subject half can, its instant half needs infrastructure rather than a crate, and its two-person half needs an external component that is its own ADR. No crate is named, admitted or authorised here, and no JWT, TLS stack, OIDC flow or asymmetric primitive is proposed in-tree. The buildable step is decision 1 alone and its whole yield today is one log field — `POST /kill-switch` journaling a person instead of a role — stated plainly so nobody measures it as capability. Costs named: revoking a person becomes a deployment, the thirty-day credential expiry is thirty days after *boot* and is not fixed here, and ADR 0075's capital route moves not one step. No new dependency; the paper-trading boundary is untouched at all three layers, and an operator identity may never reach `AutonomyController::request_change` |
| [0077](0077-a-counterfactual-finding-that-asks-a-control-to-move-is-charged-a-trial-and-shares-the-book-but-not-the-budget.md) | *Accepted* — blueprint §12.4's third guardrail: "counterfactual findings enter the same statistical gate as any other hypothesis, with the same trial accounting". ADR 0055's bar — ten scored paths, three quarters of them one way — is a fixed threshold, and a fixed threshold is a test the platform may re-run at no cost. `review_rules` runs it on every rule on every LEARN stage against a window that turns over; eight-of-ten under a coin lands about once in nineteen, and a platform that looks nineteen times opens a proposal to loosen a risk rule on it and puts two operators in front of a signature page. **Decision: every counterfactual finding that asks a control to move is charged one trial and judged against a bar corrected for the quarter's charged trials.** Two seams, both loosening: the recalibration proposal (§12.3's first row, ADR 0061's approval path) and the larger-size proposal (ADR 0063). One-sided binomial against `p₀ = 0.5` through `normal_cdf`, Bonferroni against the quarter. **The book is shared and the budget is not** — two books over one store would append at colliding sequences and read as tampering, while `TrialBook` budgets per *family*, so a reserved `COUNTERFACTUAL_FAMILY` that `enrol` refuses both crossings into keeps a quarter of rule reviews from spending the budget a sweep needs and the reverse. Quarterly and not lifetime on purpose: lifetime drives the bar to zero and turns the gate into a stop, which is `MaxExpectedShortfall` with the sign reversed. The narrowing multipliers are deliberately **not** gated, because their refusal would make the platform size *larger* — a gate that can only loosen. Costs: eight-of-ten no longer proposes anything, a spent quarterly budget defers every further finding under its own `uncharged` arm, and the default book is in-memory so a restart resets the correction. No new dependency; no branch returns more than `Decimal::ONE`; the paper-trading boundary is untouched at all three layers |
| [0078](0078-an-intent-always-names-its-venue-and-consolidation-onto-one-venue-happens-before-the-intent-exists.md) | *Proposed* — blueprint §27.2's second row, "the router may consolidate onto the best venue if strategies did not specify one". The lane was briefed that a `Consolidator` in `qip-routing` keyed on a reserved `UNSPECIFIED_VENUE` would be a no-op; **there is no consolidator in the tree**, and the finding underneath is sharper: `Signal` has no venue field, so *no strategy can specify one*, every intent is in the row's second clause, and the venue every intent carries was chosen per signal by `Cell::venue_for` before the intent existed — deterministically, so N signals on one instrument in one pass land on one netting key and become one order. The row's behaviour is the platform's only behaviour; what it lacks is the word **best**, since `venue_for` takes the first configured venue with a reachable book. **Decided: `Intent::venue` stays `VenueId`** — the per-strategy gates §28 puts before netting are questions about a book at a venue, so an intent whose gates ran has a venue by construction, and an `Option` would spell "gated, but at nothing"; **consolidation is `venue_for`'s criterion and nothing after `net()`** — the tightest usable top-of-book spread in `Decimal`, ties in configured order, every candidate journaled so the pick replays; the netting key is unchanged and no journaled net is re-derived; no sentinel venue ever, because `place_net` would send to it. `qip-edge` only; dependency direction unchanged. Rule 22 is named as remaining a policy question on ADR 0001/0011's no-async-runtime decision and is not decided. Rejected: `Option<VenueId>`, a post-net consolidation step (re-runs per-strategy gates on the net, and re-prices a net already sized on a mid at a venue), recording the row as unwanted (it is the only behaviour there is), and the sentinel |
| [0079](0079-a-dark-region-is-the-centres-word-for-silence-and-nothing-read-from-it-may-loosen-a-bound.md) | *Proposed* — blueprint §36.3's two dark-region rows. The brief said the cell publishes the fact; **it does not** (`region_outlook`/`is_region_dark` exist nowhere), and a crashed node cannot publish anything, so the fact is the centre's, derived from silence. The load-bearing finding, verified: `ingest` replaces a cell's book and recomputes the aggregate, `crowded` needs a cell count and `recall_for` names cells, so §36.3's "global exposure recomputed without it" **can only loosen** — a region going dark would read as the platform becoming safer. **Decided:** a region is dark when the centre has heard from it and then from none of its cells within `CentralConfig::region_dark_after` (refused at zero and above `MAXIMUM_ENVELOPE_VALIDITY`), derived on every read from `last_heard` and never a stored flag, with the transition journaled both ways; the centre learns a cell's region from the `region` the delta already carries via a defaulted `CellReport::region`, so **no field is added to the doubly-declared cell-to-centre wire**; **the last book is held and marked stale, never dropped** — the exposure while dark is at least what was last reported, a stated refusal of row three's literal reading on ADR 0008's own second reversal condition; **nothing new enters the region** — `issue` refuses, the region's share is frozen and never redistributed; **mirrors suspend through the one wire that may subtract** — `FeasibilityConstraints::dark_regions` under `withdrawn_venues`' exact serde and deploy-order discipline, read by `check_extension_for` against ADR 0073's `venue_regions`, and deliberately not `withdrawn_venues` because a venue is not a region and a returning region must not need two signatures; resumption is the first report through `ingest`'s existing halt-on-breaks. The invariant, testable: no derived quantity smaller than the last report's and nothing created by a region going dark. Four crates named for a lane; `qip-mesh` and both delta declarations untouched. Rejected: journal only, drop and recompute, a cell-side dark field, withdrawing the region's venues, halting cells the centre cannot reach |
| [0080](0080-an-orphaned-position-is-a-derived-state-owned-by-its-retired-strategy-and-unwound-reduce-only-by-its-cell.md) | *Proposed* — blueprint §35.1's `Orphaned` row and §35.2's first question, taken as the register asked: "an ADR rather than a wire". Two findings beyond the brief: nothing carries the journaled `RetirementDisposition` to any cell, so "never orphaned silently" is met at the record and unmet at the book; and **no cell consumes a `RecallOrder` at all**, so `RecallReason::StrategyDemoted` with `requires_immediate_flatten` — the exact shape of "flatten on the centre's instruction" — is a control that cannot fire at the cell for any reason. **Decided:** at the centre `Orphaned` is **derived** — a non-zero `StrategyLot` whose strategy is `Retired`, which `scheduled_unwinds` already is — never stored, with `DispositionRefused` as §35.2's reconciliation break; **three ownerships kept apart** — the retired strategy owns attribution permanently (no "house" bucket, ADR 0007), the cell owns execution through its own DECIDE/ACT path and ladder (ADR 0008), the centre owns the record; **no re-parenting** until the ledger records a shared thesis; **the carrier is a thirteenth payload slot** `dispositions: Slot<Dispositions>` in `scheduled_unwinds`' own shape, produced in `pending_policy`, absent from the wire when unproduced so every prior digest stands, cells before centre — a stated deviation from §36.2's twelve; **the cell applies it reduce-only against its own book**, refusing when it holds nothing or the sign would increase or flip the lot, through every gate but the envelope and region hold — the one envelope-less intent in the cell, safe only because the sign check makes it structurally reduce-only and ADR 0075 means a retired strategy can never hold an envelope again — and into `net()` as `Nettable` so it can cross internally; completion is the books and replay re-derives everything. `qip_portfolio`'s `Orphaned` is pinned to §35.1's meaning, its transition table argued rather than inherited, and its writer honestly not invented. Rejected: the recall channel (names an envelope a retired strategy lacks, and nobody consumes it), slot 7 (its doc refuses being a delivery path), a new downlink topic, the centre submitting the order, a second position book, journal only, and writing the lib state from the backtester |
| [0081](0081-the-browser-layer-stays-typescript-and-leptos-is-retired-as-a-deliberate-non-goal.md) | *Accepted on the authority the owner delegated to the policy lane on 2026-09-19* — the blocked-table row "ADR 0022/0025 \| Leptos \| a decision to authorise it" is decided: **not authorised, and retired as a deliberate non-goal.** The one concrete functional gain of a rewrite is named — shared Rust types across the browser boundary — and it is obtainable without one, by generating the portal's contract from `qip-contracts` through serde; every other candidate gain dissolves, and the blueprint's own §54.4 supply-chain argument against a JavaScript charting crate argues against a Leptos tree in the process that renders `PAPER TRADING`. §54.4's mobile row is read as its outcomes — installable PWA from one codebase, no native shell, Web Push, WebAuthn — which the portal meets or another lane is meeting, and the name is retired; rows stay `PARTIAL` and a retired clause is not `REACHED`. ADR 0022 item 4 is amended (the TypeScript surfaces are no longer transitional), ADR 0025's open half closes by its Option D and its decided half becomes the reversal mechanism, `qip-web` is not extended toward parity and stays the fallback surface. Costs stated: the blueprint stays contradicted by name in five places, two toolchains for good, identity stays in the Next.js server, and the type contract is still hand-written. Reversal: the owner wanting the name (then ADR 0025's Option B with the records it lists), the npm tree failing the platform's own gate with no upstream fix (then `qip-web`, not Leptos), the generated contract proving insufficient, or the Phase 13 push drill failing on the portal's worker. No file outside `docs/` changes |
| [0082](0082-unsafe-stays-forbidden-and-core-pinning-is-retired-until-a-deployed-node-measures-the-need.md) | *Accepted on delegated authority, 2026-09-19* — §41.3's sixteen-thread core table needs `sched_setaffinity`, which needs `unsafe` or a crate the two-dependency rule forbids. **`unsafe_code = "forbid"` stays workspace-wide with no exception, and the table is retired as a non-goal for `qip-edge-node` as built**, which has one thread by design (the one `thread::spawn` in `main.rs` is inside `mod tests`) and no node anywhere (`execution_nodes = {}` four times). The finding the record writes down: `isolcpus` without an affinity call schedules the process onto cores 0–1 beside the OS, so the Terraform half admits exactly the machine its own refusal message says it costs money to avoid, and the isolated cores idle — capability with no consumer, in ADR 0069's sense. If a thread is ever pinned, **the supervisor pins it** (`CPUAffinity=` in the unit, no `unsafe`, no crate), decided now so the first measured need does not reach for `libc`; the unit-file remedy is handed to the infrastructure owner as a plan-proven change. Reversal: a deployed node's scraped pass or fill series showing tail latency attributable to preemption — which cannot fire before something scrapes — or a second thread with a different core requirement, argued in its own ADR. A narrow `unsafe` allowance, `core_affinity`/`libc`, `taskset` from the startup script, and scoring the table as met by the kernel parameter each rejected |
| [0083](0083-a-trained-model-is-served-in-process-by-an-in-tree-provider-for-the-forms-the-platform-trains.md) | *Accepted on delegated authority, 2026-09-19; the interface is a specification for an implementing lane* — §21.2/§39.1 name ONNX, `tract`, `burn`, `linfa`, `prost`, `ring`, `polars` and `arrow`, all forbidden, and the register scored the pipeline's back half "BLOCKED-by-policy". **It was undone, not blocked.** What `qip-training` produces is four small forms — `TeacherForm::{Linear, BoostedStumps}` and `DistilledModel::{Linear, Tree}` — and the hot path already serves the second pair in-process as `Expr::Model`, "the only form in which a learned function reaches the execution path at all". Option (b): keep the rule, serve in-tree. `qip-ai` (lib) gains the interface — a **closed** `ModelFormat` enum so an unknown format is refused by serde at the wire, `ModelArtifact { reference, format, payload, digest }`, `ServedModel { arity, cost, score }` that refuses rather than clamps an arity or non-finite input, `ModelProvider { serves, serve }` — and `qip-training` (service) gains `InTreeProvider` delegating to the `predict`/`evaluate` the forms already have, so there is no second arithmetic. The hot path is untouched and `qip-strategy` gains no dependency. A model the provider cannot serve refuses with a sentence naming the format, the four it serves, and the two remedies (distil it, or mark its card research-only). Promote is `ModelRegistry::promote` plus a digest-named artifact under the mesh HMAC — **a digest is not a signature; ADR 0043's gap is inherited unchanged**; deploy is the existing `Slot<ModelManifest>` plus a cell that refuses a plan whose inline model's digest the manifest does not name. §39.1's advisory row may take a score as an input to a deterministic filter and never as a threshold, size, limit or permission — ADR 0005 applied to numeric models. Rejected: `tract`/`ort`/`burn` (ADR 0012's specialist condition fails for a dot product and a stump loop), the hosted `LanguageModel` (`NumericGuard` refuses numbers by design), a one-rule strategy per score, and leaving the row BLOCKED. Reversal: a model class outside the four with a measured benefit against the linear and stump baselines through the deflated-Sharpe gate. Rows stay `PARTIAL`; nothing is built here |
| [0084](0084-latency-equalised-dispatch-is-a-release-instant-the-cell-computes-and-the-node-honours.md) | *Accepted on delegated authority, 2026-09-19; the design is a specification for an implementing lane* — three lanes and `dispersion.rs`'s own module doc held that §32.1's single largest mechanism and rule 22 need "a timer wheel on a dispatch thread, and this process has neither". Checked: a timer wheel needs a thread, not an async runtime, and `std::thread` is permitted; the real obstacle is replay, because the cell is handed `now` and reads no clock on purpose. **The policy question dissolves: the cell computes a release schedule as a pure function of the pass** — `FillTimes::release_schedule`, offsets `max_median − median(venue)` over the cycle's venues from the per-venue fill-time median the crate already keeps, unmeasured venues sending unequalised and saying so — and `Placer::place`'s existing `at` becomes "release no earlier than", journaled on `OrderSent` with the offset. The one detail that decides whether it works: **the fill time is measured from `release_at`, not `sent_at`**, or the equaliser chases its own tail. The simulated gateway — the only arm this binary runs — honours the instant deterministically off the same `now`, so a replay releases the same orders in the same order; every offset is zero today because the simulator has no latency model, stated rather than left to read as delivery. Where a real gateway's release thread would sit, when one exists: `qip-edge-node`, one `std::thread` over a `BTreeMap<Timestamp, _>` (the wheel at this scale), recording `qip_edge_release_lag_nanos` — §49.1's arrival-dispersion descriptor ADR 0069 found had no recording site — and withdrawing rather than sending a leg whose instant is past; it is ADR 0082's "second thread". Rejected: a wheel inside the cell (reads a clock, breaks replay), an async runtime, equalising on the ack EWMA (an ack is not a fill and a mean is what one slow sample drags), and retiring a mandatory mechanism that can be built. No exception granted to any rule; rows stay `PARTIAL`; nothing is built here |
