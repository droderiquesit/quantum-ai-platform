# Technical-debt and architectural-conflict register — 2026-09-25

Classified per component group by the M2 debt pass (ADR 0099 programme), from `docs/architecture/current-state/*.json`, each item confirmed in code. The machine-readable source is `docs/reports/tech-debt/*.json`; this file is a one-off rendering of it at `3b48f91b` and is **not** kept in step — re-classify rather than edit.

**179 items.** By class: A — retain 19, B — refactor toward the target 97, D — delete 1, E — needs evidence before changing 62. By risk: medium 84, low 64, high 31.

Prefer migration over rewrite: no item is classed C (replace). Nothing reached from production, and nothing a standing ADR keeps deliberately, is classed D.


## B — refactor toward the target (97)

| Risk | Group | Item | Files | Why | Epic |
|---|---|---|---|---|---|
| high | delivery | The v11.6 live register (`docs/blueprint/traceability-matrix.md`) shows 0% complete and every one of 1566 requirements as UNASSESSED, even though a full assessment pass covering all 1566 requirements already exists in `docs/blueprint/assessment/*.json` (60 files, untracked in git) — and the acceptance suite's own staleness check fails on exactly this file right now | docs/blueprint/traceability-matrix.md:1-15; docs/blueprint/assessment/CICD-b1.json (1 of 60 untracked files; git status shows the whole directory as `??`); backend/crates/apps/qip-cli/src/blueprint.rs:6-20 (module doc: three sources, three rendered views) | ADR 0099 makes this matrix the structural, must-not-be-hand-edited live register for the whole v11.6 programme, generated by `qip blueprint render` from `docs/blueprint/assessment/*.json`. That assessment work has actually been done — 60 domain files totalling exactly 1566 rows (134 COMPLETE, 557 PARTIAL, 629 MISSING, 177 BLOCKED, 42 NEEDS-VALIDATION, 20 INCORRECT, 7 OBSOLETE) — but the files are untracked and the committed matrix predates them (matrix mtime 1790366225 vs. assessment mtimes from 1790366334 onward), so it still reads 0%/UNASSESSED everywhere. Running the acceptance suite's own guard confirms this is a present, not hypothetical, defect: `cargo test -p qip-acceptance --test documentation the_blueprint_views_are_what_their_sources_render_to` FAILED in this exact checkout with `these blueprint views differ from what their sources render to: ["docs/blueprint/traceability-matrix.md"]`. Anyone reading the register that ADR 0099 says to trust structurally is currently told the programme is at 0%, which is a false statement about the state of the system by the enterprise-governance rule's own definition. The fix is mechanical and already built (`qip blueprint render`) — what is missing is committing the assessment JSON and re-running the render as part of the workflow, not new code. | E00 |
| high | edge-cell | The regional edge cell's entire hot path (`Cell::on_bytes`/`Cell::work`/`Cell::flush`) is proven only by tests: `execution_nodes = {}` in every environment's tfvars, so no deployed process ever runs blueprint §16's Reflex Node | infrastructure/environments/dev/terraform.tfvars:227; infrastructure/environments/test/terraform.tfvars:49; infrastructure/environments/stage/terraform.tfvars:49 | REFLEX-002 requires the hot path to run with no synchronous global dependency, and ADR 0008 is exactly what `qip_edge::Cell` implements — but the binary that would run it in production, `qip-edge-node`, is never provisioned anywhere. This is not an unplanned gap: ADR 0035 already authorises one node (`newyork-1`, `us-east4`, shadow mode, dev only) against the in-process `QIP_VENUE_FEED=simulated` matcher, so it needs no venue address range; `dev/terraform.tfvars` carries the exact `execution_nodes` block, commented, ready to uncomment. What blocks it is two concrete, named prerequisites: no boot image has ever been baked (`.github/workflows/image.yml` and `infrastructure/images/execution-node/` exist and have never been dispatched) and no person has yet written the region's capital ceiling into the tfvars file (ADR 0045 proposes `"1000000"` but a proposal is explicitly not a choice). `docs/operations/deploying-an-edge-cell.md` carries the exact sequence. The fix is executing a known runbook, not designing one — prefer that over any workaround that fakes deployment evidence. | E04 |
| high | edge-cell | `Cell`'s mesh uplink/downlinks carry signed capital envelopes, policy/halt payloads and cell state over `qip_transport::MeshPublisher`, which is plaintext and unauthenticated by its own module doc — the exact gap ADR 0012 accepted `rustls` to close, but the dependency was never added | backend/crates/edge/qip-edge/src/mesh.rs:420-451 (CellUplink holds and constructs a live MeshPublisher); backend/crates/edge/qip-edge/src/mesh.rs:669,993 (CapitalDownlink, PolicyDownlink — the same transport for capital grants and halt payloads); backend/crates/libs/qip-transport/src/lib.rs:45-75 (module doc: no TLS, no peer authentication) | This is the same accepted-but-unimplemented ADR 0012 gap `libs-foundation.json` already files against `qip-transport` generally; it is listed here because this crate is the concrete case that makes the exposure legible — `CellUplink`/`CapitalDownlink`/`PolicyDownlink` are precisely the wire that carries a cell's authority to spend capital and its instruction to halt or keep trading, over a channel the library's own doc says has no TLS and authenticates no peer, relying entirely on VPC-level isolation. No fix belongs in `qip-edge` itself — the dependency has to land in `qip-transport` per ADR 0012's own scoping — but the risk this crate carries is real and should not be invisible to a reader of this component group specifically. Do not duplicate remediation effort against `libs-foundation.json`'s item; this entry exists so the edge-cell register does not read as silent on its own exposure. | E03 |
| high | edge-libs | qip-feature-dag's FeatureEngine is genuinely reached from Cell::work in the deployable qip-edge-node binary's source, but no composition root anywhere calls .register() against a single FeatureDefinition — including features::standard_suite, the crate's own twelve-feature reference set — so the live feature graph is structurally empty and evaluate() always returns an empty FeatureVector | backend/crates/edge/qip-edge/src/cell.rs:1008,2829,3015; backend/crates/apps/qip-edge-node/src/main.rs:360; backend/crates/apps/qip-edge-node/src/strategies.rs:355 | This is additive wiring, not a design change: standard_suite(subject) already exists, is tested, and returns a ready Vec<Box<dyn FeatureDefinition>>; the missing piece is one registration loop beside qip-edge-node's existing FeatureEngine::new(...) call. Until it lands, every feature-referencing StrategySpec compiled against the live FeatureCatalogue::new() is refused with 'feature {key} is not registered in the feature graph' (qip-strategy/src/compile.rs:437-441), so no strategy that reads a feature can ever deploy on the edge node — the feature engine and the strategy compiler share one root cause. | E04 |
| high | frontend | Identity Platform is enabled and its outputs computed at the Terraform layer, but no GitOps manifest wires them into the deployed portal container, so the deployment runs on the file-backed development identity store today — directly contradicting that store's own doc comment ('Nothing deployed reaches this file'), and the deployment's own comment pins Cloud Run to a single instance as the workaround rather than closing the gap | infrastructure/gitops/envs/dev/portal.yaml:94-107,122-153; infrastructure/terraform/outputs.tf:294-297; infrastructure/terraform/modules/identity/outputs.tf:1-17 | Terraform sets enable_identity_platform = true for dev, and module.identity's frontend_environment output — surfaced again as the root output identity_frontend_environment — names exactly the two variables the portal needs (ALGORIK_IDENTITY_PROJECT_ID, ALGORIK_AUTHORIZED_DOMAINS). No GitOps manifest consumes either: portal.yaml's env: list (lines 122-153) carries neither variable nor ALGORIK_IDENTITY_API_KEY, and the manifest's own comment (lines 96-106) says so directly — 'No Identity Platform key reaches this deployment... identity.ts falls back to its own on-disk account store.' identity-store.ts's doc comment states the opposite in the present tense, four lines from the code the manifest is contradicting: 'Nothing deployed reaches this file.' The manifest and the blueprint assessment's own independent finding (API-011: 'the deployed portal, if it ran today, would authenticate customers against a per-instance file store rather than Identity Platform') agree with each other and against the code comment. The manifest works around the consequence instead of closing it: maxInstanceCount is pinned to 1 specifically because the on-disk store is per-instance and in-memory, with the comment naming 'raising it is the same change as wiring Identity Platform, and neither is done here' — a named scaling limit standing in for the wiring gap rather than the gap being closed. This is the same defect class (an account store that does not survive a second Cloud Run instance) the identity-store.ts comment says was already fixed once for sessions. | E15 |
| high | frontend | identity.ts's one-time-code HMAC key silently falls back to a hardcoded literal string instead of refusing, when session.ts's own fail-closed refusal (used for cookies) is not repeated here | frontend/portal/src/lib/server/identity.ts:103-110; frontend/portal/src/lib/server/session.ts:129-162 | codeHash(), which keys the HMAC for every email-verification and password-reset one-time code, resolves its signing key as `configuredSessionSecret() ?? "algorik-development"` — a literal, publicly-readable string in this repository. configuredSessionSecret() can legitimately return null in production: the production refusal ('ALGORIK_SESSION_SECRET must be set in production') lives inside the separate signingKey() function in session.ts, guarded by an explicit NODE_ENV === "production" check that codeHash() does not call and does not repeat. If the mounted session-secret file is ever absent or empty on a production-postured deployment, sign-up and password-reset codes are silently HMAC'd against a key anyone can read from source, instead of the process refusing to issue or verify a code — the opposite of the fail-closed default (CLAUDE.md Principle 3) and of signingKey()'s own behaviour in the same file family. The function's own comment documents that this exact bug class already happened once, for the cookie signer ('this fell back to the development key in a production deployment whose secret was a file'); the fix there was resolving the secret through the _FILE-aware helper, but the literal fallback string itself was never replaced with a refusal, so the same failure mode remains open for every one-time code the identity service issues. No test exercises this branch: grep -rn 'algorik-development\|codeHash' frontend/portal/tests returns nothing. | E13 |
| high | infrastructure | The GitOps control-plane cluster is suspended and has never completed a bootstrap, so no Config Connector `RunService` manifest is reconciled anywhere — every Cloud Run workload's declared state (identity/grants half applied by Terraform) and its actual serving state (the service half, meant to be owned by Argo CD per ADR 0036) have no live controller keeping them in agreement | infrastructure/terraform/modules/gitops-control-plane/main.tf:103-131; infrastructure/gitops/bootstrap/README.md:1-96; infrastructure/docker/argocd-patched.Dockerfile:1-122 | The vendored Argo CD v3.5.2 image (row 2 of the bootstrap's own 8-step apply order) is refused by vendor.yml's CRITICAL Trivy gate on CVE-2025-68121 (vulnerable git-lfs/kustomize binaries copied in unmodified from upstream), so infra.yml's `bootstrap` step has never completed on any cluster and the one applied cluster (dev) was manually suspended — a targeted destroy of the cluster resource, keeping the etcd key and the rest of state — per ADR 0093. `infrastructure/docker/argocd-patched.Dockerfile` is the accepted, self-verifying remedy (rebuilds kustomize on a patched Go toolchain, deletes git-lfs) but has never been dispatched by vendor.yml, so its digest has never been pinned into `infrastructure/gitops/bootstrap/argocd/overlays/*/kustomization.yaml`. This is a migration in progress with a concrete, already-written next step, not a dead end — dispatching the build is the unblocking action. | E14 |
| high | kernel-planes | qip-kernel's `capacity_review` module — a complete, internally-tested implementation of blueprint §20.1's sixth risk control (the capital level at which an edge decays, from book depth, an impact law and each leg's own stated cost) — has zero callers anywhere in the workspace, so the control it implements can never fire in a deployed process | backend/crates/runtime/qip-kernel/src/capacity_review.rs:87-315; backend/crates/runtime/qip-kernel/src/lib.rs:38 | Confirmed with `rg -n 'capacity_review\|CapacityReview\|LegCapacity' backend/ --type rust`: the only hits are the module's own definitions and the bare `pub mod capacity_review;` declaration in lib.rs:38 — no call from Platform::run_cycle, no call from any crates/apps/*/src file, and no reference from any qip-acceptance suite. The module is not a stub: it is 686 lines with 4 passing unit tests covering the real edge cases (a leg whose assumed cost does not cover half the spread, a leg with no readable depth named rather than treated as roomy). This is precisely the shape the repository's own domain rule names as the template for what not to add — 'RiskState::expected_shortfall is currently always empty, so MaxExpectedShortfall can never trigger... A control that cannot fire reads as protection and is not' (.claude/rules/domains/risk-and-execution.md) — except here the control is not even wired to attempt firing; it is absent from every stage of the cycle. It is not dead code with no plan: it directly implements a numbered blueprint §20.1 control that ADR 0099 makes the architecture of record, so wiring a caller (the arithmetic is already correct and tested) is the migration this item needs, not a rewrite and not a deletion. | E07 |
| high | libs-finance | TransactionCostModel::estimate() panics on an unpriceable model inside a Result-returning production caller, instead of the caller using the safe checked_estimate() twin | backend/crates/libs/qip-financial/src/costs.rs:465; backend/crates/libs/qip-financial/src/costs.rs:468; backend/crates/services/qip-capital-fabric/src/transfer.rs:441 | costs.rs:465-469 `pub fn estimate` matches `checked_estimate`'s Err arm with `panic!("{refusal}")` (confirmed at line 468, not inside any #[cfg(test)] module). TransferCostModel::price (transfer.rs:441, a Result<TransferCost>-returning function that otherwise refuses every invalid input explicitly, e.g. a negative amount or non-finite opportunity rate) calls `self.fx.estimate(amount, participation)` at transfer.rs:472. The one finite-check made beforehand covers `participation`, not the configured bps rates on `self.fx` or an oversized notional -- both of which `checked_estimate` can still refuse -- so a bad reference-data rate reaching this path aborts the process under `panic = "abort"` rather than returning a refusal transfer.rs's caller could handle. This is the exact case core-rust.md prohibits ('todo!()/unimplemented!()/panic!() in a Result-returning function') and the exact failure CLAUDE.md principle 3 names ('a configuration that would relax [a safety default] stops the process rather than lowering it' -- here a bad config *does* stop the process, but as an uncontrolled abort with no refusal recorded in the event log, not as a deliberate refusal). Fix is small and does not require a redesign: swap the call site to `checked_estimate` and propagate the Result, or delete the panicking convenience wrapper now that its own doc comment says prefer checked_estimate 'anywhere the refusal can be handled.' | E06 |
| high | libs-finance | qip-compliance::signing::SigningKey is symmetric HMAC-SHA256; the capital-approval and artifact-provenance controls it signs have no asymmetric signing, identity binding, key rotation/revocation or countersigned timestamping | backend/crates/libs/qip-compliance/src/signing.rs:1-30; backend/crates/libs/qip-compliance/src/signing.rs:40; backend/crates/libs/qip-compliance/src/plane.rs:53 | signing.rs's own module doc (lines 1-30, read in full) states the gap plainly: HMAC is symmetric, so 'whoever can verify can also sign,' and lists four missing production requirements verbatim -- asymmetric signing, identity binding, key rotation/revocation, countersigned timestamping -- ending 'None of those are in this build.' This is not a hidden shortcut: `ControlStatus.caveats` (plane.rs:53) surfaces it on every generated ComplianceReport rather than claiming full compliance, and ADR 0043 ('The cryptography this platform has, and the three gaps no crate closes', status: proposed) names this as one of exactly three gaps no in-tree crate may close, alongside a CSPRNG and anchoring the event-log chain. docs/blueprint/assessment/GOV-012, CONTRACT-b1, SEC-b2, MODEL-b3 and REFLEX-b1 all independently confirm the same finding against the same file and agree the fix needs either a new dependency class (a fresh ADR under the two-dependency policy) or routing through Cloud KMS (GCP v2.1 §21.4), not an in-tree rewrite -- the module's own key-id field is already carried forward for exactly that migration ('so that when asymmetric signing arrives, existing records say which key they were made under'). Evolvable, not a contradiction requiring replacement of the whole control -- the HMAC layer stays as an integrity check alongside a future asymmetric signature, per its own design. | E13 |
| high | libs-finance | qip-risk's VaR/expected-shortfall/factor risk measurement never reads qip_financial::risk_profile::Greeks; an options or otherwise convex position's pre-trade risk figures are computed as if the position were linear | backend/crates/libs/qip-risk/src/metrics.rs:24; backend/crates/libs/qip-risk/src/metrics.rs:37; backend/crates/libs/qip-risk/src/metrics.rs:52 | `grep -n 'Greeks\\|delta\\|gamma\\|vega' backend/crates/libs/qip-risk/src/metrics.rs backend/crates/libs/qip-risk/src/factor.rs` returns nothing -- confirmed. `Greeks` (risk_profile.rs:10) exists and is consumed in production, but only by `hedge_review.rs` for hedge sizing, not by `qip-risk::metrics`'s historical_var/parametric_var/expected_shortfall or `qip-risk::factor`'s FactorRisk/RiskDecomposition, which are what `RiskState` feeds into the pre-trade `LimitSet` that gates order sign-off every DECIDE-stage cycle. An options book's VaR/ES is therefore priced on notional/price exposure alone, with delta/gamma/vega convexity invisible to the same limit checks the risk-and-execution domain rule calls load-bearing. Confirmed independently in docs/blueprint/assessment/RISK.json's RISK-008 (PARTIAL, work item E07): 'gap' and 'basis' are not named as distinct measures anywhere in qip-risk, and qip_financial::risk_profile::Greeks is never read by qip-risk's measurement.' This is the same shape of problem CLAUDE.md names for RiskState::expected_shortfall before it was fixed: a limit computed from an incomplete input reads as protection for an options book without being one. | E07 |
| high | libs-foundation | Central-plane state is ephemeral in every deployed environment, including prod: `QIP_STORAGE_TARGET=memory` everywhere, and the one durable target (`engine`) is wired only through an execution node that no environment provisions | infrastructure/terraform/variables.tf:365-382; infrastructure/gitops/envs/prod/api.yaml:72-73; infrastructure/gitops/envs/dev/api.yaml:72-73 | Terraform's own doc calls `memory` 'a statement rather than a placeholder', but every Cloud Run binary (api, fastbrain, deepbrain) in dev, stage AND prod runs with QIP_STORAGE_TARGET=memory, so a restart loses all in-process state. The only durable target this build implements (`engine`, an embedded WAL) is reached solely by the execution-node's startup script, and execution_nodes = {} in every environment's tfvars, so nothing durable ever runs. This directly contradicts the platform's own stated goal ('Every decision reproducible from the log alone', .claude/rules/10-product-direction.md) and the governance rule that the event log is hash-chained 'on purpose' — there is no deployed process today whose chain survives a restart. | E05 |
| high | libs-foundation | qip-transport's mesh and HTTP client remain plaintext and unauthenticated; the TLS dependency ADR 0012 accepted specifically to close this gap was never added | backend/crates/libs/qip-transport/src/lib.rs:45-75 (module doc: 'This transport has no TLS and no authentication of the peer... Production must add mutual TLS'); backend/crates/libs/qip-transport/Cargo.toml (dependencies: serde, serde_json, qip-core, qip-contracts, qip-events — no rustls); docs/adr/0012-where-a-library-earns-its-place.md (Decision → 'Admitted now: TLS. rustls... Scope: the I/O edge only. qip-transport') | ADR 0012 (accepted) is explicit that rustls is admitted at qip-transport's I/O edge specifically because 'a hand-rolled TLS stack guarding real money is indefensible' — this is the wire that carries signed capital envelopes and cell state deltas. The ADR was accepted but never acted on: qip-transport's Cargo.toml carries no rustls dependency and its own module doc still states the gap plainly, relying entirely on VPC-level network isolation. An accepted decision to close a named, acknowledged security gap sits unimplemented rather than merely deferred. | E03 |
| high | libs-foundation | `CapitalEnvelope::admit` clamps an oversized order to remaining headroom (`CapitalGrant::Reduced`) instead of refusing it whole, contradicting the house rule against silently correcting an invalid input | backend/crates/libs/qip-contracts/src/capital.rs:190-228 (the clamp: lines 219-227 compute `cap` and return `CapitalGrant::Reduced(cap)` rather than `Refused`); backend/crates/libs/qip-contracts/tests/contracts.rs:503-524 (an_oversized_order_is_reduced_rather_than_approved_as_requested); backend/crates/edge/qip-edge/src/cell.rs:7371-7373 (doc comment contrasting this with hold_region_capital's refuse-whole design right beside it) | CLAUDE.md principle 2 states plainly: 'Refuse rather than guess... A value silently corrected is a caller bug that survives.' The codebase demonstrates it knows the difference — `hold_region_capital`, right beside this call site, refuses whole rather than reducing. `CapitalEnvelope::admit` does the opposite for the gross-limit bound, and the behaviour is deliberate (it has a named test asserting the reduction). This sits in the capital-limit path every order is checked against, so the inconsistency with the house rule is load-bearing rather than cosmetic. | E07 |
| high | libs-intelligence | `Capability::WriteResearchMemory` is granted to the LearningAttribution agent but authorised nowhere, and the Desk's ResearchMemory is wired with `Gated::new` (a fixed, never-updated slot) rather than `Upstream::gate` (a live one) — so two of the eighteen governed agents read a research memory that is structurally permanent empty in every deployment | backend/crates/libs/qip-agents/src/capability.rs:44; backend/crates/agents/qip-investment-agents/src/manifests.rs:492; backend/crates/agents/qip-investment-agents/src/desk.rs:105 | `Gated::new`'s own doc says a gate built this way 'shares its slot with nothing, so what it holds is fixed for its lifetime'; `Desk::new` builds `memory` this way over the empty `ResearchMemory::new()` platform.rs constructs, and no call site anywhere calls `ctx.authorise(Capability::WriteResearchMemory, ...)` (grep across crates/agents and crates/libs/qip-agents/src confirmed). `LearningAttribution::analyse` and `AdversarialReviewer` therefore always take the `agents.is_empty()` branch in a running deployment even though both pass their own hand-populated-memory tests. This is exactly the shape the repo's own rules name as the template to avoid: 'RiskState::expected_shortfall... a control that cannot fire reads as protection and is not' (.claude/rules/domains/risk-and-execution.md). Migrating `Desk::new` to `Upstream::gate` and adding one write call site (at the LEARN-stage seam that already updates `qip_ai::EpisodicMemory`) closes it without a rewrite. | E08 |
| high | services-capital-exec | CapitalEnvelope grants are signed with symmetric HMAC-SHA-256: any cell that can verify a grant can also mint one | backend/crates/services/qip-capital/src/envelope.rs:19-40; backend/crates/services/qip-capital/src/envelope.rs:71 | EnvelopeIssuer signs with qip_core::hmac_sha256, a symmetric primitive: the same key verifies and mints, so a compromised or malicious cell that can check a grant can also forge one. The module's own doc comment names the missing production requirements verbatim (asymmetric signatures, an HSM/key store, key rotation, per-envelope revocation, replay scoping) and calls the current scheme 'adequate for a single-operator deployment and for tests, and not adequate for production.' This is exactly one of the three in-tree-crypto gaps `.claude/rules/architecture/00-boundaries.md` names as still open ('asymmetric signing, a CSPRNG, and anchoring the event-log chain are three gaps no in-tree code may close — see ADR 0043 (proposed)'), and is independently confirmed in `docs/blueprint/assessment/GOV.json` (GOV-012), `SEC-b1.json` (SEC-001) and `CONTRACT-b1.json` (CONTRACT-004), all tagged against the same gap. Cannot be closed in-tree under ADR 0002/0009 (two dependencies only); it needs a dependency-class ADR for an asymmetric primitive before code changes, which is what ADR 0043 (proposed) is for. | E13 |
| high | services-capital-exec | Evidence-weighted capacity-aware capital sizing is fully implemented but never populated in production: CentralPlane.proposals is always empty, so CapitalAllocator::allocate runs on zero strategies every cycle | backend/crates/services/qip-capital/src/allocation.rs:40-53; backend/crates/services/qip-capital/src/capacity.rs:1-226; backend/crates/runtime/qip-kernel/src/central/plane.rs:730 | StrategyProposal (which carries the CapacityModel every capital-sizing decision is supposed to respect) has exactly one insertion path into CentralPlane's `proposals: BTreeMap<StrategyId, StrategyProposal>` — `set_proposal`, called only from `central/learning.rs`'s private `resize()`. `resize()` itself only mutates a proposal that must already exist (`self.proposal(strategy)?`), so nothing in non-test code ever performs the original insert; `grep -rn 'StrategyProposal {' backend/crates/runtime backend/crates/apps` outside tests returns nothing, and `grep -rn 'CapacityModel::new\\|CapacityModel {'` over the same scope returns nothing. `CapitalAllocator::allocate(&proposals, ...)` (plane.rs:1576,1709, reached every DECIDE cycle from platform.rs:7214) therefore always allocates against an empty proposal list in production today. The machinery reads as a working, evidence-driven, capacity-bounded allocator — API surface, 127 crate tests, a public setter and getter — while being structurally inert, which is the same shape `.claude/rules/domains/risk-and-execution.md` names as the thing not to add ('a control that cannot fire reads as protection and is not'). This is a sizing feature, not a live-capital path, so it is not blocked by ADR 0099's conflict register; wiring a real proposal source in is ordinary in-tree work. | E07 |
| high | services-data-world | Two independently-versioned, non-composing event envelopes exist in the sensed-data path, contradicting qip-streaming's own explicit 'one event history' invariant | backend/crates/services/qip-streaming/src/lib.rs:1-17; backend/crates/services/qip-streaming/src/envelope.rs:160,297; backend/crates/services/qip-market-ingestion/src/connector/envelope.rs:86-110 | qip-streaming's own module doc says: 'a second parallel envelope would be the worst possible outcome for a platform whose whole audit story is that there is one event history' and claims 'Everything the platform senses enters through here... wearing the same envelope' (lib.rs:1-17). `rg -n 'qip_streaming::' backend/crates/services/qip-market-ingestion/src backend/crates/services/qip-data-finder/src` returns nothing — the SENSE-stage crates never import qip-streaming at all. Every `SensedRecord` is instead wrapped in `MarketEventEnvelope` (connector/envelope.rs:86, its own `SchemaVersion`), and `StreamEnvelope::seal` (envelope.rs:181) is called only from `qip-kernel/src/platform.rs` (7 call sites, all for cycle/decision/order facts, not for connector output). The two schemas do not compose — neither wraps the other — which is precisely the 'second parallel envelope' the doc warns against, and it is already in production, not a hypothetical. | E03 |
| high | services-data-world | qip-kernel's stream journal has exactly one construction site and it is always the in-memory transport; the file-backed constructor the same type offers is never called | backend/crates/runtime/qip-kernel/src/platform.rs:623; backend/crates/runtime/qip-kernel/src/platform.rs:4486; backend/crates/runtime/qip-kernel/src/platform.rs:16161 | `rg -n 'DurableLogTransport::in_memory\|journal:' backend/crates/runtime/qip-kernel/src/platform.rs` shows the private `journal` field (platform.rs:623) has one constructor call (platform.rs:4486) and it is always `::in_memory("kernel-journal")`; `DurableLogTransport::open` (durable.rs, file-backed) has zero callers in qip-kernel. Governance calls the event log 'hash-chained on purpose' and says 'nothing may write a record that cannot be replayed' — a journal that never survives a process restart cannot be replayed after one. This corroborates, from qip-streaming's own construction site, the same underlying fact `docs/reports/tech-debt/libs-foundation.json` raised for `QIP_STORAGE_TARGET=memory` at the central-plane storage layer; this entry is the equivalent gap in the stream-journal type itself, which offers a real alternative constructor nothing calls. | E03 |
| medium | apps | Capital-call settlement and expected-inflow posting are named in `qip-api`'s treasury routes but not implemented in this build: an admitted request never itself funds, and nothing this build runs ever posts or settles an inflow | backend/crates/apps/qip-api/src/routes.rs:504-528 (funding is deliberately two-step: Platform::fund_user is the only funder and re-runs the gates); backend/crates/apps/qip-api/src/routes.rs:540-552,563-598 (doc comments: 'nothing this build runs ever posts/settles it'); backend/crates/apps/qip-api/src/ledger_views.rs:289-627 (LedgerUsersView, InflowDeclarationBody::parse, decided_eligibility) | The 27-test route surface (ledger_routes.rs) proves the parts that exist end to end — eligibility decisions, mandate/balance views, refusal leaving the ledger row unchanged — but the two-step funding split is itself deliberate (an admitted investment request must not itself move money, matching capital protection's 'limits are checked before an order object exists'). What is genuinely incomplete against v11.6's Ledger, Accounting & State of Truth target is that nothing downstream of an accepted expected-inflow declaration ever settles it — the declaration is recorded and then nothing acts on it, which is a real gap between what the route table promises ('expected-inflow') and what the platform does with one, not a safety-motivated refusal like the replay-path finding above. | E07 |
| medium | delivery | `Makefile`'s `test` target omits `--no-fail-fast`, unlike `ci.yml`'s test job, the Makefile's own `count` target, and CLAUDE.md's documented command — a local `make test` can abort at the first failing binary and under-report, which is the exact failure mode this repository's own rules call out by name | Makefile:54-55 (test: cd backend && cargo test --workspace --all-features); Makefile:63-64 (count: ./scripts/count-tests.sh, which does use --no-fail-fast); .github/workflows/ci.yml:79 (test job, --no-fail-fast) | `.claude/rules/02-change-management.md`, the working agreement every agent in this repository reads first, states this exact risk in these exact words as the reason `--no-fail-fast` is mandatory. `make test` is the one place in the tree that does not follow its own rule: a developer or agent running `make test` locally (as opposed to `make count` or CI) gets a build that can stop at the first failing crate and silently describe a fraction of the suite as if it were the whole of it. This is a one-line fix with no design question attached. | E01 |
| medium | delivery | The two-dependency allowlist exists twice, independently hand-maintained: `scripts/check-dependencies.sh`'s PERMITTED bash array and `backend/deny.toml`'s `[bans].allow` list, kept in agreement only by discipline and by docs/DELIVERY-STATUS.md prose, not by a shared source | scripts/check-dependencies.sh:19-35 (readonly PERMITTED, 11 entries); backend/deny.toml:86-98 (allow = [...], the same 11 entries) | The two lists currently agree entry-for-entry (verified: both name serde, serde_json, serde_derive, proc-macro2, quote, syn, unicode-ident, serde_core, itoa, zmij, memchr), but they are two independent enumerations of the same fact — exactly the pattern principle 6 of the top-level CLAUDE.md warns about ("two independent claims about the same fact will disagree, and the louder one will be wrong"). A future dependency change edited in one file and not the other would pass one gate and fail the other, or worse, pass both if edited inconsistently under time pressure with nobody noticing the second file exists. Generating one from the other (deny.toml as the single source, with check-dependencies.sh reading it, or vice versa) removes the duplication without removing either gate. | E01 |
| medium | delivery | Delivery-side regression and audit tooling that exists specifically to catch governance/security regressions is not wired into any CI job or Makefile target: `test_hooks.py` (the destructive-command guard's own regression suite, 17 table-driven cases), `audit-dead-code.py`, and `audit-register.py` (the tool that catches stale evidence claims like the ones this very report was asked to distrust) | .claude/hooks/test_hooks.py:1-108 (17 GUARD_CASES, run only via `python3 .claude/hooks/test_hooks.py`); scripts/audit-dead-code.py:1-30; scripts/audit-register.py:1-40 | `test_hooks.py` is the only thing that proves the destructive-command guard still blocks what it claims to block and still allows what it claims to allow ("a guard that refuses everything is not secure, it is broken", per its own docstring) — a guard this important having its regression suite run only by hand means a future edit to the guard can silently stop catching what it exists to catch, with nothing red in CI to say so. The two audit scripts are lower-stakes (advisory, not safety-critical) but exist for the same reason this task exists: to catch drift between what the tree claims and what it does. None of the three needs new code; each needs one job step. | E01 |
| medium | delivery | The worker-orchestration concurrency ladder (8/16/32/64/100, gated on a measured ≥10% improvement in verified output per token) and the model-gateway's call-budget/pause mechanism are self-measured by policy convention and exercised by hand — no automated check enforces that a rung only climbs on real measurement, or that work queued beyond the current ceiling is refused rather than silently over-subscribed | docs/plan/algorik-orchestration-policy.md:58-83 (the ladder and its climb/drop rules, stated as policy prose); scripts/worker-capacity.sh:1-30 (reports the ceiling; does not gate anything, always exits 0); scripts/model-gateway.mjs (the fail-closed call-ceiling itself) | `worker-capacity.sh`'s own purpose statement is explicit that it reports rather than gates, and ADR 0098 records the ladder as a policy the desktop-fabric programme runs by, not as something any tool enforces. That is a reasonable shape for a human-supervised ladder, but it means the two failure modes the ladder exists to prevent — climbing a rung on noise rather than a measured improvement, and over-subscribing workers past the ceiling worker-capacity.sh actually measured — are both currently unguarded by anything that would turn red. The gateway side of this is closer to done than the ladder side: model-gateway.mjs already fails closed on a missing budget config; what is missing is a test proving run() actually refuses once ALGORIK_WORKER_MAX_CALLS is exhausted. | E01 |
| medium | edge-cell | `CellMetrics`' full Prometheus series (halt state, region-share application, quote-budget depletion, dark regions, settlement-unprojected venues, fill dispersion) is emitted and tested, but scrapes no deployed process because the binary that serves it is the same one item 1 shows is never provisioned | backend/crates/edge/qip-edge/src/telemetry.rs:72-359,381; backend/crates/edge/qip-edge/src/cell.rs:3864,4245,4426,7495,7770 (the five metrics.refusal(...) call sites); backend/crates/edge/qip-edge/tests/telemetry.rs (17 #[test]) | This is the observability domain's own conclusion for this crate, worth stating plainly here rather than left implicit: the emitting half is real, tested and correctly wired (19 named series, each proven by a dedicated test), so the remaining work is not more instrumentation code — it is item 1's deployment runbook plus, per `.claude/rules/domains/infrastructure.md`, confirming a pod is actually scraped before any alert policy naming an `edge_*` series is switched on. Writing more `qip-edge` telemetry code cannot close this gap; only a running node and a scrape can. | E12 |
| medium | edge-cell | `qip-edge` depends on `qip-execution-engine`, but zero source files in the crate import it; the dependency exists, by the crate's own committed comment, only to satisfy an architecture test's vacuity guard, not because the cell functionally uses it | backend/crates/edge/qip-edge/Cargo.toml:32-39; backend/crates/tests/qip-acceptance/tests/architecture.rs:757-786 (only_the_edge_cell_itself_holds_an_order_manager) | `grep -rn qip_execution_engine backend/crates/edge/qip-edge/src` returns nothing — confirmed. The architecture test's own vacuity-guard assertion, `reachable_from(&graph, "qip-edge").contains("qip-execution-engine")`, is satisfied purely by this unused Cargo.toml line, and the dependency's own comment says so: 'the edge is a pinned claim, not a use... the guard needs re-stating against the cell's real order path before this line can go.' A test whose vacuity guard is trivially satisfiable by an unused dependency is not proving what it claims to prove — the same class of defect `MaxExpectedShortfall` was for risk limits (a control that reads as protection and is not). The cell sends orders exclusively through its own `Placer` trait (cell.rs:8051), so the guard should assert reachability of that seam, or of a real `OrderManager`-typed field, not of a crate name alone. | E00 |
| medium | edge-cell | `cell.rs` is a single 9,040-line file — 45% of the crate's source — holding the whole per-pass cycle (pricing, netting, crossing, disposition, halts, mesh state-delta, journal writes) as one type, with five `#[allow(clippy::too_many_arguments)]` suppressions on functions whose own parameter lists trip clippy's lint | backend/crates/edge/qip-edge/src/cell.rs:1-9040 (total; struct Cell spans 1003-7846); backend/crates/edge/qip-edge/src/cell.rs:3841 (Cell::send); backend/crates/edge/qip-edge/src/cell.rs:6132 (Cell::rest_cycle_leg) | CLAUDE.md principle 4 — 'make it structural' — and the crate's own module structure argue against this: every other concern in this crate (arbitrage, decomposition, dispersion, feasibility, journal, mesh, passive, quoting, reservation, resume, settlement...) already lives in its own file with a clear boundary; `Cell` alone did not receive the same treatment and instead accreted the entire cycle inline. The five suppressed too-many-arguments lints are clippy naming the exact functions (`send`, `rest_cycle_leg`, `send_cycle_leg`, `observe_cycle_leg`, `place`) whose parameter lists are the visible symptom of state that should be grouped or of logic that should sit in the module its inputs already belong to (e.g. `send_cycle_leg`/`observe_cycle_leg` read like `arbitrage.rs`/`decomposition.rs` material). Splitting is additive — the aggregate `Cell` type can stay the composition root while its per-concern methods move into `impl Cell` blocks in per-topic files, which is refactoring in place, not a rewrite. Risk is medium rather than high: nothing here is a functional defect, but a single 9k-line file is the highest-probability merge-conflict surface in the crate for a programme running many parallel lanes, which this repository's own history (ADR-numbering collisions, stale line citations throughout `.claude/rules/domains/observability.md`) shows is a real cost here. | E00 |
| medium | edge-cell | `CellConfig`'s quoting, dispersion and decomposition policies are structurally configurable per cell but every field stays at its compile-time `::default()` — `qip-edge-node`'s composition root, the only place allowed to read deployment configuration, never sets any of the three | backend/crates/edge/qip-edge/src/cell.rs:389-405 (CellConfig::new sets quote_limits/dispersion/decomposition to ::default() unconditionally); backend/crates/edge/qip-edge/src/quoting.rs:92-103 (7 DEFAULT_* rate-limit tunables); backend/crates/edge/qip-edge/src/dispersion.rs:52 (DEFAULT_DISPERSION_BOUND=250ms, DEFAULT_MIN_SAMPLES=3, DEFAULT_WINDOW=64) | `grep -n 'DecompositionPolicy::\\|DispersionPolicy::\\|RateLimits::' backend/crates/apps/qip-edge-node/src/*.rs` returns nothing, and `main.rs`'s only field write on `cell_config` after construction is `.with_venue(...)` — confirmed. The type system supports a different policy per cell (and, by extension, per venue's real latency/message-rate profile), but no composition root has ever exercised that capability, so every cell that could ever be deployed would run identical anti-dispersion and message-budget parameters regardless of which venue it faces. This is contained today by item 1 (nothing is deployed), so the risk is latent rather than active — but it is exactly the gap that would surface silently the first time a real venue's message-rate tolerance differs from the constants qip-edge ships with, since `std::env` reads are forbidden outside `crates/apps/**` and no other seam exists to carry the value in. | E04 |
| medium | edge-libs | qip-protocols' actual venue-wire decoders (FixDecoder, ItchDecoder, SbeDecoder, JsonFrameDecoder/StreamAssembler) — the reason the crate exists — have zero production callers anywhere outside their own crate; the only decoder wired into any deployable binary is a locally-defined synthetic DepthDecoder reading a tab-separated in-process feed | backend/crates/apps/qip-edge-node/src/feed.rs:74-75,349-399; backend/crates/edge/qip-edge/src/cell.rs:44,1005,1202; backend/crates/edge/qip-protocols/src/fix.rs,itch.rs,sbe.rs,framing.rs (zero non-test, non-own-crate references confirmed by workspace-wide grep) | This is the gap GCP v2.1 §6 names directly: 'Venue sessions: direct FIX/WebSocket/native adapters... No generic HTTP service between Reflex and venue.' The code to do it already exists, is extensively tested (51 tests, including a split-at-every-byte-offset invariant and a schema-tolerance invariant), and is registry-compatible with the Decoder trait qip-edge's Cell already holds. What is missing is a real venue to decode from, which item 1's deployment gap and the paper-trading boundary jointly explain: this is work waiting on a target, not a defect to fix independently of it. | E04 |
| medium | edge-libs | L2Book and L3Book's price ladders (qip-orderbook) enforce no maximum depth, level count, or resting-order count anywhere in the crate — unlike every other bounded-state structure in this group | backend/crates/edge/qip-orderbook/src/l3.rs:95-101 (orders: BTreeMap<u64, RestingOrder>, no cap); backend/crates/edge/qip-orderbook/src/l2.rs; backend/crates/edge/qip-orderbook/src/ladder.rs | CLAUDE.md's product direction names 'bounded working sets, always' as a standing decision, and two sibling crates in this same group demonstrate the house pattern of refusing past a threshold rather than growing forever. A venue publishing pathological depth, or a bug on either end of the wire, has nothing here to stop the ladder from growing without limit; a grep for bound/cap/limit/MAX across l2.rs, l3.rs, ladder.rs and lib.rs returns nothing. Risk is contained today only because no real venue feed is connected (items 1 and 8); it should be closed with an explicit refusal at a configured bound, not a silent clamp, consistent with 'refuse rather than guess', before a real feed exists. | E04 |
| medium | frontend | The portal's (marketing) route group and frontend/landing are two independently hand-editable copies of the same eleven-to-twelve public pages, including the platform's one required paper-trading disclosure, and the surface the tree's own README names as the eventual sole owner has no deployment path in Terraform, GitOps or deploy.yml at all | frontend/landing/README.md:16-52; frontend/portal/src/app/(marketing)/welcome/page.tsx:1-30; frontend/portal/src/app/(marketing)/legal/risk-disclosures/page.tsx | frontend/landing/README.md:44-52 states plainly that its marketing copy — including /legal/risk-disclosures ('Simulated ≠ live; no live order is submitted') — is taken 'word for word' from the portal's (marketing) group today, calls this 'a holding position, not a design: the duplication is real and one of the two must go,' and names the portal's group as the one that should become redirects to this app; that change has not been made (no redirect exists under the portal's (marketing) tree). The surface the README says should become the sole owner is itself not deployed anywhere: infrastructure/environments/dev/terraform.tfvars pre-authorizes an Identity Platform domain for a landing hostname that has never been created, and its own comment states 'frontend/landing and frontend/portal exist in the tree and are in no catalogue, no image matrix and no manifest.' Until one surface is made the redirect target, an edit to the risk-disclosure wording lands in whichever app someone happened to open, and the other silently keeps stating something different. | E14 |
| medium | frontend | The platform's REST response shapes exist in three independently hand-maintained forms — Rust routes.rs, TypeScript types.ts, and a JS test-fixture server — with no shared schema and no automated equivalence check, and the browser-side client accepts each answer with a compile-time-only type assertion | frontend/portal/src/lib/api/types.ts:1-16; frontend/portal/src/lib/api/client.ts:241; backend/crates/apps/qip-api/src/routes.rs | types.ts's own doc comment says its 669 lines are 'transcribed from the handler table in backend/crates/apps/qip-api/src/routes.rs, field for field' — a person reading the Rust and typing the TypeScript by hand, with no codegen and (per ADR 0002/0009) no schema-generation dependency available to either side. client.ts:241 then hands every caller `data: body as D` — a TypeScript type assertion with zero runtime validation, so a field renamed or retyped on the Rust side produces no error on the browser side, only a wrong or undefined value reaching a risk-desk panel. Compounding it, tests/support/upstream-stub.mjs is a third, independently hand-written fixture server standing in for qip-api in two Playwright suites, and most other specs intercept at the browser boundary with their own per-file page.route fixtures — further independent copies of the same shapes. The module holding client.ts/endpoints.ts carries zero direct unit tests. Closing this does not require a new dependency: the repository's own convention of a text-parsing contract test (api_boundary.rs already parses routes.rs's route table this way) could instead assert that each field types.ts declares for a route is present in the corresponding Rust struct, catching a rename without adding a schema library to either language. | E02 |
| medium | frontend | Two independent 'no false marketing claim' mechanisms exist for a trading-adjacent public site, and neither covers the one surface the other is knowingly copied from | frontend/packages/brand/src/index.tsx:189-207; frontend/landing/lib/claims.mjs:1-40; frontend/landing/tests/claims.spec.mjs | @algorik/brand's auditCopy() is a regex denylist ('guaranteed return', 'quantum advantage', 'risk-free', 'fully autonomous live trading', ...) with zero callers anywhere in the workspace (grep auditCopy: definition only) and no test asserting it ever runs. frontend/landing/lib/claims.mjs is a different, actively enforced mechanism: every quantity on the landing site must carry a data-claim-status, a component throws on an unrecognised status, and tests/claims.spec.mjs sweeps the rendered DOM of every page for one. Neither mechanism reaches frontend/portal/src/app/(marketing) — the group the landing's own README names as the word-for-word source its copy is taken from today (see the duplication finding above) — so the one surface whose text the other is derived from carries zero automated protection against a forbidden phrase or an unlabelled figure reaching it first. | E16 |
| medium | infrastructure | The portal and landing Cloud Run services are deployed by an imperative script (`scripts/deploy-frontends.sh`) that never touches the GitOps source of truth, while `infrastructure/gitops/envs/*/kustomization.yaml` still declares `qip-portal` as `TO-PIN` in all four environments — two independent, disconnected mechanisms both claim to own how the portal reaches Cloud Run | scripts/deploy-frontends.sh:1-19,231-257; infrastructure/gitops/envs/dev/kustomization.yaml:32-45; infrastructure/terraform/catalogue.tf:876-921 | `catalogue.tf`'s own comment names `infrastructure/gitops/envs/<env>/portal.yaml` as the console's Cloud Run service of record, reconciled the same way api/fastbrain/deepbrain are — but no run of deploy.yml/Kargo has ever built or promoted `qip-portal`, so its digest stays `TO-PIN` in every environment. In its place, `scripts/deploy-frontends.sh` builds, attests (correctly — by digest, against the same Binary Authorization attestor and key version Terraform holds, per its own header comment) and deploys the portal and landing directly with `gcloud run deploy`, and never writes the resulting digest back into `kustomization.yaml` or commits anything. The two paths cannot converge: a future Argo CD sync (once the suspended cluster returns, see the GitOps item above) would find `envs/dev/portal.yaml` referencing a `TO-PIN` image and either fail validation or attempt to redeploy over whatever the script last put there, with no record connecting the two. Note that `infrastructure/docker/README.md:57-63`'s claim that the frontend deploy has 'no signing step and no attestation' is itself stale against the current script (last changed 2026-09-21, after the README) — the script does attest and does deploy by digest; that documentation drift should be corrected alongside unifying the deployment path. | E14 |
| medium | infrastructure | The GitOps control-plane cluster cannot be cleanly turned off: `gitops_enabled = false` would plan to destroy `google_kms_crypto_key.etcd`, which carries `prevent_destroy = true`, so the module stays declared (`gitops_enabled = true` in dev) while the cluster it describes was suspended out-of-band by a targeted `terraform destroy` of the cluster resource alone — configuration and reality have diverged by design, not by accident | infrastructure/terraform/modules/gitops-control-plane/main.tf:103-131; infrastructure/environments/dev/terraform.tfvars:138-139 | `prevent_destroy` on the etcd key is itself correct — destroying it makes every Secret the suspended cluster ever held unreadable at once — but it means the only way to represent 'this environment's GitOps control plane is currently suspended' in Terraform is a manual, targeted destroy that Terraform's own state does not fully agree with afterwards (a `plan` against dev would presumably want to recreate the cluster). ADR 0093 already names the durable fix — move the etcd key out of `modules/gitops-control-plane` into something that can be turned off independently of the cluster — and has not yet been done. This is a live example of the exact failure this repository's architecture rules warn about: infrastructure whose *declared* state and *actual* state can only be reconciled by a person remembering an out-of-band action. | E14 |
| medium | infrastructure | `modules/backup`'s disk-snapshot schedule and KMS key for the execution node's journal carry zero tests, and coverage is structurally zero until an operator manually runs the printed attachment command against a real node disk — which cannot happen because no execution node has ever been provisioned | infrastructure/terraform/modules/backup/main.tf:29-73; infrastructure/terraform/modules/backup/NOT-COVERED.md | Terraform cannot attach a `google_compute_resource_policy` to a disk that the instance group creates later, so the module's own design prints the attachment command as an output rather than running it — that part is a reasonable, documented limitation. What is missing is any test proving the printed command is even well-formed (0 tests today, versus the module's siblings which mostly carry `.tftest.hcl` coverage), which matters specifically because this is the one record `NOT-COVERED.md` says the disaster-recovery runbook cannot reconstruct from the venue: the journal is the thing this schedule exists to protect, and nothing currently proves the schedule it prints would actually apply cleanly the day an execution node exists to receive it. | E14 |
| medium | kernel-cycle | qip_journal_write_failures_total -- the metric run_cycle's own comment calls out as the only place a process that 'traded and could not write it down' becomes a number instead of a buried sentence -- has no Cloud Monitoring alert policy at all, gated or otherwise | backend/crates/runtime/qip-kernel/src/platform.rs:9133-9148 (journal_cycle failure path: counts JOURNAL_FAILURES and pushes a LEARN-stage problem, never aborts); backend/crates/libs/qip-observability/src/metrics.rs:834 (JOURNAL_FAILURES = "qip_journal_write_failures_total"); infrastructure/terraform/modules/observability/main.tf (9 declared alert policies -- 7 central-plane, edge_halted, edge_reconciliation_break -- grep for 'journal' matches nothing) | A cycle journal write failure is deliberately non-fatal -- the platform keeps trading and reports the failure as a stage problem rather than stopping, per the same rationale reviewed and accepted for other LEARN-stage failures. That design depends entirely on the failure being visible to an operator, and today it is not: unlike qip_kill_switch_tripped, qip_limit_breaches, edge_halted and the other seven declared policies, nothing pages on a sustained run of journal-write failures even after workload_metrics_exist is flipped true, because no policy names this series at all. The gap is cheap to close and additive -- one more gated policy alongside the existing nine, following the pattern .claude/rules/domains/observability.md already documents for the rest. | E12 |
| medium | kernel-cycle | Platform::synthesise -- the one production call site that ever constructs a SynthesisInput -- always passes models: Vec::new(), so the model-identity citation field the type carries end-to-end (SynthesisInput, Hypothesis, HypothesisDraft) never has a real value in any hypothesis this platform ships | backend/crates/runtime/qip-kernel/src/platform.rs:12354 (models: Vec::new() inside the SynthesisInput literal); backend/crates/runtime/qip-kernel/src/platform.rs:12277-12372 (synthesise, the whole call); docs/blueprint/assessment/REASON-b1.json (REASON-004: 'qip-kernel always wires models: Vec::new()... a dead field') | The composition mechanism (many agent findings folded into one hypothesis with recorded contributor run-ids) is real and reached from production. The specific model-identity citation the field exists for is not: every hypothesis this platform has ever produced names its contributing agents but never the model(s) that produced them, which sits squarely against ADR 0007's exact-attribution goal and the platform's own stated purpose ('why, on what evidence... and whether the reasoning held up', .claude/rules/10-product-direction.md). Cheap relative to most REASON gaps -- report.findings already carries per-finding origin information the field could be populated from; the missing step is wiring it and a test asserting a shipped Hypothesis names a model. | E16 |
| medium | kernel-cycle | Platform::synthesise always builds a single-item EvidenceSet (EvidenceSet::from_items(vec![Evidence::new(...)])) per anomaly, so the corroboration and contradiction machinery the reasoning engine implements (EvidenceSet::independent_weight, net_stance_disagreement) is exercised only by qip-reasoning-engine's own unit tests and has never run against genuinely independent evidence in a live cycle | backend/crates/runtime/qip-kernel/src/platform.rs:12305-12320 (the single-item `direct` EvidenceSet construction); backend/crates/services/qip-reasoning-engine/src/evidence.rs:184-463 (EvidenceSet::independent_weight, net_stance_disagreement, as_of -- built and tested, not reached with more than one item from production); docs/blueprint/assessment/EVID.json (EVID-004: 'Production only ever constructs a single-item EvidenceSet per anomaly today... exercised in unit tests but not yet in a live cycle') | This is exactly the shape the change-management evidence standard warns about: a control that reads as built and tested while never having run against the case it exists for. Corroboration/contradiction arithmetic is what ADR 0005 means by 'confidence as arithmetic rather than vibes' -- a claim synthesised from one market-observation item cannot corroborate or contradict anything, so every hypothesis this platform has shipped has had its confidence set as if only one source of evidence exists, regardless of how many independent sources the panel's findings actually represent. The fix is additive (feed report.findings' facts into the EvidenceSet alongside the anomaly observation), not a rewrite of anything. | E08 |
| medium | kernel-cycle | platform.rs is 26,172 lines holding the eight-stage core cycle interleaved in the same impl block with capital calls, the capital-fabric wallet/corridors, product/eligibility/registration catalogues and model promotion -- subsystems with no structural relationship to run_cycle -- and the cost is not hypothetical: .claude/rules/domains/observability.md records repeated, dated incidents (2026-09-06, 09-14, 09-15, 09-19) of citation line numbers into this file drifting by over a hundred lines within a single working session because a parallel lane was editing it at the same time | backend/crates/runtime/qip-kernel/src/platform.rs (26,172 lines total; core-loop surface is roughly lines 220-14300, under half the file); .claude/rules/domains/observability.md (module-doc citation drift incidents, all against this file, spanning at least four dated corrections); docs/architecture/current-state/kernel-cycle.json ("gaps_observed": "platform.rs is 26,172 lines and this record covers only its core-loop surface... the file also implements capital calls, the capital fabric wallet/corridors, product/eligibility/registration catalogues, model promotion and dozens of other subsystems not covered here") | qip-kernel is correctly the one designated place to compose services into a cycle (.claude/rules/architecture/00-boundaries.md), so this is not a crate-boundary violation and needs no ADR to fix -- splitting the core-loop functions (run_cycle, the eight stage_* methods, ingest_cell_report, learn_from(_cells)) into their own module or file inside the same crate changes no dependency direction. What it would fix is a documented, recurring, real cost: this file has been the single most frequently mis-cited source location in the observability rule file, every time because concurrent edits moved code the citation had already pointed at. A file this size is also why this very tech-debt review had to be scoped to three specific line-range excerpts rather than the whole file. | E00 |
| medium | kernel-cycle | Two disjoint prediction-scoring systems exist in qip-prediction: qip_prediction::scoring (Brier-scores the platform's own probability against a resolved market's implied probability, and explicitly refuses a market quote timestamped after the platform's probability was formed) is fully built and tested but has zero callers outside its own crate; qip_prediction::resolution::Proposition -- a different, generic mechanism -- is the one Platform::synthesise actually wires into the REASON/LEARN pipeline, and it never forms a proposition about an event-market contract | backend/crates/services/qip-prediction/src/scoring.rs:46,66,155,164,185,230 (ScoredForecast, brier_score, market_brier_score, compare -- zero production callers); backend/crates/runtime/qip-kernel/src/platform.rs:12432-12450 (Proposition::new -- the mechanism actually reached every cycle); backend/crates/runtime/qip-kernel/src/platform.rs:14403-14528 (the LEARN-stage grading path for those propositions, proven replay-safe by tests/falsification.rs) | Both mechanisms solve a version of the same problem -- was this claim right, judged against a fair comparison -- and only one is wired into the kernel cycle. The wired one (Proposition/ResolutionCriteria) is real production machinery, well-tested, and should not be replaced; the orphaned one (scoring.rs) is not vestigial, it is purpose-built for exactly the point-in-time-leakage-safe market comparison the requirement asks for and simply has nowhere to plug in, because nothing in the REASON pipeline ever constructs an EventMarket or a prediction-market-contract fixture. Not a rewrite: the two systems solving overlapping problems from different angles, with no caller deciding between them, is the same shape as the RetentionClass/RoutingClass finding already recorded for libs-foundation -- migration means giving qip-kernel a path to construct one when a thesis is actually about a market-implied probability, not deleting either half. | E17 |
| medium | kernel-planes | ADR 0008's DNA-shipping half — `StrategyDna::seal`/`CentralPlane::ship` and `CentralPlane::verify_dna` — has no production writer and no wire path to an edge cell at all: `qip-edge-node` does not depend on `qip-kernel`, and no `PolicyItem` payload slot exists to carry a sealed bundle | backend/crates/runtime/qip-kernel/src/central/dna.rs:78-136; backend/crates/runtime/qip-kernel/src/central/plane.rs:1985,2010; backend/crates/runtime/qip-kernel/tests/central.rs:524-525,1234-1235 | Confirmed with `rg -n '\.ship\(\|verify_dna\(' backend/crates/runtime/qip-kernel/src backend/crates/apps`: `verify_dna` has no call outside its own definition, and the one other `.ship(` hit under crates/apps (qip-edge-node/tests/mirror.rs:202) is an unrelated journal-mirror method (`fn ship` on a different trait, backend/crates/edge/qip-edge/src/journal.rs:570) — not `CentralPlane::ship`. `grep -n 'qip_kernel' backend/crates/apps/qip-edge-node/Cargo.toml` returns nothing: the one process architecturally positioned to receive a sealed DNA does not depend on the crate that seals it. Separately, `qip_contracts::policy::PolicyItem` names exactly twelve mesh-payload slots (TrainedModels..Dispositions) and none of them is a DNA/strategy-bundle slot, so even the wire format this platform already uses for every other centre-to-cell artifact (belief priors, causal digest, episodic digest, cycle whitelist) has no analogous slot for this one. The sealing/verification logic itself is structurally sound — `StrategyDna::seal` refuses a stage that `GateStage::holds_capital` says cannot hold capital — so this is a genuine, tested building block for ADR 0008 ('cells decide alone') left with no caller and no transport, not a design that should be discarded. | E03 |
| medium | libs-finance | FutureDetails.underlying_object_id is a bare, unvalidated String with no resolution against the Universe and no futures-to-spot/physical basis computation anywhere in the workspace | backend/crates/libs/qip-financial/src/extensions.rs:403-412 | extensions.rs:403-412 (read in full) defines `FutureDetails` with `pub underlying_object_id: String` -- a free-form string with no `ObjectId` typing, no constructor-time validation, and no lookup against `qip_financial::universe::Universe` anywhere in the crate. `grep -rin basis backend/crates/libs/qip-financial backend/crates/services` turns up only unrelated hits (cost basis, scoring basis); no futures-price-minus-spot-price computation exists in production or test code. The blueprint's Commodities domain explicitly requires 'futures, spot/physical links' and 'basis' as a named measure (also surfaced independently in docs/blueprint/assessment/COVERAGE.json's COVERAGE-010, work item E05-commodity-future-spot-link, and RISK-008's 'gap and basis are not named as distinct measures anywhere in qip-risk'). This is a structural placeholder in the sense CLAUDE.md principle 4 names -- a guarantee the type system should hold (a future is linked to a real, resolvable spot/physical instrument) is instead asserted by nothing, held only by whatever string a caller happens to write. | E05 |
| medium | libs-finance | qip-portfolio's Position/Portfolio carry no funding-currency/amount/accrued-cost record; qip-financial::costs::TransactionCostModel only prices a securities-lending borrow rate, not cash funding behind a position held in a second currency | backend/crates/libs/qip-portfolio/src/position.rs:40; backend/crates/libs/qip-portfolio/src/portfolio.rs:46; backend/crates/libs/qip-financial/src/costs.rs:230 | `grep -in funding backend/crates/libs/qip-portfolio/src/position.rs backend/crates/libs/qip-portfolio/src/portfolio.rs` returns no match -- confirmed. costs.rs's only borrow-adjacent field is `short_borrow_bps_annual` (costs.rs:230, used at :279-346), which prices a stock-loan short, not cash financing behind a long position denominated in a currency other than the book's base. A position financed in a second currency is therefore invisible to valuation: nothing carries its financing currency, amount or accrued cost. Confirmed independently in docs/blueprint/assessment/COVERAGE.json's COVERAGE-003 (work item E05-position-funding-record, tier T1): 'a position financed in a second currency has no representation and its funding is invisible to valuation.' Not a defect in existing code -- the existing borrow-rate modelling is correct for what it covers -- but a missing type the ASSET/LEDGER domain needs and qip-financial/qip-portfolio are the natural place to add it, alongside the FX and funding-curve machinery `qip-capital-fabric::TransferCostModel` already builds on top of this crate. | E05 |
| medium | libs-finance | qip-financial::ladder (asset-liquidation depth of existing holdings) and qip-financial::cashflow (capital-call/commitment forecasting) are two separate, partial liquidity-over-time models that never compose into one treasury cash-flow ladder bucketing future inflows, outflows and encumbrances by the date they fall due | backend/crates/libs/qip-financial/src/ladder.rs:44-250; backend/crates/libs/qip-financial/src/ladder.rs:493; backend/crates/libs/qip-financial/src/cashflow.rs:200-300 | `LiquidityLadder` (ladder.rs:493, built every cycle at platform.rs:6626-6650 via Platform::liquidity_ladder/funding_ladder) buckets *existing holdings* by liquidation cost/horizon; its own module doc says it 'describes depth, it never reaches for it.' `CashflowForecast`/`Commitment` (cashflow.rs) separately forecast *future* capital calls and drawdowns, and cashflow.rs:1107's `Commitment::unscheduled` constructor is explicitly documented as 'a commitment whose call timing is not yet modelled' -- an honest, tested refusal rather than a defect, but it is also the reason the two systems cannot be joined today: neither carries the other's time axis. Confirmed independently in docs/blueprint/assessment/CAPITAL.json's CAPITAL-002 (work item E07-treasury-cashflow-ladder, tier T3): 'there is no treasury cash-flow ladder anywhere in qip-capital or qip-capital-fabric.' Both existing pieces are individually hardened and well-tested (35 + 37 tests respectively per docs/architecture/current-state/libs-finance.json); the gap is that nothing unifies them into the single time-bucketed view a treasury desk needs. | E07 |
| medium | libs-foundation | Three non-unified topic-classification vocabularies stand in for the blueprint's single per-topic QoS model, with no type able to refuse a topic missing a required QoS field | backend/crates/libs/qip-events/src/topic.rs:38,327,416,532,649,658 (Topic::name/group/retention_class only); backend/crates/libs/qip-events/src/retention.rs:55-76 (RetentionClass, a nine-row data-domain taxonomy per blueprint §22.1); backend/crates/services/qip-streaming/src/routing.rs:75-110 (RoutingClass, a two-way Hot/Durable split) | v11.6 §7.2 names a five-class per-topic QoS model (partitioning key, ordering, overload, mirroring, replication semantics). `RetentionClass` and `RoutingClass` each partially cover the space from a different angle — one is a data-lifecycle taxonomy, the other a two-way delivery split — and neither is silent about its own scope, but nothing unifies them: a topic's actual delivery semantics depend on which of the two a piece of reading code happens to consult, and there is no topic-creation API that can refuse a topic missing an ordering or mirroring declaration, because no type models that concept at all. | E03 |
| medium | libs-foundation | `PolicyPayload` carries twelve typed slots; two (`trained_models`, `compiled_plan`) are never produced by the only production path that ships one | backend/crates/libs/qip-contracts/src/policy.rs:584-620 (PolicyPayload's twelve Slot<T> fields); backend/crates/apps/qip-api/src/mesh.rs:892-930 (pending_policy: produces capital_grants, risk_envelope, belief_priors only); backend/crates/apps/qip-api/tests/mesh.rs:2285 (a_shipped_payload_produces_exactly_the_six_slots_the_register_names) | The type models a twelve-slot policy contract; production wiring fills a handful every cycle and leaves trained_models/compiled_plan permanently Slot::Stale — confirmed by the mesh test's own name asserting exactly which slots ship. The two unshipped slots have no consumer either (qip-edge-node's Cell::apply_policy has no code path expecting them filled), so these are live types with no live producer and no live consumer: a smaller, more specific gap than deleting the fields would address, and one the platform's own e2e test already pins down rather than leaving implicit. | E10 |
| medium | libs-foundation | Nothing calls `Logger` per cycle today (correct), but the O(1)-log-volume property that depends on that fact has no regression test pinning it | backend/crates/runtime/qip-kernel/src/platform.rs (grep -c 'logger\.' → 0; 55 sites call metrics.* and none call logger.*, measured 2026-09-25); backend/crates/libs/qip-observability/src/logs.rs (Logger, Severity) | The platform currently avoids per-tick logging, which matters because on this cycle's cadence that would make log volume scale with tick count rather than with events actually worth recording — an explicit prohibition in .claude/rules/domains/observability.md. That absence is a fact about the code today, verified only by grep/inspection, not a property the test suite protects: a future change adding one logger.info inside a per-stage function would silently reintroduce unbounded log growth and nothing would fail. | E12 |
| medium | libs-foundation | Metrics are recorded and exposed at /metrics (55 sites, tested), but nothing has been shown to scrape any deployed process, so every Cloud Monitoring alert policy stays gated off | infrastructure/terraform/variables.tf:1136-1158 (workload_metrics_exist default = false); infrastructure/terraform/modules/observability/main.tf (9 alert policies, all `count = ... workload_metrics_exist`); backend/crates/apps/qip-api/src/routes.rs:79,1249 (scrape(&platform) exposes Prometheus text) | The emitting half of observability is real and tested; what remains is proof of ingestion. The Cloud Run half of the gap is currently blocked upstream — the only published managed-Prometheus sidecar image fails the platform's own Trivy gate on an unfixed CRITICAL CVE in golang.org/x/crypto — so this cannot be closed by writing more qip-observability code. workload_metrics_exist must stay false, and no alert policy should be flipped on, until a pod is actually shown to have been scraped; treating a deployment's mere existence as evidence would be the exact mistake .claude/rules/domains/observability.md warns against. | E12 |
| medium | libs-intelligence | Two independently-defined, identically-named `Episode` types with no shared trait or conversion between them: `qip_ai::memory::episode::Episode` (live, embedded, written every LEARN-stage pass) and `qip_agents::memory::Episode` (agent-research episode, only ever written in tests, per the item above) | backend/crates/libs/qip-ai/src/memory/episode.rs:12-40; backend/crates/libs/qip-agents/src/memory.rs:1-35; backend/crates/runtime/qip-kernel/src/platform.rs:4271 (empty ResearchMemory::new()) | Both types record 'what happened, what we believed, what followed' — the exact language `qip_ai::memory::episode`'s own module doc uses — but they encode it differently (a fixed 43-dimension feature vector for nearest-neighbour recall vs. free-text question/conclusion/evidence for lesson promotion), live in different crates one tier apart (qip-ai vs. qip-agents, both libs), and a maintainer adding an episode field has to know which of two same-named types they mean. `qip_agents::memory.rs`'s own doc already distinguishes 'episodic' from 'semantic' *within* ResearchMemory; it says nothing about the separate qip_ai type sharing its name. Left alone this is a readability/maintenance hazard today; once WriteResearchMemory (item above) is wired, it becomes two population mechanisms for two things both called 'Episode' with no cross-reference. | E08 |
| medium | libs-intelligence | `PlatformConfig::quantum_enabled` defaults `false`; its only setter, `with_quantum()`, has zero callers anywhere in `crates/apps/**`, so the Quantum Foundry / QAOA path is reached only from qip-quantum's and qip-kernel's own tests in every deployed binary | backend/crates/runtime/qip-kernel/src/config.rs:252,534,573-575; backend/crates/runtime/qip-kernel/src/platform.rs:4324-4326; backend/crates/runtime/qip-kernel/tests/solver_routing.rs:230-245 | The code itself is real, hardened and tested (57 tests in qip-quantum, the kernel's own test comment names this exact gap: 'A deployment with no quantum provider configured — which is every deployment, quantum_enabled being off by default'). This is arguably correct-by-default under QUANT-002 ('a quantum path goes to production only on a benchmarked win'), but there is no lever anywhere in a composition root to turn it on once a benchmark does win — `with_quantum()` is unreachable dead API from any deployable binary today. The gap is not 'quantum is off' (that may be right); it is that nothing wires a benchmark-gated activation decision to the one flag that would turn it on. | E10 |
| medium | libs-intelligence | `qip-quantum`'s solver/benchmark family (`ClassicalSolver`, `QuantumInspiredSolver`, `IbmQuantumSolver`, `SolverBenchmark`, `ClassicalValidator` — ~1,700 lines) — the crate's actual ADR-0006 'always compute a classical baseline' machinery — has no caller outside its own tests and qip-optimization-engine's tests; production computes its classical baseline through an independent path (`qip_numerics::anneal`/`qip_numerics::qp` called directly from `ComputeRouter::solve_classical`) | backend/crates/libs/qip-quantum/src/solver.rs:1-1029; backend/crates/libs/qip-quantum/src/benchmark.rs:1-27,109-208,462-479; backend/crates/services/qip-optimization-engine/src/router.rs:290-330,399-438,528-624 | ADR 0006 and CLAUDE.md principle 6 ('Bill what ran, not what was planned... two independent claims about the same fact will disagree, and the louder one will be wrong') describe exactly the risk here: two independently-written, independently-tested 'compute the classical answer and compare' implementations exist for the one workload family currently in production, and nothing keeps them in agreement if either changes. Production's own re-scoring (`ComputeRouter::score()`/`worst_violation()` against the real `PortfolioProblem`) is a real, tested guarantee — the gap is that it is a second implementation of the same guarantee `qip_quantum::benchmark::ClassicalValidator` already provides, not that either one is wrong today. | E10 |
| medium | services-capital-exec | OriginationMandate::admit (the five-gate market-creation approval, hard-capped at $250M) is fully implemented and tested but called from no composition root | backend/crates/services/qip-execution-engine/src/origination.rs:1-47 | Confirmed independently: no call to `OriginationMandate::admit` exists in backend/crates/runtime or backend/crates/apps outside tests (the three `.admit(` hits that do exist in production code are unrelated methods — qip-api's auth admit, asset_class_registry::admit, and an investor-request admit). The gate itself is deliberately narrow (constructs no Order, names no venue, reaches no Broker — it only bounds a ceiling), so wiring it into the quoting path does not by itself create a market-creation or live-order capability; it is the shadow-form approval step v11.6 asks be built ahead of the live Market Factory, which `docs/blueprint/assessment/EXEC-b2.json` (EXEC-027) independently confirms is 'implemented and unit/acceptance-tested but reached from nowhere.' The full governed Market Factory (create/seed/manage/publish) remains correctly BLOCKED under ADR 0099's conflict C1 (external action / live capital) and must stay that way; only the paper-form approval gate is open work. | E17 |
| medium | services-data-world | The Scout Fabric has no autonomous discovery (CRAWL): candidate sources are a fixed, committed catalogue file, never generated by observing world-model gaps or forecast error as the blueprint's Discovery Agent Loop specifies | backend/crates/services/qip-data-finder/src/finder.rs:40-169; backend/crates/apps/qip-deepbrain/src/discovery.rs:1-51; backend/crates/apps/qip-deepbrain/src/main.rs:1059-1083 | `rg -n 'follow_links\|expand_from\|fn crawl\(' backend/crates/services/qip-data-finder/src` returns nothing, confirming the current-state record. `DiscoveryDesk` (discovery.rs) and `DataFinder::assess` implement everything downstream of a candidate list — probe, classify, assess legality, score, route, register — to a hardened, type-enforced standard, but every candidate still comes from `qip_data_finder::catalogue::load(text, now)` reading a file a human wrote at `main.rs:1059-1083`. The blueprint's step 1 ('observe gaps in the world model, strategies and forecast errors') and step 2 ('generate source-discovery queries... by geography, entity, domain and language') have no code path at all: nothing here reads `WorldModel`'s coverage or the falsification ledger to propose a new source. Migrating rather than rewriting is straightforward because the admission/legality/scoring gate the crawler would feed is already built and tested. | E08 |
| medium | services-data-world | The four vendor adapters (rest/narrative/depth/alternative — ~2,500 lines, 210 tests) are complete and hardened but no vendor base_url/credential is configured for any of them anywhere in the repository | backend/crates/services/qip-market-ingestion/src/rest.rs:662-710; backend/crates/services/qip-market-ingestion/src/narrative.rs:1095; backend/crates/services/qip-market-ingestion/src/depth.rs:1138-1179 | `rg -n production_requirement` on the four files confirms every `SourceDescriptor::production_requirement` is unconditionally `Some(self.requirement())` — there is no code path in which any of the four reports itself fit for production. `depth.rs` is the only one of the four holding live book state (rebuild-on-gap via qip-sequencing/qip-orderbook) and is the closest thing this build has to blueprint §8.1's L2/L3 depth requirement, and it has never processed a real message. The implementation and its 210 tests are real engineering, not a stub; what is missing is an operator supplying a vendor, which is a deployment/procurement action rather than a further code change. | E08 |
| medium | services-data-world | IngestionService — the Telemetry-wired SENSE-stage wrapper with three metric-recording sites — is never constructed by any deployed binary | backend/crates/services/qip-market-ingestion/src/service.rs:82-248 | `rg -n 'IngestionService::new\|IngestionService::' backend/crates --glob '!**/tests/**'` returns only `service.rs` itself; `qip-api` and `qip-fastbrain` call `ConnectorFeed::open` directly instead of going through `IngestionService`. Its three `self.telemetry.metrics.count(...)`/`observe_latency_ms(...)` sites (service.rs:174,185,216) therefore never fire in a running process — the SENSE-stage-specific instance of the platform-wide 'nothing writes to Telemetry' gap `.claude/rules/domains/observability.md` already tracks at large. | E12 |
| medium | services-data-world | NetworkProbe — the one place qip-data-finder opens a real, credential-less socket in production — has zero dedicated unit tests | backend/crates/services/qip-data-finder/src/probe.rs:403-528; backend/crates/apps/qip-deepbrain/src/discovery.rs:181 | The current-state record's own `tests.count` for this module is 0, and it is exercised only indirectly through `tiers.rs`, `legality.rs` and `discovery.rs`'s own `DiscoveryDesk` test. `NetworkProbe::through` (probe.rs:418) is real network I/O reached directly from production (`discovery.rs:181`, one probe per candidate per assessment pass) and carries SSRF-relevant refusal logic (refuses `https://`, refuses an empty user agent) — exactly the class of code `.claude/rules/architecture/01-testing-strategy.md` wants directly proven, not only exercised as a side effect of a higher-level test that happens to construct one. | E08 |
| medium | services-data-world | Causal-hypothesis provenance covers 1 of the blueprint's 6 named sources in production: only the platform's own DetectedAnomaly path ever proposes a causal edge | backend/crates/services/qip-world-model/src/falsification.rs:90-124; backend/crates/runtime/qip-kernel/src/falsification.rs:237-253 | `HypothesisSource` (falsification.rs:90-106) has 7 variants: 6 named `BLUEPRINT_SOURCES: usize = 6` at falsification.rs:124 (CausalGraphGap, HighSurpriseEpisode, CounterfactualAnomaly, WorldModelTraversal, LanguageModelProposal, CrossAssetTransfer) plus the platform's own `DetectedAnomaly`. `rg -n 'SourceCensus\|HypothesisSource::' backend/crates/runtime/qip-kernel/src --glob '!**/tests/**'` shows exactly one call — `census.record(HypothesisSource::DetectedAnomaly)` at `qip-kernel/src/falsification.rs:253` — and the surrounding comment is explicit that this is deliberate ('a second producer of hypotheses would have to record its own source here; until one exists, claiming any other source would put a number against a path that does not run'), which makes this a disclosed limitation rather than a hidden one. It still means the REASON stage's causal-discovery breadth is 1/6 of the blueprint's own table (cited in-code as '§14.2'), and GCP v2.1 §21.2 step 3 names surprise/novelty detection and causal-implication detection as two separate ambient triggers this build collapses into the single detector it has. | E09 |
| medium | services-data-world | qip-chain — a complete, 69-test implementation of the blueprint's on-chain data requirement — is reached from production nowhere: observe_chain and every ChainAdapter are called only from tests | backend/crates/runtime/qip-kernel/src/platform.rs:16011-16038; backend/crates/runtime/qip-kernel/tests/bridges.rs:139-236; backend/crates/runtime/qip-kernel/tests/platform_services.rs:441-511 | `rg -n observe_chain backend/crates` shows the single production definition (`platform.rs:16011`) and every call site under a `tests/` directory across three suites; `rg -n 'SyntheticChain::\|JsonRpcChainAdapter::\|NodeChainAdapter::' backend/crates/apps backend/crates/runtime --glob '!**/tests/**'` returns nothing, so no `ChainAdapter` is ever constructed outside qip-chain's own tests either. Blueprint §6.1 lists 'blockchain state, mempools, token flows, smart-contract events and protocol governance' as one of seven source-universe categories, and §9.1 names a dedicated 'Crypto/On-chain Brain' specialist. This crate is the only implementation of that brain anywhere in the tree — reorg-aware ChainState, a real JSON-RPC client (rpc.rs, 1188 lines), AMM/mempool/bridge modelling — and it is entirely dark in every deployed binary. Migration (wiring an existing, hardened crate into a composition root) applies cleanly here; nothing about the implementation itself needs to change. | E06 |
| medium | services-data-world | Schema-drift detection is implemented and tested but never invoked in production, so its own tested guarantee ('a source that changes shape mid-stream is caught') does not hold in a deployed process | backend/crates/services/qip-data-finder/src/schema.rs:164-304; backend/crates/services/qip-data-finder/src/quality.rs:12-85 | `SchemaSpec::drift_to` (schema.rs:164) is the only place a schema comparison happens — proven by the crate's own `schema_drift.rs` test named in the current-state record ('a source that changes shape mid-stream is caught, not silently absorbed') — but `rg -n 'drift_to\(\|SchemaDrift::' backend/crates --glob '!**/tests/**'` finds zero callers outside qip-data-finder's own tests. `SourceQuality` (quality.rs:14, distinct from the sibling `SourceCost` at quality.rs:87 which *is* used in `discovery.rs`) is equally unreached. `SourceSchema` and `SourceCost` themselves do reach production via `discovery.rs`/`campaign.rs`/`references.rs`, but the drift-comparison and quality-scoring logic built on top of them do not — so a source that silently reshapes its payload in a real deployment is not, in fact, caught; only the test binary catches it. | E08 |
| medium | services-intelligence | qip-cost-router::reputation.rs (ModelReputation/ReputationBook/Judgement, per-context Bayesian-shrunk model-competence weighting) is exported from lib.rs, fully tested, and never constructed by qip-kernel or any app, so Router::select and Router::assess weigh every intelligence-tier rung as equally trustworthy regardless of the model's track record in that context | backend/crates/services/qip-cost-router/src/reputation.rs:102,217,306; backend/crates/services/qip-cost-router/src/router.rs:384,441; backend/crates/runtime/qip-kernel/src/platform.rs:120-125,6549,6594 | `grep -rn "ReputationBook\\|ModelReputation\\|Judgement\b" backend/crates/runtime/qip-kernel/src backend/crates/apps/*/src` matches only unrelated local enums (`WalletJudgement`, `InvestmentJudgement`) defined inside platform.rs itself -- confirmed by direct search, no hit inside qip-cost-router's own type names. `Router::select`/`Router::assess` route on cost, latency and strength (tier.rs's 9-rung ladder) but on nothing resembling the reputation module's per-context competence score, so a rung is chosen the same way whether the model at that rung has been reliable in this regime or wrong every time. The module is not a stub: it has its own construction discipline (Bayesian shrinkage) and 24 crate tests exercise it. This is a built capability with no caller, not a missing one. | E10 |
| medium | services-intelligence | qip-evolution::discovery.rs (FeatureScreen, feature-candidate stability/selection-bar screening) has no caller outside the crate's own tests, so the palette a strategy is generated over is never narrowed by measured stability before generation runs | backend/crates/services/qip-evolution/src/discovery.rs:158,179; backend/crates/services/qip-evolution/src/generate.rs:172,211; backend/crates/runtime/qip-kernel/src/central/foundry.rs:70-73,239 | `grep -rln "qip_evolution::discovery" backend --include='*.rs'` returns only files inside crates/services/qip-evolution/ itself -- confirmed, empty output outside the crate. `StrategyGenerator::generate` (the path foundry.rs actually calls) takes the palette as given rather than as `FeatureScreen::screen` would narrow it, so the multiple-testing discipline this crate is built around (TrialLedger, no public Candidate constructor) runs over a feature set nothing has screened for stability -- a gap in the input, not the guard. | E10 |
| medium | services-intelligence | qip-opportunity-engine's CatalystDetector is registered in every DISCOVER pass (DetectorRegistry::standard()) but its ImpactHistory calibration data is never constructed or fed by qip-kernel, so the detector runs silently and can never actually detect a catalyst | backend/crates/services/qip-opportunity-engine/src/detector.rs:923-937; backend/crates/services/qip-opportunity-engine/src/catalyst.rs; backend/crates/runtime/qip-kernel/src/platform.rs:4521,10288,10412 | `grep -n "ImpactHistory\\|CatalystDetector" backend/crates/runtime/qip-kernel/src/platform.rs` returns nothing besides the registry construction line, confirmed. detector.rs:933-935 documents this as deliberate ("Silent until the context carries event visibility ... until its history has been fed"), which is honest about the current state but leaves one of the platform's eight registered detectors permanently inert in every deployed cycle -- a control wired in but never armed, the same shape the repo's own rules name as the pattern to avoid (`MaxExpectedShortfall`, .claude/rules/domains/risk-and-execution.md), applied here to a discovery detector rather than a risk limit. | E05 |
| medium | services-intelligence | qip-optimization-engine's CentralPlane::issue (the seam that would feed family_structure's calendar of live capital-grant envelopes) has no production caller anywhere -- every call site is a test -- so family_structure returns None in every deployment and the correlation-family sizing diagnostic the module exists to compute never runs | backend/crates/services/qip-optimization-engine/src/lib.rs:93-95; backend/crates/services/qip-optimization-engine/src/families.rs; backend/crates/runtime/qip-kernel/src/central/plane.rs:1883 | The crate's own module doc states the limit precisely: "CentralPlane::issue has no production caller -- every call site is a test. Until a deployed process issues a grant, the calendar is empty, family_structure returns None." This is a measure-only diagnostic by design ("the caller measures and does not allocate"), so nothing acts on it directly -- but a measurement nobody has ever produced in a deployed process is not yet a measurement, it is a shape waiting for its first fact. | E07 |
| medium | services-intelligence | qip-optimization-engine::horizons.rs's family_horizons and family_horizons_settled have no caller outside the crate's own tests, so the blueprint's "horizon × family" cross is reconciled on the horizon axis only -- one budget per strategy -- and never on the correlation-family axis | backend/crates/services/qip-optimization-engine/src/lib.rs:110-121; backend/crates/services/qip-optimization-engine/src/horizons.rs:1068,1085; backend/crates/runtime/qip-kernel/src/central/horizon.rs:76-77 | Confirmed no caller: `family_horizons`/`family_horizons_settled` appear in horizons.rs and in the crate's own lib.rs doc/re-export only. The crate's own doc names the reason precisely: `FamilyAssignment` has no constructor, and the lifecycle crate holds no correlation matrix, so there is nothing to key a per-family horizon reconciliation on even though the function exists and is tested. The seam that does run (HorizonRegister::settle via qip-kernel's central::factory calling qip_lifecycle::attempt_promotion) reconciles a narrower budget than the blueprint's cross names. | E07 |
| medium | services-intelligence | qip-prediction's entire prediction-market trading engine -- market.rs, oracle.rs, adapter.rs, arbitrage.rs, cross.rs, pricing.rs, scoring.rs (9 of 9 non-resolution modules, ~3050 of ~3114 src lines) -- has no caller outside the crate's own tests; only the resolution-criteria vocabulary is wired into production, for hypothesis-falsification, and even the crate's paper-trading-safe SyntheticPredictionVenue adapter is never constructed by any composition root | backend/crates/services/qip-prediction/src/market.rs:333,429; backend/crates/services/qip-prediction/src/oracle.rs:160,354,388,403; backend/crates/services/qip-prediction/src/adapter.rs:1-13,71,205,400-424 | `grep -rln "qip_prediction::market\\|qip_prediction::oracle\\|qip_prediction::adapter\\|qip_prediction::arbitrage\\|qip_prediction::cross\\|qip_prediction::pricing\\|qip_prediction::scoring" backend --include='*.rs'` returns nothing, confirmed empty. qip-kernel's own Cargo.toml documents the intent: qip-prediction is one of "eleven service crates... until this change, reachable from no deployable binary," added to the kernel's dependency graph specifically to make it ship -- but wiring stopped at the resolution module. Two things are worth separating: the engine (market/oracle/arbitrage/cross/pricing/scoring, plus the deterministic-seeded SyntheticPredictionVenue) is a complete, tested, paper-trading-compatible capability with a clear plan (blueprint §19) and should be evolved into the DISCOVER/REASON loop. VenueApiAdapter is different -- it is a declared port whose every call returns `qip_core::Error::Unavailable` by design (adapter.rs:11), so the shape of a real venue integration is reviewed without any live path existing; per ADR 0099's conflict register (C1) and the paper-trading rules, that adapter must stay inert until an owner supersedes ADR 0003, and no work in this group should implement it. | E17 |
| medium | services-intelligence | qip-simulation-engine::baseline.rs (RegimeComparison -- the always-computed unconditional-vs-regime-conditional comparison that applies ADR 0006's classical-baseline discipline to regime-conditional sizing) has no production caller, so the regime-conditional cap that actually reaches live sizing (qip-kernel's regime_allocation.rs, reading qip-optimization-engine::regime.rs's AllocationRegime) has never been checked against the baseline this module exists to compute for it | backend/crates/services/qip-simulation-engine/src/baseline.rs:1-19,209,253; backend/crates/services/qip-optimization-engine/src/regime.rs:1-48; backend/crates/runtime/qip-kernel/src/regime_allocation.rs:1-52 | `grep -rln "qip_simulation_engine::baseline" backend --include='*.rs'` matches only lib.rs (the re-export) and the crate's own tests, confirmed. baseline.rs's own doc names its purpose against the exact live seam: "Until this module existed the platform had a regime detector and no comparison arm ... a method is not a finding, and a finding is a difference against a baseline computed every time" -- the failure ADR 0006 names for the quantum path, restated for regime allocation. The live path (regime_allocation.rs -> regime.rs::AllocationRegime) is a deliberately conservative, fail-closed narrowing keyed off `Platform::market_regime`'s four-branch classifier and is not itself wrong -- its own doc explains why it needs no validation to narrow ("enough evidence to take risk off ... not enough to put risk on"). What is missing is that the more rigorous, HMM-fitted, out-of-sample-validated comparison this crate built specifically for regime-conditional sizing has never run against real capital-sizing decisions, so nobody has verified whether the live narrowing is doing more good than an unconditional baseline in the deployments that use it. | E07 |
| medium | services-intelligence | qip-training's managed-cloud training port (job.rs::TrainingProvider trait, TrainingJob, JobState, run_to_completion, implemented by vertex.rs::VertexAiProvider -- 1,380 src lines, the largest file in the crate) has zero production callers; qip-deepbrain bypasses the port entirely and calls LocalTrainer/distil directly, so the abstraction meant to let local and managed training be swapped is unused on both sides | backend/crates/services/qip-training/src/vertex.rs:389,439,560,590,839,903; backend/crates/services/qip-training/src/job.rs:31; backend/crates/apps/qip-deepbrain/src/learning.rs:133-138,1316,1366 | `grep -rn "VertexAiProvider" backend --include='*.rs'` matches only the crate's own lib.rs re-export, a doc comment in job.rs, and the crate's own tests -- confirmed, no app or qip-kernel source constructs it; the only production Vertex mention is a comment in qip-deepbrain/src/openobserve.rs naming it as an egress-proxy destination, not a construction site. `grep -n "TrainingProvider\|run_to_completion\|TrainingJob\b" backend/crates/apps/qip-deepbrain/src/learning.rs` returns nothing; production code instead constructs `LocalTrainer::new()` (learning.rs:1316) and calls `distil(...)` (learning.rs:1366) directly, bypassing the port that exists precisely so the two training paths can be switched without touching call sites. GCP v2.1 §11 names "Agent Platform / Vertex custom training" as the target placement for custom model training, so this is a plan-bearing gap, not orphaned code -- but two independently-callable training entry points that both work and neither uses the interface meant to unify them is exactly the kind of parallel-path debt that goes stale silently. | E10 |
| medium | services-intelligence | qip-twin::record.rs (LearningRecord -- the JSON-serialisable persisted decision/outcome record, with_attribution/validate/to_json/from_json) has no caller outside the crate's own tests, not even internally from capture.rs or counterfactual.rs, so no counterfactual decision this crate prices is ever turned into the durable record the type exists to produce | backend/crates/services/qip-twin/src/record.rs:115,174,197,234,242; backend/crates/services/qip-twin/src/lib.rs:124 | `grep -rln "record::" src` inside qip-twin, outside lib.rs and record.rs itself, returns nothing, confirmed -- the type is exported (lib.rs:124) but nothing in capture.rs (OutcomeCapture/Decision) or counterfactual.rs (CounterfactualEngine) ever builds one. ADR 0099's title theme is "durable knowledge memory" for what was previously pass-through, and this is exactly a durability seam the crate already built and never connected: the counterfactual engine runs and produces results every cycle (platform.rs:16209,16211), but none of those results are captured into the one type designed to survive a restart. | E08 |
| low | apps | Three composition roots (`qip-fastbrain`, `qip-deepbrain`, `qip-edge-node`) each hand-roll their own minimal blocking-HTTP health/quiesce/metrics server — same shape, same function names (`Request::parse`, `respond`, `bind`, `serve`, `answer`, `json`, `exposition`), near-duplicated rather than shared | backend/crates/apps/qip-fastbrain/src/health.rs:1-554 (Request::parse at 62, respond at 135, serve at 220); backend/crates/apps/qip-deepbrain/src/health.rs:1-584 (Request::parse at 66, respond at 139, serve at 226); backend/crates/apps/qip-edge-node/src/main.rs:791,1089 (its own TcpListener bind and write_response, a third independent implementation) | The decoupling itself is deliberate and well-reasoned — each module doc says explicitly this is 'the same shape qip-edge-node uses, for the same reason': a node whose liveness probe depends on qip-api has coupled the two, and the probe must answer while a stalled cycle holds the platform lock. That reasoning does not require re-deriving the request-line parser, the read/write timeout handling and the TCP accept loop independently in each crate — the fastbrain and deepbrain files share function names because one was written by copying the other, and a bug fixed in the timeout handling of one (or a request-smuggling-shaped parsing defect) has to be independently re-fixed in the other two rather than in one place. Following the precedent already accepted for qip-transport/qip-storage (I/O admitted into a library crate specifically for the platform's own wire, per ADR 0011/0012), a shared minimal-probe-server library is a plausible target; nothing here requires a rewrite of any of the three call sites' actual business logic (what /ready checks, what /quiesce does), only the shared transport plumbing. | E12 |
| low | apps | `qip-api`'s `RateLimiter` is an in-process `BTreeMap` behind a `Mutex`, with no shared backing store, while prod's own manifest scales the service to up to 4 Cloud Run instances | backend/crates/apps/qip-api/src/auth.rs:497-528 (RateLimiter::new/permit; limits held in std::sync::Mutex<BTreeMap<...>>, process-local); backend/crates/apps/qip-api/src/main.rs (RateLimiter::new(Duration::from_secs(60), 600) constructed once per process); infrastructure/gitops/envs/prod/api.yaml:43-44 (minInstanceCount: 0, maxInstanceCount: 4) | A per-subject window of 600 requests/minute is enforced independently by each Cloud Run instance that happens to receive that subject's traffic; with up to 4 concurrent instances the effective ceiling for one credential is not the configured 600/min but anywhere up to 4x that, depending on load-balancer placement the application has no control over, and the count resets on every instance restart or scale event rather than persisting. This is the same in-memory-state pattern already flagged for the kernel's own storage target (memory, discarded on restart), applied here to a security control rather than to trading state. Not currently exercised — no Cloud Run instance is live in any environment (see the ADR 0093 finding above) — so the gap is real but dormant; it would need a shared counter (the KeyValueStore the process already holds, or an equivalent) before this service is scaled past one instance in fact. | E14 |
| low | apps | `GET /data-sources` answers `available: false` unconditionally — a fixed refusal rather than a real source list, health record, freshness instant or quality score, for every deployment that has ever run this binary | backend/crates/apps/qip-api/src/routes.rs:312-329 (route comment: 'the handler matches one expression and answers available: false unconditionally') | The route's own comment is unusually candid about its own history: a previous summary ('discovered, approved and rejected data sources with health and licensing') described an intent no code path here produces, and 'a console built against the route table was promising four fields no code path here can produce' — the current fixed-refusal summary replaced that promise with an honest one, which is the right interim fix but not the target state. `qip-data-finder`'s catalogue is the real source of this data, and `qip-deepbrain`'s DiscoveryDesk is the only production code that walks a candidate catalogue at all — but that desk is itself unconfigured in every environment (see the discovery finding below), so there is currently no producer this route could wire to even if it stopped refusing. The honest refusal is correctly the interim answer; the target-state fix is downstream of the discovery/connector wiring gap, not a defect in this route alone. | E10 |
| low | apps | `qip-cli` (an application, shipped as the `qip` binary) depends directly on `qip-api` (a different application) in its normal `[dependencies]`, not `[dev-dependencies]` — an app-depends-on-app edge the architecture rule's own diagram (`libs ← services ← runtime ← apps`) names apps as the outermost layer nothing may depend on, and which no ADR or acceptance test currently reviews | backend/crates/apps/qip-cli/Cargo.toml:18-31 (qip-api.workspace = true, under [dependencies]); backend/crates/apps/qip-cli/src/registrations.rs:14-21 (reuses qip_api::registration_views so 'the console, the gate and this command cannot disagree'); backend/crates/apps/qip-cli/src/demo/doubles.rs:11-27,52 (reuses qip_api::http::Server rather than 'copying three hundred lines of request parsing') | The reasoning in both call sites is sound and explicit about its tradeoff — reusing qip-api's real HTTP server and registration-view types avoids a second, independently-drifting copy of request parsing and of the registration secret-command text. But unlike the qip-storage/qip-transport I/O-in-a-library exception (ADR 0011/0012, both named), this edge has no ADR and `architecture.rs`'s own boundary test explicitly stops at libs-vs-upper-layers, so nothing in the suite would fail if this dependency were accidental rather than reviewed. `.claude/rules/architecture/00-boundaries.md` states the rule with no exception named, the same shape of drift that file elsewhere warns readers about regarding stale citations. The structural fix matching 'make it structural' would be to hoist the shared pieces — the minimal HTTP/1.1 server, the registration-view read model — down into a service or lib crate both applications depend on, rather than one composition root depending on another's internals. | E01 |
| low | delivery | `.claude/rules/01-security-and-safety.md` names the destructive-command guard hook `guard-dangerous-command.sh`; the real, current implementation — wired in `.claude/settings.json` and actually executed on every Bash call — is `guard-dangerous-command.py`, a rewrite that fixed a shell-quoting bug the old `.sh` version had | .claude/rules/01-security-and-safety.md:42 ("guard-dangerous-command.sh" blocks force pushes...); .claude/hooks/guard-dangerous-command.py:1-181 (the actual file on disk); .claude/settings.json:75 (hooks.PreToolUse wiring names the .py file) | The rule this file states is "If the guard fires, do not route around it" — which depends on a reader (human or agent) being able to find and reason about the actual guard. A reader who greps for guard-dangerous-command.sh as this rules file instructs finds nothing, because the file was rewritten in Python specifically to fix a quoting bug the shell version had. The behaviour is correct and enforced; only the documentation's name for it is stale. One-line fix, no design question. | E01 |
| low | delivery | `ci.yml`'s `secrets` job checks out the full git history (`fetch-depth: 0`), which is unusual among the workflow's jobs (every other checkout is a shallow default), while both the job's own comment and `check-secrets.sh` state the design is deliberately diff-only and never scans history | .github/workflows/ci.yml:390-400 (job comment: "The secrets check runs on the diff, not the history"; then `fetch-depth: 0` two lines later); scripts/check-secrets.sh:1-8 (git grep over the working tree only, no history walk) | The comment immediately above the checkout step explains, correctly, why the design does not scan history ("a secret already committed needs rotating rather than a failing build"), which makes the `fetch-depth: 0` two lines below it read as a contradiction to a reader who has not traced through to check-secrets.sh's implementation — and it buys nothing: the script only ever greps the checked-out working tree, so a shallow checkout would produce an identical result at lower cost on every push. Low-impact, but it is exactly the kind of self-contradicting configuration the codebase's own "say why" principle exists to prevent. | E01 |
| low | edge-cell | `qip-api`'s doc comments name a private (non-`pub`) helper, `qip_edge::feasibility::effective` (feasibility.rs:280), as prose describing what a shipped policy payload expects the cell to do — nothing checks that this name still exists as `qip-edge` evolves | backend/crates/edge/qip-edge/src/feasibility.rs:280 (fn effective — private, no pub qualifier, not re-exported by lib.rs); backend/crates/apps/qip-api/src/mesh.rs:724,981 (doc comments citing `qip_edge::feasibility::effective` and `qip_edge::feasibility::assess`); backend/crates/apps/qip-api/Cargo.toml:44-50 (qip-edge is a [dev-dependencies]-only entry; these are prose references, not compiled code) | `qip-api`'s serving binary never names `qip-edge` as a real dependency — it is dev-only, for tests that decode genuine cell deltas — so a doc comment naming `qip_edge::feasibility::effective` by its exact private path is prose the compiler cannot check. If `qip-edge` is refactored (exactly the kind of change item 4's cell.rs decomposition would produce) and `effective` is renamed, split, or inlined, nothing in the workspace would catch that `qip-api/src/mesh.rs`'s explanation of slot 11 now names a function that no longer exists — the same stale-citation failure mode `.claude/rules/domains/observability.md` documents happening repeatedly to itself, here with no self-correcting process watching it. Low-cost fix: cite the public `feasibility::assess` (which the second reference already does correctly) rather than a private helper, or add a doctest/reference that would fail to compile if the name moved. | E00 |
| low | frontend | Four @algorik/* packages (api-client, charts, testing, ui) are pure re-export shims with zero consumers, and each points backward at the app: a shared package importing from `../../../portal/src/**` rather than the portal importing from the package — the inverse of the lib-depended-on-by-app direction the workspace otherwise holds | frontend/packages/api-client/src/index.ts:1-23; frontend/packages/charts/src/index.ts:1-12; frontend/packages/testing/src/index.ts:1-9 | grep '@algorik/(ui\|api-client\|charts\|testing)' across frontend/portal/src and frontend/landing returns 0 hits; the portal's own 57+ consumer files import components/data/*, components/viz/primitives and lib/* directly via relative @/ paths. Each package's own doc comment names this as a deliberate, unfinished migration rather than an accident: @algorik/ui's says outright 'the components themselves currently live in the portal... re-exporting rather than moving is deliberate... the day the files move here, no consumer changes an import.' Until that move happens, these four packages are backwards by the workspace's own dependency rule (libs are depended on, they do not depend on an app) and add build cost (transpilePackages entries, four extra workspace resolutions) for zero present benefit. Evolve toward the stated target: move the implementation files into the packages and repoint the portal's own imports, rather than leaving the shim as a permanent middle state. | E00 |
| low | frontend | @algorik/design-tokens, the single source of truth for the portal's colour system, is never imported as a TypeScript module — it is consumed by a script that regex-parses its source text as a string | frontend/scripts/generate-theme-css.mjs:25-36; frontend/packages/design-tokens/src/index.ts:82-155; frontend/portal/src/app/globals.css:24,33 | grep 'from "@algorik/design-tokens"' across the workspace returns 0 hits. generate-theme-css.mjs instead opens the package's .ts source as text and matches `export const {name}: ColorScale = {...}` with a regular expression, by its own comment, 'rather than add a compiler to read it.' The generator's own drift check (npm run tokens:check, run in CI) is real coverage and is why this is scored B rather than higher — it keeps the emitted CSS from silently diverging from what was committed. But the source of truth for the whole colour system is read by a hand-rolled parser instead of the type system the package otherwise exists to provide: a syntactically valid TypeScript change a compiler would accept (a computed key, a spread, a multi-line value) can defeat the regex silently until the next tokens:check run catches the drift after the fact rather than at the edit. | E00 |
| low | frontend | The portal renders financial series through two independent chart implementations at once — hand-drawn SVG primitives and a Chart.js wrapper — a cost ADR 0015 already named and scoped, but whose completion nothing currently tracks | frontend/portal/src/components/viz/primitives.tsx:1-13; frontend/portal/src/components/viz/ChartJs.tsx; docs/adr/0015-licensed-templates-are-the-visual-source-of-truth.md | components/viz/primitives.tsx (Sparkline/AreaChart/Gauge/Bars/Heatmap, no dependency) and components/viz/ChartJs.tsx (the one place the licensed template's Chart.js is used) coexist by design, not by accident: ADR 0015 names this directly as a cost of adopting the SignalAIX template — 'the in-house chart primitives remain for pages not yet ported; two chart systems coexist until the port completes.' It already has an ADR, a stated cause and a stated end state (one system). It is listed here because the port has not completed and nothing currently names which pages still need it, so the ADR's own exit condition has no tracked completion criterion. | E00 |
| low | infrastructure | No test enumerates `infrastructure/docker/*.Dockerfile` and refuses an upstream `FROM` that is not pinned by a 64-character lowercase digest | infrastructure/docker/README.md:43-48 | Every `FROM` line in the three Dockerfiles is in fact digest-pinned today, but by convention rather than by a control: the one acceptance test that reads `Dockerfile` (`the_image_runs_as_a_non_root_user_on_an_empty_filesystem`) checks `scratch`/`USER`/`--locked` and never opens `portal.Dockerfile` or `landing.Dockerfile`, and checks no digest at all. `docker/README.md` says so itself — 'this section describes a convention rather than a control' — which is exactly the gap this repository's own testing rule exists to close: a production build input (the base image) has no test standing between a future edit and a tag that can move underneath a reviewer. | E01 |
| low | kernel-cycle | stage_act_orders only writes the qip_portfolio_leverage gauge when publishable_leverage(gross, equity) returns Some; on the None branch (non-positive equity) the gauge is left at whatever it last held rather than being cleared, zeroed, or flagged, so a book that goes to zero or negative equity reports a stale leverage figure on the one chart an operator would check first | backend/crates/runtime/qip-kernel/src/platform.rs:13372-13376 (the conditional gauge write); backend/crates/runtime/qip-kernel/src/platform.rs:2004 (publishable_leverage: returns None on non-positive equity); backend/crates/runtime/qip-kernel/tests/risk_aggregates.rs:1015,1059 (tests the Some branch; no test exercises the gauge's value across a transition into the None branch) | qip_portfolio_value is written unconditionally including to legitimate zero, by the same code's own stated principle ('a gauge written only on the interesting cycles never falls back'); qip_portfolio_leverage does not follow that principle for its one edge case. publishable_leverage itself is unit-tested for correctness, but nothing in the stage or its tests proves what an operator sees on the chart during the cycle equity turns non-positive -- the most likely single reading is the last valid leverage figure, frozen, during exactly the period the book is in its worst state. | E12 |
| low | kernel-cycle | stage_simulate's capacity_probe swallows every refusal reason (non-positive last price, fewer than two returns, a simulator or agent construction refusal) to a bare None, so a probe that has refused on every cycle for weeks is indistinguishable in the cycle report from one that has never had enough history | backend/crates/runtime/qip-kernel/src/platform.rs:12581-12588 (stage_simulate: if let Some(probe) = self.capacity_probe(now) else nothing); backend/crates/runtime/qip-kernel/src/platform.rs:12681-12749 (capacity_probe: eleven `?`/`.ok()?` exit points, none distinguished from another) | The design is deliberate and defended in the surrounding comment ('a probe is diagnostic, not a control'), and that defense is sound for why a refusal should not block the cycle. It does not cover why the refusal reason is discarded rather than recorded: SIMULATION_CAPACITY_ADVERSITY_BPS is a gauge with no companion counter for 'the probe could not run, and here is why', so an operator cannot tell a healthy instrument with too little history from a spec-construction bug that has silently refused the probe on every cycle since a bad deploy. No test in tests/kernel.rs or tests/stress.rs exercises the distinction between the probe's several refusal paths. | E12 |
| low | kernel-cycle | stage_learn folds all 15 declared blueprint §49.1 SLO objectives every cycle, but only 2 (reconciliation-breaks-zero, netting-ratio) are ever fed a real observation from any production seam -- the other 13 are permanently unmeasured, which the code's own comment already states plainly | backend/crates/runtime/qip-kernel/src/platform.rs:14250-14259 (the fold call and its comment: 'two of the fifteen objectives are fed by this process and thirteen are unobserved'); backend/crates/runtime/qip-kernel/src/platform.rs:5539,5561-5567 (ingest_cell_report -- the only two production observe() call sites for these objectives); backend/crates/libs/qip-observability/src/slo.rs:200-244,290-390 (blueprint_slos declares 15) | Declaring an objective and never feeding it is the same shape the `MaxExpectedShortfall` example in .claude/rules/domains/risk-and-execution.md names as the template for what not to add -- a control (here, a scored objective) that reads as protection and cannot fire. This instance is self-aware rather than hidden (the code comment names the 2-of-15 ratio explicitly, 'deliberately unflattering'), which lowers the severity but does not close the gap: an operator or the traceability matrix reading '15 objectives declared' without reading this specific comment would overstate what LEARN actually measures. Each of the 13 needs its own production observe() call site, one objective at a time, the same way the 2 that exist were wired. | E12 |
| low | kernel-planes | `CellInsights`' differential-privacy-gated aggregate query methods (`mean_gross_notional`, `total_realised_loss`) and the `Platform::insights_mut` accessor that reaches them are constructed on every Platform but never called by anything that could show an operator the number | backend/crates/runtime/qip-kernel/src/central/insights.rs:75,95; backend/crates/runtime/qip-kernel/src/platform.rs:4441,5377 | Confirmed with `rg -n 'insights_mut' backend/crates -g '*.rs'` and `rg -n 'mean_gross_notional\|total_realised_loss' backend/crates -g '*.rs'`: both return only their own definitions — no console route in qip-api, no CLI command, no other app calls either. `central::insights.rs` closed a real gap named in its own module doc ('qip-confidential was reachable from nothing in the kernel') by constructing `CellInsights` on every `Platform::new` (platform.rs:4441), but the aggregate-release gate it wraps produces a value for nobody: the module exists, is wired into assembly, and computes nothing any deployed process ever reads. This is the same class of finding the libs-foundation tech-debt report already raised about the underlying qip-confidential crate (its query methods have zero callers outside qip-kernel's own tests) — recorded here separately because central/insights.rs, the kernel-side seam that was supposed to close that gap, is itself unreached, confirming the gap is not closed at this layer either. | E12 |
| low | kernel-planes | Three qip-deepbrain production files import `qip-kernel`'s internal `central::{foundry,models}` submodule paths directly, bypassing the curated re-export surface that `lib.rs` and `central/mod.rs` deliberately withhold them from | backend/crates/apps/qip-deepbrain/src/learning.rs:130; backend/crates/apps/qip-deepbrain/src/evolution.rs:48; backend/crates/apps/qip-deepbrain/src/succession.rs:67 | `central/mod.rs`'s own status_reason (recorded in the current-state catalogue this report started from) states plainly that `insights::CellInsights`, `foundry::StrategyFoundry` and `models::{ModelRegistration,register_fit}` are 'NOT re-exported here (apps reach them via full crate::central::<submodule> paths)' — and confirmed with `rg -n 'use qip_kernel::central::(foundry\|models\|horizon\|insights)::' backend/crates/apps`, three qip-deepbrain files do exactly that. The dependency direction itself is fine (apps -> runtime is permitted), but the crate root's curated surface (lib.rs's explicit `pub use` list, central/mod.rs's flat re-export list) exists precisely so a caller depends on a stable contract rather than qip-kernel's internal file layout; these three imports instead make `central/`'s submodule names and paths load-bearing API for another crate without that ever being a decision anyone recorded. A routine internal reorganisation of `central/` (splitting foundry.rs, renaming a submodule) would silently break qip-deepbrain's build at compile time rather than fail predictably against a documented surface. This needs a decision, not a guess: either fold these three into the curated re-export list (matching the pattern `belief`/`causal`/`plane` already get) or explicitly document `central::<submodule>` paths as part of the crate's public contract. | E01 |
| low | libs-finance | qip-financial::physical.rs (1072 lines, LandedCost/PhysicalArbitrage/LogisticsTerms/Customs/Spoilage/MarketplaceFees/Returns) has zero production or cross-crate callers anywhere in the workspace | backend/crates/libs/qip-financial/src/physical.rs:129-138; backend/crates/libs/qip-financial/src/physical.rs:244-256; backend/crates/libs/qip-financial/src/physical.rs:260-356 | `grep -rl PhysicalArbitrage backend/crates` outside qip-financial's own src/tests returns nothing -- confirmed. This is NOT unplanned orphan code: blueprint v11.6 Section 20 ('Physical Commerce & Product Arbitrage') names a full commerce operating layer -- Product Scout, SKU/Entity Resolver, Commerce Feasibility ('All-in landed cost, expected resale, time-to-sale, return risk'), Purchase Executor, Logistics Brain, Inventory Ledger, Resale Router -- and lists 'Physical product purchase/resale has commerce, logistics and inventory capabilities, not just a price model' as a COMPLETE TARGET item. docs/blueprint/assessment/COMMERCE.json's COMMERCE-001/007/011/016/017/019 and ASSET-014/015, LEDGER-009/025 and EXEC-019 all name this exact module as the landed-cost starting point the E17 commerce-plane epic builds Product Scout/SKU Resolver/Purchase Executor/Inventory Ledger/Resale Router on top of. Deleting it would discard correctly priced, mutation-tested landed-cost/arbitrage arithmetic that a standing plan intends to wire in; classing it D would violate the instruction against deleting anything a plan covers. The debt is real (1072 lines nothing reaches), but the fix is wiring a caller (Commerce Feasibility assessment, per COMMERCE-011), not rewriting or removing the module. | E17 |
| low | libs-finance | qip-market::corporate_action::adjust_prices() has no production caller; qip-kernel's Platform::apply_corporate_action independently reimplements the identical per-bar price-adjustment loop inline | backend/crates/libs/qip-market/src/corporate_action.rs:294; backend/crates/libs/qip-market/tests/structure.rs:486; backend/crates/libs/qip-market/tests/structure.rs:579 | `grep -rn 'adjust_prices(' backend/crates` outside qip-market's own src and tests/structure.rs returns nothing -- the function is exercised only by its own three tests. Platform::apply_corporate_action (platform.rs:9607-9657, read in full) does not call it; it instead walks `bar_history` itself and calls `CorporateAction::price_adjustment_factor` and `quantity_adjustment_factor` directly, duplicating the same per-bar adjustment logic a second time in a different crate. Two independent implementations of one fact is exactly the pattern CLAUDE.md principle 6 warns will one day disagree ('Two independent claims about the same fact will disagree, and the louder one will be wrong'); today they happen to agree because both were built from the same factor methods, but nothing enforces that they stay in sync, and only the kernel's copy is covered by the production-path test in COVERAGE-005/platform.rs:25601+. Low risk today, but it is the textbook second-source-of-truth case the architecture rules name. | E05 |
| low | libs-foundation | `Telemetry` bundles three signal kinds (metrics, traces, logs) into one process-scoped value with no entity-keyed graph joining them; the blueprint's unified five-signal-class model does not exist | backend/crates/libs/qip-observability/src/lib.rs:9-47 (Telemetry{metrics, tracer, logger}, three name-keyed collections) | v11.6's observability model joins infrastructure, application, data-quality, model-health and trading-behaviour signals on shared entity nodes, so an operator can walk from one instrument's data-quality signal to its model-health signal. Telemetry here holds three separate, name-keyed collections with nothing joining a signal to the entity it concerns across signal kinds, and two of the five target classes (data quality, trading behaviour) have no representation at all. This is additive work on a sound base — the three existing collections are three of the five inputs a graph layer needs — not a rewrite. | E12 |
| low | libs-foundation | `CellInsights`' two query methods (and the `Platform::insights_mut` accessor that reaches them) have no caller anywhere outside tests, despite the gate itself being built into every cycle | backend/crates/runtime/qip-kernel/src/central/insights.rs:75-109 (mean_gross_notional, total_realised_loss); backend/crates/runtime/qip-kernel/src/platform.rs:358,4441,5377 (Platform::new constructs CellInsights unconditionally; insights_mut is the only accessor); backend/crates/runtime/qip-kernel/tests/insights.rs (only caller of either method) | The differential-privacy disclosure gate (qip-confidential) is built into every Platform::new — so its construction cost is paid every cycle — and is extensively tested, including an explicit differencing-attack demonstration, but no console route, API endpoint or CLI command in crates/apps/** ever calls the two methods that are its entire reason to exist. Not dead code with no plan (the type is fresh, tested, and reachable from Platform's own constructor), but a production surface that has never been asked a real question, which per the change-management evidence standard is a claim about protection nobody has verified end-to-end. | E12 |
| low | libs-intelligence | `qip_events::topic::Topic::SolverBenchmarked` ('optimization.benchmarked') is declared with match arms and constants but has zero production publishers, so no quantum-vs-classical comparison ever reaches the hash-chained event log | backend/crates/libs/qip-events/src/topic.rs:119,281,366,460,590; backend/crates/libs/qip-quantum/src/benchmark.rs:1-27 | The event type this topic names (`SolverBenchmark`) exists and is well-tested, but is never constructed in production (see the solver/benchmark item above), so the topic is a declared schema with nothing behind it — the same failure mode `.claude/rules/domains/observability.md` names for alert policies ('naming a metric... that nothing emits'). Wiring a publisher is contingent on resolving which classical-baseline path is canonical (the item above); listed separately because the two are independently checkable (`grep` for the topic literal vs. `grep` for solver.rs callers). | E10 |
| low | libs-intelligence | `qip-quant`'s `factor.rs`/`strategy.rs` (`FactorLibrary`, `FactorDefinition`, the `Strategy` trait, `SignalWeightedStrategy` — ~570 lines) are reached only from the crate's own tests; the production portfolio-construction path is a hand-written `Agent` impl (`PortfolioConstruction`, `impl Agent for PortfolioConstruction` at construction.rs:48) that does not implement `qip_quant::strategy::Strategy` and does not use `FactorLibrary` | backend/crates/quant/qip-quant/src/factor.rs:14-210; backend/crates/quant/qip-quant/src/strategy.rs:20-363; backend/crates/agents/qip-investment-agents/src/construction.rs:37,48 | This is not orphaned scaffolding to delete: v11.6's strategy factory ('takes strategies through generation, backtesting, simulation, stress testing, paper trading, promotion, decay and retirement' and 'sustains a population of thousands of strategies') is exactly the shape `Strategy`/`SignalWeightedStrategy` were built toward, and the repo's own assessment register already files this space under a named epic (`E10-strategy-factory-scale-benchmark`, docs/blueprint/assessment/MODEL-b1.json). Today it is a second, incompatible 'decide a portfolio' interface sitting beside the one production actually calls, with no adapter between them — worth evolving deliberately (adopt `Strategy` in `PortfolioConstruction`, or formally park the trait until the factory epic starts) rather than left to drift further apart the longer both exist unreconciled. | E10 |
| low | libs-intelligence | `qip_quantum::provider::HostedProvider`/`HostedTransport` (the IBM Quantum adapter) is real, honestly gated and zero-callers-in-production; the isolated 'Quantum Gateway' trust zone GCP v2.1 names as the only permitted path to IBM Quantum is not provisioned in any environment, and the `qip-quantum-token` secret is declared but mounted nowhere | backend/crates/libs/qip-quantum/src/provider.rs:104-105,219,288-292,438-446; infrastructure/terraform/main.tf:270; infrastructure/egress/envoy.yaml:339-358 | Nothing here is a shortcut — the adapter refuses `https` by name rather than silently downgrading, and `envoy.yaml` already declares loopback listeners for both the current and legacy IBM Quantum endpoints ahead of a workload existing to use them. The gap is that GCP v2.1's placement (an 'optimisation' trust zone with controlled egress to the gateway) has never been added to any environment's `trust_zones`, so the secret that would flow to it (`qip-quantum-token`, created in `main.tf` but mounted nowhere) is provisioned for a workload placement that does not exist yet. Consistent with quantum being off by design (item above); this is its infrastructure half. | E10 |
| low | services-capital-exec | DexVenue models pricing/feasibility only and deliberately does not implement VenueAdapter — no order entry, no fills, tracked as open work in the blueprint requirements register | backend/crates/services/qip-brokers/src/dex.rs:45-65; backend/crates/services/qip-brokers/src/dex.rs:76 | The module doc is explicit and well-reasoned ('an adapter that implemented submit_order against a chain it cannot reach would be a stub that reads as a connection') — this is not an accidental stub, it is a scoped half-build. But it is also a named, open blueprint requirement rather than a closed decision: `docs/blueprint/assessment/EXEC-b1.json` (EXEC-018) and `COVERAGE.json` (COVERAGE-018) both score the simulated-DEX order-entry path MISSING/PARTIAL against v11.6's asset-coverage requirements, with work items already named (E04-dex-venue-adapter / E17-simulated-dex-adapter). A simulated on-chain adapter (matching-engine-backed, like SimulatedExchange) does not require live capital or external action, so it is not blocked by ADR 0099's conflict register — it can be built to the same paper-only standard as the CEX side. | E04 |
| low | services-data-world | MeshTransport and PubSubTransport are complete, tested Publisher/Subscriber implementations that no composition root ever constructs | backend/crates/services/qip-streaming/src/mesh.rs:73-78; backend/crates/services/qip-streaming/src/pubsub.rs:64-68 | `rg -n 'MeshTransport::\|PubSubTransport::' backend/crates/apps backend/crates/runtime --glob '!**/tests/**'` returns nothing; both are constructed only from qip-streaming's own `tests/`/`registry.rs` helper. Both are honestly documented as deliberately incomplete rather than as a shortcut — pubsub.rs states it 'never falls back' and 'never fakes a connection', and mesh.rs explains the ADR-0011/ADR-0009 dependency-direction reasoning for living where it does — so this is not a safety problem, it is scope not yet spent: no composition root can today reach a durable cross-process transport for anything routed `RoutingClass::Durable`. | E03 |
| low | services-data-world | qip-data-finder's module doc still describes NetworkProbe as an unimplemented stub; the real ADR-0060 client has shipped and the doc was never updated | backend/crates/services/qip-data-finder/src/lib.rs:64-69; backend/crates/services/qip-data-finder/src/probe.rs:14-20 | Both locations describe `NetworkProbe` as reporting `Error::Unavailable` naming what production must supply, but the current implementation (probe.rs:403-528) is a real HTTP client that only returns `Unavailable` on a genuine failed request. A reader trusting the doc comment would believe probing is unimplemented when it has been live in production since ADR 0060 (discovery.rs:181). Already named in the current-state record's own gaps_observed; carried forward here because it is the kind of doc-vs-implementation drift this platform's governance rules have already been burned by elsewhere and it is a trivial, low-risk fix. | E08 |
| low | services-intelligence | qip-evolution::attribution.rs (AttributionLedger, this crate's own gross-vs-deduction cost-surprise decomposition against NetEdge) has no caller outside the crate's own tests, so cost-model miscalibration surprises are never fed to cost_model.rs's CostModelCalibration despite the module doc naming that feed as the reason it exists | backend/crates/services/qip-evolution/src/attribution.rs:195; backend/crates/services/qip-evolution/src/cost_model.rs:120,213 | `grep -rln "qip_evolution::attribution" backend --include='*.rs'` matches only files inside the crate itself. The module's own doc comment states its purpose as feeding two downstream consumers -- "Feeding the first into crate::scoring and the second into crate::cost_model is the division this module exists to make possible" -- and neither feed exists in production: `cost_model::CostModelCalibration::propose_all` is reached from qip-deepbrain, but nothing constructs an `AttributionLedger` to hand it a surprise to calibrate against. This is distinct from qip-learning-engine's Attribution (P&L source decomposition); both live in a file named attribution.rs in sibling crates of the same component group, which is worth a naming note but is not itself the defect -- the defect is the unreached feed. | E10 |
| low | services-intelligence | qip-twin::regret.rs (RegretAnalysis, AlternativeRegret -- systematic-regret-across-alternatives analysis) has no caller outside the crate's own tests, so the platform never asks whether it is systematically regretting the same kind of declined alternative | backend/crates/services/qip-twin/src/regret.rs:36,99,113; backend/crates/services/qip-twin/src/lib.rs:128 | `grep -rln "regret::" src` inside qip-twin, outside lib.rs and regret.rs itself, returns nothing, confirmed. The counterfactual engine already scores alternatives every cycle (AlternativeMenu, counterfactual.rs:48,117) and precedent_declines.rs consumes qip-twin's Simulated type, so the raw material RegretAnalysis needs already flows through the platform -- what is missing is the aggregation step that would turn individual scored alternatives into a systematic-bias finding for the Evaluation/Meta Brain to act on. | E08 |

## E — needs evidence before changing (62)

| Risk | Group | Item | Files | Why | Epic |
|---|---|---|---|---|---|
| high | apps | `RestGateway`'s paper/live self-report (`is_simulated`) is derived from `AdapterClass`, which has no `Live` variant by design — but the crate's own two module docs disagree about what that guarantees: `qip-brokers::adapter` says `AdapterClass::Sandbox` is 'a real protocol against a real endpoint, settling nothing. Still not real money', while `qip-edge-node::venue` says in the same breath that selects it, 'nothing in the platform's code can tell a venue's sandbox host from its production host... point the adapter at [the production host] and the orders are real while `Broker::is_simulated` still answers `true`' | backend/crates/apps/qip-edge-node/src/gateway.rs:769-780 (RestGateway doc: 'is_simulated answers true, and that is not a claim about the money'); backend/crates/apps/qip-edge-node/src/gateway.rs:929-940 (impl Placer for RestGateway: fn is_simulated reads self.adapter.is_simulated() and nothing else); backend/crates/apps/qip-edge-node/src/venue.rs:19-28 (module doc: 'nothing in the platform's code can tell a venue's sandbox host from its production host... this process has no way to ask') | This is not a hidden bug — the code names its own limit repeatedly and more plainly than most findings in this register, and the design (no Live variant can be written, so the type system rather than a runtime flag is what a live path would have to defeat) is exactly the 'make it structural' principle CLAUDE.md asks for. But `Cell::send`'s GATE_LIVE_VENUE — the third of the three independently-cited paper-trading layers in `.claude/rules/01-security-and-safety.md` ('the type system... qip-edge's Cell has no constructor taking a ceiling other than paper trading') — decides whether an order is refused entirely from `gateway.is_simulated()`, and `RestGateway.is_simulated()` is true unconditionally, for any host it is pointed at. So the one structural check this codebase's own security rule names as independent of operator diligence is, for the one gateway that can reach a real network endpoint, not independent of operator diligence: it depends on the operator never pointing `QIP_VENUE_ORDER_ENTRY_ACKNOWLEDGED` and the credential at a venue's production host, a fact `venue.rs`'s own module doc calls 'a witness, not a check'. `qip-brokers::adapter`'s doc asserts a stronger guarantee ('settling nothing... not a variant that can become one') than `qip-edge-node::venue`'s doc admits the crate can deliver, and a reader of only the first would believe the third layer holds unconditionally for this path. Today this is inert — `execution_nodes = {}` in every environment, so no process ever constructs a `RestGateway` outside tests — but per CLAUDE.md's own product-direction question ('Does it make a guarantee weaker in exchange for speed? Then say so out loud in the diff') the two docs should say the same thing, and an owner should decide whether the venue-host claim needs a structural check (e.g. an allow-listed sandbox-host registry) before `execution_nodes` is ever non-empty. This report classifies rather than resolves it, per `.claude/rules/01-security-and-safety.md`: 'If a task appears to require a live-order path, stop and ask.' | E07 |
| high | apps | No Compute Engine execution node has ever been provisioned in any environment (`execution_nodes = {}` everywhere), so `qip-edge-node` — the only binary that can hold a `Cell` — has never run outside `cargo test` in this repository's history | infrastructure/environments/dev/terraform.tfvars:1-27,227; infrastructure/environments/{test,stage,prod}/terraform.tfvars:49,49,51; backend/crates/apps/qip-edge-node/src/main.rs:1-1157 | ADR 0035 authorises exactly one node (newyork-1, shadow mode) but, per dev's own tfvars comment, needs three sequenced human acts (bake the boot image, dispatch infra.yml up, then image.yml) plus an operator-chosen capital allocation the tfvars says 'no workflow may pick'. None of that is a code defect: 129 tests exercise the gateway/mesh/arbitrage/reprice/cross-region stack, and the binary is built and Binary-Authorization-attested on every `deploy.yml` run. What is missing is evidence — the capital-allocation decision and the sequenced infra dispatch — not a rewrite, so this is not a bug to fix in this codebase; it is the one fact that makes every other apps-group finding about mesh reachability, live-venue exposure and edge telemetry currently untestable against a real deployment rather than merely untested. | E14 |
| high | delivery | The GitOps control plane (Argo CD + Config Connector on GKE Autopilot, ADR 0036) has never successfully bootstrapped in any environment: `vendor.yml`'s blocking Trivy gate refuses Argo CD's image on CVE-2025-68121 with no fixed upstream kustomize/git-lfs available, which also blocks cert-manager's images in the same fail-fast mirror loop, and `infra.yml`'s `up` action cannot complete a working control plane as a result | .github/workflows/vendor.yml:225,250 (--severity CRITICAL,HIGH and --severity CRITICAL gates); .github/workflows/vendor.yml:353 (module comment: CVE-2025-68121 lives in two third-party binaries Argo CD downloads); .github/workflows/infra.yml:120-164 (action=up's GitOps bootstrap step) | DELIVERY-STATUS.md's own measurement (not a release note) shows there is no newer kustomize to adopt — v5.8.1, the version Argo CD already ships, is upstream's latest as of 2026-02-09 — and the RC build still carries the vulnerable binary on an old Go toolchain, so this cannot be cleared by bumping a version. It explicitly instructs: do not clear with a scanner exception, a .trivyignore, or a per-image acknowledgement. The only two paths forward are (a) an upstream Argo CD release that actually fixes the CVE, which nothing in this repository can produce, or (b) the owner deciding ADR 0099's open conflict C3 (whether the platform keeps GKE/Argo CD at all, versus the Cloud Run-only direction v2.1 also allows). Until one of those exists, every ADR-0036-governed warm-service GitOps path stays unusable in every environment, which is exactly why this is a needs-evidence item rather than something an agent can refactor or replace unilaterally. | E01 |
| high | edge-libs | No Terraform environment ever provisions the execution node that is the sole production composition root wiring six of this group's seven crates (qip-arbitrage's scanner, qip-feature-dag via Cell::work, qip-protocols' registry, qip-routing's path/pathcycle/reprice/ratelimit, qip-sequencing's Sequencer, qip-strategy's live compiler), so every 'reached from production' claim in the source register is proven only by `cargo test`, never by a running process | infrastructure/environments/dev/terraform.tfvars:227; infrastructure/environments/test/terraform.tfvars; infrastructure/environments/stage/terraform.tfvars | `execution_nodes = {}` in every one of the four environments' tfvars is the single fact underneath the 'partial' status on qip-arbitrage, qip-feature-dag, qip-protocols, three of qip-routing's four sub-records, qip-sequencing and qip-strategy's live half. ADR 0099's conflict register names this precisely at row C8 and says what closes it: 'An owner cost ceiling, then a deployment record... Also blocked externally today: billing is disabled on the dev project, and deploy.yml fails at image push.' No code change in this crate group can close this; treating any of the seven crates' wiring as production-proven because a deployable binary's *source* reaches it would repeat the exact mistake .claude/rules/domains/observability.md warns against for metrics scraping. | E04 |
| high | edge-libs | qip-brokers, a service crate, depends directly on qip-routing, an edge crate, for RateLedger/RateLimits, FeeSchedule/Liquidity and GatewayCredential — and the acceptance suite's own boundary test checks libs against depending on services/apps/runtime/agents but has no test at all constraining anything against depending on edge, so this inversion of 'dependencies point inward only' is real, current and unguarded | backend/crates/services/qip-brokers/Cargo.toml:18; backend/crates/services/qip-brokers/src/exchange.rs:56-57; backend/crates/services/qip-brokers/src/credential.rs:27 | The boundaries file states 'Dependencies point inward only' and draws edge as a leaf consuming libs and services, never consumed by them. libs-foundation's own tech-debt register shows the house pattern for a deliberate exception to a boundary rule: name it explicitly, with the ADR that authorises it (qip-storage/qip-transport's I/O exception, ADR 0011/0012). No such naming exists for a service depending on an edge crate. Either this is an accepted lateral relationship that the boundaries doc should say so about, or the three type families qip-brokers needs (rate limiting, fee/liquidity schedule, gateway credential) belong in a libs crate both qip-routing and qip-brokers can depend on instead. Evidence needed: which, before either the rule file or the dependency is changed. | E00 |
| high | edge-libs | The live feature vocabulary (qip-feature-dag::features::standard_suite, unused in production) and the training/backtest feature vocabulary (qip_simulation_engine::harness::bar_catalogue, used by every deepbrain evolution/succession compile) are two independent implementations with no shared source — confirmed qip-simulation-engine does not depend on qip-feature-dag in Cargo.toml — so a strategy proven in backtesting is never proven against the same feature computation it would evaluate against live | backend/crates/apps/qip-deepbrain/src/evolution.rs:1047,1189,1208,2078; backend/crates/apps/qip-deepbrain/src/succession.rs:69,480; backend/crates/apps/qip-edge-node/src/strategies.rs:355 | v11.6 §10 point 2 requires features to be computed 'with strict lineage and leak prevention' between training and execution. Here there are two FeatureCatalogue producers with no code path connecting them — one built from qip-feature-dag's registered definitions (never populated live), one built from qip-simulation-engine's own bar-derived catalogue (the only one ever non-empty) — so lineage cannot be checked even in principle. A mismatch would surface only as a strategy that backtested well and is either refused or behaves differently live. Evidence needed: a decision on whether qip-feature-dag becomes the single catalogue source for both lanes, or an explicit equivalence is proven between the two. | E02 |
| high | infrastructure | Four RunService manifests declare `gcs` volumes (egress bootstrap, universe catalogue) that the vendored Config Connector operator's own CRD schema does not list as a valid volume kind | infrastructure/gitops/envs/dev/api.yaml:212-217; infrastructure/gitops/envs/README.md:41-80 | `gitops/envs/README.md` records a real schema check dated 2026-09-06: fetching the vendored operator's (v1.156.0) `RunService` CRD and enumerating `spec.template.volumes` returns exactly four properties — `cloudSqlInstance`, `emptyDir`, `name`, `secret` — with `grep -ci gcs` returning 0. The manifests declare `gcs { bucket, read_only }` anyway on four workloads (api, fastbrain, deepbrain, openobserve), because that is how Terraform's `google_cloud_run_v2_service` (the resource these manifests were derived from before ADR 0036) expressed the same mount. The README itself names three possible outcomes on the first real sync — refused validation, silently-different reconciliation, or a match — and says only the third is acceptable; none has been observed because the cluster that would sync them is suspended (see the GitOps item above). This is a genuine, previously-verified interface mismatch between the declared manifests and the operator that must reconcile them, not a hypothetical one. | E14 |
| high | infrastructure | All 9 Cloud Monitoring alert policies (kill switch, live fill, persistent breach, permission violation, edge halted, edge/central reconciliation break, risk figure unevaluated, sign-off withheld) are fully specified and gated behind `workload_metrics_exist`, which is `false` in every environment, and the one collector that would flip it (`cloud-run-gmp-sidecar`) is refused by the platform's own CRITICAL scan gate with no fixed upstream tag published | infrastructure/terraform/modules/observability/main.tf:1-23,313,395; infrastructure/terraform/modules/observability/NOT-SCRAPED.md; infrastructure/egress/vendored-images.txt:36-120 | Cloud Monitoring refuses a policy naming a descriptor it has never ingested, so `workload_metrics_exist` correctly stays `false` until a scrape is *observed*, not merely declared — and none has been, because `us-docker.pkg.dev/.../cloud-run-gmp-sidecar:1.9.2` fails vendor.yml's CRITICAL gate on CVE-2026-56854 (an unfixed `golang.org/x/crypto` finding) with no newer tag published by Google as of the last check. This is exactly the state `.claude/rules/domains/observability.md` insists on naming plainly: nothing currently pages a human on a kill-switch trip, a live-fill alarm, a persistent limit breach or a permission denial in any deployed environment, which is a direct gap against this platform's own stated precondition ('A process nothing can see is a process nobody can operate', `.claude/rules/10-product-direction.md`). The evidence needed is either an upstream fix to the sidecar or a decision to build/vendor an alternative collector — not a Terraform edit, since the gate is doing exactly what it should. | E12 |
| high | libs-foundation | The blueprint's Fabric target (async/Tokio, QUIC/mTLS, protobuf via prost, BLAKE3, Raft, gRPC clients) is architecturally incompatible with qip-transport/qip-events' current, deliberately different design (blocking I/O, serde_json wire, SHA-256, at-least-once HTTP/1.1, no consensus) | backend/crates/libs/qip-transport/src/lib.rs:1-75; backend/crates/libs/qip-events/src/bus.rs (590 lines, synchronous dispatch); docs/adr/0099-blueprint-v11-6-and-gcp-v2-1-are-the-architecture-of-record-in-direction-and-every-standing-decision-they-contradict-keeps-its-force-until-its-own-record.md (conflict register row C2) | This is a whole-shape conflict, not a single defect: every element of the current design traces to an accepted ADR (0001 no async runtime, 0002/0009 two dependencies, 0011 in-tree Pub/Sub substitute). ADR 0099 itself defers resolution to 'a dependency record per class (async runtime, transport security, wire schema, consensus, managed-service clients)' and explicitly forbids treating adoption of the blueprint as authorization to act ('A dependency, cluster or managed service appearing in the tree before its record... would mean this record was read as the reopening of ADR 0002, 0009 or 0024, which it explicitly is not'). No libs-foundation code should move on this until that record exists. | E03 |
| high | libs-foundation | `DegradationState::halts()` is hard-coded to always return `false`; an expired capital envelope narrows a desk's size but never halts it, withdraws its resting orders, or reports it halted | backend/crates/libs/qip-contracts/src/degradation.rs:853 (pub const fn halts(&self) -> bool { false }); backend/crates/edge/qip-edge/src/cell.rs:3493-3497 (envelope_expiry gate refuses new signals only, never calls apply_halt); backend/crates/edge/qip-edge/src/cell.rs:1956 (is_halted driven only by kill_switch/policy_halt/polled_halt) | A value that should reflect live degradation state is instead a compile-time constant, by explicit design ('halting belongs to the kill switch... not a consequence of a warm-path job being late'). Consequence: a cell whose capital envelope has expired keeps trading indefinitely at a reduced size (floor 0.375x) rather than stopping — the opposite of 'every safety default is the restrictive one' (CLAUDE.md principle 3). The code and its own tests already name the tension; what is missing is an owner decision on which side is wrong — the constant or the fail-closed principle — before either changes, not a mechanical bug fix. | E07 |
| high | services-capital-exec | qip-mesh::spine implements only centre-to-cell hub-and-spoke dispatch (CapitalDispatcher down, CellDeltaReceiver up); no peer-to-peer inter-cell Reflex Mesh exists, though v11.6 §17 requires cells to exchange compressed opportunity/edge state directly with peers for cross-region 2-20-leg arbitrage — an architecture conflict with standing ADR 0008 ('Cells decide alone') that is not yet in ADR 0099's conflict register | backend/crates/services/qip-mesh/src/spine.rs:1-30; backend/crates/services/qip-mesh/src/spine.rs:273; backend/crates/services/qip-mesh/src/spine.rs:791 | spine.rs's own module doc states the topology directly: 'the wire between the nine regional cells and the central plane' — every CapitalDispatcher's 'peer' (spine.rs:275,308,323) is, from the centre's perspective, one cell, not another cell. v11.6 §17 requires a materially different shape for arbitrage coordination: 'Reflex nodes publish compressed executable edge states... to peers or regional coordinators' over a mesh that is explicitly 'deliberately separate' from the durable Fabric this crate's sibling channel resembles, and §30's Build Order names 'Multi-Region Reflex Mesh... peer mesh, opportunity tokens, distributed reservations, 2-5 leg cycles' as Phase 4's exit capability, growing to 3-20 legs in Phase 5. Today's centre-only fan-out cannot carry that traffic without going through the centre on every hop, which the blueprint explicitly rules out for latency-sensitive coordination. This is independently corroborated by `docs/blueprint/assessment/MESH-b1.json` (MESH-003: 'no peer channel exists'). ADR 0008 ('Cells decide alone... regional low-latency execution is separate from centralised training and portfolio intelligence, and the split is load-bearing') is a standing decision under `.claude/rules/10-product-direction.md`; per that rule, no code change may reopen it — this needs its own conflict-register entry and an owner ADR, the same way ADR 0099 treats C3/C7/C8, before any peer-mesh code is written. | E11 |
| high | services-data-world | No environment in this build has ever subscribed to a licensed live feed; default node startup always falls back to the synthetic generator | backend/crates/apps/qip-deepbrain/src/main.rs:434-460; backend/crates/services/qip-market-ingestion/src/synthetic/mod.rs:1-40 | Confirmed against `main.rs:434-460`: with no `--tape`/`--replay` flag the node always constructs `SyntheticEnvironment::demo`. This is not a silent shortcut — every synthetic record is stamped `LicensingClass::Synthetic` and the type system refuses it as a production decision input — but it does mean that, today, zero of the platform's environments (dev included) ever ingest a real observation. Combined with finding 3 (2 of 7 admissible sources blocked on legal review) only five sources (coinbase, ecb, nyfed, nws, and whichever of kalshi/alpaca clears review) could ever be live, and none is configured. Evidence needed: a committed plan or date for standing up at least one real feed in a real environment, since 'strictly paper trading' is a boundary on order submission, not a reason to run permanently on synthetic data. | E08 |
| medium | apps | The mesh backbone (`qip-api::mesh::MeshBackbone`) has never run against a real edge cell outside tests: `QIP_MESH_CELLS` is unset in every committed environment, so `/api/v1/mesh` always answers with zero cell listeners | backend/crates/apps/qip-api/src/mesh.rs:1033-1205 (MeshBackbone::open/cells/regions/dispatch_policy); backend/crates/apps/qip-api/src/main.rs:315-332 (constructed only when QIP_MESH_CELLS is set); infrastructure/terraform/catalogue.tf:29-34 (module comment: unset, qip-api builds no mesh) | 21 tests (including `a_reconciliation_break_crossing_the_wire_halts_that_cell_and_only_that_cell` and `a_dispatched_envelope_survives_the_spool_and_verifies_at_a_real_downlink`) prove the backbone's logic in isolation, but it is structurally downstream of the same gap as the qip-edge-node finding above: there is no deployed cell anywhere to speak the mesh protocol to, because no execution node exists. Wiring `QIP_MESH_CELLS` ahead of a real cell existing would not close this gap, only relocate it, so the evidence needed here is the same owner decision (capital allocation, boot image, infra dispatch) the qip-edge-node finding names, not independent API-side work. | E03 |
| medium | apps | Every feed `qip-fastbrain` can actually select in a default deployment (synthetic exchange, replay, committed tape) is explicitly marked not production-grade; only a licensed `Live` connector can be, and none is configured in any committed environment | backend/crates/apps/qip-fastbrain/src/feed.rs:701-705 (no_source_this_build_has_is_production_grade_and_each_says_what_is_missing); backend/crates/apps/qip-fastbrain/src/feed.rs:999-1010 (a_live_vendor_is_licensed_and_may_drive_a_capital_decision — the one path that could be, unconfigured everywhere); backend/crates/apps/qip-fastbrain/src/main.rs (banner text naming the active feed) | This is deliberate and structurally honest — `is_production_grade` is read from the feed's own `LicensingClass`, not asserted by configuration, so a synthetic tape cannot masquerade as market data (the same pattern as the deep brain's `Provenance::synthetic` stamping). No code defect follows from this; it correctly reflects that no committed environment has a licensed vendor wired in. It is recorded here because it is the fast-path half of the same fact the deep brain's discovery/connector finding states for the slow path: every capital-relevant feed in this workspace is currently either synthetic or unconfigured, which bears directly on how much of the platform's REASON/DECIDE behaviour has ever run against anything but its own generated data. | E05 |
| medium | apps | `QIP_CENTRAL_HORIZONS_PATH` is read by `qip-deepbrain`'s composition root but wired in no committed environment, so the §23.4 self-improvement/promotion horizon gate refuses to arm everywhere this binary has ever run | backend/crates/apps/qip-deepbrain/src/main.rs:1005-1025 (doc: 'grep -rn QIP_CENTRAL_HORIZONS_PATH infrastructure/environments finds it wired nowhere') | The gate is fail-closed by design — refusing to arm without a named policy rather than defaulting to an implicit one, consistent with 'every safety default is the restrictive one' — so this is not a bug in the refusal itself. But it means the controlled-self-improvement loop v11.6 §23.4 describes ('curriculum generation, independent evaluation, promotion by lane') is architecturally present in this crate yet has never operated in any deployed environment, because the one file that would arm it has never been written and pointed to by any committed tfvars or GitOps manifest. Evidence needed before wiring it: an owner-approved horizons policy document and a decision on where it is sourced from (operator-authored, or generated by the platform itself, which would be a different trust boundary). | E10 |
| medium | delivery | Twelve non-Rust delivery-tooling files (eight Python, four Node) exist under `.claude/hooks/` and `scripts/`, with no ADR recording the deviation from the platform's "Rust everywhere" rule — the acceptance suite pins the set as accepted rather than arguing for it, and one of the twelve, `terraform-undeletable.py`, is load-bearing on the infrastructure pipeline itself | backend/crates/tests/qip-acceptance/tests/blueprint_rules.rs:130-143 (ACCEPTED_NON_RUST_TOOLING, 12 entries); backend/crates/tests/qip-acceptance/tests/blueprint_rules.rs:146-190 (the_non_rust_tooling_outside_the_browser_layer_is_exactly_the_accepted_set); backend/crates/tests/qip-acceptance/tests/blueprint_rules.rs:148 ("the deviation is real, no ADR records it") | ADR 0001/0011 and v11.6's own §2 Non-Negotiable Architecture Rules (rule 1's tooling clause, per the test's own comment) commit to Rust everywhere with the browser as the sole named exception; delivery tooling is a second, unrecorded exception the tree already carries. This is not hypothetical drift — the very next commit in this repository's history (3b48f91b) shows a thirteenth Python file landing and being caught only because a later, unrelated commit ran the full acceptance suite instead of the narrower suite the change looked like it touched; had that gate lapse not been repaired, the deviation would have grown silently again. Fixing the twelve that remain the way the renderer was fixed (migrate into `qip-cli`, which already proved it can do JSON-in/Markdown-out work with only `serde_json`) is possible for some of them, but not obviously right for all — `terraform-undeletable.py` runs inside `infra.yml` itself and a browser-automation job (`venue-signup/`) is explicitly walled off from the platform's own dependency graph by design. The decision needed is which of the twelve get an ADR carve-out and which get migrated, not a unilateral rewrite. | E01 |
| medium | delivery | `deploy.yml`'s execution-node rolling-replace step and `image.yml`'s boot-image bake have never run in any environment — `execution_nodes = {}` in every environment's tfvars — leaving ADR 0035's one authorised node (dev, `newyork-1`, shadow mode) unbuilt pending three sequenced human acts none of which any workflow performs on its own | .github/workflows/deploy.yml:585-592,739-765 (replace every execution node under its group's rolling policy — documented no-op); .github/workflows/image.yml:101-110 (manual-dispatch-only bake, ADR 0035 reference in its own input description); infrastructure/environments/dev/terraform.tfvars:1-27 (execution_nodes = {} — "a working configuration, not an incomplete one", names the exact blocked sequence) | This is not silently broken: deploy.yml's own step detects the empty group and prints "no execution node group exists in ${TARGET_ENVIRONMENT}; nothing to replace" rather than failing, and dev's tfvars carries a five-line comment naming exactly what is missing — a capital-allocation number ADR 0045 proposes but does not set, and a three-step human sequence (uncomment the image-bake subnet CIDR, dispatch infra.yml action=up, only then dispatch image.yml) where doing it out of order refuses by design. The evidence needed before this can move to B or be scored differently is simply that the sequence has been run: an image self-link written into dev's tfvars and a capital number chosen. Nothing here is a defect to refactor; it is a deliberately incomplete migration waiting on two decisions no workflow may make. | E14 |
| medium | delivery | No branch-protection or required-review configuration exists anywhere in the tracked tree — no CODEOWNERS file, no `hashicorp/github` Terraform provider, no ruleset — so nothing in the repository can prove or refute whether `main` actually requires status checks or independent review before merge | (negative evidence, independently confirmed: `find . -iname CODEOWNERS` and `grep -rn 'provider "github"' infrastructure/` both return nothing); backend/crates/tests/qip-acceptance/tests/infrastructure.rs:4724-4743 (production_is_never_deployed_automatically — asserts only that a GitHub environment: name exists, not that it requires review); .github/workflows/deploy.yml:598 (the named GitHub environment: per job, where required reviewers would attach if configured) | This is a case the acceptance suite itself cannot close: branch protection is a GitHub repository setting, not repository content, so no test over the tree can assert it either way (docs/blueprint/assessment/CICD-b2.json scores this row NEEDS-VALIDATION for exactly this reason). Before this becomes a B (encode it as a `hashicorp/github` Terraform resource) or is scored A (already configured out-of-band), someone needs to run `gh api repos/:owner/:repo/branches/main/protection` against the live repository and read the answer — an agent cannot infer it from files. | E01 |
| medium | edge-cell | `MAX_OPEN_ORDERS = 256` bounds every strategy, venue and instrument a cell serves combined, with no override path and no test that drives the cell to the bound to prove the `open_orders` refusal actually fires | backend/crates/edge/qip-edge/src/cell.rs:915 (pub const MAX_OPEN_ORDERS: usize = 256 — a crate-level constant, not a CellConfig field); backend/crates/edge/qip-edge/src/cell.rs:3912-3925 (has_open_capacity / refuse_for_capacity — the gate that fires at the bound) | The bound is deliberate and its refusal is journaled like any other gate — this is not a silent limit — but two things are true together: it is a single constant shared across every strategy/venue/instrument the cell serves rather than a per-scope figure, and `grep -n 'MAX_OPEN_ORDERS\\|256' backend/crates/edge/qip-edge/tests/*.rs backend/crates/tests/qip-acceptance/tests/stress.rs` finds no test that actually opens 256 orders and asserts the 257th is refused under the `open_orders` gate. Blueprint §18.1 (Market Making) is one of the capabilities this crate is scored against, and a market maker resting passive quotes across many instrument×venue pairs is exactly the shape of workload that could reach 256 working orders in one cell. Evidence needed before treating this as fine as is: a measurement of how many open orders a realistic market-making deployment actually holds, and a boundary test proving the refusal fires (and that a cell at capacity keeps trading rather than wedging) — not a code change on its own. | E04 |
| medium | edge-libs | qip-routing::gateway (the Gateway trait, SimulatedGateway, NativeGateway) has zero production constructors anywhere in the workspace and is the only impl of the Gateway trait that exists at all, yet a structural acceptance test explicitly forbids qip-edge's own Cell from importing qip_routing::gateway — qip-edge-node instead built and wired its own, independent SimulatedGateway/NodeGateway | backend/crates/edge/qip-routing/src/gateway.rs:398,684 (the only two `impl Gateway for` sites in the workspace); backend/crates/apps/qip-edge-node/src/gateway.rs:47-65,179,1077,1160; backend/crates/tests/qip-acceptance/tests/path_router.rs:513-521 | Two independent implementations of 'how a cell places an order at a venue' exist in the tree: the one this crate's lib.rs names as a first-class contract is reached by nothing but its own tests, and the one actually running in qip-edge-node's source is unrelated code with a similarly-named type (SimulatedGateway exists in both, doing different things). The acceptance test's own comment reads as institutional wariness of letting cell.rs anywhere near qip_routing::gateway ('a second order path... is always the one nobody adds a refusal to'), not a stated plan to use it from somewhere else. Neither qip-brokers' SimulatedExchange nor anything else implements or constructs it. Evidence needed: a named future caller, or this is a D-delete case once the absence of a plan is confirmed rather than inferred from silence. | E04 |
| medium | edge-libs | qip-routing::router::Router (venue all-in-cost selection) and ordertype::select_order_type both have zero production call sites; the one production repricing path that needs an order type hard-codes RoutedOrderType::Limit directly rather than calling the crate's own urgency/touch-based selection algorithm | backend/crates/edge/qip-routing/src/router.rs:503 (select_order_type's only non-test caller, itself unreached); backend/crates/apps/qip-edge-node/src/reprice.rs:377,693 (RoutedOrderType::Limit constructed directly); backend/crates/edge/qip-routing/src/ordertype.rs:211,371-390 (select_order_type and its passive/aggressive ladder) | The crate's own lib.rs names venue selection on all-in cost as its first capability and urgency/touch-based order-type selection (market/limit/peg/IOC/FOK) as its second; production reaches neither. Every resting order the edge node places today is a plain limit order by construction, not by a computed routing decision — the selection ladder select_order_type builds for exactly this purpose sits unreached in a deployable binary's source. This is more specific than ordinary dead code: it is a live decision point standing in for one the crate already computes. | E04 |
| medium | edge-libs | qip-sequencing's ClockDiscipline, LineArbiter and FailoverReconciler — three of the crate's four disciplines — have no production caller anywhere; ClockDiscipline and FailoverReconciler have zero references outside their own crate at all, including in the acceptance suite | backend/crates/edge/qip-sequencing/src/clock.rs; backend/crates/edge/qip-sequencing/src/arbitration.rs; backend/crates/edge/qip-sequencing/src/failover.rs | The crate exists to stand 'between a decoded feed and a book that is quietly wrong' across four failure modes; only reordering/dedup (Sequencer/SequenceTracker) is wired into Cell::on_bytes. qip-edge-node's only production source is a single in-process simulated feed (see item 1 and item 8), which has no venue clock to discipline, no redundant A/B line to arbitrate and nothing to fail over from — so today's absence of a caller may track the absence of a multi-line real feed rather than an abandoned design. Evidence needed either way before choosing between wiring toward a real dual-line feed and deleting. | E04 |
| medium | edge-libs | qip-orderbook's and qip-sequencing's other production dependent, qip-market-ingestion, reaches this group's types exclusively through IngestionService, which is constructed only inside its own #[cfg(test)] block — no qip-api/qip-fastbrain/qip-deepbrain src file constructs one or references depth:: outside tests | backend/crates/services/qip-market-ingestion/src/service.rs:378-554; backend/crates/services/qip-market-ingestion/src/depth.rs:134-137 | Each of qip-orderbook and qip-sequencing has two named production dependents in the source register — the qip-edge/qip-edge-node chain and qip-market-ingestion — and both terminate the same way: the edge-node chain is undeployed (item 1), and qip-market-ingestion's own caller of these types never leaves its test module even though qip-market-ingestion itself is a real dependency of the deployed qip-api/qip-fastbrain/qip-deepbrain binaries. So these two crates are reached from a deployable binary's *source* twice each, and from a running process zero times. Evidence needed: whether qip-market-ingestion's depth/sequencing integration is active near-term work or an earlier prototype now superseded by the qip-edge path. | E04 |
| medium | infrastructure | No execution cell has ever been provisioned in any environment: `execution_nodes = {}` everywhere, and the one boot image the node would run (`infrastructure/images/execution-node`) has never actually been baked against a real project | infrastructure/terraform/variables.tf:245-313; infrastructure/environments/dev/terraform.tfvars:200-227; infrastructure/terraform/modules/image-bake/main.tf:1-16,134-260 | `modules/execution-node`'s `for_each` over `var.execution_nodes` instantiates zero nodes in dev/test/stage/prod, so the platform's whole edge/Reflex execution layer — the one part of the blueprint this repository builds toward directly, not just researches — is unreached in every deployed environment. The image bake is individually verified offline (crane extraction, the secret-fetch helper against stub metadata/Secret Manager servers) but `images/execution-node/README.md` states plainly the bake workflow itself has never been dispatched, so cloud-side behaviour (serial-console handshake, `update-grub`, guest OS features) has never been proven. ADR 0099's conflict register (C8) already names the blocker precisely: an owner cost ceiling is required, and billing is independently disabled on the dev project today (`deploy.yml` fails at image push with 'This API method requires billing to be enabled'). The evidence needed before building further here is the owner's cost decision plus a `region_allocation` choice (deliberately never defaulted) — not a code change. | E14 |
| medium | infrastructure | `control_fabric_topic` is hard-coded to `null` in the composition root; no native Event & Control Fabric resource exists anywhere in the Terraform tree, so the blueprint's internal nervous system has no place to run | infrastructure/terraform/main.tf:493; infrastructure/terraform/modules/data/main.tf:1-19 | v11.6 replaces Pub/Sub and Kafka with a native Rust Event & Control Fabric (Tokio, QUIC/mTLS, Protobuf via prost, BLAKE3, a Rust Raft) run on dedicated Compute Engine VMs across three zones (GCP v2.1 §26/§19). This workspace permits exactly two dependencies (`serde`, `serde_json`; ADR 0002/0009) and no async runtime (ADR 0001/0011), so nothing in this configuration can provision a Fabric broker today — there is no crate, no module, no resource, confirmed by grep across the tree. `control_fabric_topic = null` is the honest expression of that gap rather than a bug, and ADR 0099's own conflict register (C2) already schedules the fix as 'a dependency record per class (async runtime, transport security, wire schema, consensus, managed-service clients), each weighed against an in-tree or egress-proxy alternative' — exactly the evidence this item needs before it can move. | E03 |
| medium | infrastructure | Kargo's promotion chain beyond `dev` is structurally unable to deploy anything: the one warehouse subscribes to `dev`'s registry, and a promotion to `test`/`stage`/`prod` would commit `dev`'s digests into a `kustomization.yaml` naming a registry that does not hold them and an attestor that never signed them | infrastructure/gitops/kargo/README.md:30-49; infrastructure/gitops/kargo/base/stages.yaml:14-105 | This is documented by the module's own README as a known-unresolved architecture question ADR 0036 itself anticipated in its 'what would make this wrong' section: either one registry and one attestor serving every environment (so a digest means the same thing everywhere), or one warehouse per environment subscribed to that environment's own builds. Until that record exists, a promotion past `dev` would commit, the target's Argo CD `Application` would report `Degraded` (when that environment even has a control plane, which none but dev does), and nothing would deploy — not a bug in the promotion task itself, but a real gap in the environment-progression story every other CI/CD control in this repository assumes exists. | E14 |
| medium | infrastructure | `modules/data`'s six managed data services (BigQuery, Cloud Storage archive, AlloyDB, Bigtable, Memorystore, Spanner — including the global ledger database `trust-zones` wires its grants against) are fully implemented Terraform, gated off in every environment, with no driver or adapter for any of them anywhere in the Rust workspace | infrastructure/terraform/modules/data/main.tf:1-19,85,214-343; infrastructure/terraform/modules/data/NOT-PROVISIONED.md; infrastructure/environments/dev/terraform.tfvars:252-258 | All six `enable_*` flags are `false` in every environment's tfvars by design — `StorageTarget::is_implemented` in the Rust workspace returns true only for `memory`/`file`/`engine`, and provisioning an unopenable managed database would be a bill and an attack surface with nothing behind it (the module's own header names this explicitly). v11.6/GCP v2.1 name Spanner Enterprise Plus as the global, multi-region ledger (GCP-004) with its KMS keys isolated in a dedicated `prod/ledger` project (SEC-007), which directly contradicts ADR 0002/0009's two-dependency posture and ADR 0089's file-backed log. ADR 0099's conflict register (C4) already schedules the fix as 'a storage record per store... the ledger comes first' — that record, not a Terraform flag flip, is the evidence this item is waiting on. | E05 |
| medium | infrastructure | `modules/connectivity`'s Partner Interconnect VLAN attachment is fully built and entirely gated off in every environment, and its own module header notes an attachment with no ordered partner circuit sits `PENDING_PARTNER` and bills hourly once activated whether or not any traffic ever crosses it | infrastructure/terraform/modules/connectivity/main.tf:59-162; infrastructure/terraform/modules/connectivity/NOT-ORDERED.md | `enable_partner_interconnect` and `enable_private_service_connect` are both `false` in every environment's tfvars, so nothing bills today — but this is the answer to prod's 3 colocated cells (chicago-1, newyork-1, dubai-1) sitting ~300-400km from their venues with no local GCP region, and `NOT-ORDERED.md` names manual, out-of-band steps (the partner circuit itself, the cross-connect, the pairing key) that Terraform cannot perform and that this repository cannot verify have happened before a future `apply` flips the flag. The cost risk (an activated attachment with no partner circuit billing indefinitely) is a real scaling-limit finding worth a runbook check gating any future enablement, not just a flag. Blocked on the same owner cost ceiling as the execution nodes it exists to serve (ADR 0099 C8). | E14 |
| medium | infrastructure | Six optional per-workload data files (`venue_registrations_file`, `wallet_statement_file`, `capital_fabric_file`, `central_horizons_file`, `risk_limits_file`, `source_candidates_file`) are `null`/commented-out in every environment's tfvars, so every corresponding `QIP_*_PATH` mount is absent from every deployed workload in every environment | infrastructure/terraform/catalogue.tf:93-225; infrastructure/terraform/variables.tf:470-610; infrastructure/environments/dev/terraform.tfvars:315-425 | Each variable follows the platform's own principle 2 ('refuse rather than guess') correctly — an unset file is absent, not defaulted to an empty or fabricated one, and the validation regex refuses a path outside `data/` or containing `..` — so this is not a code defect. It is nonetheless a real, structural gap against the blueprint: the desk's venue registrations, wallet statement, capital-fabric declaration, §23.4 central-horizons policy, signed risk limits and source-candidate catalogue reach no deployed binary anywhere today, and `dev`'s own tfvars comment (line 425) notes this is consistent with `source_candidates_file`'s absence producing zero passes. What is needed is not a Terraform change but the desk actually producing and committing these files (or an explicit decision that a given one stays empty for longer), which is why this is evidence-needed rather than a refactor. | E06 |
| medium | kernel-cycle | Platform::run_cycle is a single synchronous eight-stage pass invoked once per call, shared unmodified across ms-cadence (qip-fastbrain), request-driven (qip-api) and minute-to-day-cadence (qip-deepbrain) composition roots; v11.6 describes a federation of independently-paced lanes plus an always-on ambient layer that 'does not wait for a user prompt or a strategy tick', and neither the cycle's stage sequence nor its callers have a concept of a background pass, a selectively-woken stage, or a lane running at its own cadence | backend/crates/runtime/qip-kernel/src/platform.rs:9009-9157 (run_cycle: all eight stages run unconditionally, once per call); backend/crates/runtime/qip-kernel/src/platform.rs:10412 (stage_discover -> self.opportunities.scan runs every cycle regardless of what the last cycle found); backend/crates/runtime/qip-kernel/src/platform.rs:10998 (stage_reason -> self.organisation.dispatch runs every cycle unconditionally; no 'wake on detection' path) | The kernel-cycle's whole reason to exist is legibility -- 'a cycle that produced nothing should be as legible as one that traded' (cycle.rs:1-8) -- and that property is real and well-tested for the tick-driven model it implements. But v11.6's federation (Reflex Mesh, Cognitive Slow lane, always-on ambient background models) is a different shape: cadence-per-lane and event-triggered activation, not one enum walked start to finish on every call regardless of caller. This is not one of ADR 0099's eight named conflict-register rows (C1-C8), which is itself worth flagging -- the register may be incomplete on this axis. Migration looks plausible (an ambient lane could run alongside the existing synchronous cycle rather than replacing it, the way qip-edge-node's reflex loop already sits beside qip-deepbrain's slow loop per ARCH-001), so this is not scored C-replace. What is missing before any code moves: an architecture record, in the shape of ADR 0099's own conflict rows, saying how a background/ambient lane relates to Platform::run_cycle -- a new stage, a second entry point, or a wrapper that schedules cycles at variable cadence. | E09 |
| medium | kernel-planes | family_review.rs's own safety argument for why no code path can move a family's capital weight rests on a factual claim that is now false: it says `CentralPlane::issue` 'is the only writer of a capital envelope and is reached from nothing but a test' — it is now reached from a live, human-dual-signed API route | backend/crates/runtime/qip-kernel/src/family_review.rs:19-24; backend/crates/runtime/qip-kernel/src/central/plane.rs:1883; backend/crates/runtime/qip-kernel/src/platform.rs:7190,7315 | Confirmed with `rg -n '\.issue\(' backend/crates -g '*.rs'`: `self.central.issue(...)` is called at platform.rs:7315, inside `Platform::issue_capital` (platform.rs:7190), which is itself called from a production API handler at qip-api/src/routes.rs:1915. That call site requires two distinct authenticated operator signatures (platform.rs:7240-7291 refuses a single operator signing twice, refuses a stale first signature, and requires a countersignature rationale) and never consumes a `family_review::MisallocationFinding` as input — it is a separate, human-gated capital-grant workflow. So the specific invariant the module cares about (no *automated* path from a family finding to a capital move) is not demonstrated to be violated by this caller. But the sentence in the module doc is an unhedged factual claim, and it is now wrong, in the exact way the module's own comment warns about two lines later: 'this line said "the absence is the guarantee" until 2026-09-14 and three rounds of an acceptance scan were built on that reading... An independent review defeated each of the three rounds.' A stale premise in a load-bearing safety comment in the highest-consequence domain is worth an explicit re-check before the next reviewer inherits it at face value — the evidence needed is a reviewer confirming `issue_capital`'s new production path still cannot be reached from `family_review`'s output, and then correcting the sentence (or replacing the assertion with the verify-command discipline the comment already uses one line later) rather than leaving it stated as settled fact. | E07 |
| medium | libs-foundation | qip-storage's managed-store adapters (Cloud Storage, BigQuery, Redis/Memorystore — 4,486 lines combined) are fully implemented and unit-tested but structurally unreachable from any deployed configuration | backend/crates/libs/qip-storage/src/gcp/storage.rs (563 lines); backend/crates/libs/qip-storage/src/gcp/bigquery.rs (1038 lines); backend/crates/libs/qip-storage/src/redis.rs (2089 lines) | is_implemented()==true for CloudStorage, BigQuery and Memorystore, and each adapter has its own test suite, but Terraform's storage_target variable refuses any value naming them, so no composition root can select one — confirmed by no non-test reference to CloudStorageBlobStore/BigQueryWarehouse/RedisKeyValueStore in backend/crates/apps/*/src. This is not dead code with no plan: the type is fresh and tested, and ADR 0099 names the missing piece explicitly — 'a storage record per store, the ledger first' (C4). Widening the Terraform gate or wiring a caller ahead of that record would be building ahead of a decision, not migrating toward one; the evidence needed is that record. | E05 |
| medium | libs-foundation | qip-storage's `DurableStore` is a single-node WAL+checkpoint engine; no replication, quorum or broker-fleet concept exists anywhere in the crate | backend/crates/libs/qip-storage/src/engine/mod.rs (906 lines: WAL + checkpoints + crash recovery, single process); infrastructure/terraform/modules/execution-node (one local disk per node, no clustering) | v11.6's Fabric wants regional broker fleets with replicated, quorum-acknowledged logs; the only durable store this workspace has is one process's embedded engine on local disk with no consensus protocol, and a Raft (or equivalent) implementation is a new dependency ADR 0002/0009 currently refuse. Evidence needed before any multi-node design starts: a consensus/replication record under ADR 0099 C2. Today's actual exposure is bounded because no environment provisions the node that would run this engine at all (execution_nodes = {} everywhere, per item 1), so this is a target-state gap rather than a live incident. | E03 |
| medium | libs-foundation | `PrivacyLedger`'s spent-epsilon state is process-memory only and does not survive a restart | backend/crates/libs/qip-confidential/src/budget.rs (Budget, PrivacyLedger, SpentEpsilon — no persistence); backend/crates/libs/qip-confidential/src/lib.rs:1-191 (no qip_core::KeyValueStore usage anywhere in the crate) | The differential-privacy defence is only as strong as its budget accounting. If a Cloud Run instance restarts — which item 1 shows is routine, not exceptional, for every deployed environment today — every cell's spent epsilon resets to zero, and a sequence of releases that would have been refused as over-budget across the restart boundary is instead admitted as if it were the first query ever made. This is contained today only because the query surface has no production caller (see the CellInsights finding above); wiring one without first deciding where the ledger persists would reopen a real disclosure risk. Evidence needed: a decision on which durable store the ledger persists through, consistent with whatever item 1 resolves to for central-plane durability generally. | E12 |
| medium | libs-intelligence | `qip_ai::memory::store::EpisodicMemory` and `qip_agents`'s `BudgetLedger`/`AuditTrail`/`ResearchMemory` are all in-process-only: none has a call site persisting to `qip-storage`, so every learned episode, spend record, audit entry and lesson is lost on process restart | backend/crates/libs/qip-ai/src/memory/store.rs:184-230; backend/crates/libs/qip-agents/src/budget.rs:159-260; backend/crates/libs/qip-agents/src/governance.rs:88-300 | This is the same class of gap the libs-foundation report already flags at the platform level ('central-plane state is ephemeral in every deployed environment... QIP_STORAGE_TARGET=memory everywhere'), so before this is worked as its own item it needs evidence on scope: does closing the central-plane durability gap (that report's item, `docs/reports/tech-debt/libs-foundation.json`) automatically carry these four types along, or do EpisodicMemory/BudgetLedger/AuditTrail/ResearchMemory need their own explicit write-through because nothing in `qip-kernel` currently attempts it for any of them (distinct from the storage-target question, which is about where an attempt goes, not whether one exists)? Filed here rather than folded into the platform-level item because the absence of an attempt is a fact about this crate group specifically. | E08 |
| medium | services-capital-exec | qip-edge-node (the only binary that constructs qip-brokers' venue adapters, and the intended host for qip-execution-engine's OMS at the edge) is deployed to no environment — execution_nodes = {} everywhere | infrastructure/environments/dev/terraform.tfvars:1-27,227; infrastructure/environments/stage/terraform.tfvars:49; infrastructure/environments/test/terraform.tfvars:49 | 89 tests in qip-brokers, 105 in qip-execution-engine, all structurally paper-only, and none of it runs anywhere: `execution_nodes = {}` in every tfvars, confirmed directly. This is not neglect — dev's tfvars header explains precisely what blocks it: ADR 0035 authorises exactly one node (newyork-1, shadow mode, dev only) but no boot image has ever been baked (the bake workflow exists and has never been dispatched), and ADR 0045 proposes a capital allocation figure without setting one ('a number a person chooses and no workflow may pick'). ADR 0099's conflict C8 separately notes billing is disabled on the dev project today, so `deploy.yml` cannot even push an image. Needs: an owner to (1) dispatch the image bake per `docs/operations/deploying-an-edge-cell.md`'s documented sequence, (2) set the capital allocation ADR 0045 proposes, (3) resolve the billing block named in C8 — none of which an agent may do unilaterally. | E14 |
| medium | services-capital-exec | Two independently-defined margin models exist with no shared type and no reconciliation: qip-brokers::ledger::MarginPolicy (per-venue account, Reg-T defaults) and qip-capital::margin::MarginModel (central-plane portfolio margin with a concentration add-on) | backend/crates/services/qip-brokers/src/ledger.rs:37-52; backend/crates/services/qip-capital/src/margin.rs:34-44 | MarginPolicy models what a simulated venue would require of one account (initial/maintenance fractions for long and short, defaulting to Regulation T); MarginModel models the central plane's own portfolio-wide margin (initial/maintenance rate plus a concentration add-on above a threshold). They may be legitimately different concerns — venue-quoted margin vs. platform risk margin — rather than true duplication, but nothing in either crate states that relationship, no shared trait or conversion exists between them, and their defaults (Reg-T vs. an arbitrary concentration threshold) could silently diverge from what the other believes is true collateral. Needs evidence: a product decision on whether AccountLedger's venue-reported margin should feed or be reconciled against qip-capital's MarginModel (per v11.6 §14's 'Counterparty/Venue' and 'Capital' risk domains, which name exactly this reconciliation), or whether the two are deliberately independent and should say so in their module docs. | E07 |
| medium | services-capital-exec | qip-mesh's six data-mesh ports (Lakehouse/Analytical/HotSeries/MasterData/Graph/Evidence) are implemented, tested and never constructed by any composition root; every managed-GCP MeshProvider target unconditionally returns Error::Unavailable | backend/crates/services/qip-mesh/src/adapters.rs:24-351; backend/crates/services/qip-mesh/src/ports.rs:185-322; backend/crates/services/qip-mesh/src/provider.rs:1-17 | Confirmed independently: none of MeshLakehouse/MeshAnalytics/MeshHotSeries/MeshMasterData/MeshGraph/MeshEvidence, nor the six port traits, appear in kernel or app non-test source. This is a designed-ahead-of-the-store-decision port abstraction (the provider explicitly refuses to silently fall back to a local store when configured for a managed target, which is the right failure mode), and it is exactly ADR 0099's conflict C4: BigLake/BigQuery/Bigtable/Spanner/Spanner Graph/GCS-WORM are the blueprint's named backing services, and ADR 0002/0009 permit only serde+serde_json today. FileBacking (the one working implementation) is real file I/O — read_to_string/create_dir_all/write-then-rename — suitable for dev/single-node only, not the multi-region target. Needs an owner storage-record ADR per store, which ADR 0099 says the ledger should get first. | E06 |
| medium | services-capital-exec | Central-plane capital state (allocations, envelopes, ledgers) and the capital-fabric journal persist only through qip-kernel's in-tree, file-backed event log — not the blueprint's authoritative multi-region Spanner Enterprise Plus ledger | backend/crates/services/qip-capital/src/lib.rs:1-40; backend/crates/services/qip-capital-fabric/src/journal.rs:866,944; backend/crates/services/qip-capital/src/ledger/mod.rs:1-40 | None of qip-capital, qip-capital::ledger or qip-capital-fabric perform their own I/O (correctly, per the services-layer rule); qip-kernel's Platform composes and journals their state through its own event log, which is serde_json over the file system, not Spanner. This is ADR 0099's conflict C4 exactly, and `docs/blueprint/assessment/ARCH-b3.json` (ARCH-064, `blocked_by: 'C4'`) independently confirms it at the ledger seam: 'the literal same outcome delivered three times commits once to a Spanner ledger scenario cannot be demonstrated — only the equivalent property against the file-backed log.' ADR 0099 names the ledger as the storage record that should come first among C4's stores. No code action is appropriate before that owner-level storage-record ADR exists; noted here because it is the substrate every other finding in this group's capital/ledger path ultimately rests on. | E05 |
| medium | services-data-world | Two of the seven 'shipped, real' connectors (Kalshi, Alpaca) carry `LicensingPosture::Ambiguous` in the admission catalogue, so `admit()` structurally refuses them in production regardless of the connector code's own maturity | backend/crates/services/qip-data-finder/src/admission.rs:376-397; backend/crates/services/qip-data-finder/src/legal.rs:317-330; backend/crates/services/qip-data-finder/src/legal.rs:349-373 | `catalogue()`'s `kalshi-markets` and `alpaca-daily-bars` entries (admission.rs:378-397) both hold `LicensingPosture::ambiguous("...terms... have not been read against this platform's usages; ADR 0034 names the source as a candidate only")`. `LicensingPosture::legality_for` maps `Ambiguous` to `Legality::unknown` (legal.rs:365-368), and this crate's own structural guarantee is that `admit` never proceeds on `Unknown`. So `KalshiMarketsConnector` and `AlpacaBarsConnector` — counted among qip-market-ingestion's 'seven real, shipped connectors' in its own purpose statement and covered by 62 connector-level tests — can never be admitted into a real run until a human reads each vendor's terms of service and either replaces the posture with `Declared` or confirms `Forbidden`. This is not fixable by writing more Rust; the evidence needed is the legal review ADR 0034 already flags as outstanding. | E08 |
| medium | services-data-world | Only 7 real venues/vendors are shipped, with no listed-options, futures, or broad equities-exchange tape — well short of blueprint §8.1's tick-universe breadth | backend/crates/services/qip-market-ingestion/src/connectors/mod.rs:87-101 | Confirmed by reading connectors/mod.rs: the shipped set is a crypto ticker (Coinbase), two FX/rate sources (Frankfurter, ECB), one US rate (NY Fed), a weather-station feed (NWS), a prediction market (Kalshi — itself blocked, see finding 3), and one equities-bars connector (Alpaca — also blocked). Blueprint §8.1 names trades/quotes/NBBO, L1/L2/L3 depth, options chains with implied-volatility surfaces and Greeks, and futures curves/basis/funding as first-class tick-universe members; none of those exists in this build. This is a scope/roadmap gap rather than a defect in what exists — the connector framework and contract-test harness both generalise cleanly — so the evidence needed is which additional venues are in scope next and under what licensing, which ties directly to findings 3 and 5. | E08 |
| low | apps | `DiscoveryDesk` (source discovery) and `ConnectorArm` (catalogued vendor connectors) are structurally complete and tested but have never run outside this crate's own tests: no environment sets a candidate-catalogue path or a connector-feed variable | backend/crates/apps/qip-deepbrain/src/discovery.rs:66-383 (DiscoveryDesk::new/with_probe/maybe_run); backend/crates/apps/qip-deepbrain/src/connectors.rs:67-374 (ConnectorArm::open/journal_to/shutdown); backend/crates/apps/qip-deepbrain/src/main.rs:475-594 (wiring, gated on QIP_DEEPBRAIN_SOURCE_CANDIDATES_PATH and connector-feed variables; doc at 512-517: 'None in every environment today') | Not dead code with no plan — the types are fresh, the wiring is reachable from `main.rs` the moment either variable is set, and this is precisely the §6 Global Scout & Data Acquisition Fabric loop the blueprint describes ('observe gaps... generate source-discovery queries... register approved source metadata... promote, throttle, quarantine or retire'). What is missing is an operator-authored candidate catalogue and a decision about which connector feeds to license, not a code change — building either ahead of that decision would be speculative work against an unapproved source list, which `qip-data-finder`'s own licensing-gate-before-use rule exists to prevent. | E10 |
| low | delivery | ADR number 0097 has been claimed since 2026-09-21 (four days as of this report) with no body written — the README row describes a real, apparently-unimplemented control (a cell's resting order leg re-measured against its own path-5 check on every pass it waits, withdrawn once the spread it earns stops covering the concession a sweep would extract) but nothing decides or implements it, and the number is deliberately left unlinked | docs/adr/README.md:137 ("Number claimed 2026-09-21; body to follow, and the number is deliberately not a link"); (negative evidence: `ls docs/adr/ \| grep -E '^0097'` returns nothing) | `.claude/rules/architecture/00-boundaries.md` documents a real incident on 2026-09-14 where six parallel lanes each read the ADR register, each found the same highest number, and each claimed it — six collisions that took a day to renumber across 74 files. A number claimed and left unwritten for days is exactly the state that makes a second, unrelated claim of the same number-adjacent risk more likely, and it also means whatever behaviour 0097 was going to decide (the resting-leg withdrawal rule) is currently undecided rather than merely undocumented. What is needed is not code: it is confirmation from whoever claimed 0097 that the decision is still live (and a timeline for the body) or a decision to release the number. | E00 |
| low | edge-cell | `qip_edge::journal::FileMirror` has zero callers anywhere in the workspace — not even in its own crate's tests — yet five blueprint-assessment traceability documents cite it as "the current" journal-shipping mechanism, while the real production composition root implements a different, unmentioned type | backend/crates/edge/qip-edge/src/journal.rs:617-643 (struct FileMirror, impl Mirror for FileMirror); backend/crates/edge/qip-edge/src/lib.rs:78 (pub use journal::{..., FileMirror, ...}); backend/crates/apps/qip-edge-node/src/mirror.rs:1-70 (StoreMirror — the actual production Mirror, built against qip_storage::kv::KeyValueStore, never mentioned in the assessment docs below) | `grep -rn FileMirror backend/crates` matches only the type's own definition and its `pub use` re-export — no construction site exists anywhere, including `#[cfg(test)]`. Meanwhile `docs/blueprint/assessment/*.json` — generated by `qip blueprint render`, 'do not edit by hand' — repeatedly names `journal::{...,FileMirror}` as the crate's current durable-shipping mechanism for REFLEX-b1, FABRIC-b1, MESH-b1, MESH-b2 and RES-b1, and never mentions `qip-edge-node::mirror::StoreMirror`, the type that actually ships a deployed cell's journal into the platform's configurable `KeyValueStore` (the same durability seam item 1 of `libs-foundation.json` covers). This is not simple dead code with no plan behind it — the blueprint's own generated traceability register is the plan, and it currently points at the wrong implementation. Evidence needed before acting: whether the render tool should be taught to look at `qip-edge-node`'s composition root (retargeting the citation, after which `FileMirror` becomes deletable with nothing left depending on it, not even documentation) or whether `FileMirror` is meant to gain a real caller (e.g. a `qip-cli` demo path with no store dependency). Left as is, an auditor reading the traceability matrix — the document this platform exists to make trustworthy — is told the wrong type ships a cell's decision record. | E00 |
| low | edge-libs | qip-routing::health::HealthTracker and consolidate::Consolidator (venue health scoring; cross-strategy venue consolidation) have zero non-test call sites anywhere in backend/crates | backend/crates/edge/qip-routing/src/health.rs; backend/crates/edge/qip-routing/src/consolidate.rs | Both are fully tested (folded into the crate's 130-test count) but nothing constructs either outside qip-routing's own tests. v11.6 §16 names 'intent netting' and implicit venue-health awareness as reflex-node responsibilities; whether these two are the intended implementation awaiting a caller in qip-edge-node, or an earlier design already superseded by logic inside qip-edge's own Cell, is not decided by the code as it stands. | E04 |
| low | edge-libs | qip-orderbook::auction::AuctionState is wired into the public shape of VenueState and BookSnapshot (an Option<AuctionState> field on each) but is never constructed, populated or read by anything outside qip-orderbook's own crate | backend/crates/edge/qip-orderbook/src/venue.rs:23,51,141,214; backend/crates/edge/qip-orderbook/src/snapshot.rs:13,111 | Confirmed by grep: no reference to AuctionState or auction:: anywhere in qip-edge or qip-edge-node non-test source. The field exists in every VenueState and every BookSnapshot taken, always None in practice — the same shape as PolicyPayload's unshipped trained_models/compiled_plan slots flagged in the libs-foundation report: a live type with no live producer and no live consumer, carried through a structure that is itself production-reached. | E04 |
| low | frontend | @algorik/feature-flags — seven typed, always-false-by-default flags with zero call sites; every capability it would gate is currently either always-on or entirely absent rather than flag-controlled, but two of the seven (passkeys, googleSignIn) name work the programme already tracks as near-term | frontend/packages/feature-flags/src/index.ts:1-69; docs/blueprint/assessment/API.json (API-012 work_item E15-customer-mfa-passkeys, API-011 work_item E15-identity-platform-wiring) | grep '@algorik/feature-flags' across frontend/portal/src and frontend/landing returns 0 hits. Unlike analytics/shared-types above, this package is not obviously orphaned: passkeys and googleSignIn are exactly the capabilities ADR 0038 (proposed) and the blueprint assessment's API-012/API-015 items name as pending identity-plane work, and the identity-platform wiring gap recorded elsewhere in this report is the precondition for most of the other five flags to mean anything (enterpriseSso, kycOnboarding, developerPortal). Needs evidence: confirm with the owner whether ADR 0038 and the identity-platform wiring item are scheduled next; if so this package is pre-built scaffolding worth keeping, if not it should be retired alongside analytics and shared-types. | E15 |
| low | frontend | useTreasury.ts's INFLOW_ROUTES names two operator routes the console deliberately never adds to the gateway's write allowlist, because the backend refuses both today for want of a per-person credential this platform cannot yet issue — honestly rendered, but blocked on an ADR still in Proposed status | frontend/portal/src/lib/hooks/useTreasury.ts:194-227; docs/adr/0076-a-per-person-operator-identity-is-the-consoles-asserted-subject-and-a-two-person-rule-needs-a-credential-this-platform-cannot-issue.md:1-15 | The console names two operator routes (declare/cancel an expected inflow, ADR 0085 §4) it deliberately keeps out of REST's declaresWrite allowlist, because the backend refuses both in fact for every credential the console can hold: the routes date the operator by Principal::authentication_instant, which a standing bearer token cannot carry (ADR 0065, ADR 0075), and only a per-person credential (ADR 0076) can satisfy the check. The page already renders this as the contract's own stated refusal rather than a form nobody could submit, so there is no live risk today. What is open is the precondition: ADR 0076's own header reads 'Status: Proposed,' not Accepted, so the frontend change this would unblock (turning INFLOW_ROUTES from text into a working form, and adding the two routes to REST) has no accepted design to build against. Needs evidence: confirm with the owner whether ADR 0076 is being taken to Accepted, and on what schedule, before scoping this as frontend work. | E15 |
| low | frontend | frontend/landing ships a real runtime dependency (swiper) that ADR 0015 — the record that admitted the template's one other runtime dependency, chart.js, by name — never names or reviews | frontend/landing/package.json:9-19; frontend/landing/components/sections/home1/Banner.js:1-15; frontend/landing/app/layout.js:4-6 | ADR 0015's 'Runtime dependency admitted: chart.js' paragraph states the admission was 'recorded here per the frontend dependency rule' because the template's charts pull it in. swiper (^12.1.2), the template's home-page carousel library, is a real dependency of the same app (imported in Banner.js and layout.js, with its own CSS) but appears in no equivalent committed review. 00-enterprise-governance.md requires every package.json addition be reviewed with its transitive tree as part of the change, the same as Cargo.toml. Needs evidence: confirm with the owner whether swiper was a deliberate, reviewed admission that should be named in ADR 0015 the way chart.js is, or whether it should be replaced with a dependency-free carousel before landing is ever deployed. | E00 |
| low | infrastructure | Two independent, fully-built front-door systems exist for administrative/console access — `modules/iap-edge` (custom-domain load balancer + Cloud Armor + managed certificate) and `modules/iap-run` (IAP directly on the Cloud Run service) — and the module chosen by ADR 0094 is blocked by a hard, zero-value GCP quota that this repository cannot self-remediate, while its DNS dependency (`modules/dns-zone`) is separately superseded and also instantiates nothing anywhere | infrastructure/terraform/modules/iap-edge/main.tf:85-284; infrastructure/environments/dev/terraform.tfvars:583-613; infrastructure/terraform/modules/dns-zone/main.tf:41-64 | `infra.yml` run 71 failed applying `iap-edge`'s Cloud Armor policy with `Quota 'SECURITY_POLICY_RULES' exceeded. Limit: 0.0 globally` — not exceeded-by-one, a project with no allowance at all, and raising it is a quota request Google may refuse. ADR 0095 responded by narrowing `iap-edge` to a secondary option and making `iap-run` (per-service IAP, no load balancer, no DNS) the primary door in every environment; `dns-zone`, whose only committed consumer was `iap-edge`'s certificate, now has `dns_zone_domain = ""` everywhere and creates nothing. Both modules remain fully built and heavily tested (14 and 17 `.tftest.hcl` runs respectively) for a role ADR 0095 already assigned elsewhere. The evidence needed is an owner decision: either request the Cloud Armor quota and keep `iap-edge` as the documented secondary path, or write the record that formally retires it and `dns-zone` rather than leaving two live-looking, mutually-exclusive-by-construction systems in the tree indefinitely. | E14 |
| low | infrastructure | `modules/ai` provisions a KMS-encrypted bucket, a Vertex AI metadata store and a serving endpoint, fully built and off by default, while `qip_training`'s Vertex port has no client, no credential and no egress path in the Rust workspace | infrastructure/terraform/modules/ai/main.tf:1-25,29,52,95,113; infrastructure/environments/dev/terraform.tfvars:258 | `var.enable_vertex_ai` is `false` everywhere, so the module creates zero resources anywhere it has been applied — but the module's own output (`reachable_by_this_build`) states the gap explicitly rather than implying a working capability: enabling the variable today would produce a healthy, empty, billable service nothing in this repository can submit a job to. This is the shadow-scope form v11.6 asks the platform to build first ('Research/simulation may be broader than executable scope', §25), correctly held short of the part that needs a research-runtime decision (ADR 0099 C5, Python/JAX/PyTorch vs. ADR 0001's Rust-everywhere) before it goes further. | E10 |
| low | infrastructure | `terraform-catalogue` wires exactly 3 of the blueprint's roughly 70 named workloads (`api`, `fastbrain`, `deepbrain`) into Cloud Run — the whole per-brain federation (Asset, Capital, Risk, Evidence, World-model, Reflex, Fabric brokers, and the rest) has no deployable identity anywhere in the tree | infrastructure/terraform/catalogue.tf:234-570 | v11.6/GCP v2.1 describe a federation of separate warm services per brain (GCP-002: 'Stateful and long-running warm services run on regional GKE Standard'); this repository composes those same brains as library crates inside five binaries (ADR 0091), currently deployed as three Cloud Run services. ADR 0099's conflict register (C7) already declines to resolve this by fiat — 'v2.1 describes placement, not a process count, and a brain can be a crate in a binary until independent scaling is *measured* to require otherwise' — taken together with C3 (GKE Standard vs. Cloud Run). The gap is real and worth tracking precisely because the honest completion percentage against the blueprint's catalogue depends on it, but splitting further ahead of a measured scaling need would be the wrong direction per the platform's own principle 4 ('make it structural... beats one a runtime check holds'). | E14 |
| low | infrastructure | Four `removed` blocks in the Terraform root release `google_cloud_run_v2_service`/`_iam_member` resources for `cloud_run` and `openobserve` from state without destroying them (ADR 0036 decision 5), and their own comment says they may be deleted once every environment that ever held those resources has applied them — a condition that may already be true for `dev`, the only environment that ever did | infrastructure/terraform/main.tf:1004-1034 | The blocks are a no-op for any environment whose state never held the resource (test/stage/prod, which have never been applied at all), and dev's own history in `infrastructure/CLAUDE.md` shows a re-apply on 2026-09-20 reporting `0 added, 1 changed, 0 destroyed`, which is consistent with dev's state having already migrated past the addresses these blocks name. This is genuinely temporary migration scaffolding with a self-declared removal trigger; the evidence needed before deleting it is a `terraform state list` (or an equivalent plan) against dev's real state confirming `module.cloud_run.google_cloud_run_v2_service.workload` and the three sibling addresses are already absent — a cheap check this report cannot perform without touching the environment, and one worth doing before the next time this file is edited near these lines. | E14 |
| low | kernel-cycle | A cycle refused for being dated before the platform's own reasoned_through watermark is not written to the event log at all -- by explicit design -- leaving only a metric counter (qip_stage_runs_total{ran="false"}) as evidence the refusal happened | backend/crates/runtime/qip-kernel/src/platform.rs:9010-9012 (run_cycle delegates to refuse_cycle_before instead of running); backend/crates/runtime/qip-kernel/src/platform.rs:9174-9209 (refuse_cycle_before: 'It is deliberately not journalled: the event log is the record of what the platform did, and the one thing worse than a backwards cycle is a backwards record of one') | The reasoning in the comment is coherent (a refused cycle changed no state, so journalling one would misrepresent what the log means), but 'Every decision reproducible from the log alone' is listed as a mandatory architecture concern in .claude/rules/10-product-direction.md, and a caller handed a backwards clock is a decision this platform made (refuse rather than run) that leaves no record a replay can see -- only a metric an operator has to already be watching. The trade-off is defended in a code comment, not in an ADR. Evidence needed before changing anything: an owner ruling on whether a metric-only trail satisfies the auditability principle for a refusal, or whether a minimal non-cycle record (distinct from a CycleReport) belongs in the log for this case. | E12 |
| low | kernel-cycle | learn_from_cells and learn_from -- the two entry points through which a cell's resolved outcomes and a thesis's resolved claims reach belief calibration -- have zero direct callers anywhere in backend/crates/apps/*/src; both are reached only transitively, through stage_learn's own private wrappers (review_strategies, calibrate_resolved) | backend/crates/runtime/qip-kernel/src/platform.rs:8109 (learn_from_cells); backend/crates/runtime/qip-kernel/src/platform.rs:8423 (learn_from); backend/crates/runtime/qip-kernel/src/platform.rs:14140,14287 (stage_learn -> review_strategies -> learn_from_cells) | This is 'reached from production' in the sense that matters -- stage_learn calls both unconditionally every cycle -- so neither function is dead code and neither should be touched on the strength of this finding alone. What is missing is independent verification: because both are private-by-convention entry points called only from within the same file's own stage_learn, no app-level integration test (as opposed to qip-kernel's own tests/kernel.rs and tests/learning.rs) exercises either function as the boundary of a real composition root's behaviour. Evidence needed before treating this as settled: whether the existing kernel-level test suite's coverage of these two paths is accepted as sufficient, or whether an app-level (qip-api / qip-cli) integration test is expected per the crate-contract tier of .claude/rules/architecture/01-testing-strategy.md. | E00 |
| low | kernel-planes | adversary_review.rs deliberately answers only 1 of blueprint §15.2's 5 questions and builds none of its 4 named responses — an honest, well-reasoned scope reduction, but recorded only in a module comment, not in an ADR the way family_review's identically-shaped reduction is (ADR 0064) | backend/crates/runtime/qip-kernel/src/adversary_review.rs:1-50 | The module doc is explicit and honest: '§15.2... asks five questions and names four responses. This module answers one of the five and produces none of the four... the other four questions need data nothing in this platform ingests, and three of the four responses need a seam this platform does not have.' That is a real, reasoned architectural scope decision — but `grep -rli adversary docs/adr/*.md` finds no ADR naming it, unlike `family_review.rs`'s structurally identical reduction (measures but never revises weights), which cites ADR 0064 by number. The project's own architecture rule is direct on this point: 'If you find yourself explaining an architectural choice in a PR comment, it needed an ADR' (.claude/rules/architecture/00-boundaries.md). The evidence needed is a decision, recorded as an ADR: either that the remaining four §15.2 questions are out of scope for the v11.6 programme (as ADR 0064 settled for family-weight revision), or a plan naming when the missing data/seams will exist and the questions will be answered. | E00 |
| low | libs-intelligence | `qip_ai::language::RemoteModel` — a generic hosted-LanguageModel adapter whose `is_available()` is unconditionally `false` and which has zero callers outside its own crate's tests — duplicates, in effect, `qip_reasoning_engine::providers::huggingface::HuggingFaceModel` (ADR 0037), the adapter actually constructed in qip-deepbrain and reached from production | backend/crates/libs/qip-ai/src/language.rs:454-535; backend/crates/services/qip-reasoning-engine/src/providers/huggingface.rs:250,487; backend/crates/apps/qip-deepbrain/src/language.rs:41,182-224 | RemoteModel predates ADR 0037's decision; ADR 0037's Context section says exactly this ('RemoteModel, which reports itself unavailable by construction because this build had no credential path, no TLS client and no egress rule'), but does not say what should happen to RemoteModel once HuggingFaceModel existed and was wired in. HuggingFaceModel implements `LanguageModel` directly rather than through RemoteModel's `RemoteModelConfig`/credential-resolution shape, so the platform now carries two 'hosted model' constructs with no relationship. Evidence needed before touching either: an owner/architecture-review decision on whether RemoteModel is retired now that ADR 0037 settled on a specific provider, or kept as the generic multi-provider port a second vendor (e.g. Vertex) would implement against, in which case HuggingFaceModel should be migrated to implement it. Neither reading is free to assume. | E10 |
| low | libs-intelligence | `qip-investment-agents` (an `agents/`-tier crate the component catalogue classifies as `"kind": "lib"`) carries a `[dev-dependencies]` edge on `qip-reasoning-engine`, a `crates/services/` crate, to build its own integration tests — and `.claude/rules/architecture/00-boundaries.md`'s inward-dependency diagram names `libs`, `services`, `runtime`, `apps` and `edge` but never `crates/agents/`, so which side of 'a lib may not depend on a service' this crate sits on is undefined | backend/crates/agents/qip-investment-agents/Cargo.toml:16-17; backend/crates/agents/qip-investment-agents/tests/organisation.rs:1559-1562,1686-1689; .claude/rules/architecture/00-boundaries.md | The edge never reaches a compiled production binary (Cargo dev-dependencies are excluded from a `[dependencies]` build graph), so there is no runtime coupling risk today. What is missing is a stated rule: does the boundaries diagram's silence on `crates/agents/` mean it is a fifth first-class tier exempt from 'inward only', or an omission that should place it beside `libs/`? Either answer is cheap to write down and neither should be assumed by an agent editing this crate; a `services/`-tier production dependency landing here later, reasoned by analogy from this dev-only precedent, would be the kind of boundary erosion the rule exists to prevent. | E01 |
| low | services-capital-exec | The crate name 'qip-capital-fabric' and its FabricJournal/FabricCommand/FabricState types collide in name with the blueprint's platform-wide native Rust Event & Control Fabric (fabricd, fabric-schema, fabric-mirror, ...), which is a distinct, not-yet-built subsystem | backend/crates/services/qip-capital-fabric/src/lib.rs:1-40; backend/crates/services/qip-capital-fabric/src/journal.rs:405-427,866,944 | qip-capital-fabric's 'Fabric' is a bounded-context capital pre-positioning and transfer-control journal (forecast/plan/transfer/gate), an in-tree, file-backed, serde_json event log. GCP v2.1 §7 names a completely different, platform-wide subsystem also called 'the Fabric' (fabricd broker on Tokio/QUIC/mTLS, fabric-schema on protobuf/prost, a Raft metadata quorum) — this is ADR 0099's conflict C2, not yet resolved. No code defect exists today (the two never share a wire format because the real Fabric does not exist yet), but the name collision is a documentation/clarity hazard for whoever builds C2's Fabric next to a crate already called 'capital-fabric' with its own FabricCommand/FabricJournal/FabricState vocabulary. Needs an owner decision, folded into C2's resolution: rename this crate's internal vocabulary, or reserve 'Fabric' for the platform subsystem and pick another name here. | E03 |
| low | services-capital-exec | qip-lifecycle's corridor::emit (the function that actually derives a CorridorPolicy/CorridorRuling from strategy standings) has zero production callers; only the plain data record CorridorSubject is referenced from the kernel | backend/crates/services/qip-lifecycle/src/corridor.rs:1-30,303 | Confirmed independently: `grep -rn 'corridor::emit\\|CorridorPolicy\\|CorridorRuling' backend/crates/runtime/qip-kernel/src backend/crates/apps/*/src` outside tests returns nothing; `central/factory.rs:28` references only `CorridorSubject`, the input record. The lifecycle ladder around it (gates, ledger, demotion, horizon reconciliation) is deeply wired into qip-deepbrain and qip-kernel with 77 tests, so this is not an abandoned crate — it is one function whose output (a computed policy/ruling) nothing downstream consumes. Needs evidence: was corridor::emit meant to feed venue-ladder or strategy-promotion decisions and the wiring was never finished, or has its purpose been superseded by something else in the ladder (e.g. VenueLadder/VenuePromotionPolicy)? The answer determines whether this is B (wire it in) or D (remove a function nothing was ever going to call). | E07 |
| low | services-data-world | `qip_data_finder::ingestion::plan_for` / `descriptor_for` — the function that would turn a registered source into an operational ingestion plan — is called only from the crate's own lifecycle test, never from any composition root | backend/crates/services/qip-data-finder/src/ingestion.rs:52-95; backend/crates/services/qip-data-finder/src/lib.rs:116; backend/crates/services/qip-data-finder/tests/lifecycle.rs:184-195 | `rg -n 'plan_for\(\|descriptor_for\(' backend/crates` shows the two call sites are `ingestion.rs` itself (line 80, `descriptor_for` calling `plan_for`) and `tests/lifecycle.rs:184,195`; no non-test line in `crates/apps` or `crates/runtime` calls either. A `RegisteredSource` today goes admission → registration → nothing: `qip-api`/`qip-fastbrain` resolve a feed through `ConnectorFeed::open`/`shipped_class` instead, bypassing `IngestionPlan` entirely. Either this is the missing link the E08 adapter-lifecycle work is supposed to wire in, or it has been superseded by the shipped-manifest path and is stale scaffolding; the evidence needed is which the plan intends, not a code guess. | E08 |
| low | services-intelligence | qip-simulation-engine::montecarlo.rs (MonteCarlo/Distribution/Generator -- bootstrap, parametric and antithetic path generation with standard-error reporting) has no caller anywhere in the workspace outside its own crate's tests, and is not even used internally by the crate's other modules (scenario.rs's StressTester is a separate, causal/adversarial mechanism) | backend/crates/services/qip-simulation-engine/src/montecarlo.rs:1-14,118; backend/crates/services/qip-simulation-engine/src/lib.rs:52-64 | `grep -rln "qip_simulation_engine::montecarlo" backend --include='*.rs'` matches only lib.rs and the crate's own tests, and `grep -rln "montecarlo::" src` inside the crate outside lib.rs also returns nothing, confirmed -- unlike venue.rs/conditions.rs (used internally by market.rs/execution.rs), this module is unused even by its own siblings. It is a well-built, general-purpose utility with no evident duplicate elsewhere in the workspace (no other `MonteCarlo` struct exists), so it is not a candidate for deletion, but no named consumer or blueprint requirement calls for path-level Monte Carlo confidence intervals specifically -- evidence needed: which stage (risk sizing? evaluation-brain confidence reporting?) is meant to consume it, before deciding whether to wire it in or park it as intentionally-ahead-of-need. | E07 |

## A — retain (19)

| Risk | Group | Item | Files | Why | Epic |
|---|---|---|---|---|---|
| medium | apps | No Cloud Run service exists today for any of the three warm composition roots (`qip-api`, `qip-fastbrain`, `qip-deepbrain`); every one of their apps.json records is 'reached_from_production: partial' for the same single reason | docs/adr/0093-the-dev-control-planes-cluster-is-suspended-while-no-argo-cd-release-passes-the-scan-gate.md; infrastructure/gitops/envs/dev/{api,fastbrain,deepbrain}.yaml; backend/crates/apps/qip-api/src/main.rs:1-235 (composition root serving nothing today) | ADR 0093 is explicit that this 'suspends a deployment, not a decision': ADR 0036 stands unamended, every GitOps manifest stays in the tree, and 'the reversal is one word in one tfvars line', done on the owner's 2026-09-20 instruction to minimise cost. There is nothing for a tech-debt programme to migrate or refactor here — the code these three composition roots run is production-grade (262+98+169 tests respectively) and was observed serving in dev on 2026-09-04 before teardown. This is recorded because every apps-group finding that says 'X is real code but currently unreached' traces to this one root cause, and a reader should not have to rediscover that three times. | E14 |
| medium | apps | `qip-api`'s OpenObserve drain thread has never had anything answer on the other end: the managed-Prometheus/OpenObserve collector sidecar for Cloud Run has no working image (the only published version fails the platform's own Trivy gate on an unfixed CRITICAL CVE), and no environment has ever run this process to begin with | backend/crates/apps/qip-api/src/openobserve.rs:144-197,516 (OpenObserveConfig::from_env/read/parse; spawn -> DrainHandle); backend/crates/apps/qip-api/src/main.rs:339-347 (spawned conditionally on OPENOBSERVE_URL); .claude/rules/domains/observability.md (Cloud Run collector sidecar blocked on CVE-2026-56854, unfixed upstream) | The emitting half is real and 16-test-covered; what is missing is proof of ingestion, and that half is blocked upstream — no change in this repository clears an unfixed CVE in a vendor image, and the observability domain rule is explicit that the blocker should not be cleared with a scanner exception. This is a distinct mechanism from the kernel's own /metrics Prometheus surface (already covered under the libs/foundation report): this is qip-api's own background POST-based drain thread, gated on a separate variable, with its own separate blocker. Recorded so the two observability gaps are not conflated as one. | E12 |
| medium | infrastructure | Binary Authorization's signing chain is only as strong as the pipeline's own deploy identity: whoever can run a `deploy.yml` step can produce a valid attestation, with no two-person or HSM-gated control in front of it | infrastructure/terraform/modules/binaryauthorization/main.tf:54-199; infrastructure/terraform/modules/binaryauthorization/OUT-OF-BAND.md | This is disclosed by the module's own honest-limits document rather than hidden, and `OUT-OF-BAND.md` states plainly that a stronger arrangement is out of reach of this repository alone (it would require an organisational control — a second approver, an HSM-gated key ceremony — outside what Terraform or GitHub Actions alone can enforce). The chain itself (KMS signing key, Container Analysis note, attestor, `REQUIRE_ATTESTATION` policy, no repository-variable identity) is otherwise hardened and applied; nothing here should be built around or silently accepted further without the same disclosure this file already gives it. | E13 |
| medium | infrastructure | OpenObserve is genuinely anonymous on the public internet by owner instruction (ADR 0030): `module.openobserve`'s derived posture sets `INGRESS_TRAFFIC_ALL` with an `allUsers` invoker, and nothing structurally prevents a future deployment from making the store non-empty while that posture stands | infrastructure/terraform/modules/openobserve/main.tf; infrastructure/gitops/envs/dev/openobserve.yaml | The module is deliberately resource-free (a pure decision module) and its own header states the exposure is acceptable only while the store holds nothing, naming the exact trigger to re-argue it — any deployment that sets `QIP_OPENOBSERVE_URL`, meaning a platform binary is actually shipping telemetry into it. Kept as-is per ADR 0030, with the re-argue trigger flagged here so this item is not read as settled forever: the moment OpenObserve stops being empty, this posture needs the same decision revisited rather than inherited by default. | E12 |
| medium | services-capital-exec | The seven-veto TransferGate (CorridorAuthority/Caps/MinimumInterval/StatedPurpose/SourceBalance/VelocityAndAnomaly/KillSwitch) is fully implemented and hardened but has no production emitter — deliberately, and documented as such | backend/crates/services/qip-capital-fabric/src/gate.rs:540-560; backend/crates/services/qip-capital-fabric/src/gate.rs:727,741-753; backend/crates/services/qip-capital-fabric/src/journal.rs:405-427 | `grep -rn "GateCommand {" backend/crates/apps backend/crates/runtime` outside tests returns nothing — confirmed independently. But `pre_positioning.rs:22-31` states the reason in the kernel's own module doc: a `GateCommand` carries the three enforcement-point attestations of custody class, corridor signature and destination allowlist, and those 'are facts a human establishes out of band... A machine that manufactured them from its own plan would be attesting to itself, which is the control measuring itself.' Building an automatic emitter would be the defect, not the fix. `platform.rs:7583` shows the kernel already validates a Gate command's funding field when one arrives, so the seam is wired correctly and simply unused — matching ADR 0021's paper boundary and the blueprint's own 'begin shadow-only' directive (v11.6 §30, Phase 10) for anything requiring an external, human-attested act. Independently corroborated by `docs/blueprint/assessment/RISK.json` (RISK-001). Retained as-is; do not build an emitter without an owner decision that resolves ADR 0099's conflict C1 (autonomous treasury transfer is LIVE_CAPITAL/EXTERNAL_ACTION and structurally blocked). | E07 |
| low | apps | `qip-deepbrain`'s replay path (`QIP_DEEPBRAIN_REPLAY_PATH`) has no reference-data source of its own, so every backtest run against it refuses with 'no fill' rather than register a flat curve as evidence | backend/crates/apps/qip-deepbrain/src/main.rs:417-426 (doc comment); backend/crates/apps/qip-deepbrain/src/campaign.rs:269-308 (replay admission routes through the shipped connector's own licensing gate under its named source) | This is the fail-closed direction working as intended — the alternative (registering a flat curve when no reference data exists) would let an evidence-poor backtest read as a real result, which is exactly the outcome `.claude/rules/architecture/01-testing-strategy.md`'s 'asserts its own premise first' principle and ADR 0006's classical-baseline rule both exist to prevent. Recorded here rather than left silent because a reader who only sees 'the replay path refuses' without this context might read it as a bug to fix by relaxing the refusal, which would be the wrong direction to move it. | E05 |
| low | edge-cell | `GATE_SETTLEMENT` refusal counting is correctly wired through the standard `Cell::refuse` seam and is explicitly asserted by a test — the current-state architecture doc's own hedge ("verify against the current CellMetrics wiring before relying on the count") does not hold up on inspection | backend/crates/edge/qip-edge/src/cell.rs:5760,5782,5802 (admit_settlement calls self.refuse(report, GATE_SETTLEMENT, ...)); backend/crates/edge/qip-edge/src/cell.rs:7486-7503 (fn refuse — pushes to WorkReport.refusals AND calls self.metrics.refusal(gate) unconditionally); backend/crates/edge/qip-edge/tests/settlement.rs:349-359 (asserts metrics.snapshot().counter(EDGE_REFUSALS, {gate: GATE_SETTLEMENT}) == 1, named "the refusal was not counted under its own gate") | The current-state architecture doc flags an open question rather than a defect, and this entry closes it with evidence rather than leaving it standing as unresolved debt: every `GATE_SETTLEMENT` call site routes through the crate's single generic `refuse()` function, which both pushes onto the pass's `WorkReport.refusals` and unconditionally calls `self.metrics.refusal(gate)` — there is no separate, uncounted refusal path for this gate the way there genuinely is for `GATE_LIVE_VENUE` and `GATE_QUOTE_BUDGET`'s `Cell::send`-time recordings (which have no `WorkReport` to push onto). A dedicated test even names the property directly. No code change is warranted; the current-state doc's hedge should be resolved rather than re-verified by the next reader who runs into it. | E12 |
| low | edge-cell | `qip-mesh::delta::WireDelta` is a hand-maintained, field-by-field mirror of `qip_edge::mesh::CellStateDelta`, required because the dependency direction forbids a service from naming an edge crate — the module doc already corrects a prior false claim about how the duplication is guarded, and a real cross-crate round-trip test exists in `qip-api` | backend/crates/services/qip-mesh/src/delta.rs:1-33 (module doc: "Two declarations of one wire shape, on purpose"); backend/crates/services/qip-mesh/src/delta.rs:136-143 (self-correction: "It previously said it was held equal ... by the round-trip tests; nothing compared the two, and this type is private, so nothing could"); backend/crates/edge/qip-edge/src/mesh.rs:201 (struct CellStateDelta, the declaration this mirrors) | Two independent struct declarations of one wire shape is exactly the pattern this task is asked to flag, and on inspection it is the correct answer to a real constraint rather than drift: `qip-mesh` is a service and `qip-edge` is an edge crate, and the dependency-direction rule in `.claude/rules/architecture/00-boundaries.md` forbids a service from depending on an edge crate, so `qip-mesh` cannot import `CellStateDelta` directly. The schema-version number is deliberately NOT duplicated — both sides read `qip_contracts::wire::CELL_DELTA_SCHEMA_VERSION` — and the module doc is unusually honest about what is and is not proven: a prior version of this same file claimed the two struct shapes were "held equal by the round-trip tests", which was false (the tests only proved `WireDelta` round-trips through itself), and that claim has since been corrected in place. What actually closes the gap is `qip-api/tests/mesh.rs`, the one composition root allowed to name both types, building a genuine `CellStateDelta` and decoding it through `qip-mesh`'s function — confirmed present. No change needed; worth recording as intentional so a future reader does not "fix" the duplication by weakening the dependency-direction rule. | E02 |
| low | frontend | lib/sim (seeded deterministic simulation) has zero current page consumers, but this is the correct state of the approved mechanism, not abandoned code | frontend/portal/src/lib/sim/index.ts:1-9; frontend/portal/src/components/data/Simulated.tsx; frontend/portal/CLAUDE.md | lib/sim is built exactly to portal/CLAUDE.md's own rule: 'simulated data is deterministic and labelled, or it does not exist... under SimulatedBanner.' Every current (portal) page reads real qip-api data instead (grep 'SimulatedBanner\|SimChip' under src/app: 0 hits) — meaning there is currently no page anywhere showing simulated data as if it were real, which is the exact failure this rule exists to prevent. Zero callers today reflects that nothing currently needs it, not that it is broken or orphaned; it remains the approved fallback for the day a page's subsystem has no platform surface yet. Retain as-is. | E00 |
| low | frontend | endpoints.ts's NOT_YET_SERVED table — eleven backend routes the platform does not yet serve, each named, cited to the exact routes.rs behaviour that is missing, and rendered per-page as a distinguishable named absence rather than an empty panel or synthetic data | frontend/portal/src/lib/api/endpoints.ts:153-300; frontend/portal/src/components/data/States.tsx; frontend/portal/src/lib/api/client.ts:20-56 | Positions, cash, data-sources/health, compliance, compliance/attestations, kill-switch/history, topology, account/me, ledger/users/{user}, and the three explanation routes are each named with the reason no route answers them, and rendered through one component per ApiOutcome kind (MissingEndpointBlock, DeniedBlock, UnreachableBlock, ...) rather than collapsing to a generic error or a fabricated value — directly implementing client.ts's own stated design intent. One of the eleven, topology, is assembled client-side from four separate reads (/system, /mesh, /regions, /agents) rather than one backend document — a mild form of incorrect coupling (a page doing aggregation a backend route should do) — but it is disclosed and deliberate, not accidental: endpoints.ts's own comment states 'the platform serves no single topology document.' This pattern is the standard the rest of this report's findings are measured against; the gaps it names are backend work, not frontend debt. | E14 |
| low | frontend | frontend/mobile holds one README and no code, by design — the mobile channel is the portal installed as a PWA, a standing decision, not an unstarted feature | frontend/mobile/README.md:1-31; frontend/portal/src/app/manifest.ts:16-49; frontend/portal/tests/mobile.spec.ts | find frontend/mobile -type f returns only README.md; the actual mobile behaviour (manifest.ts, public/sw.js, AppShell's off-canvas nav, InstallApp.tsx) is implemented and tested inside frontend/portal (tests/mobile.spec.ts, run against the Pixel 7 viewport). mobile-as-PWA is a decision this repository already made and keeps deliberately: ADR 0015's supersession note states 'its package structure, mobile-as-PWA and no-second-router decisions stand' about ADR 0014. The directory's own README states it becomes non-empty only 'when the desk decides to list in a store' — a placeholder with a plan and a citation, not a gap. | E00 |
| low | infrastructure | Two of the egress bootstrap's seven listeners (`frankfurter`, `huggingface`) are fully wired — named clusters, SAN-pinned upstream certificates, path/method-restricted routes — but dark in every environment: no deployed configuration sets `QIP_CONNECTOR_BASE_URL` or `QIP_LANGUAGE_MODEL_BASE_URL`/mounts `QIP_HF_TOKEN` to route real traffic through them | infrastructure/egress/envoy.yaml:451-480; infrastructure/egress/envoy.yaml:518-537 | This is scaffolding built ahead of its caller rather than a defect: the routes exist and are tested (`the_hugging_face_host_is_one_value_in_the_adapter_the_bootstrap_and_the_allowlist`, `egress.rs`), matching this platform's principle of never having a vendor call without a timeout or a route the bootstrap doesn't declare. The huggingface listener is explicitly the ADR 0037 model-vendor path and stays dark until a language-model configuration is actually set; nothing here is presented as a working capability while empty. Listed as a placeholder rather than debt, and worth revisiting once the corresponding Rust-side connector/config lands. | E09 |
| low | libs-foundation | qip-storage and qip-transport perform real I/O (file fsync, TCP sockets, HTTP calls) from inside crates/libs/, which reads as a flat violation of the architecture rule 'No lib performs I/O... a lib that opens a socket is a service in the wrong directory' unless ADR 0011/0012 are already known | .claude/rules/architecture/00-boundaries.md (states the rule with no exception named); backend/crates/libs/qip-storage/src/fsio.rs (flushes every write to the storage device); backend/crates/libs/qip-transport/src/http.rs (opens raw std::net::TcpStream connections) | The code is correct and the exception is deliberate and doubly ADR-sanctioned (ADR 0011 designed these two crates as the in-tree substitutes for Spanner and Pub/Sub specifically as libraries; ADR 0012 explicitly scopes the one admitted TLS dependency to 'the I/O edge... qip-transport'). No code change is warranted. But the architecture rule a reader (or an agent) consults first states the boundary with no exception named, which is exactly the shape of drift .claude/rules/architecture/00-boundaries.md itself warns about elsewhere in its own text regarding stale citations — the next reader who has not read ADR 0011/0012 will flag this as a fresh violation. Naming the exception in the rule file is a documentation fix, not a code change. | E00 |
| low | libs-intelligence | `qip_quantum::statevector::MAX_QUBITS = 24` bounds the in-tree simulator and is enforced by refusal (`StateVector` construction errors above the limit) rather than by clamping or silent truncation | backend/crates/libs/qip-quantum/src/statevector.rs:10,23,112-114 | This is the target shape already: a documented, structural ceiling (16 GiB of amplitudes at 24 qubits) that fails closed with a message naming exactly why, matching CLAUDE.md principle 2 ('Refuse rather than guess... a value silently corrected is a caller bug that survives'). Included because the task asks scaling limits be found and judged, and this one is already correct — no work item follows from it. | E10 |
| low | services-capital-exec | custody.rs / destination.rs / assessment.rs (three-enforcement-point custody model, venue allowlist, content-digest assessment binding) are fully implemented and tested in isolation but have zero production references anywhere | backend/crates/services/qip-capital-fabric/src/custody.rs:388,979,1067,1185; backend/crates/services/qip-capital-fabric/src/destination.rs:311; backend/crates/services/qip-capital-fabric/src/assessment.rs:114 | Confirmed independently: `grep -rn 'CustodyPolicy\\|TransferAuthority\\|DestinationRegistry' backend/crates/runtime/qip-kernel/src backend/crates/apps/*/src` outside tests returns nothing. These types exist purely as the required parameters of `TransferGate::assess` (gate.rs:741-753), which per the previous finding has no caller. `FabricState` (journal.rs:529-535) even carries a `DestinationRegistry` field but no `CustodyPolicy`/`TransferAuthority` field, so the journal's own persisted state could not supply a Gate assessment's arguments without an external caller regardless. This is the same deliberate 'shadow-form only, human attests the rest' posture as the gate itself (see previous finding's pre_positioning.rs citation), not abandoned code — 51 dedicated tests across tests/corridor_and_gate.rs, tests/attestation_binding.rs and tests/custody_mirror.rs exercise it. Retained; wiring it live is the same owner decision (ADR 0099 C1) as the gate itself. | E07 |
| low | services-capital-exec | LiveBroker exists as a complete interface with no credential, no transport and no egress path — reports itself unavailable rather than trading | backend/crates/services/qip-execution-engine/src/broker.rs:641-666 | Confirmed: LiveBroker::new sets credential_present/transport_present/enabled_by_operator to false unconditionally, and a `configured` constructor exists 'for testing the logic' only. This is the intended shape of the paper-trading boundary at this layer, matching the type-system layer ADR 0021 relies on and blueprint conflict C1's correct treatment (LIVE_CAPITAL/EXTERNAL_ACTION, structurally BLOCKED). Nothing here should change without the owner decision C1 names; retained as a deliberately inert, self-reporting-unavailable interface rather than an accidental stub. | E00 |
| low | services-capital-exec | ORIGINATION_MAX_EXPOSURE ($250,000,000, raw i128) is hard-coded rather than configurable | backend/crates/services/qip-execution-engine/src/origination.rs:44-47 | This is the requested category surfaced honestly rather than omitted, but it is not debt: the module's own doc explains the hard-coding is the point ('a number a deployment can raise is not that... a ceiling a deployment can edit is a ceiling an incident can edit'), it refuses above the ceiling rather than clamping to it (matching working-agreement principle 2, 'refuse rather than guess'), and OriginationMandate has no Deserialize, so the ceiling cannot be decoded into existence from a config file either. Retained deliberately. | E17 |
| low | services-capital-exec | qip-risk-engine (autonomy/kill switch, pre-trade checks, monitor) has no gaps observed against the target and is reached from every composition root | backend/crates/services/qip-risk-engine/src/lib.rs:1-21; backend/crates/apps/qip-fastbrain/src/main.rs:46; backend/crates/runtime/qip-kernel/src/platform.rs:181-183 | Included for full component-group coverage rather than left silent: this crate is the deterministic Risk Gate the blueprint's §14 splits explicitly from the reasoning brain ('the brain can use complex AI/causal/scenario models... but every executable action still passes deterministic limits'), no model is consulted anywhere in this crate by design, and it is reached from qip-api, qip-fastbrain, qip-deepbrain, qip-cli and qip-kernel's Platform with 41 tests including exact-boundary and dual-approver cases. No debt found here worth tracking; already fits the target. | E00 |
| low | services-capital-exec | qip-portfolio-engine (proposal construction, dual sign-off, veto-from-any-state) has no gaps observed against the target | backend/crates/services/qip-portfolio-engine/src/lib.rs:1-22; backend/crates/runtime/qip-kernel/src/platform.rs:169-170,232,4514 | Included for full component-group coverage rather than left silent: directly instantiated in Platform's constructor and exercised every DECIDE cycle, with 31 tests covering the dual-control approval rule and the veto-from-any-pre-release-state rule explicitly. A leg with no hypothesis fails validation structurally. No debt found here worth tracking; already fits the target. | E00 |

## D — delete (1)

| Risk | Group | Item | Files | Why | Epic |
|---|---|---|---|---|---|
| low | frontend | @algorik/analytics and @algorik/shared-types are complete, independent implementations with zero call sites anywhere in the workspace and no near-term consumer named by any ADR or blueprint requirement | frontend/packages/analytics/src/index.ts:1-89; frontend/packages/shared-types/src/index.ts:1-22; frontend/portal/next.config.ts:22-34 | grep '@algorik/(analytics\|shared-types)' across frontend/portal/src and frontend/landing returns 0 hits for both. Neither is reached from production, and no standing ADR or blueprint requirement keeps either deliberately: analytics' own doc comment describes a privacy-aware abstraction admitting no vendor SDK at all, so nothing currently needs it to exist in code before it is used; shared-types' own doc comment records having already deleted one prior unused export set (readConfig/describeProblems/AlgorikConfig) after an identical zero-importer search, which is the package's own established precedent for exactly this trim. Delete both, and their next.config.ts transpilePackages and package.json entries, rather than carrying two unused dependency-tree members indefinitely. | E00 |
