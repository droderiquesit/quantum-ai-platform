# Gap matrix and delivery plan

The canonical architecture's nine areas against what the repository has, and
the ordered work that closes the distance. Ordering is by dependency and by
consequence, not by how much of the diagram a task colours in.

## The nine areas

| # | Canonical area | State | Principal gap |
|---|---|---|---|
| 1 | Autonomous Data Mesh | Partial | Absorption, dedup, sequencing and bitemporal stamping all work. A live source is now wired — `apps/qip-fastbrain/src/feed.rs` has a `Feed::Live` arm and a `Feed::live` constructor that evaluates licensing before it opens a socket — but no deployment has been observed running on it; the normalizer sits outside the runtime path |
| 2 | Regional AI Brains | Partial | `qip-edge-node` runs and is structurally paper-only. Degraded-mode policy exists; regional partition recovery is tested in `resilience.rs` but not against a live cluster |
| 3 | Global Opportunity Brain | Partial | Detectors, correlation and scoring work. Cross-market graph search is narrower than the diagram implies |
| 4 | Capital & Strategy Brain | Partial | Sizing, cost model, the capital ledger and capital reservation work. `RiskState::expected_shortfall` and `value_at_risk` are populated from the book's own equity path in the kernel, so `MaxExpectedShortfall` and `MaxValueAtRisk` both fire (item 5) — in tests; neither has been observed firing in a deployment, which is area 1's evidence gap wearing a different name |
| 5 | Regional Execution Mesh | **Closed** | Orders reach the simulated broker through the deterministic control path, `LegGroup` bounds the risk between legs of one decision, and a passing capital check now holds what it approved: `qip_capital::ReservationLedger` is wired into the kernel — `Platform`'s decide stage calls `self.reservations.reserve` for each sized proposal and its act stage calls `.release` for a refused one (item 10, commit `0c6c17f`). An earlier revision of this row said reservation was still unbuilt while item 10 said it was done; the code agreed with item 10 |
| 6 | Outcome & Telemetry | Partial | Attribution is exact and works. Metrics are emitted at the seams — the kernel's cycle in `qip-kernel/src/platform.rs` counts `names::CYCLES_RUN` on entry and on the way out records `STAGE_RUNS`, `STAGE_DURATION_MS`, `STAGE_PROBLEMS`, `CYCLE_DURATION_MS` and the `KILL_SWITCH_TRIPPED` and `EVENT_LOG_ENTRIES` gauges (at `68b7da6`, `:1693` and `:1753-1818`; the earlier citations here pointed at a closing brace) — so `/metrics` is no longer empty once a cycle has run. What remains is that `workload_metrics_exist` is still `false` in every environment, correctly, because no pod has been observed to scrape |
| 7 | Evolution Brain | Partial | The loop now trades, crowns a champion and challenges it under a trial count. Its backtests produced flat equity curves until an earlier pass, so every candidate registered before that was admitted on evidence from a strategy that never traded. Drift detection is now wired — `apps/qip-deepbrain/src/learning.rs:425` and `:279`. What remains is the Deep Brain's empty reference universe (item 11) |
| 8 | Intelligence Fabric | Partial | Cost router now decides and records. Quantum adapter is real with a classical baseline always computed; the digital twin is not driven from production decisions |
| 9 | Governance & Guardrails | Strong | Three independent paper-trading layers, hash-chained log, two-signature approval, deterministic pre-trade checks. Alerting is blocked behind area 6 |

## Ordered work

Consequence-ordered. Each item names the evidence that closes it.

| # | Work | Why it is here | Evidence that closes it |
|---|---|---|---|
| 1 | ~~Close the trading spine~~ | **Done.** Nine diagram rows starved on it and LEARN had no fill to attribute | 3 tests, 4 mutations fired |
| 2 | ~~Emit telemetry~~ | **Done.** It also uncovered that the alert policies named metrics nothing emitted — the layer was unreachable, not merely gated | Metrics at their seams, a collector, and a test binding both halves to the same names |
| 3 | ~~Wire the mesh in manifests~~ | **Done.** | The env-var correspondence test, both directions |
| 4 | Deploy the TLS egress proxy | **Described, not deployed.** An earlier revision struck this row through on the strength of a committed manifest. The manifest exists in two byte-identical copies (`infrastructure/helm/qip/templates/egress.yaml`, `infrastructure/kubernetes/base/egress.yaml`) and in both the `ServiceAccount` and the `Deployment` are commented out, so Argo CD renders the chart and no proxy pod exists. Commit `64b765a` made the egress suite able to tell a described proxy from a deployed one instead of stripping `#` and passing. Until a proxy runs, the vendor half of item 6 stays open and a pod has no outbound HTTPS path | A proxy pod observed serving a vendor request through the allowlist, and the suite's described-versus-deployed check flipping on the same commit |
| 5 | ~~Compute expected shortfall~~ | **Done.** Two limits, not one — VaR shared the defect | Both firing on a book with a tail, both quiet on one without |
| 6 | ~~Prove one live market source~~ | **Done to the boundary this environment can honestly reach.** The full path exists and was exercised against the real endpoint: `coinbase-spot-ticker` (Coinbase Exchange public BTC-USD ticker, keyless) through the connector SDK's runtime — health probe, rate limit, dedup, knowability — bridged into the loop's own `DataAdapter` contract (`connector_feed.rs`), selectable in the deployed binary (`QIP_CONNECTOR_SOURCE`/`QIP_CONNECTOR_BASE_URL`), and **gated on a licensing catalogue evaluated before use** (`qip-fastbrain/src/licensing.rs`): the terms were read as internal-use-no-redistribution, the manifest's class was corrected `public` → `internal` to match, a class disagreement refuses the source, and a research-only licence is refused for the trading path by name. In-session evidence: one real tick fetched live through a TLS-terminating egress bridge — `live evidence: 1 record(s) from coinbase-spot-ticker (Internal)`, opt-in suite 5 passed 0 failed. **What remains is deployment, not code:** an egress proxy in the cluster allowlisting the vendor, and the blueprint's Phase 1 exit — seven days of stable streaming — which no session can prove | A cycle absorbing live data behind the licensing gate — the in-session fetch is that cycle's SENSE half; sustained streaming in a deployment is the rest |
| 7 | ~~Multi-leg execution~~ | **Done.** Leg risk, deadlines and unwind. The invariant: a group that cannot complete is unwound, never abandoned | 15 tests, 17 mutations fired — one found a deadline test that never reached the deadline branch |
| 8 | ~~Champion/challenger~~ | **Done.** It was unreachable by construction: `Challenger` has no constructor but `Mutator::mutate`, which nothing called. Wiring it exposed that the loop's backtests had never filled an order | 13 mutations across four files, all fired |
| 9 | ~~Drift detection~~ | **Done.** The control now fires: `apps/qip-deepbrain/src/learning.rs:425` builds the report from a live feature window against its reference and `:279` records it, both above the `#[cfg(test)]` boundary at line 516 | A production caller comparing a live feature window against its reference, and a test asserting the report is recorded |
| 10 | ~~Capital reservation~~ | **Done, in two halves.** The mechanism landed crate-locally (`qip_capital::ReservationLedger`, reserve/commit/release with expiry, six mutation-verified tests) and the kernel now calls it: the decide stage anchors free to tracked equity every pass, sizes against equity minus active holds, and reserves what each proposal was granted; the act stage commits a placed proposal's hold and releases a refused one, sweeping lapsed holds on the same clock | `platform.rs::a_second_proposal_is_sized_against_what_the_first_still_holds` — the second proposal's budget is exactly equity minus the first one's hold (200,000 − 16,000 = 184,000 in the test's own numbers) — plus `::a_released_proposal_commits_its_hold_and_a_refused_one_returns_it` and the acceptance anchor test; four kernel-side mutations fired |
| 11 | ~~Reference data for the Deep Brain~~ | **Done for the default path.** The reference universe is derived from the synthetic environment's own instrument list — one definition for prices and listings, provenance and licensing stamped `Synthetic`, an empty environment refused rather than yielding an empty universe (`qip-deepbrain/src/reference.rs`) — and the deployed binary now constructs through it: `main.rs`'s synthetic branch builds `EvolutionEngine::over_synthetic(...)`, taking the environment before it is boxed, which is the only point the instrument list is reachable. **Residual, by design:** the replay path keeps the empty universe and the visible no-fill refusal, because a tape carries bars, not listings; its comment now says exactly that | `reference.rs` suite (4 mutations fired, per its author) and `evolution.rs:962`'s constructor test; the branch wiring itself is composition-root code with no direct test, stated here rather than papered over |

## Risk register

| Risk | Consequence | Mitigation |
|---|---|---|
| A control that cannot fire | Reads as protection, is not. Found twenty times so far, seventeen closed and three open. Closed: `MaxExpectedShortfall`, `MaxValueAtRisk`, `Proposal::approve`, `Router::select`, `StageOutcome::with_elapsed`, the alert policies, the champion/challenger ladder, both halves of the evolution loop's backtest, drift detection (item 9), the reservation ledger the kernel never called (`0c6c17f`), the venue-credential IAM predicate — inverted in five copies and re-armed once more by the execution node's wiring block (`e614e50`, `704247d`), the API's brute-force lockout, keyed on a subject it could only learn after a match (`ad0d38d`), the console's order ticket, a live-order path waiting on a route (`87a69ba`), the GitOps job authenticating with outputs its identity step never wrote (`41a0689`), `Platform::attribute` handing LEARN an empty hypothesis vector so the join was empty for everything traded (`e86531e`), and the schema-version test that compared a constant with itself (`8e84bb7`). Open at `68b7da6`: the egress proxy, whose `Deployment` is committed commented out in both manifest copies (item 4); `NumericFact::observed`, which no production code calls; and `qip-api`'s `Web::record_cycle`, which nothing calls | Every closure ships with a test that fails when the control is removed; the standing question for any new control is which running code path reaches it; and this row is recounted each time one is found, because a count that lags its own list is the same defect in a document |
| Documentation drifting ahead of code | An operator believes a control exists | `documentation.rs` refuses overclaims; it has already caught one |
| A test that passes by measuring nothing | Permanent false confidence. Caught so far: a deadline test whose verdict came from the risk branch; a subject-selection test whose filter did the ranking's work; the venue-credential test that asserted the source text of the predicate it guarded, so it certified the inversion (`e614e50`); the pipeline test that looked for the substring `workload_identity_provider:` and could not tell a populated value from an empty one (`41a0689`); and the egress tests that stripped `#` before reading, so a commented-out pod passed as a deployed one (`64b765a`) | Mandatory mutation verification; premise-before-conclusion |
| Parallel agents overwriting each other | Work destroyed and redone, discovered late | Disjoint path ownership named in every brief, including the other agents' paths |
| An agent dying mid-task | Half-finished code in the tree | Finish it or park it on a WIP branch; never discard, never leave the tree red |
| A default tuned for a different frequency | Silent and total. A one-day decision lag and ten million of capital are right for daily institutional bars and wrong for minute bars, and applied there they produced a flat equity curve the gate scored as a result | A backtest that produced no fill is refused, not scored; superseded orders are counted rather than dropped |
| Latency claims outrunning measurement | A promise the physics does not support | No latency class stated without a reproducible benchmark |

## Assumptions, each reversible

1. **Paper trading stays absolute.** No live-order path is built. Reversing this
   is an ADR and three deliberate layer changes, not a flag.
2. **Two dependencies stay the limit.** Any addition needs an ADR first.
3. **The simulated broker is the execution target.** Provider sandboxes come
   only after the egress proxy is deployed and certified.
4. **Regional topology stays as the tfvars declare it** — one cell in dev, three
   in stage. Nine cells would cost production money to learn nothing more.
5. **No cloud spend beyond the existing dev environment** without explicit
   approval.
